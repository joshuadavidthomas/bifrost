# Test slice of CodeQL Models-as-Data for Java.
#
# Upstream: https://github.com/github/codeql
#   path:     java/ql/lib/ext/org.springframework.web.util.model.yml
#   revision: c9142680f5b6409dbe0944350321c54e8c801e61
#   license:  MIT (see LICENSE in the upstream repository)
#
# These two rows are the corpus's only use of the `Argument[this,0]` set form,
# where one row names more than one input port. Both rows are verbatim.
extensions:
  - addsTo:
      pack: codeql/java-all
      extensible: summaryModel
    data:
      - ["org.springframework.web.util", "DefaultUriBuilderFactory", False, "uriString", "", "", "Argument[this,0]", "ReturnValue", "taint", "manual"]
      - ["org.springframework.web.util", "UriBuilderFactory", True, "uriString", "", "", "Argument[this,0]", "ReturnValue", "taint", "manual"]
