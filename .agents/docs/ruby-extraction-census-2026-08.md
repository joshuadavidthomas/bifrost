# Ruby seam census for the stage-3 fleet (2026-08-05)

Agent-run census. Coordinator dispositions: (1) RubySemanticIndex
re-parameterizes on a Ruby source trait; the 17-name get_definition inversion
follows the rust/python precedent (one-way, no C#-class bidirectionality).
(2) Zeitwerk state stays shim-side and the decision logic moves; do NOT hoist
or eagerly materialize the lazy zeitwerk_reference_files build -- the timing
is part of the augmentation contract even though no assertion pins it. The
four RubyQueryResolver pins live in shim-side ruby_graph.rs and survive by
staying put. (3) forward_owner_relation_facts stays shim-side and the decoded
Vec<RubyOwnerRelationFact> crosses the trait (the Py-2 collect_bounded
precedent) -- no FileState exposure. (4) walk_tree_iterative lowers to core
verbatim (shared with java/js_ts). (5) The 4,777-LOC pack band parks on
semantic_model with the two public API paths byte-identical. Ruby runs as a
single combined pass like PHP.

## Ruby seam census for the stage-3 fleet (2026-08-05)\n\nTaken at the committed tip `03fe1fbc` (working tree clean; no concurrent-agent churn observed in `analyzer/ruby/` or `usages/ruby_graph/` — last seam-touching commit is `62b1ac76`, pre-campaign). Same shape as the Go/Rust/Python/C#/PHP predecessors.\n\n**Verdict summary: Ruby needs one new small shared lowering (`walk_tree_iterative`) and has exactly one coupling with no landed precedent (`fetch_file_state` reach-in). It is the fleet's largest `semantic_model` park (4,777 LOC across five files) and its second-smallest first-landing dependency block (three lines). The R1-class inherent language logic is 428 LOC spread over four files, not one. Projection: ~5.8k moves, ~1.1k production shim — essentially level with PHP.**\n\n---\n\n## 1. The seam, exact paths and `wc -l`\n\n### `crates/bifrost-analysis/src/analyzer/ruby/` — 13,888 (12,253 prod / 1,635 in-file test)\n\n| File | LOC (total / prod) | Purpose |\n|---|---|---|\n| `semantic.rs` | 4,587 / 4,392 | Ruby → executable-semantics IR lowerer; `impl_program_semantics_provider!(RubyAnalyzer, RubySemanticLowerer)` at `:26`, `ADAPTER_VERSION = b\"ruby-value-semantics-v2\"` at `:24` |\n| `rbs_artifact.rs` | 1,498 / 1,290 | RBS/RBI projection over `tree_sitter_rbs` (`:38`) into `semantic_model` facts; `RbsProjection`, `RubyMemberAlias`, `project_rbs` |\n| `source_artifact.rs` | 1,107 / 860 | `.rb` source → `semantic_model` facts (`project_ruby_source`, `RubySourceProjection`) |\n| `external.rs` | 958 / 645 | `RubyDependencyPackAdapter: DependencyPackAdapter` (`:20`) — **public API** |\n| `declarations.rs` | 916 / 916 | `RubyVisitor`, `parse_ruby_tree` (`:43`), `extract_name_path`/`extract_name_segments`, `RubyFieldScope`, `RubyNamePath`, `ruby_field_short_name`, `ruby_variable_field_name`, FQ naming over `FqName`/`SegmentInterner` |\n| `mod.rs` | 894 / 894 | free fns `:67–116` (5) and `ruby_semantic_identifier_range` `:118–158`; `RubyAnalyzer` struct + **17 fields** `:160–182`; `impl_forward_query_provider!` `:184`; **inherent block `:186–340`** (16 methods); `RubySemanticFacts` + `build` `:342–385`; `push_ordered_mixin` `:387–392`; `CodeUnitIndex` `:394–521` (pure forwarding); `IAnalyzer` `:523–695`; `AnalyzerTestHooks` `:698–730`; `RubySupport`/`LanguageSupport` `:732–809`; `RubyEdgePass` `:811–826`; `StructuralReceiverResolver` `:828–858`; `RubyDeadCodeBulk` `:860–894` |\n| `tests.rs` | 759 / 605 | **Production** — `ruby_contains_tests` (`:61`), `impl TestDetectionProvider for RubyAnalyzer {}` (`:81`, empty), `detect_ruby_test_assertion_smells{,_limited}` (`:90`,`:98`), minitest/RSpec signal classifiers |\n| `dependency_discovery.rs` | 655 / 495 | `resolve_ruby_semantic_pack_dependencies` (`:18`) — **public API**; `semver`, `CatalogCoordinate`, lockfile digests |\n| `imports.rs` | 594 / 594 | `parse_ruby_require_call` (`:14`) + path resolution `:14–127`; **`impl RubyAnalyzer` `:129–330`** (17 methods: `required_files`, `autoload_constant_files`, the five zeitwerk builders, `effective_imported_code_units`, `transitive_referencing_files_of`); `collect_ruby_autoload_edges` `:332`; `parse_ruby_autoload_call`/`is_ruby_autoload_symbol_argument`/`ruby_symbol_name` `:402–437`; `impl ImportAnalysisProvider` `:439–476`; Gemfile/Gemfile.lock zeitwerk detection + reference-identifier collection `:478–594` |\n| `gem_artifact.rs` | 559 / 452 | `.gem` tar/gzip walk over `flate2`; `RubyGemDeclarationEntry`/`Kind`, `read_gem_declaration_entries` |\n| `mixins.rs` | 454 / 216 | **`impl RubyAnalyzer` `:15–122`** (8 methods incl. `mixin_relations`, `forward_mixin_specs`, `forward_superclass_targets`, `forward_owner_relation_facts`); free fns `:124–216` (`raw_mixin_specs_for_type`, relation encode/decode, `mixin_call_kind`); 238-LOC analyzer-bound suite from `:217` |\n| `structural.rs` | 352 / 339 | `RUBY_STRUCTURAL_SPEC` (`:21`), `RUBY_KIND_TABLE` (`:23`), `impl StructuralSpec` (`:186`); 13-LOC grammar-table test |\n| `diagnostics.rs` | 343 / 343 | `collect_ruby_semantic_diagnostics` (`:44`), `RUBY_UNRECOGNIZED_SYMBOL`, `RUBY_SEMANTIC_DIAGNOSTIC_SOURCE = \"bifrost-ruby\"` |\n| `adapter.rs` | 95 / 95 | `RubyAdapter: LanguageAdapter` (`:29`), `RUBY_COGNITIVE_CONFIG` (`:11`), query dir `\"resources/treesitter/ruby\"` |\n| `hierarchy.rs` | 74 / 74 | `impl RubyAnalyzer` `:5–47` (`resolve_supertype`, `types_by_identifier`); `impl TypeHierarchyProvider` `:49–74` |\n| `clones.rs` | 31 / 31 | `RUBY_CLONE_SYNTAX: CloneSyntaxProfile` (`:7`) + 8-LOC entry |\n| `cache.rs` | 12 / 12 | `weight_code_unit_vec` |\n\n### `crates/bifrost-analysis/src/analyzer/usages/` — 5,625 (4,434 prod / 1,191 test)\n\n| File | LOC (total / prod) | Purpose |\n|---|---|---|\n| `get_definition/ruby.rs` | 2,437 / 1,516 | `RubyDefinitionProvider` (`:8`) over `ResolutionSession` (11 refs), `resolve_ruby`, `resolve_ruby_bounded`, `ruby_type_lookup_resolution_bounded`, `ruby_site_for_focus` |\n| `ruby_graph/extractor.rs` | 1,193 / 1,193 | Per-symbol forward scan; `RubyFileScan` (`:23`), receiver/factory/seed inference, `Parser` via `parse_ruby_tree` (`:764`) |\n| `ruby_graph/resolver.rs` | 719 / 719 | `RubyTargetSpec` (`:18`), **`RubySemanticIndex` (`:127`)**, `ruby_field_target`, `ReceiverMode`/`ReceiverType`, `ruby_method_lookup_mode_matches`; two scan-local `RefCell` memos (`:132–133`) |\n| `ruby_graph.rs` | 499 / 229 | `build_ruby_usage_edges{,_weights}`, **`RubyQueryResolver` (`:72`)** with the four pinned asymmetries, `RubyUsageGraphStrategy`; 270-LOC pin suite from `:230` |\n| `ruby_graph/inverted.rs` | 438 / 438 | Whole-workspace per-file walk (`build_ruby_edges` `:31`); already on the W2 contract |\n| `ruby_graph/syntax.rs` | 142 / 142 | Pure node predicates; **zero analysis imports** |\n| `ruby_graph/hits.rs` | 87 / 87 | `record_usage_hit`/`record_unproven_usage_hit`/`record_self_receiver_usage_hit` |\n| `get_type/ruby.rs` | 61 / 61 | `resolve_ruby_type_bounded` — thin, no test module |\n| `ruby_graph/shared.rs` | 49 / 49 | `RubyEdgeResolver` — the one `resolve_analyzer::<RubyAnalyzer>` orchestration site |\n\n**Seam total: 19,513 LOC** (16,687 prod / 2,826 in-file test) **+ 49 LOC of `.scm`** — `resources/treesitter/ruby/{definitions.scm (39), imports.scm (6), identifiers.scm (4)}`, `include_str!`'d at `store/epoch.rs:235–247`, `lang_epoch!(Ruby, \"ruby\", \"treesitter/ruby/\", \"synthetic-file-scope-code-units-2026-07;attr-macro-accessor-identities-2026-07;fq-interned-segments-2026-07\")` at `epoch.rs:481–486`. Relocation requires a bump. **Unlike PHP, there is no `#[cfg(test)]` prior-salt helper for Ruby** — `epoch.rs` carries exactly one (PHP's, `:456–462`).\n\n### Ruby root test suites (names only) — 5,455 LOC\n`tests/suite_analyzers/`: `ruby_analyzer_test.rs` (485), `ruby_import_test.rs` (467), `ruby_test_detection_test.rs` (90), `ruby_type_hierarchy_test.rs` (86).\n`tests/suite_usages/`: `usages_ruby_test.rs` (2,094), `usage_graph_ruby_test.rs` (145).\n`tests/suite_smells/`: `ruby_test_assertion_smells.rs` (476), `ruby_dead_code_smells.rs` (222), `ruby_structural_clone_smells.rs` (194), `go_rust_ruby_structural_clone_smells.rs` (115, shared).\n`tests/suite_semantic/`: `ruby_dependency_semantic_pack.rs` (353), `semantic_value_ruby_contract.rs` (274), `ruby_semantic_diagnostics.rs` (267).\n`tests/suite_lsp_parity/`: `ruby_lsp_goto_definition.rs` (105), `ruby_lsp_find_references.rs` (82).\nFixtures: `tests/fixtures/{testcode-ruby, usage-graph-ruby}`.\nCross-cutting suites exercising Ruby without a Ruby name (26 files), incl. `analyzer_persistence.rs`, `common/{mod,value_flow_scenarios}.rs`, `suite_cross_language/{language_reach_in_gate, code_query_pipelines, code_query_value_flow, cross_language_receiver_definition, cross_language_return_type_definition, cross_language_self_usages, cross_language_attribute_target_declarations, structural_search_cross_language, structural_search_planner}.rs`, `suite_persistence/{multi_analyzer_routing, multi_analyzer_get_test_modules_test}.rs`, `suite_usages/receiver_language_{acceptance_matrix,scope}.rs`, `suite_symbols/{get_definition_test, searchtools_service, searchtools_definition_selectors}.rs`, `suite_mcp_cli/*`.\n\n---\n\n## 2. Per-file classification\n\n**(a) core-resident already** — everything PHP's census listed, plus (verified in `bifrost-core`): `RubyMethodDispatchMode` (`core/analyzer/model.rs:36`), `RubyAnalyzerConfig`/`RubyDependencyApiEvidence`/`RubyGemApiArtifact` (`core/analyzer/config.rs:18,25,37`), `DispatchExtensibility`, `TypeRelation`/`TypeRelationKind` (`core/analyzer/type_relations.rs`), `ParsedFile` (W1, carries `ruby_method_dispatch_modes` at `parsed_file.rs:44`), `PreparedSyntaxTree` (W5, `core/analyzer/prepared_syntax.rs:90`), `BoundedDefinitionLookup` (W3, `core/analyzer/definition_lookup.rs:22`), `usages::{common, local_inference, model, inverted_edges, receiver_analysis, reference_site, scan_scope, graph_core, outcome, reexport_seeds}`, `test_assertions` (Py-2), `build_direct_descendant_index`, `structural::*`.\n\n**Consequence worth stating plainly: Ruby has no `ScalaExportInfo`-class prerequisite.** The `ruby_method_dispatch_modes` side table is threaded through `tree_sitter_analyzer.rs` (`:499, 1791, 6235–6284, 8537`) and `store/mod.rs` (~50 sites, its own SQLite table `ruby_method_dispatch_modes`, blob-envelope count column, bulk readers) — but every type in those signatures is already core, so all of it is framework-side and stays put without blocking anything.\n\nClean today, zero analysis machinery: **`declarations.rs` (916), `tests.rs` prod (605), `structural.rs` (352), `imports.rs`'s three free-fn bands (337), `mixins.rs:124–216` (93), `mod.rs:67–158` (92), `ruby_graph/syntax.rs` (142), `adapter.rs` bodies + `RUBY_COGNITIVE_CONFIG` (~60).**\n\n**(b) retarget-only after the landed workstreams** — `ruby_graph/hits.rs` (core `usages::common::{SNIPPET_CONTEXT_LINES, usage_hit, reclassify_self_receiver_hit_at}` + `enclosing_code_unit`, 2 calls), `ruby_graph/inverted.rs`'s W2 half (`FileEdgeScanInput`/`PerFileEdges`/`classify_reference_node`/`ClassRangeIndex` core; `build_edge_output`/`parse_and_collect` stay driver-side), `ruby_graph/resolver.rs`, `ruby_graph/extractor.rs`.\n\n**(c) analysis machinery, per file**\n\n| File | Machinery | Covered by an established pattern? |\n|---|---|---|\n| `ruby_graph/extractor.rs` | `&dyn IAnalyzer`; only **three** methods reached — `global_usage_definition_index` (3), `ranges` (1), `parent_of` (1); `DefinitionIndexHandle` in `RubyFileScan.support`; `tree_walk::{TreeWalkAction, walk_tree_iterative}` | **Yes** — `PythonGraphSource`/`GoGraphSource` idiom, plus Py-2's *callback for the global definition index*. `walk_tree_iterative` needs lowering (see below) |\n| `ruby_graph/resolver.rs` | `&RubyAnalyzer` (5 sigs) → `required_files` ×4, `zeitwerk_visible_files_for`, `forward_raw_supertypes`, `forward_mixin_specs`, `autoload_visible_files_for_constant`, `method_dispatch_mode` ×2, `semantic_facts`; `&dyn IAnalyzer` → `parent_of`, `definitions`; `RubySemanticFacts` via the hub | **Mostly** — graph-source idiom with a 7-method Ruby extension. `RubySemanticFacts` is a pure 4-map struct that moves |\n| `ruby_graph/inverted.rs` | W2 contract + `walk_tree_iterative` + `local_inference` (core) + `&RubyAnalyzer` (1 sig). **Zero `same_owner` use** — Ruby does not need PHP's `route_same_owner` lowering | **Yes**, once `walk_tree_iterative` is core |\n| `ruby_graph.rs` | `resolve_analyzer::<RubyAnalyzer>`, `GraphUsageAnalyzer`/`UsageAnalyzer` impls, `analyzed_files_for_language` (analysis `pub(crate)`, `usages/common.rs:45`), the four pinned asymmetries | **Yes** — analysis-side residue, the Python `python_graph.rs` shape. Stays whole |\n| `ruby_graph/shared.rs` | `resolve_analyzer`, `analyzed_files_for_language` | **Yes** — same |\n| `ruby/mod.rs` inherent block `:186–340` | `TreeSitterAnalyzer<RubyAdapter>` field, `LimitedQueryRows` ×6, `moka::sync::Cache` ×3, `PoolSafeMemo`, `build_weighted_cache` | **Yes for the shim half** (W6 landed `LimitedQueryRows`). Only ~30 LOC / 2 methods are language logic |\n| `ruby/imports.rs:129–330` | `self.inner.{all_files, project, top_level_declarations, import_info_of}` + 5 `OnceLock` fields + `PoolSafeMemo` | **Yes** — graph-source idiom; state stays, decisions leave |\n| `ruby/mixins.rs:15–122` | `self.inner.fetch_file_state(…)` → `Arc<FileState>.{declarations, raw_supertypes, supertype_lookup_paths}` | **NO — see §3.3.** Crate-private persisted state; no landed crate reaches it |\n| `ruby/hierarchy.rs:5–47` | `self.inner.{definitions, all_declarations}` + `types_by_identifier` `OnceLock` | **Yes** — `CodeUnitIndex` surface |\n| `ruby/diagnostics.rs` | `RubySemanticIndex` (from `ruby_graph`), `resolve_analyzer::<RubyAnalyzer>`, `ruby.{zeitwerk_visible_files_for, has_unresolved_load_directive}` | **Partly.** Unlike Go/Python/PHP (which take `&dyn BoundedDefinitionLookup`), Ruby's diagnostics route through the *graph* semantic index, so it follows `resolver.rs` rather than being independently movable |\n| `ruby/clones.rs` | `clone_detection::{CloneSyntaxProfile, build_tree_sitter_clone_candidate_data}` (analysis) | **Yes** — the `CloneSyntaxProfile` shape is shared with go/rust/kotlin, and **Go's and Rust's `clones.rs` both stayed in analysis after their extractions**. All 31 LOC stay |\n| `ruby/adapter.rs` | `ParsedFile` (core), `LanguageAdapter` (analysis) | **Yes** — Go's forwarding shell |\n| `ruby/semantic.rs` | `analyzer::semantic::*`, `PreparedSyntaxTree`, `lexical_definitions::formal_parameter_slots_for_owner`, registration macro naming `RubyAnalyzer` | **Parked** — fleet-wide |\n| `ruby/{external, dependency_discovery, gem_artifact, rbs_artifact, source_artifact}.rs` | `analyzer::semantic_model::*` (≈35 distinct imports), `flate2`, `semver`, `tree_sitter_rbs` | **Parked** — the `semantic_model` workstream |\n| `get_definition/ruby.rs`, `get_type/ruby.rs` | `ResolutionSession` (11), `use super::*` glob (`sort_units`, `candidates_outcome*`, `BoundedResolution`, `DefinitionLookupOutcome`), `usages::target_kind::TypeLookupTargetKind`, `lexical_definitions::formal_parameter_slots_for_owner_bounded` (`ruby.rs:587`, one site) | **Parked**; **W7-movable except for one Py-2-class forwarder** |\n\n**R1-class inherent language logic — sized.** Ruby has **four** `impl RubyAnalyzer` blocks, not one:\n\n| Site | LOC / methods | Disposition |\n|---|---:|---|\n| `mod.rs:186–340` | 155 / 16 | Constructors, `self.inner` forwarders, six `*_limited` accessors → **shim**. Language logic ≈ 30 LOC / 2 methods (`declaration_candidates_by_fqn_limited`'s `.`/`$` leaf split, `member_candidates_for_owner_limited`'s `.` join) |\n| `imports.rs:129–330` | 202 / 17 | **Moves behind the source trait** |\n| `mixins.rs:15–122` | 108 / 8 | **Moves except `forward_owner_relation_facts`** (§3.3) |\n| `hierarchy.rs:5–47` | 43 / 2 | **Moves** |\n| **Total needing free-function rewrite** | **≈ 383 / 29** | vs Rust 2,896/73, C# 1,210/52, Python 916/37, PHP 131/15 |\n\nRuby therefore sits between PHP and Python on R1 mass, but is the only fleet language whose R1 block is *split across four files* — the mechanical cost is in the file-crossing, not the LOC.\n\nSeparately, **only 16 free functions/signatures take `&RubyAnalyzer`** (resolver 5, diagnostics 2, get_definition 2, clones/mod/tests/inverted/ruby_graph/shared/dependency_discovery 1 each) — an order of magnitude fewer than PHP's 65, because Ruby put its language logic on inherent methods instead.\n\n**`PreparedSyntaxTree` outside the lowerer.** **None.** Four hits, all in the parked `ruby/semantic.rs` (`:19, 631, 810, 1186`). Zero in `get_definition/ruby.rs`, zero in `ruby_graph/`. Rust's W5-motivating problem does not exist for Ruby, and unlike PHP there is not even a forwarder in the parked route.\n\n**moka / global caches / rayon / regex.** **No `static` or `LazyLock` cache anywhere in the seam.** The only `LazyLock` is `RUBY_COGNITIVE_CONFIG` (`adapter.rs:11`, pure data); the only `static` is `RUBY_USAGE_STRATEGY` (`mod.rs:732`, a ZST). **Zero `regex`. Zero `rayon`** — Ruby joins C# and PHP as the third fleet language with none. **Zero `Mutex`.** All caching is instance-resident on `RubyAnalyzer` (`mod.rs:160–182`): **three** moka `Cache`s (`imported_code_units` budget/4, `referencing_files` budget/8, `direct_ancestors` budget/8), **nine** `Arc<OnceLock<…>>` (`direct_descendant_index`, `autoload_constant_files`, the five zeitwerk cells, `mixin_relations`, `semantic_facts`, `types_by_identifier`), and **one** `PoolSafeMemo` (`reverse_import_index`). Invalidation is wholesale: `IAnalyzer::update`/`update_all` call `Self::from_inner`, which reconstructs every cell. The two `RefCell`s (`ruby_graph/resolver.rs:132–133`) are scan-local and move with the file. **Verdict: moka stays in analysis; no deviation to record.** No parse memo — six production sites do a bare `parse_ruby_tree` (a fresh `Parser::new()` + `set_language`) per call, plus `inverted.rs:42`'s per-scan language handle.\n\n**Dead-code bulk-proof shape.** Ruby is the **inline-predicate** shape (C#'s, not PHP's delegating one). `RubyDeadCodeBulk::needs_precise_scan` (`mod.rs:868–870`) is a one-liner — `routing.candidate.is_field()`, i.e. **bulk unless `is_field`**, with the doc comment recording why (synthesized `attr_*` readers/writers are invisible to the inverted pass). There is **no `dead_code_bulk_eligibility` in `usages/ruby_graph.rs`** and **no Ruby entry-point predicate in `code_quality/dead_code_smells.rs`** — only `ruby_graph_finding` scoring (`:810, 1205–1213`), a supported-language arm (`:466`) and a `\"Ruby\"` label (`:1649`). Ruby therefore has neither PHP's split nor C#'s homeless-predicate problem; the whole proof stays shim-side unchanged.\n\n---\n\n## 3. Ruby-specific hard spots, examined\n\n### 3.1 `RubySemanticIndex` is a three-way shared type and the re-export inverts through a 17-name block\n\n`RubySemanticIndex` (`ruby_graph/resolver.rs:127–662`) holds `analyzer: &'a dyn IAnalyzer`, `ruby: &'a RubyAnalyzer`, `facts: Option<&'a RubySemanticFacts>`, and two `RefCell` memos. It has two constructors — `build(analyzer, ruby, spec)` (target-scoped, populates `facts`) and `build_for_lookup(analyzer, ruby)` (target-free, `facts = None`) — and its consumers are in **three different bands**:\n\n| Consumer | Band | Sites |\n|---|---|---|\n| `ruby_graph/{extractor,inverted}.rs` | moves | 15 + 1 |\n| `ruby/diagnostics.rs:68` | moves with resolver | `build_for_lookup` + `visible_files_from_bounded` |\n| `get_definition/{mod,ruby}.rs` | **parked** | `mod.rs:59–70` imports **17 names** from `ruby_graph`; `ruby.rs:982` calls `build_for_lookup`, and 6 signatures take `&RubySemanticIndex<'_>` |\n\n`get_definition/mod.rs:59–70` is the largest single reach-in block into a language's graph module in the fleet (PHP's equivalent is 6 names). When `ruby_graph/*` moves and `get_definition/ruby.rs` parks, that block must be rebuilt as `use brokk_bifrost_ruby::graph::{…}` — the precedent is exact (`get_definition/{rust,python}.rs`), and decisively **`ruby_graph/*` names `ResolutionSession`, `get_definition`, `get_type` and `DefinitionBatchContext` zero times** (verified by grep across all six files). Ruby has none of C#'s bidirectional-resolver problem and none of Rust's receiver-type back-reference.\n\nThe re-parameterization work is real, though: the struct's `ruby: &RubyAnalyzer` field must become a source trait, because `RubyAnalyzer` cannot leave. Its `visible_files_from` (`:172–192`) reads `zeitwerk_visible_files_for` then `required_files` in a specific closure order to build the transitive closure; `visible_files_from_bounded` (`:194–212`) deliberately **omits** the zeitwerk arm and fails closed above `max_files` (documented at `:186–193` as a diagnostics-only contract). Those two must not converge.\n\n### 3.2 The zeitwerk web, and the four `RubyQueryResolver` pins\n\nFive of `RubyAnalyzer`'s nine `OnceLock`s are zeitwerk cells (`mod.rs:170–174`), built in `imports.rs:178–280`:\n\n- `has_zeitwerk_autoload_conventions` (`:178`) reads `Gemfile`/`Gemfile.lock` through `project_file_contents` and matches `rails|zeitwerk` (`:532`);\n- `zeitwerk_autoload_files` (`:195`) filters `self.inner.all_files()` by `is_zeitwerk_autoload_file` (`:536`);\n- `zeitwerk_consumer_files` (`:213`), `zeitwerk_autoload_code_units` (`:226`);\n- `zeitwerk_reference_files` (`:261`) — **a whole-workspace scan**: for every consumer file, `read_source` + `parse_ruby_tree` + `collect_ruby_reference_identifiers`, indexed by identifier;\n- consumed by `zeitwerk_reference_files_for_identifier` (`:238`) and `zeitwerk_visible_files_for` (`:252`).\n\n`RubyQueryResolver::find_usages` (`ruby_graph.rs:83–170`) carries all four campaign pins, each documented in the struct's own doc comment at `:62–71`:\n\n1. **exact failure strings** — `\"target shape is unsupported\"` (`:97`), `\"target is not Ruby\"` (`:200`), `\"Ruby analyzer is unavailable\"` (`:207`), asserted verbatim (prefixed `\"RubyUsageGraphStrategy: \"`) at `:328, 345, 359`;\n2. **post-budget scan-set augmentation** — `:103–111` takes `scan_scope.candidate_files().clone()` (already truncated by the caller's `max_files`/`max_source_bytes`), then inserts `target.source()` and extends with `zeitwerk_reference_files_for_identifier(&spec.member_name)`, **each still filtered through `scan_scope.allows`**;\n3. **partial-results-on-cancel** — `:115–118` `break`s the loop on `is_cancelled()` and falls through to a success return, where siblings return `FuzzyResult::empty_success`;\n4. **`Resolved`-wrapped `TooManyCallsites`** — `:156–163` returns `GraphUsageOutcome::Resolved(FuzzyResult::TooManyCallsites{…})`, not a failure.\n\nPinned by four in-file tests (`:305, 368, 407, 471`) inside the 270-LOC `#[cfg(test)]` block from `:230`, which builds a real `RubyAnalyzer` over a `tempfile::TempDir`.\n\n**The favorable finding: all four pins live in `ruby_graph.rs`, which is shim-side residue and does not move.** The augmentation ordering is textual in a file that stays. What crosses the boundary underneath it is `RubySemanticIndex::build`, `parse_ruby_tree`, and `RubyFileScan` — plus `zeitwerk_reference_files_for_identifier`, whose *state* stays on the analyzer and whose *body* is in the band-B `impl RubyAnalyzer`. The risk is not losing a pin; it is that `zeitwerk_reference_files`' lazy whole-workspace build currently fires inside the augmentation line, and any refactor that hoists or eagerly materializes it changes when the scan cost is paid without changing any assertion.\n\n### 3.3 `forward_owner_relation_facts` reaches `TreeSitterAnalyzer::fetch_file_state` — no landed precedent\n\n`mixins.rs:67–88`:\n\n```rust\nfn forward_owner_relation_facts(&self, owner: &CodeUnit) -> Vec<RubyOwnerRelationFact> {\n    let Some(state) = self.inner.fetch_file_state(owner.source()) else { return Vec::new(); };\n    if !state.declarations.contains(owner) { return Vec::new(); }\n    state.raw_supertypes.get(owner) … .zip(state.supertype_lookup_paths.get(owner) …)\n        .filter_map(|(raw, encoded)| decode_owner_relation(encoded, raw)).collect()\n}\n```\n\n`fetch_file_state` is `pub(crate)` on `TreeSitterAnalyzer` (`tree_sitter_analyzer.rs:3083`) and returns `Arc<FileState>`, a crate-private struct; the code reads three of its fields directly. **No landed language crate (`brokk-bifrost-{go,rust,python,csharp}`) names `fetch_file_state` or `FileState`.** This one 22-LOC function is the sole feeder of the entire mixin chain:\n\n`forward_owner_relation_facts` → `forward_mixin_specs`/`forward_superclass_targets` → `mixin_relations` + `TypeHierarchyProvider::get_direct_ancestors` → `RubySemanticFacts::build` (`mod.rs:349–385`) → `RubySemanticIndex.facts` → every proven method/constant resolution in the graph scans.\n\nThere is no `ForwardQueryProvider` method for it — `impl_forward_query_provider!` (`global_usage_definition_index.rs:34–64`) exposes only `forward_definition_fqn`, `forward_file_identifier`, `forward_direct_children`, `forward_package_exists`, `forward_fqn_prefix_exists`. Options are (a) leave `forward_owner_relation_facts` shim-side and pass the decoded `Vec<RubyOwnerRelationFact>` across the trait (the Py-2 `collect_bounded` precedent), or (b) add a per-owner raw-supertype accessor to a capability trait. Note the encoding itself (`encode_superclass_relation`/`encode_mixin_relation`/`decode_owner_relation`, `mixins.rs:165–197`) is pure and moves either way — it is the *read* of persisted state that cannot.\n\n### 3.4 The `semantic_model` park — the fleet's largest\n\n`external.rs` (958) + `rbs_artifact.rs` (1,498) + `source_artifact.rs` (1,107) + `gem_artifact.rs` (559) + `dependency_discovery.rs` (655) = **4,777 LOC**, against Go's 4,353, Rust's 4,233, Python's 1,795 and PHP's 0. Together they import ≈35 distinct `semantic_model` items (`AuthoredSemanticModelPack`, `DependencyPackAdapter`, `TypeFact`/`MemberFact`/`HierarchyFact`, `ArtifactProducerLimits`, `BoundedProducerDiagnostics`, `DependencyDiscoveryOutcome`, `read_exact_artifact_while`, …). Two names in this band are **documented public API** (`docs/src/content/docs/rust-library.md:127`, `semantic-model-packs.md:548,598`): `resolve_ruby_semantic_pack_dependencies` and `RubyDependencyPackAdapter`, re-exported at `analyzer/mod.rs:197` → `bifrost-analysis/src/lib.rs:55,62` → root `src/lib.rs:25,32`, and consumed by `tests/suite_semantic/ruby_dependency_semantic_pack.rs:13–14,94,101,111,115`. Whatever the shim's shape, those two paths must survive byte-identically.\n\n`source_artifact.rs` is the only file in this band with an in-seam back-edge to the movable half: `use super::{extract_name_segments, parse_ruby_tree, ruby_call_arguments, ruby_symbol_name}` and `super::declarations::ruby_node_text` (`:3,5`). After the move it imports those five from `brokk_bifrost_ruby`.\n\n### 3.5 Two small lowerings with no current carrier\n\n- **`tree_walk::{TreeWalkAction, walk_tree_iterative}`** (`analyzer/tree_walk.rs:21–…`, `pub(crate)`). Used by `ruby_graph/extractor.rs:4,45,69–102` and `ruby_graph/inverted.rs`, and also by `java_graph/extractor.rs:2,507` and `js_ts_graph/inverted.rs:31,786,860`. The *preorder* family (`WalkControl`, `walk_named_tree_preorder`, `collect_parse_errors`) is already core and re-exported (`tree_walk.rs:11–16` records this explicitly); the enter/exit iterative walker was left behind. Verbatim core lowering of the `test_assertions`/`subtree_contains` class, shared with java and js_ts.\n- **`CloneSyntaxProfile` / `build_tree_sitter_clone_candidate_data`** (`clone_detection.rs`). Shared by ruby/go/rust/kotlin. Go and Rust both left their `clones.rs` in analysis when they extracted, so the precedent is \"stays\" and Ruby's 31 LOC follow it — but that is 17 LOC of pure grammar data stranded shim-side, and lowering the profile type would free four languages at once.\n\n---\n\n## 4. Reverse edges\n\n**Production, framework → Ruby (all allowlisted or assembly; the allowlist is `tests/suite_cross_language/language_reach_in_gate.rs:42,50,68`):**\n\n| Site | What |\n|---|---|\n| `analyzer/mod.rs:31, 195–197` | `mod ruby` (private); `pub use ruby::RubyAnalyzer`; `pub(crate) use ruby::RubySemanticFacts`; `pub use ruby::{RubyDependencyPackAdapter, resolve_ruby_semantic_pack_dependencies}`. **Three lines — the smallest re-export hub of any fleet language** (PHP has 11 names, Rust 8) |\n| `analyzer/usages/mod.rs:34,86` | `pub(crate) mod ruby_graph` + `pub use ruby_graph::RubyUsageGraphStrategy` |\n| `analyzer/lib.rs:55,62` | Crate-root surface (4 Ruby names) |\n| `analyzer/languages.rs:29, 253, 274, 290, 731, 764, 816` | `Language::Ruby` + `Language::Ruby => Some(&ruby::RubySupport)` (assembly); the capability-matrix row at `:816`; doc comments at `:163–170` naming Ruby as the *only* language whose symbol literal text differs from the name it denotes, and `:793` naming Ruby+Scala as bounded-receiver-without-unbounded-type-lookup |\n| `analyzer/multi_analyzer.rs:9, 47, 64, 83, 100, 117, 134, 151, 168, 185, 219, 265` | `AnalyzerDelegate::Ruby` (assembly) |\n| `analyzer/usages/get_definition/mod.rs:59–70, 83, 119, 151–152, 742, 1161–1163, 1337` | **17-name `ruby_graph` import block**, `RubyAnalyzer`, `mod ruby`, the 3-item re-export, the `Language::None | Language::Ruby` guard, the `ruby_site_for_focus` refocus, the dispatch arm |\n| `analyzer/usages/get_type/mod.rs:20,32` | `mod ruby` + `pub(crate) use ruby::resolve_ruby_type_bounded` |\n| `analyzer/usages/get_definition/call_sites.rs:54, 100–112, 362, 422, 461, 709, 749–784, 1013–1058` | ~10 production `Language::Ruby` arms + 5 Ruby-specific free fns (`ruby_call_reference_candidate`, `ruby_bare_call_identifier`, `ruby_callable_object_call`, `ruby_exact_bare_call_identifier`, `ruby_nested_callable_node`) — **the densest per-language residue in this file** |\n| `analyzer/usages/call_relations.rs:434–437` | `ExactCallReferenceGap::RubyCallableObject` + its diagnostic string (a Ruby-named variant in a framework enum) |\n| `analyzer/lexical_definitions.rs:482, 981, 1008, 1048, 1076, 1096, 1157, 1197, 1216, 1241` | 10 `Language::Ruby` grammar arms (shared machinery, parks) |\n| `analyzer/exception_handling.rs:82, 599–629, 827–852` | `analyze_ruby` + `classify_ruby_type`/`classify_ruby_name`, ~55 LOC of rescue-clause knowledge in a framework file |\n| `analyzer/store/mod.rs` (~50 sites) + `analyzer/tree_sitter_analyzer.rs:37, 499, 1791, 6235–6284, 8537` | The `ruby_method_dispatch_modes` persistence path. **All types core; framework-side; stays** |\n| `analyzer/store/epoch.rs:49, 235–247, 481–486` | `.scm` include paths + `lang_epoch!` salt. **No cfg(test) prior-salt pin** |\n| `code_quality/dead_code_smells.rs:466, 810, 1205–1213, 1649` | `ruby_graph_finding` scoring + label. No entry-point predicate |\n| `analyzer/reference_candidates.rs:204, 228` | 2 arms |\n| `analyzer/usages/receiver_query.rs:2750, 2863` | 2 arms, no type reach-in |\n| `analyzer/structural/lexical_environment.rs:820` | 1 arm |\n| `analyzer/usages/workspace_graph.rs:35, 61` | `UsageEcosystem::Ruby` |\n| `analyzer/value_flow/mod.rs:23` | supported set |\n| `searchtools/{selectors, scan_usages, navigation}.rs` | 3 label sites |\n\nNotably absent: **zero Ruby references in `analyzer/common.rs`, `analyzer/symbol_lookup.rs`, `analyzer/index_warmer.rs`, `analyzer/workspace.rs`, `analyzer/global_usage_definition_index.rs`, `analyzer/usages/finder.rs` (one doc-comment mention only, `:45`).** `RubySupport` overrides `declaration_name_range` and `symbol_literal_name` — it is the **only** `LanguageSupport` impl that overrides both, and they call the two registry-era capability conversions `ruby_semantic_identifier_range` (`mod.rs:124`, strips the `:` from `simple_symbol`, unwraps `delimited_symbol`'s `string_content`) and `ruby_symbol_name` (`imports.rs:435`). Both are pure `Node`+`&str` → move cleanly; `ruby_semantic_identifier_range` has an in-seam consumer at `ruby_graph/hits.rs:19`.\n\n**cfg(test)-only Ruby reach-ins** (each verified to sit after its file's `#[cfg(test)]`): `analyzer/usages/get_definition/call_sites.rs:1401–1436` (gate `:1052`; uses `crate::analyzer::ruby::structural::RUBY_STRUCTURAL_SPEC` — the only cross-module reach into `ruby::structural`), `analyzer/languages.rs:882, 1044–1045` (gate `:742`; `AnalyzerDelegate::Ruby(RubyAnalyzer::from_project(…))`), `analyzer/usages/call_relations.rs:1769–1975` (gate `:902`; six Ruby dispatch tests), `analyzer/structural/execution/derived.rs:1155` (gate `:1152`), `code_quality/comment_density.rs:471` (gate `:246`), `analyzer/declaration_range.rs:297–305` (gate `:262`), `analyzer/structural/capabilities.rs:371–380` (gate `:246`), `analyzer/usages/candidates.rs` (gate `:658`), `analyzer/semantic/icfg.rs:3243–3277`, `analyzer/semantic/workspace_oracle/dispatch.rs:2297–2330`, `analyzer/structural/search/tests.rs:7,21,…` (whole file gated). All need `RubyAnalyzer`/`RubyAdapter` re-exported for dev-dependency use, exactly as Go and Python.\n\n**Cross-crate reach-ins: none of substance.**\n- `src/lib.rs:24–25, 32` — facade re-exports (`RubyAnalyzer`, `RubyAnalyzerConfig`, `RubyDependencyApiEvidence`, `RubyDependencyPackAdapter`, `RubyGemApiArtifact`, `resolve_ruby_semantic_pack_dependencies`); the three config types come from **core**, not the seam.\n- `bifrost-lsp`: **zero type reach-ins.** `Language::Ruby` arms only — `handlers/formatting.rs:297,852`, `handlers/hover.rs:123`, `handlers/semantic_tokens.rs:456,486`; `tests/bifrost_lsp_server.rs:4971–4998, 6909–7174` are behavior tests.\n- `bifrost-mcp`: `searchtools_service.rs:333` (`\"rubygems\"` label), two tool-description strings.\n- `src/` root crate: `benchmark/manifest.rs:1466`, `reference_differential/mod.rs:476` — corpus-language strings only.\n- `bifrost-core`, `bifrost-runtime`, `bifrost-policy`, `bifrost-semantic-packs`, `bifrost-nlp`: none.\n\n---\n\n## 5. crates.io deps a `brokk-bifrost-ruby` crate needs\n\n| crate | version in `bifrost-analysis/Cargo.toml` | needed by |\n|---|---|---|\n| `brokk-bifrost-core` | path, `=` pin | everything |\n| `tree-sitter` | 0.25.10 | everything |\n| `tree-sitter-ruby` | **0.23.1** | `declarations.rs:43` (`parse_ruby_tree`), `structural.rs:347`, `ruby_graph/inverted.rs:42` — 3 of the 7 in-repo sites |\n\n**A three-line dependency block — the smallest in the fleet**, one line under C#'s four (core/tree-sitter/grammar/regex). Explicitly **not**: `regex` (zero uses), `rayon` (zero), `moka` (all three caches, nine `OnceLock`s and the `PoolSafeMemo` stay on the analyzer), `serde_json`, `semver`, `flate2`, `goblin`, `walkdir`, `tempfile` beyond dev.\n\n**Grammar notes.**\n1. `tree-sitter-ruby 0.23.1` is a minor generation behind the fleet's 0.25 line — same class as `tree-sitter-php 0.23.11` and `tree-sitter-c-sharp 0.23.1`; the Ruby crate cannot inherit the 0.25 grammar version.\n2. `tree_sitter_ruby` exports one language constant (`LANGUAGE`) plus `HIGHLIGHTS_QUERY`. `RubySupport::parser_language` (`mod.rs:799`) and `RubySupport::highlight_query` (`mod.rs:807`) both answer from the grammar crate and both stay in the shim — **so `bifrost-analysis` keeps its `tree-sitter-ruby` dependency after the move.** This is the landed fleet pattern, not a Ruby quirk: `go/mod.rs:790,798`, `python/mod.rs:891,899`, `csharp/mod.rs:1066,1074` and `rust/mod.rs:1066` all do the same, and analysis still lists all four grammars. `parser_language` ignores its `ParserFlavor` argument.\n3. **`tree-sitter-rbs = \"=0.2.2\"`** (an *exact* pin, the only one among the non-vendored grammars) has exactly **one production site** — `rbs_artifact.rs:38` — and one test at `:1366`. It parks with `semantic_model` and does **not** enter the Ruby crate's first landing. It would enter only if/when the RBS/RBI pack band follows.\n\n---\n\n## 6. LOC accounting\n\nCalibration: Go 8,544 moved / 1,598 prod shim; Rust 16,375 / ~2,540; Python 10,129 / ~1,590 prod (2,471 incl. retained tests); C# 4,410 (Scenario A) / ~1,660; PHP projected 5,935 / ~1,120.\n\n| Band | LOC | Content |\n|---|---:|---|\n| **Movable now** (retarget only) | **~2,597** | `declarations.rs` 916, `tests.rs` prod 605, `structural.rs` 352, `imports.rs` free-fn bands (`:14–127`, `:332–437`, `:478–594`) 337, `ruby_graph/syntax.rs` 142, `mixins.rs:124–216` 93, `mod.rs:67–158` 92, `adapter.rs` bodies + `RUBY_COGNITIVE_CONFIG` ~60 |\n| **Movable after the graph-source + R1 pass** | **~3,208** | `ruby_graph/extractor.rs` 1,193, `resolver.rs` 719, `inverted.rs` 438 (needs `walk_tree_iterative` lowered), `hits.rs` 87; `diagnostics.rs` 343; `imports.rs:129–330` 202; `mixins.rs:15–122` 108 (minus `forward_owner_relation_facts`); `hierarchy.rs:5–47` 43; `mod.rs` `RubySemanticFacts`+`push_ordered_mixin`+2 fqn helpers ~75 |\n| **Parked on `analyzer/semantic`** | **4,587** | `ruby/semantic.rs` (registration macro textually requires the `TreeSitterAnalyzer` field) |\n| **Parked on `semantic_model`** | **4,777** | `rbs_artifact.rs` 1,498, `source_artifact.rs` 1,107, `external.rs` 958, `dependency_discovery.rs` 655, `gem_artifact.rs` 559 — **the fleet's largest pack band** |\n| **Parked on `ResolutionSession`** (movable after W7) | **~2,518** | `get_definition/ruby.rs` 2,437 + `get_type/ruby.rs` 61 + ~20 LOC of `get_definition/mod.rs` wiring. **One residual blocker after W7**: `formal_parameter_slots_for_owner_bounded` at `ruby.rs:587` (single site) — the Py-2 `lexical_scope.rs` forwarder precedent applies verbatim |\n| **Shim floor (production)** | **~1,105** | `mod.rs` shim half ~669 (imports/mod decls 65, struct 23, ctors/forwarders ~80, `CodeUnitIndex` 128, `IAnalyzer` 173, `AnalyzerTestHooks` 33, SPI block 78, `RubyEdgePass` 16, `StructuralReceiverResolver` 31, `RubyDeadCodeBulk` 35); `ruby_graph.rs` prod 229 (incl. the four pins); `ruby_graph/shared.rs` 49; `imports.rs` `ImportAnalysisProvider` 38; `adapter.rs` `LanguageAdapter` shell ~35; `clones.rs` 31; `hierarchy.rs` `TypeHierarchyProvider` 26; `diagnostics.rs` entry ~15; `cache.rs` 12; `tests.rs` `TestDetectionProvider` 1 |\n| **Retained analyzer-bound tests** | **~662** | `ruby_graph.rs:230–499` 270 (the four pins), `mixins.rs:217–454` 238, `tests.rs:606–759` 154 |\n\n**Total 19,513** (16,687 prod / 2,826 in-file test) **+ 49 `.scm`.**\n- Moves ≈ **5,805 (30 % of the seam, 76 % of the seam outside the parks)**.\n- Parked ≈ **11,882 (61 %)** — `semantic.rs` 4,587 + the pack band 4,777 + the definition/type routes 2,518.\n- Analysis residue ≈ **13,708**, of which **~1,105 is production shim** — **below the Go 1,598 floor and level with PHP's ~1,120**, because Ruby has a 3-line re-export hub, no `ImportAnalysisProvider` logic outside cached forwarders, an empty `TestDetectionProvider`, a one-line dead-code predicate, and a `CodeUnitIndex` impl that is 128 LOC of pure `self.inner` forwarding.\n- Predicted analysis-frontend effect at Go's measured 0.47 s/kLOC: **≈ −2.7 s**. If W7 lands first and the definition route follows (net of the `formal_parameter_slots` forwarder), moves rise to ~8,323 → **≈ −3.9 s**.\n\nMove rate outside the parks (76 %) is between PHP's 80 % and Rust's 68 %. Ruby's low *overall* rate is entirely the 4,777-LOC pack band: strip it and the seam is 14,736 with a 39 % move rate, PHP-shaped.\n\n---\n\n## The three hardest couplings\n\n**1. `RubySemanticIndex` is one type serving three bands, and it carries `&RubyAnalyzer`.** It is built per query in the shim (`ruby_graph.rs:102`), per file in the parked definition route (`get_definition/ruby.rs:982`), and per diagnostic pass (`ruby/diagnostics.rs:68`); `get_definition/mod.rs:59–70` imports **17 names** from `ruby_graph` to feed the `use super::*` glob that `get_definition/ruby.rs` resolves its six `&RubySemanticIndex<'_>` signatures through. Its `ruby: &'a RubyAnalyzer` field cannot cross the crate boundary, so it must be re-parameterized on a source trait while `visible_files_from` (`resolver.rs:172`) and `visible_files_from_bounded` (`:194`) keep their *deliberate divergence* — the first walks zeitwerk visibility plus the `require` closure, the second omits zeitwerk entirely and returns `None` above `max_files`, documented at `:186–193` as the diagnostics contract. Favorable: the direction is one-way — **`ruby_graph/*` names `ResolutionSession`, `get_definition`, `get_type` and `DefinitionBatchContext` zero times**, so this is PHP's inversion at 17 names rather than C#'s bidirectional resolver or Rust's receiver back-reference.\n\n**2. The zeitwerk state/decision split, under the four pinned asymmetries.** Five `Arc<OnceLock<…>>` fields (`mod.rs:170–174`) hold whole-workspace derived state built in `imports.rs:178–280` from `Gemfile`/`Gemfile.lock` text plus an `all_files() × read_source × parse_ruby_tree × collect_ruby_reference_identifiers` scan; they are invalidated only wholesale, by `IAnalyzer::update` → `Self::from_inner`. The state must stay shim-side while `imports.rs:129–330`'s decision logic leaves. Three consumers depend on it with different semantics: `RubyQueryResolver`'s **post-budget** augmentation (`ruby_graph.rs:103–111`, extending an already-truncated candidate set, each addition re-filtered through `scan_scope.allows`), `RubySemanticIndex::visible_files_from` (`resolver.rs:175`), and `diagnostics.rs:54`, where `zeitwerk_visible_files_for(file).is_some()` is an **early-return gate** that suppresses the whole diagnostic pass. The four pins (exact strings `:97,200,207`; post-budget augmentation `:103–111`; cancel-keeps-partials `:115–118`; `Resolved`-wrapped `TooManyCallsites` `:156–163`) all live in `ruby_graph.rs`, which does not move, and are asserted at `:305,368,407,471` — so they survive by staying put. The exposure is timing, not assertions: the `zeitwerk_reference_files` build currently fires lazily *inside* the augmentation line, and any hoisting or eager materialization during the trait extraction changes when a whole-workspace parse is paid without failing a single test.\n\n**3. `forward_owner_relation_facts` reads `TreeSitterAnalyzer::fetch_file_state`.** `mixins.rs:67–88` calls a `pub(crate)` accessor (`tree_sitter_analyzer.rs:3083`) returning `Arc<FileState>` — a crate-private struct — and reads `declarations`, `raw_supertypes` and `supertype_lookup_paths` off it directly. **No landed language crate names either symbol**, and there is no `ForwardQueryProvider` method for it (`impl_forward_query_provider!` exposes five fqn/identifier/children/package/prefix queries and nothing about supertypes). This 22-LOC function is the single root of Ruby's whole mixin/ancestry chain — `forward_mixin_specs`/`forward_superclass_targets` → `mixin_relations` + `TypeHierarchyProvider::get_direct_ancestors` → `RubySemanticFacts::build` (`mod.rs:349–385`) → `RubySemanticIndex.facts` → every proven resolution in both scans. Either the read stays shim-side and the decoded `Vec<RubyOwnerRelationFact>` crosses the trait (Py-2's `collect_bounded` precedent), or a raw-supertype accessor is added to a capability trait. The encode/decode pair itself (`mixins.rs:165–197`) is pure and moves regardless.\n\nThree smaller items for completeness: `tree_walk::{TreeWalkAction, walk_tree_iterative}` (`tree_walk.rs`, `pub(crate)`) needs a verbatim core lowering — Ruby's extractor and inverted scan use it, and so do `java_graph/extractor.rs` and `js_ts_graph/inverted.rs`, while the preorder family beside it is already core; `CloneSyntaxProfile` keeps all 31 LOC of `ruby/clones.rs` in analysis (Go's and Rust's did the same, so this is precedent, not a gap); and `analyzed_files_for_language` (`usages/common.rs:45`, analysis `pub(crate)`) is reached from `ruby_graph/shared.rs:18` and `ruby_graph.rs`, both of which stay shim-side — so unlike PHP it needs nothing."}],"stop_reason":"end_turn","stop_sequence":null,"stop_details":null,"usage":{"input_tokens":2,"cache_creation_input_tokens":3833,"cache_read_input_tokens":154178,"output_tokens":21642,"server_tool_use":{"web_search_requests":0,"web_fetch_requests":0},"service_tier":"standard","cache_creation":{"ephemeral_1h_input_tokens":0,"ephemeral_5m_input_tokens":3833},"inference_geo":"not_available","iterations":[{"input_tokens":2,"output_tokens":21642,"cache_read_input_tokens":154178,"cache_creation_input_tokens":3833,"cache_creation":{"ephemeral_5m_input_tokens":3833,"ephemeral_1h_input_tokens":0},"type":"message"}],"speed":"standard"},"diagnostics":null},"requestId":"req_011CdjygmWNFjqewiuvisvKE","attributionAgent":"Explore","type":"assistant","uuid":"92a704a2-36b1-40a5-b414-2921e4082064","timestamp":"2026-08-05T17:18:48.757Z","effort":"high","userType":"external","entrypoint":"cli","cwd":"/home/jonathan/Projects/bifrost2","sessionId":"8f94fdd6-1d55-4f9d-8f78-532b5b220270","version":"2.1.220","gitBranch":"bifrost2","slug":"solve-https-github-com-brokkai-bifrost-i-atomic-hopper"}
