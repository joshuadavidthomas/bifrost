# JVM realm seam census for the stage-3 fleet (2026-08-05)

Agent-run census. Coordinator dispositions: (1) JvmSourceRealm -- approve the
crate-local JvmRealmMember: CodeUnitIndex trait (~40 LOC) with the shim-side
three-downcast builder; this is the campaign's one sanctioned new abstraction,
designed here rather than improvised in-flight. (2) The Scala inversion runs
the graph-source idiom at scale; the analyzer<->graph type cycle is acceptable
inside one crate. (3) The vendor trees, build.rs, cc build-dep, and all ten
exclude entries move; bifrost-analysis re-imports the two vendored LanguageFns
from the JVM crate (the mirror image of the kept crates.io grammar deps), and
the dev-dependency crates.io tree-sitter-scala stays in analysis for its two
deliberately-different-parser call sites. (4) BoundedJavaResolution's enum
lowers to core post-W7 (name it to avoid the BoundedResolution collision);
the ~1,352-LOC Java receiver route STAYS in analysis as framework-set residue
with a named follow-up -- three receiver architectures in one ecosystem is a
fact to record, not flatten. (5) analyzer/jvm/ splits down the middle:
realm.rs moves, the 11.9k artifact band parks; the bifrost-semantic-packs
downstream imports are untouched by parking. Schedule: three passes with
master merges between -- Jvm-1 (skeleton + vendor/build.rs + realm trait +
Java band), Jvm-2 (Scala band), Jvm-3 (Kotlin band + wiring) -- exploiting
the per-language separability the census proves; the two hottest upstream
files are parked and never held.

# JVM realm seam census for the stage-3 fleet (2026-08-05)\n\nTaken at working tip `27d6a022`. Scope: `analyzer/{java,scala,kotlin,jvm}/`, `usages/{java,scala,kotlin}_graph/`, the JVM definition/type routes, the three semantic lowerers, the three structural specs, the three SPI blocks, and the framework-resident JVM implementation sets. Concurrent agent's C++ graph files excluded (no overlap). Same shape as the Go/Rust/Python/C#/PHP/Ruby/C++ predecessors.\n\n**Verdict summary: the JVM realm is 84,364 LOC of seam — 1.71× C++ (49,219) and 3.5× C#. It is the fleet's largest seam by a wide margin and the only one that must ship as one crate, because `JvmSourceRealm` makes Kotlin's import/type/hierarchy/diagnostic routes depend on `JavaAnalyzer` and `ScalaAnalyzer` by name and `java_graph/jvm_scala.rs` makes Java's forward scan depend on `scala_graph::syntax`. Two things no landed fleet crate has: 2.12 M LOC / 68 MB of vendored C grammar with a `cc` build.rs and symbol renaming (Scala + Kotlin), and a `semantic_model` park whose products are `pub` API consumed by a *downstream* crate (`bifrost-semantic-packs`). Projection: ~36.5k moves (43 %), ~4.65k production shim (2.5× the Go 1,598 floor), frontend ≈ −17.2 s; if the definition routes follow after W7, ~53.8k / ≈ −25.3 s — the largest single opportunity in the fleet. Churn: 66 seam-touching `origin/master` commits in seven days, second only to C++'s 72.**\n\n---\n\n## 1. The seam, exact paths and `wc -l`\n\n### `crates/bifrost-analysis/src/analyzer/java/` — 5,810 (5,810 prod / 0 in-file test)\n\n| File | LOC | Purpose |\n|---|---:|---|\n| `declarations.rs` | 1,282 | `JavaVisitor` declaration walk; `parse_tree` (`:771`, `tree_sitter_java::LANGUAGE`), `determine_package_name`, `normalize_java_full_name`, `class_like_body_children_rev`, `is_class_like_declaration_kind`, `node_text` — the six names `jvm/external.rs:1` imports |\n| `mod.rs` | 1,072 | `JavaAnalyzer` struct `:61–67` (4 fields incl. `external_index: Arc<OnceLock<JvmExternalDeclarationIndex>>` `:65`); `impl_forward_query_provider!` `:68`; inherent block `:70–363` (294 LOC / 24 methods); `CodeUnitIndex` `:367–523`; `IAnalyzer` `:524–830`; `AnalyzerTestHooks` `:831–876` (`cfg(any(test, feature = \"test-support\"))`); `JAVA_USAGE_STRATEGY` `:877`; `JavaSupport`/`LanguageSupport` `:879–941`; `JavaEdgePass` `:942–958` (`EdgePassId::Java`); `TypeLookupResolver` `:959–977`; `JavaDeadCodeMemo`+`JavaDeadCodeBulk` `:978–1072` |\n| `imports.rs` | 897 | Two inherent `impl JavaAnalyzer` blocks (`:202–367` 6 methods, `:371–750` 16 methods) + ~351 LOC of free functions |\n| `tests.rs` | 785 | **Production** — Java test detection + JUnit assertion-smell classification, AST-based (no regex) |\n| `structural.rs` | 672 / 551 prod | `JAVA_STRUCTURAL_SPEC` `:33`, `JAVA_KIND_TABLE` `:35`, `impl StructuralSpec` `:340`, `materialization_support()` `:371` |\n| `hierarchy.rs` | 278 | inherent block `:40–170` (8 methods) + free fns |\n| `exceptions.rs` | 211 | `analyze_catch_clause`, `extract_catch_type`, `is_likely_log_only_body` |\n| `adapter.rs` | 209 | `JavaAdapter: LanguageAdapter` `:46–166`, `JAVA_COGNITIVE_CONFIG` `:16`, `query_directory() -> \"resources/treesitter/java\"` `:51` |\n| `clones.rs` | 157 | 4 `LazyLock<HashSet>` token tables + normalizers |\n| `diagnostics.rs` | 132 | `collect_java_semantic_diagnostics` `:16` |\n| `cache.rs` | 115 | `JavaMemoCaches` + 7 weight/estimate fns |\n\n### `crates/bifrost-analysis/src/analyzer/scala/` — 10,567 (9,571 prod / 996 in-file test)\n\n| File | LOC (total / prod) | Purpose |\n|---|---|---|\n| `semantic.rs` | 3,472 / 3,472 | Scala → executable-semantics IR lowerer; `impl_program_semantics_provider!(ScalaAnalyzer, ScalaSemanticLowerer)` `:22` |\n| `declarations.rs` | 1,745 / 1,745 | Declaration walk; publishes `ScalaExportInfo` (core) and `scala_traits` |\n| `mod.rs` | 1,588 / 1,588 | `ScalaForwardOwnerFacts` `:181`; `ScalaAnalyzer` struct `:256–288` — **17 fields**: 4 moka `Cache`, 6 `Arc<OnceLock<…>>`, 2 `PoolSafeMemo`, `external_index`, `type_relations`, and **`project_types: Arc<OnceLock<Arc<usages::scala_graph::ScalaProjectTypes>>>` `:272`** (an analyzer field typed on a graph type); inherent block `:291–823` (533 LOC / **39 methods**, 11 of them `*_limited` leaf splits); `TypeAliasProvider` `:826`; `CodeUnitIndex` `:834–989`; `IAnalyzer` `:990–1198`; `AnalyzerTestHooks` `:1199–1263`; `ScalaSupport` `:1266–1353`; `ScalaEdgePass` `:1354–1370` (`EdgePassId::Scala`); `StructuralReceiverResolver` `:1371–1402`; `TypeLookupResolver` `:1403–1506`; `ScalaDeadCodeMemo`/`Bulk` `:1507–1588` |\n| `imports.rs` | 748 / 692 | inherent block `:21–244` (10 methods) + `scala_import_infos_from_node` and the wildcard/explicit-import tier machinery |\n| `hierarchy.rs` | 689 / 492 | inherent block `:127–422` (12 methods); `ScalaLazyHierarchyIndex` holding `Arc<ScalaProjectTypes>` `:56`; **imports `ScalaNameResolver`/`ScalaProjectTypes` from `usages::scala_graph` `:4`** |\n| `structural.rs` | 537 / 524 | `SCALA_STRUCTURAL_SPEC` `:21`, `SCALA_KIND_TABLE` `:23`, `materialization_support()` `:393` |\n| `tests.rs` | 470 / 470 | **Production** — 9 `LazyLock<Regex>` (ScalaTest/FunSuite/JUnit/Mockito) |\n| `wildcard_imports.rs` | 401 / 401 | Ordered wildcard-import environment |\n| `supertypes.rs` | 280 / 216 | Raw-supertype extraction |\n| `diagnostics.rs` | 245 / 245 | `ScalaSemanticDiagnostic` + `collect_scala_semantic_diagnostics` |\n| `adapter.rs` | 149 / 149 | `ScalaAdapter`, `SCALA_COGNITIVE_CONFIG`, `query_directory() -> \"resources/treesitter/scala\"` |\n| `clones.rs` | 136 / 136 | Normalizers + `parse_scala_tree` (vendored `LANGUAGE` `:110`) |\n| `language.rs` | 107 / 10 | **`unsafe extern \"C\" { fn brokk_bifrost_tree_sitter_scala() }` + `LanguageFn::from_raw`.** 97 LOC of grammar-contract tests (Scala 2 implicit class vs Scala 3 `extension`, empty block lambda, self types, enum bodies) |\n\n### `crates/bifrost-analysis/src/analyzer/kotlin/` — 10,318 (9,478 prod / 840 in-file test)\n\n| File | LOC (total / prod) | Purpose |\n|---|---|---|\n| `semantic/control.rs` | 2,431 / 2,431 | CFG/ICFG lowering |\n| `declarations.rs` | 1,103 / 877 | Declaration walk (no `.scm` — Kotlin is declaration-walk-only) |\n| `mod.rs` | 846 / 846 | `KotlinAnalyzer` struct `:112–137` — **13 fields**: 4 moka `Cache` (two of them **realm-scoped**: `realm_imported_code_units` `:126`, `realm_direct_ancestors` `:133`), 3 `Arc<OnceLock>` (incl. `realm_direct_descendant_index` `:135`), 2 `PoolSafeMemo`, `external_index`; inherent `:140–279`; `CodeUnitIndex` `:288–440`; `IAnalyzer` `:441–632`; `AnalyzerTestHooks` `:633–676`; `KotlinSupport` `:681–784`; `KotlinEdgePass` `:785–801` (`EdgePassId::Kotlin`); `StructuralReceiverResolver` `:802–833`; `TypeLookupResolver` `:834–846`; `highlight_query` `include_str!`s `resources/treesitter/kotlin/highlights.scm` `:777` |\n| `semantic/syntax.rs` | 822 / 822 | |\n| `tests.rs` | 617 / 617 | **Production** — JUnit4/5, kotlin.test, Kotest spec supertypes, Kotest DSL/`should` blocks |\n| `structural.rs` | 603 / 570 | `KOTLIN_STRUCTURAL_SPEC` `:62`, `KOTLIN_KIND_TABLE` `:64` |\n| `imports.rs` | 524 / 427 | inherent `:154–318` (7 methods); **4 signatures take `Option<&JvmSourceRealm<'_>>`** |\n| `semantic/inventory.rs` | 459 / 459 | |\n| `semantic/values.rs` | 432 / 432 | |\n| `types.rs` | 430 / 430 | inherent `:205–430` (12 methods); **5 realm-parameterized signatures**; imports `JvmExternalType` `:34` |\n| `syntax.rs` | 420 / 420 | |\n| `semantic/mod.rs` | 345 / 244 | `impl_program_semantics_provider!(KotlinAnalyzer, KotlinSemanticLowerer)` `:58` |\n| `adapter.rs` | 229 / 229 | `KotlinAdapter`, `KOTLIN_COGNITIVE_CONFIG`, `query_directory() -> \"resources/treesitter/kotlin\"` |\n| `hierarchy.rs` | 184 / 184 | inherent `:35–184` (5 methods); **5 realm-parameterized signatures** |\n| `diagnostics.rs` | 170 / 170 | `collect_kotlin_semantic_diagnostics` `:62`; realm-gated `:66,:117` |\n| `language.rs` | 176 / 13 | Vendored Kotlin FFI binding + 163 LOC of grammar tests (`.kt`/`.kts`, recovery, incremental reparse vs cold parse) |\n| `supertypes.rs` | 159 / 78 | |\n| `clones.rs` | 34 / 34 | |\n| `semantic/tests.rs` | 54 / 54 | |\n\n### `crates/bifrost-analysis/src/analyzer/jvm/` — 12,046 (8,834 prod / 3,212 in-file test)\n\n| File | LOC (total / prod) | Purpose |\n|---|---|---|\n| `external.rs` | 3,882 / 2,337 | `JvmExternalDeclarationIndex` `:56`, `JvmExternalType`, `JvmDependencyPackAdapter`, `resolve_jvm_semantic_pack_dependencies`. Imports **24 names from `semantic_model`** `:14–24`, 6 from `java::declarations`, all five artifact producers, `jclassfile::{attributes,class_file,constant_pool}`, `zip::ZipArchive`, `semver::Version`, and the Kotlin vendored `LANGUAGE` `:2057` |\n| `java_artifact.rs` | 2,728 / 2,395 | `JavaJarPackProducer`; `ZipDirectoryStatus`/`zip_directory_status`; the shared jar/zip reader the other three producers import from |\n| `kotlin_artifact.rs` | 2,059 / 1,714 | `KotlinSourceJarPackProducer` (vendored Kotlin grammar `:336`) |\n| `dependency_discovery.rs` | 1,205 / 843 | Maven POM (`quick_xml`), Gradle lockfile, `GRADLE_INIT_SCRIPT` (a Groovy literal), `discover_build_tools` over **`crate::process::{BoundedProcessRequest, read_limited, run_bounded_process}`** — analysis-resident subprocess execution, shared only with `analyzer/go/dependency_discovery.rs`; `is_jvm_dependency_input` `:228` |\n| `scala_artifact.rs` | 1,191 / 815 | `ScalaSourceJarPackProducer` (vendored Scala grammar `:261`, `:1101`) |\n| `jdk_artifact.rs` | 864 / 613 | `JdkSourceArchivePackProducer`, `JdkSourceArchiveLayout`, `detect_jdk_source_archive_layout` |\n| `realm.rs` | 102 / 102 | **`JvmSourceRealm`** — §3.1 |\n| `mod.rs` | 15 / 15 | Seven `pub(crate) mod` declarations |\n\n### `crates/bifrost-analysis/src/analyzer/usages/` — 42,361 (39,731 prod / 2,630 in-file test)\n\n| File | LOC (total / prod) | Purpose |\n|---|---|---|\n| `scala_graph/inverted.rs` | 11,774 / 11,774 | **The fleet's largest single seam file.** 106 free fns, 17 types. Whole-workspace per-file walk on the W2 contract; **119 `&ScalaAnalyzer`**; `NameResolver`/`ProjectTypes` (re-exported as `ScalaNameResolver`/`ScalaProjectTypes`); imports `tree_sitter_analyzer::FileState`, `usage_facts::CallableFacts`, `GlobalUsageDefinitionIndex`, `UsageFactsIndex`, `same_owner::route_same_owner` (core), `local_inference` (core), 26 names from `scala::`, 47 from `syntax.rs` |\n| `get_definition/scala.rs` | 11,486 / 11,486 | `ScalaDefinitionProvider`, `ScalaLookupCache` `:122`, `resolve_scala_bounded`, `scala_type_lookup_resolution{,_in_session}`; 27 `&ScalaAnalyzer`, 7 `resolve_analyzer`; **`pub` test counters at `:10452–10472`** behind a `cfg(any(test, feature = \"test-support\"))` `thread_local!`; vendored Scala `LANGUAGE` `:11119` |\n| `scala_graph/syntax.rs` | 2,837 / 2,379 | 40+ pure node predicates/extractors reused by `java_graph/jvm_scala.rs` and `get_definition/mod.rs` |\n| `get_definition/java.rs` | 2,725 / 2,725 | **Defines `BoundedJavaResolution<T>` `:11–104`** and `JavaResolutionSession`; `resolve_java_bounded` `:439`; 32 `&JavaAnalyzer`, 4 `resolve_analyzer`; `tree_sitter_java::LANGUAGE` `:716` |\n| `get_definition/kotlin.rs` | 2,428 / 2,428 | `KotlinDefinitionProvider`, `resolve_kotlin_bounded`, `kotlin_type_lookup_resolution{,_in_session}` |\n| `java_graph/resolver.rs` | 1,127 / 1,127 | `TargetSpec`/`TargetKind`, `signature_arity` (exported to `get_definition/mod.rs` as `java_signature_arity`) |\n| `scala_graph/shared.rs` | 1,113 / 1,113 | `ScalaEdgeGraph`, `ScalaQueryResolver`/`ScalaEdgeResolver`, `FileState` reach-in |\n| `kotlin_graph/resolver.rs` | 1,087 / 1,087 | |\n| `kotlin_graph/extractor.rs` | 900 / 900 | Vendored Kotlin `LANGUAGE` `:175` |\n| `java_graph/extractor.rs` | 895 / 895 | `ScanState`/`ScanCtx`; `tree_sitter_java::LANGUAGE` `:111` |\n| `scala_graph/resolver.rs` | 737 / 737 | 16 `&ScalaAnalyzer` |\n| `java_graph/inverted.rs` | 728 / 728 | W2 contract + `FileState` |\n| `kotlin_graph/inverted.rs` | 692 / 692 | W2 contract + `FileState`; vendored Kotlin `LANGUAGE` `:99` |\n| `java_graph/return_type.rs` | 609 / 562 | `pub(super) mod`; 6 `&JavaAnalyzer`; `tree_sitter_java::LANGUAGE` `:253` |\n| `scala_graph.rs` | 598 / 212 | Entry + `pub(crate) use inverted::{NameResolver as ScalaNameResolver, ProjectTypes as ScalaProjectTypes}` `:23`, an 8-name `pub(in crate::analyzer::usages)` re-export `:24–29` |\n| `java_graph/jvm_scala.rs` | 510 / 510 | **The Java↔Scala cross-language scan.** `scan_scala_files_for_java_target`; imports **10 names from `scala_graph::syntax`**, `ScalaAnalyzer`, `resolve_analyzer`; vendored Scala `LANGUAGE` `:77` |\n| `get_type/scala.rs` | 397 / 117 | |\n| `scala_graph/namespace.rs` | 285 / 285 | Lexical type-namespace resolution |\n| `kotlin_graph/shared.rs` | 257 / 257 | 6 `KotlinAnalyzer`, 3 `resolve_analyzer`, `FileState` |\n| `kotlin_graph.rs` | 243 / 159 | |\n| `java_graph.rs` | 201 / 201 | |\n| `java_graph/shared.rs` | 197 / 197 | `JavaQueryResolver`/`JavaEdgeResolver`; `FileState` |\n| `kotlin_graph/hits.rs` | 139 / 139 | |\n| `java_graph/hits.rs` | 117 / 117 | |\n| `get_type/kotlin.rs` | 106 / 106 | |\n| `get_type/java.rs` | 105 / 105 | Imports `BoundedJavaResolution` `:4` |\n| `scala_graph/local.rs` | 68 / 68 | `pub(in crate::analyzer::usages) mod` |\n\n**All three graph modules are `pub(crate) mod` (`usages/mod.rs:25,27,35`)** — unlike C++'s `pub mod cpp_graph`. No JVM graph type crosses a crate boundary.\n\n**Seam total: 84,364 LOC** (78,674 prod / 5,690 in-file test): **Java 16,566 · Scala 39,862 · Kotlin 15,890 · shared `jvm/` 12,046.**\n\n### Framework-resident JVM implementation sets (outside the seam count, ~1,880 LOC)\n- `usages/receiver_query/mod.rs` (3,518 LOC): `analyze_java` `:1394–1948` (~555), eleven `java_*` free fns `:2797–3518` (~722), `PreparedJavaReceiverFile`/`JavaReceiverResolutionInput` `:136–160`, `java_bounded_outcome` `:355–380`. **≈ 1,352 LOC.** Allowlisted with the note \"Follow-up: lower `BoundedJavaResolution` with the extraction plan.\"\n- `usages/get_definition/mod.rs`: 9 names from `scala_graph` + `java_signature_arity` `:34`, `mod java/kotlin/scala` `:124,126,131`, re-exports `:150–172` (incl. **`pub use scala::{reset_scala_active_path_node_visits_for_test, scala_active_path_node_visits_for_test}` `:170–172`**), `SCALA_UNSUPPORTED_*` consts `:361–362`, `ScalaDefinitionContext` `:795–807`, **three Scala-typed `DefinitionBatchContext` fields `:808–810`**, `scala_context`/`scala_package_prefixes`/`scala_lookup_cache_counts` `:936–992`, dispatch arms `:1294,1340,1356`. **≈ 230 LOC** + two in-file tests `:1968–2025`.\n- `usages/get_definition/call_sites.rs`: `java_call_reference_candidate` `:848`, `scala_call_reference_candidate` `:983`, and five Scala call-shape helpers `:368,476,486,494,740`. **≈ 250 LOC.**\n- `usages/get_definition/trace.rs:540–554`: the `Language::Java` arm of `boundary_evidence`, calling `java.external_boundary_evidence`. **≈ 20 LOC.**\n\n### `.scm` and vendored grammar assets\n`resources/treesitter/java/{definitions.scm (148), imports.scm (2), identifiers.scm (1)}` = **151**, `include_str!`'d at `epoch.rs:170–182`.\n`resources/treesitter/scala/{definitions.scm (155), imports.scm (1)}` = **156**, at `epoch.rs:212–220`.\n`resources/treesitter/kotlin/{highlights.scm (378), tags.scm (43)}` = **421** — **neither is in `EMBEDDED_QUERIES`**; `highlights.scm` is `include_str!`'d directly at `kotlin/mod.rs:777` and `tags.scm` is `exclude`d from the package. Kotlin's `lang_epoch!(Kotlin, \"kotlin\", \"treesitter/kotlin/\", …)` therefore selects **zero** query content: its epoch is grammar fingerprint + salt only.\n`vendor/tree-sitter-scala/` (35 MB) and `vendor/tree-sitter-kotlin/` (33 MB): `parser.c` **1,216,246** + **902,345** LOC, `scanner.c` 601 + 965, plus `grammar.js`, `tree-sitter.json`, `LICENSE`, `BIFROST_PATCH.md`/`BIFROST_PROVENANCE.md`, and Scala's `queries/{highlights.scm (260), locals.scm (29)}`. **2,120,157 LOC of C, ~3.5 MB gzipped.**\n\n### Root test suites (names only) — 46,423 LOC in JVM-named files (excluding `javascript_*`)\n`tests/suite_usages/`: `usages_scala_graph_test.rs` (**14,749**, second-largest test file in the repo), `usages_java_graph_test.rs` (4,746), `usage_graph_scala_test.rs` (4,288), `usages_kotlin_graph_test.rs` (2,093), `scala_bounded_receiver_hardening.rs` (928), `usage_graph_java_test.rs` (755), `usage_graph_kotlin_test.rs` (241).\n`tests/suite_analyzers/`: `scala_definition_precedence_test.rs` (3,569), `kotlin_analyzer_test.rs` (891), `kotlin_imports_and_hierarchy.rs` (843), `scala_analyzer_test.rs` (747), `scala_type_hierarchy_test.rs` (711), **`jvm_shared_realm.rs` (570)**, `scala_ordered_wildcard_imports_test.rs` (472), `java_imports_and_hierarchy.rs` (469), `java_import_detail_parity.rs` (323), `scala_parameterized_enum_case_test.rs` (244), `java_field_parity.rs` (238), `java_parity_edges.rs` (233), `java_search_parity.rs` (220), `scala_definition_scope_residual_test.rs` (215), `java_parallel_and_cache.rs` (195), `java_update_parity.rs` (190), `scala_import_test.rs` (189), `java_fixture_parity.rs` (179), `java_declarations_parity.rs` (160), `scala_skeleton_test.rs` (144), `kotlin_test_detection_test.rs` (140), `java_scope_analysis.rs` (127), `java_modules_and_constructors.rs` (122), `java_update_regressions.rs` (121), `java_lambda_parity.rs` (112), `java_test_detection_test.rs` (104), `java_fixture_provenance.rs` (104), `scala_test_detection_test.rs` (98), `java_source_and_skeleton.rs` (88), `scala_source_code_test.rs` (82), `scala_extension_soft_keyword_test.rs` (79), `java_comment_source.rs` (75), `java_signature_normalization.rs` (71), `java_analyzer_smoke.rs` (37).\n`tests/suite_smells/`: `scala_dead_code_smells.rs` (721), `java_dead_code_smells.rs` (648), `java_test_assertion_smells.rs` (633), `java_structural_clone_smells.rs` (548), `scala_php_test_assertion_smells.rs` (411), `kotlin_structural_clone_smells.rs` (298), `scala_structural_clone_smells.rs` (244), `kotlin_dead_code_smells.rs` (138), `kotlin_test_assertion_smells.rs` (114), `java_comment_density.rs` (47).\n`tests/suite_semantic/`: `jvm_standard_library_pack.rs` (511), `semantic_value_kotlin_contract.rs` (403), `kotlin_dependency_semantic_pack.rs` (341), `semantic_value_scala_contract.rs` (327), `kotlin_semantic_diagnostics.rs` (217), `scala_semantic_diagnostics.rs` (155), `java_semantic_diagnostics.rs` (52), `external_artifact_pack.rs` (Java arm).\n`tests/suite_lsp_parity/`: `intellij_java_definition.rs` (244), `intellij_java_find_usages.rs` (169), `intellij_scala_goto_definition.rs` (83).\n`tests/`: `scala_descendant_index_bench.rs` (157).\nFixtures: `tests/fixtures/{testcode-java (66 files), usage-graph-java, usage-graph-scala, usage-graph-kotlin, usage-graph-overloads-java, testcode-git-rank-java, scala-issue-1016, scala-issue-1068}`.\nCross-language suites exercising JVM without a JVM-only name (20+ files), incl. `analyzer_persistence.rs`, `common/value_flow_scenarios.rs`, `suite_symbols/{get_definition_test, searchtools_definition_selectors, searchtools_service, searchtools_list_symbols, searchtools_summary_ranges, searchtools_fuzzy_symbol_lookup}.rs`, `suite_persistence/{multi_analyzer_test, analyzer_capability_parity}.rs`, `suite_semantic/{semantic_model_overlay, generated_behavior_models, semantic_language_conformance}.rs`, `suite_bench_policy/{scan_usages_same_owner_policy, policy_edge_parity_assertions}.rs`, `suite_cross_language/language_reach_in_gate.rs`.\n\n---\n\n## 2. Per-file classification\n\n**(a) core-resident already** — everything the C++ census assumed, plus (verified in `bifrost-core`): **`ScalaExportInfo` and `ScalaExportSelector` (`core/analyzer/model.rs:44,58`)** and **`ParsedFile.scala_exports` / `ParsedFile.scala_traits` (`core/analyzer/parsed_file.rs:30,38,127,135,376,418`)** — W1 landed, so the census-era `ScalaExportInfo` prerequisite named in the fleet plan is **retired**. Also `Language::{Java,Scala,Kotlin}` with their extension tables (`model.rs:66–118`), `has_test_filename_convention`'s three JVM arms (`test_paths.rs:50–62`), `CodeUnitIndex` (`core/analyzer/code_unit_index.rs:23`), `TypeAliasProvider`/`ImportAnalysisProvider`/`TypeHierarchyProvider` (`core/analyzer/capabilities.rs`), `local_inference` (`core/analyzer/usages/mod.rs:17`), `same_owner::route_same_owner`, `ClassRangeIndex`/`FileEdgeScanInput`/`PerFileEdges`/`classify_reference_node`/`build_edge_output` (W2), `PreparedSyntaxTree` (W5), `QueryBatch`/`LimitedQueryRows` (W6), `BoundedDefinitionLookup` (W3), `ResolutionSession` (W7), `node_for_exact_range` and `parse_symbol_path_fq` (Cpp-1), `TypeRelation`/`TypeRelationKind`, `cognitive_complexity::Config`.\n\n**Consequence: the persistence side is already framework-clean.** `store/mod.rs` carries `scala_exports` and `scala_traits` as two SQLite tables with blob-envelope count columns (`:253–254, 3265, 3623–3626, 3764, 3895–4018, 4138–4194, 4305–4342, 4617+`) — ~64 sites — but every type in those signatures is core, exactly Ruby's `ruby_method_dispatch_modes` and C++'s `cpp_template_metadata` shape. All of it stays put and blocks nothing. Same for `tree_sitter_analyzer.rs:501,511,1792,1802,6625–6634` (`scala_exports`/`scala_traits` accumulator fields + `is_scala_trait`/`scala_traits()`).\n\nClean today, zero analysis machinery: **`java/declarations.rs` 1,282, `java/tests.rs` 785, `java/structural.rs` prod 551, `java/exceptions.rs` 211, `java/clones.rs` ~140, `java/diagnostics.rs` 132, `java/hierarchy.rs` free-fn band 147, `java/imports.rs` free-fn band 351, `java/adapter.rs` bodies ~90; `scala/declarations.rs` 1,745, `scala/structural.rs` prod 524, `scala/tests.rs` 470, `scala/wildcard_imports.rs` 401, `scala/imports.rs` free-fn band 468, `scala/diagnostics.rs` 245, `scala/supertypes.rs` prod 216, `scala/hierarchy.rs` free-fn band 196, `scala/clones.rs` 136, `scala/adapter.rs` bodies ~60, `scala/language.rs` prod 10; `kotlin/declarations.rs` prod 877, `kotlin/tests.rs` 617, `kotlin/structural.rs` prod 570, `kotlin/syntax.rs` 420, `kotlin/imports.rs` free-fn band 262, `kotlin/types.rs` free-fn band 204, `kotlin/diagnostics.rs` 170, `kotlin/adapter.rs` bodies ~120, `kotlin/supertypes.rs` prod 78, `kotlin/hierarchy.rs` free-fn band 34, `kotlin/clones.rs` 34, `kotlin/language.rs` prod 13; `scala_graph/syntax.rs` prod 2,379, `scala_graph/namespace.rs` 285, `scala_graph/local.rs` 68, `java_graph/return_type.rs` prod 562.** ≈ **14,910.**\n\n**(b) retarget-only after the landed workstreams** — `java_graph/{hits,extractor,resolver,inverted}.rs`, `kotlin_graph/{hits,extractor,resolver,inverted}.rs`, `scala_graph/{resolver,inverted}.rs` and the resolver half of `scala_graph/shared.rs`, all on the `<Lang>Source` graph-support trait idiom (`bifrost-ruby/src/graph_support.rs` is the landed template). `java_graph/jvm_scala.rs` travels with them because Java and Scala land in the same crate.\n\n**(c) analysis machinery, per file**\n\n| File | Machinery | Covered by an established pattern? |\n|---|---|---|\n| `scala_graph/inverted.rs` | 119 `&ScalaAnalyzer` reaching ~20 accessors; `tree_sitter_analyzer::FileState` (analysis `pub`); `usage_facts::CallableFacts`, `UsageFactsIndex`, `GlobalUsageDefinitionIndex` (analysis); `record_query_parse`/`record_query_walk` production-path counters `:6868,6902`; 26 names from `scala::`; core W2 + `local_inference` + `route_same_owner` | **Mostly** — graph-source idiom at unprecedented width. Two residues: the `FileState` reach-in (Ruby's `forward_owner_relation_facts` precedent: decode shim-side, hand facts across) and the counters (C++'s `test-support` chain) |\n| `scala_graph/shared.rs`, `java_graph/shared.rs`, `kotlin_graph/shared.rs` | `resolve_analyzer`, `FileState`, `<Lang>QueryResolver`/`<Lang>EdgeResolver` | **Yes** — `python_graph.rs`/`cpp_graph/shared.rs` shape; splits at the downcast |\n| `java_graph.rs`, `scala_graph.rs`, `kotlin_graph.rs` | `resolve_analyzer`, `GraphUsageAnalyzer`/`UsageAnalyzer`, `dead_code_bulk_eligibility` | **Yes** — the `php_graph.rs` shape |\n| `{java,scala,kotlin}_graph/inverted.rs` | W2 contract + `build_file_declarations_from_state`/`class_range_index_from_state` (both take `&FileState`) | **Yes** for the walk; the two state decoders stay shim-side |\n| `scala/hierarchy.rs` | imports `ScalaNameResolver`/`ScalaProjectTypes` **from the graph**; holds `Arc<ScalaProjectTypes>` | **Yes within one crate** — analyzer↔graph is bidirectional but both sides land together |\n| `scala/mod.rs` inherent | `TreeSitterAnalyzer<ScalaAdapter>`, `LimitedQueryRows` ×11, 4 moka, 6 `OnceLock`, 2 `PoolSafeMemo`, `build_weighted_cache`, `BulkFileStateSource` | **Yes for the shim half**; ~177 LOC is R1-class |\n| `java/mod.rs`, `kotlin/mod.rs` inherent | same shape | **Yes**; ~179 / ~39 LOC R1-class |\n| `kotlin/{imports,types,hierarchy,diagnostics}.rs` | **`Option<&JvmSourceRealm<'_>>` in 15 signatures** | **NO — §3.1** |\n| `jvm/realm.rs` | `resolve_analyzer::<{Java,Scala,Kotlin}Analyzer>` ×3; `ForwardQueryProvider` (**analysis `pub(crate)`, `global_usage_definition_index.rs:26`**) | **NO — §3.1** |\n| `jvm/external.rs`, `jvm/{java,jdk,kotlin,scala}_artifact.rs`, `jvm/dependency_discovery.rs` | 24 `semantic_model` names; `crate::process` | **Parked** — the C#/Ruby `external.rs` + `*_artifact.rs` precedent, with the §3.3 twist |\n| `{java,scala,kotlin}/semantic*` | `analyzer::semantic::*`, registration macro naming the analyzer type | **Parked** — fleet-wide |\n| `get_definition/{java,scala,kotlin}.rs`, `get_type/{java,scala,kotlin}.rs` | `ResolutionSession`, `use super::*` glob, `ScalaAdapter`, `declaration_range::*` | **Parked**; W7-movable **except** the `ScalaLookupCache`/`BoundedJavaResolution` dependencies (§3.2, §3.4) |\n| `receiver_query/mod.rs` Java route | `BoundedJavaResolution`, `JavaResolutionSession`, `resolve_java_bounded`, `resolve_java_type_bounded` | **NO — §3.2** |\n\n**R1-class inherent language logic — sized.** Eleven inherent `impl <Lang>Analyzer` blocks, **2,598 LOC / 153 methods** total:\n\n| Site | LOC / methods | Disposition |\n|---|---:|---|\n| `java/mod.rs:70–363` | 294 / 24 | ~115 ctors/`clone_with_project` → **shim**; ~179 language logic (`normalize_full_name`, `extract_type_identifiers`, `resolve_type_name_*`, `resolve_type_name_candidates_in_realm`, `package_name_of`, `constructor_context`) |\n| `java/imports.rs:202–367` + `:371–750` | 546 / 22 | **Moves behind the source trait** |\n| `java/hierarchy.rs:40–170` | 131 / 8 | **Moves** |\n| `scala/mod.rs:291–823` | 533 / 39 | ~250 ctors/`from_inner`/memo cells → **shim**; the 11 `*_limited` leaf splits + `forward_owner_facts` + `simple_type_knownness` + `is_full_enum_case_declaration` ≈ 177 move |\n| `scala/imports.rs:21–244` | 224 / 10 | **Moves** |\n| `scala/hierarchy.rs:127–422` | 296 / 12 | **Moves** |\n| `kotlin/mod.rs:140–279` | 139 / 14 | ~100 ctors/hydration counters → **shim**; ~39 moves |\n| `kotlin/imports.rs:154–318` | 165 / 7 | **Moves** (realm-parameterized) |\n| `kotlin/hierarchy.rs:35–184` | 150 / 5 | **Moves** (realm-parameterized) |\n| `kotlin/types.rs:205–430` | 226 / 12 | **Moves** (realm-parameterized) |\n| **Total needing free-function rewrite** | **≈ 2,133 / 138** | vs Rust 2,896/73, C# 1,210/52, Python 916/37, C++ 448/20, Ruby 383/29, PHP 131/15 |\n\nPer language: Java ≈ 856/54, Scala ≈ 697/61, Kotlin ≈ 580/38. **The JVM realm is the fleet's second-largest R1 mass and its largest by method count** — 138 methods vs Rust's 73, because each of the three analyzers repeats the `*_limited` leaf-split and hierarchy/import families.\n\n**`PreparedSyntaxTree` outside the lowerer: none.** All 16 sites are inside `{java,kotlin}/semantic/` and `scala/semantic.rs` (parked). None of the JVM graph or definition files touches it — unlike C++ (27 sites) and Rust.\n\n**moka / global caches / rayon / regex.** **Zero `rayon` in the seam.** No `static` or `LazyLock` cache: the 27 `LazyLock`s are 3 `cognitive_complexity::Config`s (one per adapter), 4 clone-token tables (`java/clones.rs`) and **9 `Regex`es in `scala/tests.rs`** — `regex` is used nowhere else in the seam except `java/mod.rs`'s type-identifier extraction. One `thread_local!` (`get_definition/scala.rs:10452`, `cfg(any(test, feature = \"test-support\"))`). Instance-resident caching: **ScalaAnalyzer 4 moka + 6 `OnceLock` + 2 `PoolSafeMemo`; KotlinAnalyzer 4 moka (two realm-scoped) + 3 `OnceLock` + 2 `PoolSafeMemo`; JavaAnalyzer 1 `OnceLock` + `JavaMemoCaches` (`cache.rs`)**. Invalidation is wholesale via `from_inner`/`clone_with_project`. **Verdict: moka stays in analysis** (Ruby/C++ rule; no JVM analogue of Rust's `RUST_TREES`).\n\n**Dead-code bulk-proof shape.** Java and Scala are the **delegating** shape (`DeadCodeBulkProof` with `EdgePassId::{Java,Scala}`, `JavaDeadCodeMemo`/`ScalaDeadCodeMemo`, `preflight` → `Ready`); **Kotlin has `bulk: None`** — every Kotlin candidate takes the per-symbol path (`languages.rs:398,789`). Three distinct `EdgePassId`s (`Java`, `Scala`, `Kotlin`) in one `LanguageEcosystem::Jvm` — the only ecosystem in the repo where the ecosystem-to-pass mapping is 1:3 (JS/TS is 2:1).\n\n**The vendored-grammar build question.** `crates/bifrost-analysis/build.rs` (95 LOC) compiles both vendored grammars through `cc::Build` with `std(\"c11\")`, `-Wno-unused`, `-utf-8` on MSVC, and **per-grammar `-D` symbol renaming** (7 defines for Scala including `token_name`, 6 for Kotlin) so the vendored parsers cannot collide with crates.io `tree-sitter-scala`/`tree-sitter-kotlin`. A JVM crate needs: `build.rs` verbatim, `[build-dependencies] cc = \"1.2\"`, `tree-sitter-language = \"0.1\"`, the two `vendor/` trees (68 MB, ~3.5 MB gzipped), and the ten `exclude` entries currently in `bifrost-analysis/Cargo.toml:11–22`. **`bifrost-analysis` must then depend on the JVM crate for the two `LANGUAGE` constants**, because four framework/store files reach for them: `store/epoch.rs:369` (`scala_epoch_before_scalachess_fqn_recovery`), `store/mod.rs:9050,10222,10248` (blob-eviction tests), `lexical_definitions.rs:1289` (cfg(test)), and — for the *upstream* crate — `structural/extract.rs:320` uses the crates.io `tree_sitter_scala::LANGUAGE` from the `tree-sitter-scala = \"=0.25.1\"` **dev-dependency**, which is a different grammar from the vendored one and must not be confused with it.\n\n---\n\n## 3. JVM-specific hard spots, examined\n\n### 3.1 `JvmSourceRealm` — exact shape\n\n```rust\n// analyzer/jvm/realm.rs:36\npub(crate) struct JvmSourceRealm<'a> {\n    members: Vec<(Language, &'a dyn ForwardQueryProvider)>,\n}\n```\nOne field. Four methods: `of(&'a dyn IAnalyzer)` (three `resolve_analyzer` calls, `:47–58`), `has_peers_of(Language)`, `peer_types_by_fqn(&str, Language)`, `peer_declarations_by_fqn(&str, Language)`. It **holds nothing** and is built per query — the module doc states the reason explicitly: \"Giving each JVM analyzer a handle to its siblings would make Java, Scala, and Kotlin mutually reference-counted, and every one of those cycles would leak.\"\n\n**This is the entire technical basis for the one-crate decision.** Fifteen signatures across `kotlin/{imports.rs:161,209,225,250 · types.rs:242,273,301,351,362 · hierarchy.rs:45,62,76,94,156 · diagnostics.rs:66,117}` take `Option<&JvmSourceRealm<'_>>`; those files are otherwise clean (a)-class movers. Construction sites: `multi_analyzer.rs:377–381` (`kotlin_realm()`), and the diagnostics/import paths.\n\nTwo sub-problems for a crate:\n1. **`ForwardQueryProvider` is analysis-`pub(crate)`** (`global_usage_definition_index.rs:26`, with the `impl_forward_query_provider!` macro at `:34` applied to all three analyzers). The realm's member type cannot cross the boundary until this trait is lowered or replaced by a crate-local trait.\n2. **`of()` names all three concrete analyzer types.** The C++/PHP precedent puts `resolve_analyzer` sites in the shim, so `of()` stays behind and the crate receives a constructed realm — but then the *type* must be defined in the crate (its consumers are all crate-side) while its only constructor is shim-side. The clean split is: crate defines `JvmSourceRealm<'a> { members: Vec<(Language, &'a dyn JvmRealmMember)> }` over a crate-local `JvmRealmMember: CodeUnitIndex` trait with one method (`forward_definition_fqn`); the shim implements it for the three analyzers and supplies the builder. That is ~40 LOC of new trait, and it is the only new abstraction the seam requires.\n\nBehavior pinned by `tests/suite_analyzers/jvm_shared_realm.rs` (570 LOC, 16 tests) including `kotlin_class_implementing_a_java_interface_resolves_the_java_declaration`, `kotlin_class_extending_a_scala_trait_resolves_the_scala_declaration`, `java_interface_descendants_include_its_kotlin_implementors`, `lone_kotlin_workspace_handle_resolves_without_realm_widening`, and `a_java_name_the_kotlin_file_cannot_see_stays_unresolved`. Plus Kotlin's three realm-scoped caches (`realm_imported_code_units`, `realm_direct_ancestors`, `realm_direct_descendant_index`) whose keying must survive.\n\n### 3.2 `BoundedJavaResolution` — what lowering it takes post-W7\n\n`BoundedJavaResolution<T>` is a three-variant enum defined at **`get_definition/java.rs:11–104`** (`Complete { value, work }`, `Exceeded { work, limit }`, `Cancelled { work }`) with `JavaResolutionSession::finish<T>` `:79` as its only producer. `receiver_query/mod.rs` imports it at `:22` and uses it in 15 places, three of which are *function return types* (`:2915`, `:2969`, `:3016–3031`); `get_type/java.rs:4,61` imports and returns it too.\n\nThe reach-in gate's recorded reason (`language_reach_in_gate.rs:146–157`): \"Java's receiver route: it answers `None` to `structural_receiver` by design and runs a resolution session instead, and `BoundedJavaResolution` carries the work accounting the receiver query threads back through its budget. A capability would put that Java type in a framework signature — a type-level leak, not a method addition. **This is the class of leak that `ScalaExportInfo` was until it moved to the core model layer.** Follow-up: lower `BoundedJavaResolution` with the extraction plan.\"\n\n**Post-W7 the lowering is mechanical but not free.** `ResolutionSession` is already core, so the work/limit/cancellation vocabulary the enum carries has a core home. The enum is a pure generic sum type with no Java content — three variants over `work: usize` and `limit: usize`. Lowering it to `core::analyzer::usages` as `BoundedResolution`-style (core already has a `BoundedResolution` in `get_definition`, so the name needs care) is a verbatim move plus a rename of `JavaResolutionSession::finish`. What does **not** lower with it: the ~1,352 LOC of Java route in `receiver_query/mod.rs` — eleven `java_*` free functions and `analyze_java` — which is a per-language implementation set in a framework file, the `get_definition/mod.rs` class. That block either moves to the crate behind a bounded-receiver capability (a new SPI method returning the core-typed outcome) or stays in analysis and keeps a `resolve_java_bounded` call across the boundary. `receiver_query/tests.rs` pins three of the variants directly (`:1465, 1483, 1500, 1528, 1551`).\n\n### 3.3 The external index and the pack producers\n\n`JvmExternalDeclarationIndex` (`jvm/external.rs:56`) is field #4/#17/#13 on all three analyzers (`java/mod.rs:65`, `scala/mod.rs:259`, `kotlin/mod.rs:119`), each an `Arc<OnceLock<…>>` built lazily by `build_for_project(&self.{java,jvm}_config, self.inner.project())` (`java/mod.rs:277`, `scala/mod.rs:538`, `kotlin/mod.rs:252`) and reset on `clone_with_project`. This is exactly the C#-class analyzer-resident external index, and the C# and Ruby precedents both **park**: `analyzer/csharp/external.rs` (3,811) and `analyzer/ruby/external.rs` (958) + `ruby/{rbs,source,gem}_artifact.rs` all stayed in `bifrost-analysis` because they import `semantic_model`.\n\n`analyzer/jvm/` imports **24 names from `semantic_model`** and `semantic_model/` is 18,103 LOC that stage-3 does not lower. So the whole `jvm/` module parks — 11,929 LOC after `realm.rs` and `mod.rs` — **the fleet's largest `semantic_model` park by 2.5× (Ruby's was 4,777)**.\n\n**The twist with no landed precedent:** five of the parked types are `pub` at `analyzer/mod.rs:146–150` (`JvmDependencyPackAdapter`, `resolve_jvm_semantic_pack_dependencies`, `JavaJarPackProducer`, `JdkSourceArchiveLayout`, `JdkSourceArchivePackProducer`, `KotlinSourceJarPackProducer`, `ScalaSourceJarPackProducer`) and **`crates/bifrost-semantic-packs/src/release_bundle.rs:21–22, 349–366` imports four of them across the crate boundary**, dispatching on `PinnedJvmPackKind::{ScalaSourceJar, KotlinSourceJar}` and `PinnedJdkSourceLayout::{ModulePrefixed, Flat}`. `bifrost-semantic-packs` publishes at order 4, after `bifrost-analysis` at 3 — so as long as the producers park, nothing changes. If they ever move, the DAG needs a JVM crate at order 2 that `bifrost-semantic-packs` also depends on. Also consuming: `analyzer/workspace.rs:12–16, 292–298` (allowlisted as \"Python-specific workspace surface\" — the entry does not mention the JVM arm), `tests/suite_semantic/{jvm_standard_library_pack, external_artifact_pack, kotlin_dependency_semantic_pack}.rs`.\n\n`jvm/dependency_discovery.rs` additionally uses `crate::process::{BoundedProcessRequest, read_limited, run_bounded_process}` (`analysis/src/process.rs`, 678 LOC) to shell out to Maven and Gradle. Only `analyzer/go/dependency_discovery.rs` shares it. Parking `jvm/` sidesteps this entirely; moving it would require lowering `process.rs`.\n\n### 3.4 Java's `summary.rs`\n\n`crates/bifrost-analysis/src/summary.rs` — 252 LOC, `pub mod summary` at `lib.rs:28`, `pub use summary::{RenderedSummary, SummaryInput, summarize_inputs}` at `lib.rs:70`. **Eight signatures typed `&JavaAnalyzer`** (`:2, 19, 30, 56, 103, 119, 156, 176`) plus a hardcoded `.java` extension check `:63` and the error string `\"File is not analyzable by the Java analyzer\"` `:89`. Allowlisted as \"intentionally Java-specific public API.\"\n\n**Grep across the whole workspace and `tests/` finds zero consumers of `summarize_inputs`, `SummaryInput` or `RenderedSummary` outside `summary.rs` and `lib.rs` itself** — not `bifrost-mcp`, not `bifrost-lsp`, not `bifrost-runtime`, not `bifrost-nlp`, not `tests/public_facade_compat.rs`. It is live public API with no in-repo caller. Disposition options are therefore unconstrained by callers: keep the eight signatures as a shim over crate-side rendering helpers, or leave the file untouched. It is the cheapest of the five hard spots and the only one where \"do nothing\" is defensible.\n\n### 3.5 Epoch salts ×3 and the prior-salt pin\n\n| Language | `lang_epoch!` site | Salt tokens | Query content in the salt | Prior-salt helpers |\n|---|---|---:|---|---:|\n| Java | `epoch.rs:245–256` | **10** (latest `static-import-path-kind-2026-08`, #1611) | 151 LOC from `resources/treesitter/java/` | 0 |\n| Scala | `epoch.rs:462–471` | **13** (latest `scalachess-fqn-recovery-2026-07`) | 156 LOC from `resources/treesitter/scala/` | **1** — `scala_epoch_before_scalachess_fqn_recovery` `:367–372` |\n| Kotlin | `epoch.rs:502–510` | **7** (first token `tree-sitter-kotlin-fwcd-c8ac3d26-2026-07` pins the vendored revision) | **none** — no Kotlin entry in `EMBEDDED_QUERIES` | 0 |\n\nRelocating Java's and Scala's `.scm` assets changes the salted content path and requires a bump (the Go/C#/Ruby/C++/PHP precedent, spelled out in each of their salt comments). **Kotlin's does not** — its `treesitter/kotlin/` prefix selects nothing, so moving `highlights.scm` changes only the `kotlin/mod.rs:777` `include_str!` path. A bump is still correct for consistency but is not forced by the mechanism.\n\nScala's single prior-salt pin is asserted by `store/mod.rs:10204` (`let prior_epoch = epoch::scala_epoch_before_scalachess_fqn_recovery()`), and **three `store/mod.rs` blob-eviction tests parse with the vendored `crate::analyzer::scala::language::LANGUAGE` (`:9050, 10222, 10248`)**. Both `compute_epoch::<Scala>` in `epoch.rs:368` and the store tests take the vendored `LanguageFn` directly, so the two grammar constants must remain reachable from `bifrost-analysis` after the vendor trees leave — that is `bifrost-analysis` re-importing them from the JVM crate, the mirror image of the C++/Ruby situation where analysis kept its crates.io grammar dependency.\n\nScala's salt also carries `tree-sitter-scala-bifrost-patches-1016-1068-1073-2026-07` and both Scala and Kotlin carry a \"keep the vendored revision in the salt so conflict-resolution-only grammar changes cannot reuse analysis produced by an older parser\" comment — i.e. **the vendored `parser.c` bytes are part of the invalidation contract**, and `build.rs`'s `cargo:rerun-if-changed` lines are what keep them honest.\n\n### 3.6 Cross-language realm tests\n\nBeyond `jvm_shared_realm.rs` (§3.1), 20 root suites exercise two or more JVM languages in one workspace: `tests/suite_persistence/multi_analyzer_test.rs`, `analyzer_capability_parity.rs`, `tests/analyzer_persistence.rs`, `tests/suite_symbols/{get_definition_test, searchtools_definition_selectors, searchtools_service, searchtools_list_symbols, searchtools_summary_ranges, searchtools_fuzzy_symbol_lookup}.rs`, `tests/suite_semantic/{semantic_model_overlay, generated_behavior_models, semantic_language_conformance, jvm_standard_library_pack, kotlin_semantic_diagnostics}.rs`, `tests/suite_bench_policy/{scan_usages_same_owner_policy, policy_edge_parity_assertions}.rs`, `tests/common/value_flow_scenarios.rs`, `tests/suite_smells/java_dead_code_smells.rs`, `tests/suite_analyzers/kotlin_analyzer_test.rs`. `tests/suite_symbols/searchtools_definition_selectors.rs:609,706` parses with the **crates.io** `tree_sitter_scala::LANGUAGE` — the dev-dependency, not the vendored grammar; that dev-dependency must stay in `bifrost-analysis` or move to the test target explicitly.\n\nThree suites pin the Scala test-support counters through the facade: `tests/suite_symbols/get_definition_test.rs:26757, 26811, 26855` call `brokk_bifrost::usages::get_definition::reset_scala_active_path_node_visits_for_test()` and read the counter at `:26763, 26817, 26861`. The counter's `thread_local!` and both `pub fn`s live in `get_definition/scala.rs:10452–10472` under `cfg(any(test, feature = \"test-support\"))` — so **Scala already has the C++ `test-support` feature-chain shape in place**, but the chain must extend to a new JVM crate exactly as `brokk-bifrost-cpp`'s does (`Cargo.toml` feature + `dev-dependencies` self-reference with the feature on).\n\n---\n\n## 4. Reverse edges\n\n**Framework files inside `bifrost-analysis` naming a JVM concrete type (production):**\n- `analyzer/mod.rs:139,151,203` — `pub use {java::JavaAnalyzer, kotlin::KotlinAnalyzer, scala::ScalaAnalyzer}`; `:146–150` — the five pack-producer re-exports. Allowlisted (\"re-export hub\").\n- `analyzer/languages.rs:721,729,732` — `Language::{Java,Scala,Kotlin} => Some(&{Java,Scala,Kotlin}Support)`. Assembly file.\n- `analyzer/multi_analyzer.rs:2,199,377–381` — `JvmSourceRealm` import, `is_jvm_dependency_input`, `kotlin_realm()`. Assembly file.\n- `summary.rs` — 8 `&JavaAnalyzer`. Allowlisted (§3.4).\n- `analyzer/usages/get_definition/mod.rs` — 9 `scala_graph` names + `java_signature_arity` + three Scala-typed batch fields. Allowlisted.\n- `analyzer/usages/get_definition/call_sites.rs` — `java_call_reference_candidate`, `scala_call_reference_candidate` + 5 Scala helpers. Allowlisted.\n- `analyzer/usages/get_definition/trace.rs:546–554` — `resolve_analyzer::<JavaAnalyzer>` + `external_boundary_evidence`. Allowlisted.\n- `analyzer/usages/receiver_query/mod.rs` — §3.2, ~1,352 LOC. Allowlisted.\n- `analyzer/usages/get_type/mod.rs` — re-export hub. Allowlisted.\n- `code_quality/dead_code_smells.rs` — 43 JVM mentions; the allowlist entry names \"the name-shape scoring the JVM realm shares\" explicitly.\n- `analyzer/exception_handling.rs` — 11 JVM mentions, `Language::*` arms only; **not allowlisted, does not trip the gate** (arms, not modules/types).\n- `analyzer/lexical_definitions.rs` — 80 JVM mentions, all `Language::*` arms **except `:1289`, which imports `crate::analyzer::kotlin::language::LANGUAGE` inside a `#[cfg(test)]` module.** The gate skips `cfg(test)` items (`language_reach_in_gate.rs:344, 382`), so **this reach-in is invisible to the gate** — the same class of gate blindness the C++ census recorded for the searchtools identity block.\n- `code_quality/maintainability_size.rs:330–334` — comments only.\n\n**`cfg(test)`-only, inside `bifrost-analysis` (production-compile-clean but they break on a move):**\n`analyzer/store/mod.rs:8121+` — **87 `JavaAdapter`/`ScalaAdapter` uses** (all inside `mod tests`, first at `:8125,8130`), plus `crate::analyzer::scala::language::LANGUAGE` at `:9050,10222,10248`; `analyzer/tree_sitter_analyzer.rs:7894–9017` — 23 uses (`JavaAdapter`, `JavaAnalyzer`); `analyzer/usages/workspace_graph.rs:524–566` — builds a three-delegate `MultiAnalyzer`; `relevance.rs:2337–2951`; `analyzer/structural/{search/tests/{index_access,mod,execution}.rs, execution/derived.rs:1155–1635, extract.rs:320}`; `searchtools/{summaries.rs:1340, tests.rs}`; `analyzer/usages/receiver_query/tests.rs`; `analyzer/lexical_definitions.rs:1289`.\n\n**Cross-crate:**\n- **`bifrost-lsp/src/lsp/handlers/import_ambiguity.rs:2–3, 17–21, 30, 60, 92`** — imports `JavaAnalyzer`, `ScalaAnalyzer` **and `CSharpAnalyzer`** and dispatches `Language::{Java, CSharp, Scala}` to three per-language ambiguity resolvers. **`CSharpAnalyzer` is still the analysis-side shim type after the C# extraction landed**, so this file already survives one crate move by re-pointing through the shim; the JVM move is the same shape at two types. Fifteen `Language::{Java,Scala,Kotlin}` arms total in `bifrost-lsp`, the rest in `formatting.rs`, `hover.rs`, `semantic_tokens.rs`.\n- **`bifrost-semantic-packs/src/release_bundle.rs:21–22, 349–366`** — four pack producers (§3.3).\n- **`bifrost-nlp`** — `materialize.rs:329, 407, 451, 476, 501` and `chunker.rs:152–174` build `JavaAnalyzer` fixtures. **All `cfg(test)`.**\n- **`bifrost-mcp`** — two `Language::Java` arms in `searchtools_service.rs`, no type reach-ins.\n- **`bifrost-core`** — `Language::{Java,Scala,Kotlin}` in `model.rs:66–118`, `test_paths.rs:50–62`, `structural/routes.rs:487`, `usages/reference_site.rs:377,386`; `ScalaExportInfo`/`ScalaExportSelector` in `model.rs:44,58`; `ParsedFile.scala_exports`/`scala_traits`. **There is no `JavaAnalyzerConfig`, `ScalaAnalyzerConfig` or `KotlinAnalyzerConfig`** — the three analyzers share **`JvmAnalyzerConfig` (`core/analyzer/config.rs:198`)** with `JvmExternalDependencies`, `JvmDependencyDiscoveryConfig`, `JvmStandardLibraryDiscoveryConfig`, `JvmExternalArtifact`, `JvmExternalArtifactOrigin`, `JvmMavenCoordinate`, `JvmDependencyDiscoveryMode`, all already core and all re-exported from `lib.rs:52–54`. **One shared config for three languages is a second, independent argument for one crate.**\n- **`bifrost-runtime`, `bifrost-policy`**: none.\n\n---\n\n## 5. crates.io deps a `brokk-bifrost-jvm` crate needs\n\n| crate | version in `bifrost-analysis/Cargo.toml` | needed by |\n|---|---|---|\n| `brokk-bifrost-core` | path, `=` pin | everything |\n| `tree-sitter` | 0.25.10 | everything |\n| `tree-sitter-java` | **0.23.5** | `java/{declarations,adapter,diagnostics,structural,mod,semantic/tests}.rs`, `java_graph/{extractor,inverted,return_type}.rs`, `get_definition/java.rs` |\n| `tree-sitter-language` | 0.1 | `scala/language.rs`, `kotlin/language.rs` (`LanguageFn::from_raw`) |\n| `regex` | 1.12.2 | `scala/tests.rs` (×9), `java/mod.rs` |\n| **`cc`** | 1.2 | **`[build-dependencies]` — the two vendored grammars** |\n\n**A six-line dependency block plus a build-dependency** — the first fleet crate to need `build.rs`. Explicitly **not**: `rayon` (zero uses in the seam), `moka` (all instance caches stay on the analyzers), `serde`/`serde_json`, `jclassfile`, `zip`, `quick-xml`, `semver`, `walkdir`, `flate2`, `tar`, `goblin` — every one of those is used **only** by `analyzer/jvm/`, which parks.\n\n**Parked-module deps (stay in `bifrost-analysis`, do not move):** `jclassfile 0.6.0` (only `jvm/{external,java_artifact}.rs`), `zip 2.4.2` (only the five `jvm/` files), `quick-xml 0.40` (only `jvm/dependency_discovery.rs`). `semver 1.0.28` is shared with eight other modules. **`jclassfile`, `zip` and `quick-xml` have no consumer outside `analyzer/jvm/` in the entire workspace** — if `jvm/` ever follows `semantic_model` out, all three move with it in one block.\n\n**Grammar notes.**\n1. `tree-sitter-java 0.23.5` is a generation behind the fleet's 0.25 line — the C#/C++/PHP/Ruby class. `JavaSupport::parser_language` (`java/mod.rs:927`) and `::highlight_query` (`:935`, `tree_sitter_java::HIGHLIGHTS_QUERY`, plural) both answer from the grammar crate. Following the five landed precedents, **`bifrost-analysis` keeps its `tree-sitter-java` dependency after the move.**\n2. Scala and Kotlin have **no crates.io grammar in `[dependencies]`.** `tree-sitter-scala = \"=0.25.1\"` appears **only in `[dev-dependencies]`**, used by `structural/extract.rs:320` and `tests/suite_symbols/searchtools_definition_selectors.rs:609,706` — a *different parser* from the vendored one, deliberately.\n3. `bifrost-analysis`'s `exclude` list (`Cargo.toml:11–22`) has ten entries, nine of them vendored-grammar or Kotlin-resource paths; **all ten move to the JVM crate's manifest.** The `!vendor/tree-sitter-scala/queries/highlights.scm` re-include is load-bearing (`ScalaSupport::highlight_query` reads it).\n4. Publication-set impact: the JVM crate's `.crate` archive carries ~3.5 MB gzipped of `parser.c`, well under the crates.io limit; the same payload leaves `brokk-bifrost-analysis`'s archive. `CONTRIBUTING.md:222–290` requires the table, the release DAG, the trusted-publisher bootstrap and `release.yml`'s graph to be updated in the same change; the JVM crate lands at publication order **2** alongside the seven existing language crates.\n\n---\n\n## 6. LOC accounting\n\nCalibration: Go 8,544 moved / 1,598 prod shim; Rust 16,375 / ~1,900 (+1.3k rider); Python 10,129 / ~1,590; C# 8,180 / ~1,660; PHP 5,935 / ~1,120; Ruby 5,805 / ~1,105; C++ 30,321 / ~1,822.\n\n| Band | LOC | Content |\n|---|---:|---|\n| **Movable now** (retarget only) | **~14,910** | Java 4,248 (`declarations.rs` 1,282, `tests.rs` 785, `return_type.rs` prod 562, `structural.rs` 551, `imports.rs` free band 351, `exceptions.rs` 211, `hierarchy.rs` free band 147, `clones.rs` ~140, `diagnostics.rs` 132, `adapter.rs` bodies ~90); Scala 7,203 (`declarations.rs` 1,745, `scala_graph/syntax.rs` prod 2,379, `structural.rs` 524, `tests.rs` 470, `imports.rs` free band 468, `wildcard_imports.rs` 401, `namespace.rs` 285, `diagnostics.rs` 245, `supertypes.rs` 216, `hierarchy.rs` free band 196, `clones.rs` 136, `local.rs` 68, `adapter.rs` bodies ~60, `language.rs` 10); Kotlin 3,399 (`declarations.rs` 877, `structural.rs` 570, `tests.rs` 617, `syntax.rs` 420, `imports.rs` free band 262, `types.rs` free band 204, `diagnostics.rs` 170, `adapter.rs` bodies ~120, `supertypes.rs` 78, `hierarchy.rs` free band 34, `clones.rs` 34, `language.rs` 13); `jvm/realm.rs` query methods ~60 |\n| **Movable after the graph-source + R1 pass** | **~21,639** | `scala_graph/inverted.rs` 11,774, `scala_graph/{shared (moving half) 800, resolver 737}`, `kotlin_graph/{resolver 1,087, extractor 900, inverted 692, hits 139}`, `java_graph/{resolver 1,127, extractor 895, inverted 728, jvm_scala 510, hits 117}`; R1 free-function rewrite 2,133 (138 methods). Three residues: the `FileState` reach-in in all six `inverted.rs`/`shared.rs` files (Ruby's `forward_owner_relation_facts` precedent), the two production-path counters `record_query_parse`/`record_query_walk`, and `ForwardQueryProvider`'s lowering (§3.1) |\n| **Parked on `analyzer/semantic`** | **11,557** | `scala/semantic.rs` 3,472 + `java/semantic/` 3,542 + `kotlin/semantic/` 4,543 (registration macro textually requires the analyzer type) |\n| **Parked on `semantic_model`** | **11,929** | all of `analyzer/jvm/` except `realm.rs` and `mod.rs` — **the fleet's largest pack park, 2.5× Ruby's 4,777** |\n| **Parked on `ResolutionSession`** (movable after W7, except §3.2/§3.4) | **~17,477** | `get_definition/{scala 11,486, java 2,725, kotlin 2,428}` + `get_type/{scala 397, java 105, kotlin 106}` + ~230 LOC of `get_definition/mod.rs` JVM wiring. **Residual blocker after W7: `ScalaLookupCache`, `ScalaDefinitionContext` and `ScalaPackageContextIndex` are `DefinitionBatchContext` fields — this band cannot compile until those types are importable from the crate** |\n| **Shim floor (production)** | **~4,650** | Java ~1,516 (`mod.rs` imports 60 + struct 7 + ctors ~115 + `CodeUnitIndex` 157 + `IAnalyzer` 307 + `AnalyzerTestHooks` 46 + SPI 196; `cache.rs` 115; `adapter.rs` `LanguageAdapter` ~120; `java_graph.rs` 201; `java_graph/shared.rs` 197). Scala ~1,770 (imports ~180 + struct 33 + ctors/memo ~250 + `CodeUnitIndex` 156 + `IAnalyzer` 209 + hooks 65 + SPI 325; `scala_graph.rs` prod 212; `scala_graph/shared.rs` residue ~300; adapter ~90). Kotlin ~1,360 (imports 111 + struct 26 + ctors ~140 + `CodeUnitIndex` 153 + `IAnalyzer` 192 + hooks 44 + SPI 168; `kotlin_graph.rs` prod 159; `kotlin_graph/shared.rs` 257; adapter ~110). Plus `jvm/{mod.rs 15, realm.rs of() ~30}` and the `JvmRealmMember` trait ~40 |\n| **Retained analyzer-bound tests** | **~1,200** | Of the 5,690 in-file test LOC, 3,212 sit in the parked `jvm/` files and travel nowhere; `scala_graph/syntax.rs`'s 458, `kotlin/declarations.rs`'s 226, `get_type/scala.rs`'s 280, `scala/hierarchy.rs`'s 197 and the two grammar-contract suites (`scala/language.rs` 97, `kotlin/language.rs` 163) are analyzer-free and move with their files. What is retained: `scala_graph.rs`'s 386, `kotlin_graph.rs`'s 84, `java_graph/return_type.rs`'s 47 and the `AnalyzerFixture`-based residue |\n\n**Total 84,364** (78,674 prod / 5,690 in-file test) **+ 728 `.scm` + 2,120,157 vendored C.**\n\n- Moves ≈ **36,549 (43 % of the seam, 82 % of the seam outside the parks)** — **the largest absolute move in the fleet, 1.21× C++'s 30,321.**\n- Parked ≈ **40,963 (49 %)** — semantic 11,557 + `semantic_model` 11,929 + definition/type 17,477. **Highest park fraction in the fleet after PHP's 49 %**, driven entirely by `analyzer/jvm/` and `get_definition/scala.rs`.\n- Analysis residue ≈ **47,815**, of which **~4,650 is production shim** — **2.9× the Go 1,598 floor, 2.5× C++'s 1,822**, for one structural reason: three analyzers means three `IAnalyzer` impls (708 LOC), three `CodeUnitIndex` impls (466), three `AnalyzerTestHooks` (155), three SPI blocks (689) and three graph entry-point pairs (1,026). None of that is avoidable; it is the cost of one crate carrying three `LanguageSupport` registrations.\n- Predicted analysis-frontend effect at Go's measured 0.47 s/kLOC: **≈ −17.2 s**. If the definition routes follow after W7 and the `ScalaLookupCache` inversion, moves rise to 54,026 → **≈ −25.4 s**.\n\n**Per-language bands** (each language's moves are separable; the crate is not):\n\n| | Seam LOC | Movable now | After graph+R1 | Moves | Parked | Shim | Frontend |\n|---|---:|---:|---:|---:|---:|---:|---:|\n| **Java** | 16,566 | 4,248 | 4,233 | **8,481** | 6,367 (semantic 3,542 + defn 2,825) | ~1,516 | **−4.0 s** |\n| **Scala** | 39,862 | 7,203 | 14,008 | **21,211** | 15,355 (semantic 3,472 + defn 11,883) | ~1,770 | **−10.0 s** |\n| **Kotlin** | 15,890 | 3,399 | 3,398 | **6,797** | 7,077 (semantic 4,543 + defn 2,534) | ~1,360 | **−3.2 s** |\n| **`jvm/`** | 12,046 | 60 | 0 | **60** | 11,929 | ~45 | ~0 |\n| **Combined** | **84,364** | **14,910** | **21,639** | **36,549** | **40,963** | **~4,650** | **−17.2 s** |\n\nMove rate outside the parks (82 %) is the highest in the fleet (Ruby 76 %, C++ 71 %, Rust 68 %) — Java's, Scala's and Kotlin's language knowledge is overwhelmingly in free functions. **Scala alone (21,211 moved) exceeds Rust's entire pilot-scale move (16,375)**; `scala_graph/inverted.rs` at 11,774 LOC is a single-file move larger than the whole PHP or Ruby seam.\n\n---\n\n## The five hardest couplings\n\n**1. `JvmSourceRealm` is the crate boundary's reason for existing, and its member type is analysis-private.** Fifteen signatures in four otherwise-clean Kotlin files take `Option<&JvmSourceRealm<'_>>` (`kotlin/imports.rs:161,209,225,250`, `types.rs:242,273,301,351,362`, `hierarchy.rs:45,62,76,94,156`, `diagnostics.rs:66,117`). The struct itself is 102 LOC with one field — `Vec<(Language, &'a dyn ForwardQueryProvider)>` — but `ForwardQueryProvider` is `pub(crate)` in `analyzer/global_usage_definition_index.rs:26`, applied to all three analyzers by the `impl_forward_query_provider!` macro at `:34`, and `JvmSourceRealm::of` does `resolve_analyzer::<JavaAnalyzer>`, `::<ScalaAnalyzer>` and `::<KotlinAnalyzer>` in eleven lines. So the *type* must live in the crate (all consumers are crate-side) while its *only constructor* must live in the shim (three downcasts). No landed language crate has a type in this position; the smallest resolution is a crate-local `JvmRealmMember: CodeUnitIndex` trait with one method plus a shim-side builder — about 40 LOC of genuinely new abstraction, which the fleet rules otherwise forbid. Behavior is pinned by 16 tests in `tests/suite_analyzers/jvm_shared_realm.rs` and by three realm-scoped moka/`OnceLock` cells on `KotlinAnalyzer` (`mod.rs:126,133,135`) whose keying must survive. The realm is also *why* one crate: the module doc records that giving each analyzer a sibling handle \"would make Java, Scala, and Kotlin mutually reference-counted, and every one of those cycles would leak.\"\n\n**2. `DefinitionBatchContext` carries three Scala-typed fields, and 167 signatures across the seam take `&ScalaAnalyzer`.** `get_definition/mod.rs:808–810` gives a framework struct `scala_contexts: HashMap<ProjectFile, ScalaDefinitionContext>`, `scala_package_contexts: HashMap<ProjectFile, ScalaPackageContextIndex>` and `scala_lookup_cache: scala::ScalaLookupCache`, with accessors at `:936–962` that call `scala_package_name_of(scala, file)` on a `&ScalaAnalyzer` and `ScalaPackageContextIndex::new(root, source)`, plus a `cfg(test)` build-counter reader at `:989–992` pinned by two in-file tests at `:1968, 2000`. This is the `CppVisibilityIndex` inversion, and **favorably it is one-way** — `scala_graph/*` names `ResolutionSession`, `get_definition`, `get_type` and `DefinitionBatchContext` zero times, and the reach-in is 9 names rather than C++'s 27. What is *worse* than C++: the `&ScalaAnalyzer` density. **119 in `scala_graph/inverted.rs` alone, 27 in `get_definition/scala.rs`, 16 in `scala_graph/resolver.rs`, 167 across the seam** — vs C++'s 22 `&CppAnalyzer`, PHP's 65, C#'s 132. Every one is a graph-source-trait conversion, and the largest of them sits in the fleet's largest single file. `ScalaAnalyzer` additionally holds a *graph* type as field `:272` (`project_types: Arc<OnceLock<Arc<usages::scala_graph::ScalaProjectTypes>>>`) while `scala/hierarchy.rs:4,56` imports `ScalaNameResolver`/`ScalaProjectTypes` back from the graph — a bidirectional analyzer↔graph cycle that is harmless only because both halves land in the same crate.\n\n**3. Two vendored grammars — 2.12 M LOC / 68 MB of C, a `cc` build.rs with symbol renaming, and four framework files that need the grammar back.** `build.rs` (95 LOC) compiles `vendor/tree-sitter-scala/src/{parser.c (1,216,246), scanner.c}` and `vendor/tree-sitter-kotlin/src/{parser.c (902,345), scanner.c}` under thirteen `-D` symbol renames (including Scala's `token_name`) so the vendored parsers cannot collide with the crates.io grammars — and `tree-sitter-scala = \"=0.25.1\"` **is** in `[dev-dependencies]` and **is** used by `structural/extract.rs:320` and `tests/suite_symbols/searchtools_definition_selectors.rs:609,706`, so the collision is not hypothetical. The `LANGUAGE` constants are read at 30 sites, four of which are framework-side and must survive: `store/epoch.rs:369` (Scala's prior-salt helper), `store/mod.rs:9050,10222,10248` (three blob-eviction tests), `lexical_definitions.rs:1289` (a `cfg(test)` reach-in the gate cannot see, `language_reach_in_gate.rs:344,382`), and `structural/extract.rs:320`. So the vendor trees move, `build.rs` and `cc` move, the ten `exclude` entries move, `tree-sitter-language` moves — and `bifrost-analysis` must then import both `LanguageFn`s back from the JVM crate. No landed fleet crate has a `build.rs`; none ships native source; none has a grammar whose *bytes* are named in an epoch salt (`tree-sitter-kotlin-fwcd-c8ac3d26-2026-07`, `tree-sitter-scala-bifrost-patches-1016-1068-1073-2026-07`).\n\n**4. `BoundedJavaResolution` is a Java type in a framework return position, wrapping ~1,352 LOC of Java that lives in `receiver_query/mod.rs`.** The enum is defined at `get_definition/java.rs:11–104` and returned by three functions in `receiver_query/mod.rs` (`:2915, 2969, 3016`); `get_type/java.rs:61` returns it too; `receiver_query/tests.rs` pins all three variants at `:1465–1551`. The gate's own note calls it \"the class of leak that `ScalaExportInfo` was until it moved to the core model layer\" — and `ScalaExportInfo` has since moved, so the precedent is proven. But lowering the *enum* (a pure three-variant sum over `work`/`limit`, mechanical post-W7) does not move the *route*: `analyze_java` (`:1394–1948`, 555 LOC), eleven `java_*` free functions (`:2797–3518`, ~722 LOC) and `PreparedJavaReceiverFile`/`JavaReceiverResolutionInput` are a per-language implementation set in a framework file, and Java is the **only** language that takes this path — `JavaSupport` answers `None` to `structural_receiver` by design (`languages.rs:105`, `:806`) while Scala and Kotlin both implement `StructuralReceiverResolver`. So the JVM realm carries three *different* receiver architectures across three languages in one ecosystem, and only one of them needs the framework file changed.\n\n**5. `analyzer/jvm/` is 11,929 LOC of `semantic_model` park whose products are `pub` API consumed by a downstream crate, and it is the shared state of all three analyzers.** `JvmExternalDeclarationIndex` is a field on `JavaAnalyzer`, `ScalaAnalyzer` and `KotlinAnalyzer` (`:65`, `:259`, `:119`), built through three near-identical `external_declaration_index()` accessors, reset in three `clone_with_project`s, and consumed by `kotlin/types.rs:34` (`JvmExternalType`) and Java's `external_boundary_evidence` (which `get_definition/trace.rs:553` reaches from a framework file). The module parks by the C#/Ruby precedent — `external.rs` imports 24 `semantic_model` names, `dependency_discovery.rs` shells out through analysis-resident `crate::process` — but unlike C# and Ruby, **five of the parked types are exported at `analyzer/mod.rs:146–150` and four are imported by `bifrost-semantic-packs/src/release_bundle.rs:21–22` and dispatched at `:349–366`**. No landed park is public API of a downstream crate. Three crates.io dependencies (`jclassfile`, `zip`, `quick-xml`) exist in the workspace *solely* for this module and cannot leave with the language crate. And the park is asymmetric in a way the other seams are not: `realm.rs` (102 LOC) sits in the same directory as 11,929 LOC of parked artifact machinery, so `analyzer/jvm/` splits down the middle rather than moving or parking as a unit.\n\n**Two smaller items for completeness:** `crates/bifrost-analysis/src/summary.rs` (252 LOC, 8 `&JavaAnalyzer` signatures, `pub` at the crate root, allowlisted as \"intentionally Java-specific public API\") has **zero in-repo consumers** outside its own `lib.rs:70` re-export — grep finds `summarize_inputs`/`SummaryInput`/`RenderedSummary` nowhere in `bifrost-mcp`, `bifrost-lsp`, `bifrost-runtime`, `bifrost-nlp` or `tests/`, including `public_facade_compat.rs`; and `bifrost-lsp/src/lsp/handlers/import_ambiguity.rs` names `JavaAnalyzer`, `ScalaAnalyzer` **and `CSharpAnalyzer`** in one three-arm dispatch (`:17–21`), which means the C# extraction already proved this file re-points cleanly through the shim — the JVM move repeats a solved problem at two more types.\n\n---\n\n## Schedule risk: upstream churn\n\n`origin/master` commits touching each language's seam in the last seven days:\n\n| cpp | jvm (combined) | rust | scala | kotlin | java | csharp | python | go | js_ts | php |\n|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|\n| 72 | **66** | 51 | 32 | 29 | 28 | 26 | 25 | 24 | 20 | 17 |\n\nThe JVM realm's 66 is **second only to C++**, and unlike Rust's 51 (in-campaign) it is upstream product work. It also has a shape C++ does not: the three per-language streams are largely disjoint, so a single-pass extraction holding all four directories simultaneously accumulates conflicts from three independent workstreams.\n\nFour-week churn on the largest movable files: `get_definition/scala.rs` 12 commits (last touched 2026-08-03), `scala_graph/inverted.rs` 11 (2026-08-03), `scala_graph/syntax.rs` 10 (2026-08-04), `jvm/java_artifact.rs` 10 (2026-08-04, **parked**), `scala/mod.rs` 9 (2026-08-05), `jvm/external.rs` 9 (2026-08-05, **parked**), `get_definition/java.rs` 7 (2026-08-05), `get_definition/kotlin.rs` 6 (2026-08-03), `scala/declarations.rs` 4 (2026-08-03).\n\nTwo facts favor a split schedule. First, **the two hottest recently-touched files (`jvm/external.rs`, `jvm/java_artifact.rs`, 19 commits between them) are in the parked band** and never need to be held. Second, the three languages' movable masses are genuinely separable — Java 8,481, Kotlin 6,797, Scala 21,211 — even though the crate is not. The Kotlin band is the only one with a hard ordering constraint: its four realm-parameterized files cannot compile until §3.1's `JvmRealmMember` trait exists, and that trait cannot exist until Java's and Scala's analyzer types are reachable from the crate.\n\nLowest-churn seam files (safe to move first): `kotlin/clones.rs`, `scala_graph/local.rs`, `java/cache.rs`, `java/exceptions.rs`, `kotlin/syntax.rs`, `scala/wildcard_imports.rs`, `java_graph/hits.rs`, `kotlin_graph/hits.rs`, `scala/clones.rs`, `java/clones.rs`, `jvm/realm.rs`, `scala/language.rs`, `kotlin/language.rs`."}],"stop_reason":"end_turn","stop_sequence":null,"stop_details":null,"usage":{"input_tokens":2,"cache_creation_input_tokens":2261,"cache_read_input_tokens":194415,"output_tokens":33208,"server_tool_use":{"web_search_requests":0,"web_fetch_requests":0},"service_tier":"standard","cache_creation":{"ephemeral_1h_input_tokens":0,"ephemeral_5m_input_tokens":2261},"inference_geo":"not_available","iterations":[{"input_tokens":2,"output_tokens":33208,"cache_read_input_tokens":194415,"cache_creation_input_tokens":2261,"cache_creation":{"ephemeral_5m_input_tokens":2261,"ephemeral_1h_input_tokens":0},"type":"message"}],"speed":"standard"},"diagnostics":null},"requestId":"req_011CdkPP1gSz5KpXGX4DErPM","attributionAgent":"Explore","type":"assistant","uuid":"125f1712-a15b-410d-bac3-7e88f68ded4d","timestamp":"2026-08-05T22:31:30.472Z","effort":"high","userType":"external","entrypoint":"cli","cwd":"/home/jonathan/Projects/bifrost2","sessionId":"8f94fdd6-1d55-4f9d-8f78-532b5b220270","version":"2.1.220","gitBranch":"bifrost2","slug":"solve-https-github-com-brokkai-bifrost-i-atomic-hopper"}
