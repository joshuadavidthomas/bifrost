# C++ seam census for the stage-3 fleet (2026-08-05)

Agent-run census. Coordinator dispositions: (1) The CppVisibilityIndex
inversion is one-way (PHP/Ruby class): the index re-parameterizes its
cpp field on a C++ source trait and moves; DefinitionBatchContext keeps its
six fields, importing the type from the crate. (2) The searchtools identity
block: pure types and the classifier move; the evidence root
(cpp_header_body_files_are_related) keeps a thin shim wrapper owning the
downcast and passing the include index in; the gate blindness to this block
is recorded, not fixed (suffix-rule limitation, same class as the
GO_MODULE_SCOPE_SEGMENT finding). (3) CppAuthoritativeUsageBatch stays
shim-side in shared.rs with its public path unchanged; the crate gains a
test-support feature for the production-path counters (first in the fleet;
chain it exactly like core's). Schedule: the churn risk dominates -- run C++
as two back-to-back passes (Cpp-1: low-churn files + declarations + R1 +
identity/compile-context; Cpp-2: the graph trio as one body) with a master
merge between, and do not let either pass hold the five hot files across
days.

# C++ seam census for the stage-3 fleet (2026-08-05)\n\nTaken at the committed tip `e26d6ee7` (working tree clean). Scope excludes Ruby files and `rust_graph/inverted.rs` (concurrent agent). Same shape as the Go/Rust/Python/C#/PHP/Ruby predecessors.\n\n**Verdict summary: C++ is 49,219 LOC of seam — twice the next-largest (C# 24,311) and 2.5× Ruby. It needs no new shared prerequisite beyond the `walk_tree_iterative` lowering already landing on the Ruby pass, and its `semantic_model` park is zero (PHP-shaped: no `external.rs`, no pack adapter). It has three couplings with no landed precedent, and one non-technical risk that dominates the schedule: `origin/master` landed 72 commits touching the C++ seam in the last seven days — more than Rust (51, in-campaign) and 3.5× Ruby (20). Every one of the five largest seam files was rewritten today. Projection: ~30.3k moves (62 %), ~1.82k production shim (above the Go 1,598 floor), frontend ≈ −14.3 s — the largest single opportunity in the fleet and the highest merge-conflict exposure.**\n\n---\n\n## 1. The seam, exact paths and `wc -l`\n\n### `crates/bifrost-analysis/src/analyzer/cpp/` — 19,808 (17,441 prod / 2,367 in-file test)\n\n| File | LOC (total / prod) | Purpose |\n|---|---|---|\n| `declarations.rs` | 10,501 / 8,732 | **208 free functions.** `CppVisitor`, FQ/namespace naming over `FqName`/`SegmentInterner` (`cpp_segment` `:16`, `cpp_push_type_chain` `:39`, `cpp_class_fq` `:92`), and the entire error-recovery corpus: `recover_exported_class_declaration` `:287`, `recover_malformed_exported_multiple_base_class` `:325`, `fragmented_export_body_region` `:426`, `cpp_sentinel_recovered_classes`, `recovered_macro_return_type_node`, `cpp_export_macro_token` `:242`, `is_recovered_exported_class_container` `:1085`, `recovered_exported_class_has_body` `:1127`, `extract_function_info` `:3502`, `split_structured_templated_cpp_name` `:3946`. Imports `structural::materialization::{GenerationKind, MaterializationRecord}` (#1637) |\n| `semantic.rs` | 4,927 / 4,927 | C++ → executable-semantics IR lowerer; `impl_program_semantics_provider!` |\n| `mod.rs` | 1,371 / 1,371 | `CppAnalyzer` struct + **17 fields** `:75–108` (6 of them `cfg(test)`/`test-support` counters); `ReconciledDefinitionIndex` `:120–128`; `impl_forward_query_provider!` `:129`; **inherent block 1 `:131–467`** (10 methods, incl. the #1134 reconciliation `:207–422`); **inherent block 2 `:468–719`** (35 methods, 24 of them test hooks); `CodeUnitIndex` `:720–933`; `IAnalyzer` `:934–1126`; `AnalyzerTestHooks` `:1127–1160`; `TypeAliasProvider` `:1161–1169`; `CppSupport`/`LanguageSupport` `:1172–1256`; `CppEdgePass` `:1257–1271`; `StructuralReceiverResolver` `:1272–1303`; `CppDeadCodeMemo`+`CppDeadCodeBulk` `:1304–1371` |\n| `tests.rs` | 540 / 285 | **Production** — `cpp_contains_tests`, `detect_cpp_test_assertion_smells`; 6 `LazyLock<Regex>` (gtest/Catch2/Boost.Test/MSTest) |\n| `imports.rs` | 425 / 396 | `TestDetectionProvider` (`:5`, empty), `ImportAnalysisProvider` `:7–103`; **inherent block `:104–135`** (3 methods); `IncludeTargetIndex` `:136–247`; `parse_quoted_include`/`parse_include_path`/`resolve_include_targets{,_with_index}`/`include_paths`/`quoted_include_paths` `:248–396` |\n| `structural.rs` | 419 / 406 | `CPP_STRUCTURAL_SPEC`, `CPP_KIND_TABLE`, `impl StructuralSpec`, `materialization_support() -> &CPP_MATERIALIZATION_SUPPORT` (`:266`) |\n| `identity.rs` | 285 / 285 | **The searchtools identity block.** `CppCallableUnitRole`, `CppOccurrenceRole`, `CppOccurrenceClassifier` (own `Parser`), `cpp_callable_unit_role`, `cpp_indexed_callable_linkage`, `cpp_callable_definitions_share_identity_evidence`, `cpp_is_range_for_binding_name`, `cpp_header_body_files_are_related` (**downcasts `resolve_analyzer::<CppAnalyzer>` at `:211`**), `cpp_occurrence_role_for_range` |\n| `diagnostics.rs` | 283 / 209 | `collect_cpp_semantic_diagnostics` (`:14`) — gated on `CppCompileContext`, the fleet's only compile-database-gated diagnostic pass |\n| `reconcile.rs` | 283 / 139 | The #1134 pure decision function: owner segments + candidate namespaces + include-visible class table → canonical `(package, owner-chain, member)`. **Zero imports.** Analyzer-free and I/O-free by design |\n| `compile_context.rs` | 253 / 170 | `CppCompileContexts::load(&dyn Project)` over `compile_commands.json` (`serde`); `CppCompileContext` |\n| `hierarchy.rs` | 256 / 256 | **inherent block `:4–143`** (4 methods); `TypeHierarchyProvider` `:167–183`; free fns `:144–166`, `:184–256` |\n| `clones.rs` | 132 / 132 | Token/AST-label normalization `:45–131` + `build_clone_candidate_data` entry |\n| `adapter.rs` | 92 / 92 | `CppAdapter: LanguageAdapter` (`:33`), `CPP_COGNITIVE_CONFIG` (`:9`), query dir `\"resources/treesitter/cpp\"`; `parse_file` is a 19-LOC shell over `CppVisitor` |\n| `cache.rs` | 41 / 41 | `weight_code_unit_set_by_file`, `weight_code_unit_vec_by_file`, `weight_project_file_set` |\n\n### `crates/bifrost-analysis/src/analyzer/usages/` — 29,411 (26,670 prod / 2,741 in-file test)\n\n| File | LOC (total / prod) | Purpose |\n|---|---|---|\n| `cpp_graph/resolver.rs` | 11,018 / 9,304 | **197 functions, ~40 structs/enums.** `TargetSpec`/`TargetKind`, `EffectiveUsingIndex`, `SourceUsingIndex`/`ProjectUsingIndex`, the macro machinery (`MacroEnvironment`, `MacroEvent`, `MacroDefinition`, `PreprocessorGuard`, `MacroIncludeProtection`, `ParsedMacroReplacement`), `IncludeGraph` `:4523`, and **`VisibilityIndex<'a>` `:681–744` — 38 fields, 18 of them `Mutex`/`RwLock` memo caches, holding `cpp: &'a CppAnalyzer` + `analyzer: &'a dyn IAnalyzer`**. 16 `&CppAnalyzer` signatures, 85 `&dyn IAnalyzer`. Two `thread_local!` (`:9086`, `:9158`) |\n| `cpp_graph/extractor.rs` | 8,489 / 8,047 | Per-symbol forward scan; `ScanState`/`ScanCtx`; imports `resolver::*` (glob), 9 items from `hits.rs`, 4 recovery names from `cpp::declarations`. One `thread_local!` (`:33`, cfg(test)) |\n| `get_definition/cpp.rs` | 7,087 / 6,977 | `CppNavigationIndex` (`:23`), `declaration_at_offset`, `resolve_cpp`, `resolve_cpp_bounded`, `cpp_type_lookup_resolution_in_session`, `select_navigation_targets`, `CPP_UNPROVEN_LINK_UNIT_DIAGNOSTIC`. **181 functions; 48 signatures take `&CppVisibilityIndex`**; 19 `ResolutionSession` refs; 8 `CppAnalyzer` |\n| `cpp_graph/inverted.rs` | 1,235 / 1,175 | Whole-workspace per-file walk; on the W2 contract (`FileEdgeScanInput`/`PerFileEdges`/`ClassRangeIndex`/`classify_reference_node`/`build_edge_output`/`parse_and_collect`, all core). Imports **10 names from `extractor.rs` and 30 from `resolver.rs`**. Uses `tree_walk::{TreeWalkAction, walk_tree_iterative}` (`:50`) |\n| `cpp_graph/shared.rs` | 501 / 321 | `CppQueryResolver`/`CppEdgeResolver` (the `resolve_analyzer::<CppAnalyzer>` sites) and **`pub struct CppAuthoritativeUsageBatch<'a>`** (`:64`), holding `CppQueryResolver` + `VisibilityIndex<'a>` |\n| `cpp_call_match.rs` | 461 / 290 | **C++-only module outside `cpp_graph/`.** `CppArgType`, `cpp_signature_param_types`, `cpp_filter_candidates_by_args`, `cpp_literal_arg_type`, `cpp_split_top_level_commas`, `normalize_cpp_type_name`, `cpp_type_text_pointer_depth`. Imports only `CodeUnit` + two `cpp_*` text helpers |\n| `cpp_graph.rs` | 243 / 179 | `build_cpp_usage_edges{,_weights}`, `CppDeadCodeBulkEligibility` + `dead_code_bulk_eligibility` `:76–99`, `is_cpp_global_main` `:101`, `CppUsageGraphStrategy`; **a 5-name `extractor` + 18-name `resolver` outward re-export block `:20–38`** plus `pub use shared::CppAuthoritativeUsageBatch` `:39` and `cpp_type_owner_for_test` `:42` |\n| `cpp_graph/hits.rs` | 229 / 229 | `push_hit`/`push_type_hit`/`push_definition_hit`/`push_self_receiver_hit`/`push_recursive_reference_hit`/`push_unproven_*`/`enclosing_context` |\n| `get_type/cpp.rs` | 80 / 80 | `resolve_cpp_type_bounded` — thin, no test module |\n| `cpp_graph/syntax.rs` | 68 / 68 | `explicit_qualified_callable_value` + one helper. **Zero analysis imports** |\n\n**Seam total: 49,219 LOC** (44,111 prod / 5,108 in-file test) **+ 94 LOC of `.scm`** — `resources/treesitter/cpp/{definitions.scm (91), imports.scm (2), identifiers.scm (1)}`, `include_str!`'d at `store/epoch.rs:207–216`, `lang_epoch!(Cpp, \"cpp\", \"treesitter/cpp/\", …)` at `:327–332` with a **37-token salt, 12 of them added in 2026-08** by the recovery campaign. Relocation requires a bump — and unlike PHP's single prior-salt pin, **C++ carries five** (§3.5).\n\n### C++ root test suites (names only) — 33,704 LOC in C++-named files\n`tests/suite_usages/`: `usages_cpp_graph_test.rs` (**21,253** — the largest test file in the repo), `usage_graph_cpp_test.rs` (1,180), `usages_cpp_sentinel_visibility_test.rs` (414), `usages_cpp_recursive_free_function_test.rs` (247), `usages_cpp_macro_sentinel_owner_test.rs` (222), `usages_cpp_macro_sibling_class_test.rs` (136), `usages_cpp_macro_sentinel_receiver_test.rs` (117), `usages_cpp_macro_return_test.rs` (103).\n`tests/suite_analyzers/`: `cpp_analyzer_test.rs` (2,417), `cpp_macro_sentinel_recovery_test.rs` (985), `cpp_macro_call_arity.rs` (845), `cpp_type_hierarchy_test.rs` (378).\n`tests/suite_issues/`: `issue_1185_cpp_member_calls.rs` (1,088), `issue_1184_cpp_file_local_globals.rs` (528), `issue_1121_cpp_nested_class_out_of_line.rs` (471), `issue_1120_cpp_bare_call_lexical_scope.rs` (413), `issue_1092_cpp_header_source_identity.rs` (322), `issue_1093_cpp_using_namespace_owner.rs` (265).\n`tests/suite_smells/`: `cpp_dead_code_smells.rs` (556), `cpp_test_assertion_smells.rs` (337), `cpp_structural_clone_smells.rs` (241).\n`tests/suite_cross_language/`: `code_query_cpp_receiver.rs` (898). `tests/suite_semantic/`: `semantic_value_cpp_contract.rs` (288).\nFixtures: `tests/fixtures/{testcode-cpp, usage-graph-cpp}` plus **9 standalone `cpp_macro_sentinel_*.h` fixtures** at `tests/fixtures/` root (abseil/nlohmann/log4cxx-derived), all added by the recovery campaign.\nCross-cutting suites exercising C++ without a C++ name (22 files), incl. `analyzer_persistence.rs`, `common/{mod,value_flow_scenarios}.rs`, `issue_1175_scan_usages_reparse.rs`, `mcp_property_fuzzer_service.rs`, `suite_semantic/{reference_differential, reference_differential_backlog_test, icfg_contract, semantic_language_conformance}.rs`, `suite_symbols/{get_definition_test, searchtools_definition_selectors, searchtools_service, searchtools_list_symbols, searchtools_summary_ranges, searchtools_fuzzy_symbol_lookup, most_relevant_files}.rs`, `suite_persistence/{analyzer_capability_parity, multi_analyzer_routing}.rs`, `suite_issues/{issue_1158_boundary_claim_gate, issue_1162_separator_aware_enclosing_scope}.rs`, `suite_bench_policy/scan_usages_same_owner_policy.rs`, `suite_usages/random_alias_recovery_test.rs`.\n\n---\n\n## 2. Per-file classification\n\n**(a) core-resident already** — everything Ruby's census assumed, plus (verified in `bifrost-core`): the **`CppTemplateMetadata` family** (`core/analyzer/model.rs:1555–1603` — `CppTemplateParameterKind`, `CppTemplateExpression`, `CppTemplateTerm`, `CppTemplateParameterMetadata`, `CppTemplateAliasTargetMetadata`, `CppTemplateMetadata`), `ParsedFile.cpp_template_metadata` (`core/analyzer/parsed_file.rs:36,367,416`), the cache migration `0008-cpp-template-metadata.sql` (`core/src/cache_db.rs:41`), `CPP_MATERIALIZATION_SUPPORT` (`core/analyzer/structural/materialization.rs:471`), `ClassRangeIndex` (`core/analyzer/usages/inverted_edges.rs:47`), `route_same_owner` (`core/analyzer/usages/same_owner.rs:36`), `PreparedSyntaxTree` (W5), `QueryBatch`/`LimitedQueryRows` (W6), `BoundedDefinitionLookup` (W3), `ResolutionSession` (W7), `StructuredTypeIdentityBuilder`, `profiling::scope`, `path_utils`/`path_normalization`, `Language::Cpp`'s `symbol_path.rs:30–58` operator-token parsing.\n\n**Consequence worth stating plainly: C++ has no `ScalaExportInfo`-class prerequisite and no `semantic_model` park.** The `cpp_template_metadata` side table threads through `store/mod.rs` (~50 sites, its own SQLite table `unit_cpp_template_metadata`, blob-envelope count column at `:225–227`, bulk readers) and `tree_sitter_analyzer.rs` (5 sites) — but every type in those signatures is core, so all of it is framework-side and stays put without blocking anything. This is exactly Ruby's `ruby_method_dispatch_modes` shape.\n\nClean today, zero analysis machinery: **`declarations.rs` prod (8,732), `reconcile.rs` prod (139), `cpp_call_match.rs` prod (290), `structural.rs` (406), `tests.rs` prod (285), `compile_context.rs` prod (170), `cpp_graph/syntax.rs` (68), `imports.rs`'s free-fn + index band (261), `clones.rs` normalizer (~120), `adapter.rs` bodies + `CPP_COGNITIVE_CONFIG` (~60).**\n\n**(b) retarget-only after the landed workstreams** — `cpp_graph/hits.rs` (core `usages::common` + `CodeUnitIndex::enclosing_code_unit`), `cpp_graph/inverted.rs`'s W2 half (all six imported `inverted_edges` names are core; needs `walk_tree_iterative`), `cpp_graph/resolver.rs`'s session-free body, `cpp_graph/extractor.rs`.\n\n**(c) analysis machinery, per file**\n\n| File | Machinery | Covered by an established pattern? |\n|---|---|---|\n| `cpp_graph/extractor.rs` | `&dyn IAnalyzer` — only `parent_of` (8), `ranges` (3), `type_alias_provider` (3), `type_hierarchy_provider` (2), `enclosing_code_unit` (1); one `&CppAnalyzer`; `PreparedSyntaxTree` (core) | **Yes** — `PythonGraphSource`/`GoGraphSource` idiom |\n| `cpp_graph/resolver.rs` | 16 `&CppAnalyzer` sigs reaching `prepared_syntax` (27 calls across the graph), `include_target_index` (9), `template_metadata` (2), `structural_parent_of`, `analyzed_files`; `declaration_range::node_for_exact_range` (**analysis `pub(crate)`, no landed carrier**); `resolve_analyzer` at 9 sites; 3 `record_*_for_test` calls into `cfg(test-support)` analyzer counters | **Mostly** — graph-source idiom with a 5-method C++ extension. Two residues: `node_for_exact_range` (23 LOC) and the test-counter recording |\n| `cpp_graph/inverted.rs` | W2 contract + `walk_tree_iterative` + `local_inference` (core) + 40 names from its two siblings. **Zero `same_owner` use** — C++ does not need PHP's `route_same_owner` | **Yes**, once `walk_tree_iterative` is core (Ruby pass) |\n| `cpp_graph/shared.rs` | `resolve_analyzer`, `analyzed_files_for_language` (analysis `pub(crate)`), `cpp_sentinel_recovered_classes`, `VisibilityIndex`, **and the `pub` cross-crate batch API** | **Partly** — the resolver/orchestration half is Python's `python_graph.rs` shape; `CppAuthoritativeUsageBatch` is §3.2 |\n| `cpp_graph.rs` | `resolve_analyzer::<CppAnalyzer>`, `GraphUsageAnalyzer`/`UsageAnalyzer`, `dead_code_bulk_eligibility` downcasting into `TargetSpec::from_target`, the 23-name outward re-export block | **Yes** — the PHP `php_graph.rs` shape; the eligibility fn splits at the downcast |\n| `cpp/mod.rs` inherent blocks | `TreeSitterAnalyzer<CppAdapter>` field, `LimitedQueryRows` ×5 (core after W6), 5 `moka::sync::Cache`, 3 `OnceLock`, 1 `PoolSafeMemo`, `BulkFileStateSource` (analysis `pub(crate)`), `build_weighted_cache` | **Yes for the shim half.** ~276 LOC is R1-class language logic (below) |\n| `cpp/hierarchy.rs:4–143` | `self.inner.{definitions, all_declarations, ranges}`, `visible_type_units_by_file` moka cache, `build_direct_descendant_index` (core) | **Yes** — `CodeUnitIndex` surface |\n| `cpp/imports.rs:104–135` | `self.inner.{all_files, import_info_of}` + `OnceLock` + `PoolSafeMemo` | **Yes** — graph-source idiom; state stays, decisions leave |\n| `cpp/identity.rs` | `resolve_analyzer::<CppAnalyzer>` + `cpp.include_target_index()` inside `cpp_header_body_files_are_related`; `declaration_range::node_for_exact_range`; `tree_walk::subtree_contains` (core); `tree_sitter_cpp::LANGUAGE` | **NO — see §3.1** |\n| `cpp/diagnostics.rs` | `CppAnalyzer` + `CppCompileContext` + `semantic_diagnostics::{node_range,node_text}` (core) + `collect_parse_errors` (core) | **Yes** — the Go/Python shape once `compile_context_for` is on the source trait |\n| `cpp/clones.rs` | `clone_detection::{CloneCandidateData, compact_clone_excerpt}` (analysis) | **Yes** — Go's and Rust's `clones.rs` both stayed in analysis; normalizer moves, ~12-LOC entry stays |\n| `cpp/adapter.rs` | `LanguageAdapter` (analysis), `ParsedFile` (core), `cognitive_complexity` (core) | **Yes** — Go's forwarding shell |\n| `cpp/semantic.rs` | `analyzer::semantic::*`, registration macro naming `CppAnalyzer` | **Parked** — fleet-wide |\n| `get_definition/cpp.rs`, `get_type/cpp.rs` | `ResolutionSession` (19 + 1), `use super::*` glob, `CppAdapter` (`:3`), `declaration_range::code_unit_declaration_name_range_for_range`, `target_kind::TypeLookupTargetKind` | **Parked**; W7-movable **except** the `CppVisibilityIndex` dependency (§3.1) |\n\n**R1-class inherent language logic — sized.** Three inherent `impl CppAnalyzer` blocks:\n\n| Site | LOC / methods | Disposition |\n|---|---:|---|\n| `mod.rs:131–467` | 337 / 10 | Constructors + `from_inner`/`with_updated_inner` (the wholesale cache rebuild) → **shim**. Language logic = the **#1134 reconciliation `:207–422` ≈ 216 LOC / 3 methods** |\n| `mod.rs:468–719` | 252 / 35 | 24 test hooks (~150 LOC) + forwarders → **shim**. Language logic ≈ 60 LOC (`receiver_query_supported`, `structural_parent_of`, `extract_type_identifiers`'s `Regex`, the two `*_limited` leaf splits) |\n| `hierarchy.rs:4–143` | 140 / 4 | **Moves** |\n| `imports.rs:104–135` | 32 / 3 | **Moves behind the source trait** |\n| **Total needing free-function rewrite** | **≈ 448 / 20** | vs Rust 2,896/73, C# 1,210/52, Python 916/37, Ruby 383/29, PHP 131/15 |\n\nC++ sits between Ruby and Python on R1 mass despite being 2.5× the seam size, because its language knowledge lives in **free functions** — 208 in `declarations.rs`, 197 in `resolver.rs`, 181 in `get_definition/cpp.rs`. Only **22 signatures across the whole seam take `&CppAnalyzer`** (resolver 16, shared 2, extractor 1, get_definition 1, plus 2 in `cpp/`), an order of magnitude fewer than PHP's 65 or C#'s 132.\n\n**`PreparedSyntaxTree` outside the lowerer.** **Yes, heavily — but core after W5.** `cpp_graph/resolver.rs` (6 signatures, `:3,411,1869,2016,2043,5264,5313,5646`), `extractor.rs` (`:5,92,101`), `get_definition/cpp.rs:447`, `cpp/mod.rs:478`. All 27 call sites route through `CppAnalyzer::prepared_syntax{,_limited_cancellable}`, `pub(crate)` forwarders to `self.inner`. This is Rust's W5-motivating problem, already solved.\n\n**moka / global caches / rayon / regex.** **No `static` or `LazyLock` cache anywhere in the seam.** The only `LazyLock`s are `CPP_COGNITIVE_CONFIG` (`adapter.rs:9`, pure data) and 6 `Regex`es (`tests.rs`); the only `static` is `CPP_USAGE_STRATEGY` (`mod.rs:1170`, a ZST). One lazy `OnceLock<Regex>` in `mod.rs:681`. **Zero `rayon` in the seam** (the root crate's `reference_differential` uses it around the batch, not inside). `regex` used only in `tests.rs` + `mod.rs:681`. Instance-resident caching on `CppAnalyzer` (`mod.rs:75–108`): **five** moka `Cache`s (`imported_code_units` budget/4; `referencing_files`, `direct_ancestors`, `visible_type_units_by_file` budget/8; `reconciled_definitions_by_fq` **fixed `max_capacity(2048)`, not budget-derived**), **three** `Arc<OnceLock<…>>` (`include_target_index`, `direct_descendant_index`, `compile_contexts`), **one** `PoolSafeMemo` (`reverse_include_index`), plus **six** `cfg(test)`/`test-support` `AtomicUsize` counters. Invalidation is wholesale via `from_inner`/`with_updated_inner`. **Separately**, `VisibilityIndex` (`resolver.rs:681`) carries **18 scan-scoped `Mutex`/`RwLock` memo cells** including `macro_environment_cursors: Mutex<HashMap<(ProjectFile, ThreadId), …>>` — thread-keyed, the only such structure in the fleet. **Verdict: moka stays in analysis; the `VisibilityIndex` cells move with `resolver.rs`.**\n\n**Dead-code bulk-proof shape.** C++ is the **delegating** shape with **`strategy: None`** (`mod.rs:1234–1239`) — deliberately no per-symbol strategy, bulk-only. `CppDeadCodeBulk::needs_precise_scan` (`mod.rs:1327–1350`) memoizes `analyzable_file_count` + `overloaded_function_fqns` in a `CppDeadCodeMemo` and delegates to `usages::cpp_graph::dead_code_bulk_eligibility` (`cpp_graph.rs:76–99`), which downcasts through `TargetSpec::from_target`: `Type` → `BulkSafe`; effectively-free `FreeFunction|Method` → `BulkSafe` unless overloaded or `cpp_global_main`; everything else → `NeedsPrecise`. `code_quality/dead_code_smells.rs` holds only `cpp_graph_finding` scoring (`:808, 1113–1128`), `cpp_public_like_declaration` (`:1506`), a `\"C++\"` label (`:1647`), and `cpp_implicit_entry_point` (`:1523`) which is a **one-line delegate into `cpp_graph::is_cpp_global_main`** — C++ has neither C#'s homeless-predicate problem nor PHP's split.\n\n---\n\n## 3. C++-specific hard spots, examined\n\n### 3.1 The searchtools identity block: allowlisted, invisible to the gate, and it downcasts\n\n`cpp/identity.rs` (285 LOC) is re-exported `pub(crate)` through `analyzer/mod.rs:92–97` (a **14-name block**) and consumed in four bands:\n\n| Consumer | Names | Band |\n|---|---|---|\n| `searchtools/selectors.rs:13–14, 266–346, 1016–1199` | `CppCallableUnitRole`, `cpp_callable_unit_role`, `cpp_indexed_callable_linkage`, `cpp_callable_definitions_share_identity_evidence`, `CppOccurrenceClassifier` (a `HashMap<ProjectFile, Option<CppOccurrenceClassifier>>` field on `CppIdentityRenderCache`) | **framework, parks** |\n| `searchtools/sources.rs:47, 56, 559, 566, 584` | `CppIdentityRenderCache::{canonical_selector, occurrence_role}` | **framework, parks** |\n| `searchtools/mod.rs:310–333` | `CppOccurrenceClassifier::new`, `CppOccurrenceRole::Definition`, `primary_range_with_cpp_classifier` | **framework, parks** |\n| `usages/candidates.rs:8, 78, 160–200` | `cpp_callable_definitions_share_identity_evidence` inside `cpp_related_callable_source_files` (~45 LOC, production) | **framework, parks** |\n| `analyzer/reference_candidates.rs:1, 129` | `cpp_is_range_for_binding_name` — **a direct `crate::analyzer::cpp::` module import**, allowlisted at `language_reach_in_gate.rs:146–151` (\"upstream #1641-era C++ recovery added a direct cpp module import; it re-points through the shim when the C++ crate extracts\") | **framework, parks** |\n| `get_definition/cpp.rs:4–7` (+ `:100–145, 3995, 4310, 4804–4806, 4967`) | `CppOccurrenceRole`, `cpp_occurrence_role_for_range`, `cpp_indexed_callable_linkage`, `cpp_header_body_files_are_related`, `cpp_is_range_for_binding_name`, `cpp_callable_definitions_share_identity_evidence` | **parked** |\n| `cpp_graph/extractor.rs:22, 5588`; `cpp/mod.rs:300–304` | `cpp_callable_definitions_share_identity_evidence`, `cpp_callable_unit_role` | **moves** |\n\nThe census-era disposition was allowlist-with-follow-up, recorded verbatim at `searchtools/selectors.rs:3–11`: *\"the operation is 'decide which of several same-fqn callables is the canonical one, and whether an occurrence is a declaration or a definition', and its evidence is the C++ header/implementation split (`cpp_header_body_files_are_related`). Turning that into a shared capability would be a four-method trait with exactly one implementer and no second language able to answer honestly.\"*\n\nThree facts extraction must answer:\n\n1. **The gate does not see this block.** `language_reach_in_gate.rs`'s `is_language_type` requires prefix ∈ `{Cpp, …}` **and** suffix ∈ `{Adapter, Analyzer, Support, UsageGraphStrategy}`; `CppCallableUnitRole`/`CppOccurrenceClassifier`/`CppOccurrenceRole` match none. `is_language_module` matches path segments, and every searchtools import goes through `crate::analyzer::{…}`, not `crate::analyzer::cpp::{…}`. So `searchtools/{selectors,sources,mod}.rs` and `usages/candidates.rs` have **no allowlist entry** — they pass silently. Only `reference_candidates.rs`, which imports the module directly, needed one.\n2. **`cpp_header_body_files_are_related` downcasts.** `identity.rs:211–223` does `resolve_analyzer::<CppAnalyzer>(analyzer)`, then `cpp.include_target_index()`, then `analyzer.import_statements` + `include_paths` + `resolve_include_targets_with_index`. It is the evidence root of the whole block, and it cannot leave without either the downcast staying shim-side or `IncludeTargetIndex` crossing on a capability.\n3. **`CppOccurrenceClassifier` owns a `tree_sitter_cpp::Parser`** (`identity.rs:40–45`), and searchtools instantiates it per file. After the move, `bifrost-analysis` still needs the type — via re-export from the shim, or via a capability that returns an opaque classifier.\n\n### 3.2 `CppAuthoritativeUsageBatch` is public cross-crate API over the graph's internals\n\n`usages/mod.rs:19` declares **`pub mod cpp_graph`** — the **only non-`pub(crate)` graph module in the repo** (all ten siblings are `pub(crate)`). The reason is `cpp_graph.rs:39`'s `pub use shared::CppAuthoritativeUsageBatch` and `:42`'s `cpp_type_owner_for_test`, consumed from the **root crate in production**:\n\n- `src/reference_differential/mod.rs:5` — `use crate::analyzer::usages::cpp_graph::CppAuthoritativeUsageBatch;` (production, not `cfg(test)`), constructed at `:963` and consulted at `:990`.\n- `src/reference_differential/mod.rs:7` — `cpp_type_owner_for_test` under `#[cfg(test)]`, used at `:2048, 2058`.\n- Four `#[cfg(test)]` functions at `:1685, 1747, 1917, 1997` do `resolve_analyzer::<CppAnalyzer>` and assert on `authoritative_visibility_build_count_for_test` / `target_spec_scan_count_for_test`.\n\nThe type is `pub struct CppAuthoritativeUsageBatch<'a> { analyzer: &'a dyn IAnalyzer, resolver: CppQueryResolver<'a>, visibility: VisibilityIndex<'a> }` (`shared.rs:70–74`) — i.e. the public API's two private fields are the resolver's `&'a CppAnalyzer` wrapper and the 38-field `VisibilityIndex`. Its doc comment (`shared.rs:59–63`) pins the contract: *\"This seam is intentionally limited to the reference-differential batch, which has no cancellation input. Cancellable `UsageFinder` requests keep using `build_with_cancellation` and never enter this batch.\"* No landed language crate exports anything comparable.\n\n### 3.3 `CppVisibilityIndex` is the definition route's vocabulary — a 27-name reach-in plus six fields on the framework batch context\n\n`get_definition/mod.rs:8–17` imports **27 names from `cpp_graph`** — the largest single reach-in block into a language's graph module in the fleet (Ruby 17, C# 15, PHP 6):\n\n```\nCallArityEvidence, CppBareCallTargetResolution, CppDesignatedInitializerOwner,\nCppLexicalScopeResolution, CppLexicalTypeResolution, CppTargetKind, CppVisibilityIndex,\ncpp_argument_children, cpp_constructor_type_node, cpp_designated_initializer_owner,\ncpp_enclosing_lexical_scope_components, cpp_field_declared_type_binding, cpp_first_type_child,\ncpp_function_return_type_text, cpp_initialized_effective_using_imports, cpp_is_declaration_name,\ncpp_is_declarator_node, cpp_name_for, cpp_reference_fqn_candidates, cpp_resolve_bare_call_target,\ncpp_signature_arity, cpp_split_top_level_commas, cpp_template_reference_arguments,\ncpp_type_name_components, extract_variable_name, is_globally_qualified_cpp_name,\nnormalize_cpp_type_text\n```\n\nBeyond the import block, `DefinitionBatchContext` — the framework's per-batch cache in `get_definition/mod.rs` — carries **six C++-typed fields and a counter**:\n\n```rust\ncpp_visibility: HashMap<ProjectFile, Arc<CppVisibilityIndex<'a>>>,   // :803\ncpp_indexed_sources: HashMap<ProjectFile, Option<Arc<String>>>,       // :806\ncpp_indexed_trees: HashMap<ProjectFile, Option<Tree>>,                // :807\ncpp_navigation_indexes: HashMap<ProjectFile, Option<Arc<cpp::CppNavigationIndex>>>, // :808\ncpp_structural_alias_paths: HashMap<CodeUnit, Vec<String>>,           // :809\ncpp_class_ranges: HashMap<ProjectFile, Arc<ClassRangeIndex>>,         // :810\ncpp_class_range_builds: usize,                                        // :815\n```\nwith five accessor methods `:954–1020` (`cpp_visibility` calls `CppVisibilityIndex::build(cpp, analyzer, &roots)` directly, taking `&'a crate::analyzer::CppAnalyzer`). Two in-file tests pin the build counts (`:2228`, `:2277`).\n\n**Favorable: the direction is one-way.** `cpp_graph/*` names `ResolutionSession`, `get_definition`, `get_type` and `DefinitionBatchContext` **zero times** (verified across all six files). The only `use …get_definition…` in the C++ seam is `cpp/mod.rs:33`, the `StructuralReceiverResolver` SPI shell. So this is PHP's/Ruby's inversion at 27 names, not C#'s bidirectional resolver — but `CppVisibilityIndex<'a>` must cross as `brokk_bifrost_cpp::graph::VisibilityIndex<'a>` while keeping its `cpp: &'a CppAnalyzer` field re-parameterized on a source trait, and the framework struct that stores it stays behind.\n\n### 3.4 The three graph files are one body of code\n\n`extractor.rs` does `use super::resolver::*` (glob) and imports 4 recovery names from `cpp::declarations` + 9 from `hits.rs`. `inverted.rs` imports **10 named items from `extractor.rs` and 30 from `resolver.rs`**. `shared.rs` imports from all three. The recovery-campaign symbols cross every boundary: `CppSentinelRecoveredClass`, `cpp_sentinel_recovered_classes`, `cpp_sentinel_recovered_scope_for_node`, `recovered_macro_return_type_node`, `cpp_export_macro_token`, `is_direct_recovered_exported_class_field_declaration`, `is_recovered_exported_class_container`, `recovered_exported_class_has_body` — all defined in `cpp/declarations.rs`, consumed at 18 sites across `extractor.rs`, `inverted.rs`, `resolver.rs`, `shared.rs`, and one in `cpp/mod.rs:1182` (inside `LanguageSupport::skips_local_declaration`). They all move together; there is no partial split.\n\n### 3.5 Five prior-salt epoch pins, each with a blob-invalidation test\n\n`epoch.rs:334–373` carries **five** `#[cfg(test)] pub(super) fn cpp_epoch_before_*` helpers, each recomputing the epoch from a **literal historical salt string** plus `tree_sitter_cpp::LANGUAGE`:\n`cpp_epoch_before_sentinel_class_before_member_callable`, `cpp_epoch_before_plain_fragmented_class_sibling_ownership`, `cpp_epoch_before_fragmented_export_sibling_class_parent_scope`, `cpp_epoch_before_recovered_typedef_base`, `cpp_epoch_before_complete_sentinel_class_tail`.\n\nEach is asserted end-to-end by a blob-eviction test in `store/mod.rs` (`:9862–9903`, `:9926–9945`, `:9968–9987`, `:10011–10030`, `:10063–10082`), all of which write a blob under the old epoch with `parse_state(&CppAdapter, &file)` and assert the new epoch evicts it. PHP had one such pin; C++ has five, plus two more `CppAdapter` parse sites at `:12068, 12118`. The literals, the `tree_sitter_cpp` dependency and `CppAdapter` all have to survive in `bifrost-analysis` after the grammar and `.scm` files leave, or five invalidation guarantees lose their only tests.\n\n### 3.6 The `test-support` counters are called from production code\n\nSix `AtomicUsize` counters on `CppAnalyzer` are gated `#[cfg(any(test, feature = \"test-support\"))]` (plus one `#[cfg(test)]`). Three of their `record_*` methods are invoked from **production files in the graph**: `resolver.rs:8640` (`record_cpp_parent_resolution_for_test`, inside `precise_parent_resolution`), `resolver.rs:8938` (`record_cpp_class_strength_parse_for_test`), `shared.rs:87,139` (`record_authoritative_visibility_build_for_test`), `shared.rs:206` (`record_target_spec_scan_for_test`), `cpp/hierarchy.rs:25` (`record_visible_type_units_build_for_test`). Each is wrapped in its own `#[cfg(any(test, feature = \"test-support\"))]` block with a `resolve_analyzer::<CppAnalyzer>` downcast. The `test-support` feature must be threaded to `brokk-bifrost-cpp` and the counters must be reachable across the crate boundary — no landed language crate does this.\n\n### 3.7 The #1134 reconciliation overlay sits inside `CodeUnitIndex`\n\n`CppAnalyzer::reconciled_definitions` (`mod.rs:207`) → `build_reconciled_definitions` (`:221`) → `reconcile_definition_identity` (`:345`) feeds a `ReconciledDefinitionIndex { rekeyed: Vec<CodeUnit>, provisional_of: HashMap<CodeUnit, CodeUnit> }` that **four `CodeUnitIndex`/`IAnalyzer` methods consult**: `definitions` (`:781–803`), `ranges` (`:808–825`), `signature_metadata` (`:840–861`), `get_definitions` (`:878–894`). The re-keyed units are *not in the store*, so `provisional_of` maps them back for range and metadata lookups. The doc at `:196–206` pins the shape: *\"Deliberately not a workspace-wide index: building one would need a full declaration scan, and a warm forward lookup must not trigger one (`tests/analyzer_persistence.rs`'s candidate-bounded contract).\"* The pure decision moved to `reconcile.rs` (139 zero-import LOC) in #1121/#1134, so what remains in `mod.rs` is the 216-LOC analyzer wiring — which straddles the shim/crate line exactly where the trait must cut.\n\n---\n\n## 4. Reverse edges\n\n**Production, framework → C++:**\n\n| Site | What |\n|---|---|\n| `analyzer/mod.rs:90, 92–97` | `pub use cpp::CppAnalyzer`; the **14-name `pub(crate) use cpp::{…}`** block (`CppCallableUnitRole`, `CppOccurrenceClassifier`, `CppOccurrenceRole`, `IncludeTargetIndex`, `cpp_callable_definitions_share_identity_evidence`, `cpp_callable_unit_role`, `cpp_indexed_callable_linkage`, `cpp_template_term`, `include_paths as cpp_include_paths`, `node_text as cpp_node_text`, `normalize_cpp_whitespace`, `recovered_exported_class_has_body`, `resolve_include_targets`, `resolve_include_targets_with_index`). Allowlisted. **No public C++ API beyond `CppAnalyzer`** — no pack adapter, no `resolve_*_semantic_pack_dependencies`, nothing in `docs/` |\n| `analyzer/usages/mod.rs:18, 19, 66` | `mod cpp_call_match`; **`pub mod cpp_graph`** (the only `pub` graph module); `pub use cpp_graph::CppUsageGraphStrategy` |\n| `analyzer/lib.rs:48` | Crate-root surface (`CppAnalyzer`) |\n| `analyzer/languages.rs:29, 251, 272, 288, 723, 808` | `Language::Cpp` + `Language::Cpp => Some(&cpp::CppSupport)`; the capability-matrix row at `:808` |\n| `analyzer/multi_analyzer.rs:6, 39, 56, 75, 92, 109, 126, 143, 160, 177, 211, 257` | `AnalyzerDelegate::Cpp` (assembly) |\n| `analyzer/usages/get_definition/mod.rs:8–17, 81, 84, 107, 110, 130, 637–653, 683, 803–846, 954–1020, 1321, 1359, 1513–1545, 1604` | **The 27-name block**, `CppAnalyzer`, `cpp_include_paths`/`cpp_node_text`, `CPP_NAVIGATION_STRUCTURE_UNAVAILABLE` (`:107`), `mod cpp`, the 2-item re-export, the six `DefinitionBatchContext` fields + five accessors, the dispatch arm, the C++-only navigation-selection and unproven-link-unit diagnostic paths. Allowlisted |\n| `analyzer/usages/get_type/mod.rs` | `mod cpp` + `resolve_cpp_type_bounded` |\n| `analyzer/usages/get_definition/call_sites.rs:154, 340, 409–411, 528–529, 555, 662–663, 777, 815, 826, 838, 886–890` | `cpp_call_reference_candidate`, `cpp_terminal_callee_leaf` + ~10 `Language::Cpp` arms. Allowlisted |\n| `analyzer/usages/candidates.rs:8, 78, 160–200` | `cpp_related_callable_source_files` (~45 LOC of production C++ candidate expansion reaching `cpp_callable_definitions_share_identity_evidence`). **Not allowlisted; does not trip the gate** |\n| `analyzer/reference_candidates.rs:1, 129, 224, 233` | `use crate::analyzer::cpp::cpp_is_range_for_binding_name` + 3 arms (`operator_name`/`destructor_name`/`this`). Allowlisted `:146–151` |\n| `analyzer/lexical_definitions.rs:444, 489, 860, 936, 999, 1024, 1069, 1092, 1172, 1188, 1210, 1233` | 12 `Language::Cpp` grammar arms (shared machinery, parks) |\n| `analyzer/exception_handling.rs:31, 72, 248–271, 805, 870, 918` | `analyze_cpp` + `classify_cpp_type`/`cpp_catch_type`/`cpp_statement_count`, ~90 LOC of catch-clause knowledge in a framework file |\n| `analyzer/usages/receiver_query.rs:1343, 1349, 1993–1999, 2068, 2742, 2811` | 6 production arms, no type reach-in. **`:2068` is the `Language::Cpp \\| Language::Go` pairing** in `structural_member_is_statically_bound_data_member` — a single-target `is_field()` member is treated as statically bound for exactly these two languages |\n| `analyzer/symbol_lookup.rs:555, 720–723, 744` | `Java \\| CSharp \\| Cpp` nested-name arm + `strip_cpp_tag_keyword` (a framework free fn stripping `struct`/`class`/`enum` tag keywords) |\n| `analyzer/store/mod.rs` (~50 sites) + `analyzer/tree_sitter_analyzer.rs` (5) | The `cpp_template_metadata` persistence path (`unit_cpp_template_metadata` table, blob-envelope count, bulk readers). **All types core; framework-side; stays** |\n| `analyzer/store/epoch.rs:41, 207–216, 327–332, 334–373` | `.scm` include paths + `lang_epoch!` salt (37 tokens) + **five `#[cfg(test)]` prior-salt helpers** |\n| `code_quality/dead_code_smells.rs:341, 371, 464, 808, 1113–1128, 1506, 1523–1525, 1647` | `cpp_graph_finding` scoring, `cpp_public_like_declaration`, `cpp_implicit_entry_point` (one-line delegate to `is_cpp_global_main`), label. Allowlisted |\n| `analyzer/structural/lexical_environment.rs:818`, `analyzer/value_flow/mod.rs:15`, `analyzer/usages/workspace_graph.rs:33,59` | 1 arm each; `UsageEcosystem::Cpp` |\n| `searchtools/selectors.rs:13–14, 266–346, 1016–1199`; `sources.rs:47,56,559,566,584`; `mod.rs:310–333` | **The identity block (§3.1)**, ~300 LOC of C++-aware framework code |\n| `searchtools/navigation.rs:2300–2325` | `search_symbol_implementation_tier` — a C++-only header/implementation ranking tier keyed on `.c/.cc/.cpp/.cxx/.m/.mm` and a `{...}` signature suffix |\n| `searchtools/scan_usages.rs:1113, 1134` | `\"C/C++\"` label + a `Language::Cpp && target.is_macro()` arm |\n\nNotably absent: **zero C++ references in `analyzer/common.rs` (one test fixture only, `:307`), `analyzer/index_warmer.rs`, `analyzer/workspace.rs`, `analyzer/global_usage_definition_index.rs`, `analyzer/usages/finder.rs`, `analyzer/usages/get_definition/trace.rs`.** `CppSupport` overrides `skips_local_declaration` (the only `LanguageSupport` impl doing so, reaching `declarations::is_direct_recovered_exported_class_field_declaration`), `package_separator` (`\"::\"`), `signature_metadata_limited`, `declaration_ranges_limited`, `forward_query_provider`, and none of `display_symbol_name`/`source_identifier`/`alias_name_segment`.\n\n**cfg(test)-only C++ reach-ins** (each verified to sit after its file's `#[cfg(test)]`): `analyzer/semantic/icfg.rs:3046–4484` (gate `:2472`; **34 references**, the densest cfg(test) C++ block in the framework), `analyzer/usages/call_relations.rs:1475–1741` (gate `:1394`; 24 refs, C++ dispatch/linkage/header-projection tests), `analyzer/store/mod.rs:8123, 9862–10082, 12068, 12118` (gate `:8120`; `CppAdapter` + the five epoch pins), `analyzer/usages/get_definition/mod.rs:2228–2348` (gate `:1803`; `CppAnalyzer::new`, the class-range and batch-validation counters), `analyzer/usages/get_definition/call_sites.rs:1154–1220` (gate `:1052`), `analyzer/languages.rs:747, 756, 876, 1012–1013` (gate `:742`; `AnalyzerDelegate::Cpp(CppAnalyzer::from_project(…))`), `analyzer/reference_candidates.rs:362–393` (gate `:236`), `analyzer/structural/search/tests.rs` (whole file), `analyzer/structural/materialization_rows.rs:673–675` (gate `:447`), `analyzer/symbol_lookup.rs:913` (gate `:869`), `code_quality/comment_density.rs:435–436` (gate `:246`), `searchtools/tests.rs:529`. All need `CppAnalyzer`/`CppAdapter` re-exported for dev-dependency use, exactly as Go and Python.\n\n**Cross-crate reach-ins:**\n- **`src/reference_differential/mod.rs:5` — a production import of `CppAuthoritativeUsageBatch` from `cpp_graph` (§3.2)**, plus `cpp_type_owner_for_test` (`:7`, cfg(test)) and four cfg(test) `resolve_analyzer::<CppAnalyzer>` fixtures asserting on `test-support` counters (`:1685, 1747, 1917, 1997`). **The only production cross-crate reach-in into a language graph module in the fleet.**\n- `src/lib.rs:18` — facade re-export of `CppAnalyzer`.\n- `bifrost-lsp`: **zero type reach-ins.** `Language::Cpp` arms only — `handlers/formatting.rs:286,475` (clang-format + `.clang-format` discovery), `handlers/hover.rs:115`, `handlers/semantic_tokens.rs:510` (test fixture).\n- `bifrost-mcp`: `searchtools_service.rs:352`, one `Language::Cpp | Language::Php | Language::None => \"language\"` label arm.\n- `bifrost-core`: `Language::Cpp` in `model.rs`, the `CppTemplateMetadata` family, `CPP_MATERIALIZATION_SUPPORT`, `symbol_path.rs:30–58`'s operator-token parsing, the `0008-cpp-template-metadata.sql` migration. **There is no `CppAnalyzerConfig` in `core/analyzer/config.rs`.**\n- `bifrost-runtime`, `bifrost-policy`, `bifrost-semantic-packs`, `bifrost-nlp`: none.\n\n---\n\n## 5. crates.io deps a `brokk-bifrost-cpp` crate needs\n\n| crate | version in `bifrost-analysis/Cargo.toml` | needed by |\n|---|---|---|\n| `brokk-bifrost-core` | path, `=` pin | everything |\n| `tree-sitter` | 0.25.10 | everything |\n| `tree-sitter-cpp` | **0.23.4** | `declarations.rs`, `structural.rs`, `identity.rs:42`, `adapter.rs`, `clones.rs`, `diagnostics.rs:58`, `get_definition/cpp.rs` |\n| `regex` | 1.12.2 | `tests.rs` (×6), `mod.rs:681` |\n| `serde` | 1.0.228 (derive) | `compile_context.rs` only |\n\n**A five-line dependency block** — identical in shape to PHP's, with `serde` where PHP has `serde_json`. Explicitly **not**: `rayon` (zero uses in the seam — C++ joins C#, PHP and Ruby), `moka` (all five caches and the `PoolSafeMemo` stay on the analyzer), `serde_json`, `semver`, `goblin`, `walkdir`, `flate2`, `toml`.\n\n**Grammar notes.**\n1. `tree-sitter-cpp 0.23.4` is a minor generation behind the fleet's 0.25 line — the C# class exactly (`tree-sitter-c-sharp 0.23.1`, `tree-sitter-php 0.23.11`, `tree-sitter-ruby 0.23.1`). The C++ crate cannot inherit the 0.25 grammar version.\n2. `tree_sitter_cpp` exports one language constant (`LANGUAGE`) plus `HIGHLIGHT_QUERY` (note the singular, unlike Ruby's `HIGHLIGHTS_QUERY`). `CppSupport::parser_language` (`mod.rs:1240`, ignoring its `ParserFlavor` argument) and `::highlight_query` (`mod.rs:1252`) both answer from the grammar crate and both stay in the shim — **so `bifrost-analysis` keeps its `tree-sitter-cpp` dependency after the move**, as it does for all five landed grammars. The five epoch pins and their `store/mod.rs` tests reinforce this independently.\n3. `tree-sitter-cpp` is *also* the C parser here (`Language::Cpp` covers `.c`); `CppAnalyzer::receiver_query_supported` (`mod.rs:497`) is the file-extension gate that turns the receiver route off for plain C, and `get_type/cpp.rs:30` emits the `\"cpp_c_receiver_unsupported\"` diagnostic.\n\n---\n\n## 6. LOC accounting\n\nCalibration: Go 8,544 moved / 1,598 prod shim; Rust 16,375 / ~1,900 (+1.3k rider); Python 10,129 / ~1,590; C# 8,180 (Scenario B) / ~1,660; PHP 5,935 / ~1,120; Ruby projected 5,805 / ~1,105.\n\n| Band | LOC | Content |\n|---|---:|---|\n| **Movable now** (retarget only) | **~10,815** | `declarations.rs` prod 8,732, `structural.rs` 406, `cpp_call_match.rs` prod 290, `identity.rs` 285 (less the downcast, §3.1), `tests.rs` prod 285, `imports.rs` free-fn + `IncludeTargetIndex` band 261, `compile_context.rs` prod 170, `reconcile.rs` prod 139, `clones.rs` normalizer ~120, `cpp_graph/syntax.rs` 68, `adapter.rs` bodies + `CPP_COGNITIVE_CONFIG` ~60 |\n| **Movable after the graph-source + R1 pass** | **~19,506** | `cpp_graph/resolver.rs` 9,304, `extractor.rs` 8,047, `inverted.rs` 1,175 (needs `walk_tree_iterative`), `hits.rs` 229; `diagnostics.rs` prod 209; `hierarchy.rs` 234 (less the 18-LOC provider); `mod.rs` R1 276 (the #1134 wiring 216 + ~60); `imports.rs:104–135` 32. Two small residues: `declaration_range::node_for_exact_range` (23 LOC, analysis `pub(crate)`, no landed carrier) and the `test-support` counter recording |\n| **Parked on `analyzer/semantic`** | **4,927** | `cpp/semantic.rs` (registration macro textually requires the `TreeSitterAnalyzer` field) |\n| **Parked on `semantic_model`** | **0** | C++ has no `external.rs`, no pack adapter, no `dependency_discovery.rs`, no workspace surface — **the fleet's second zero-pack seam after PHP** |\n| **Parked on `ResolutionSession`** (movable after W7, except §3.3) | **~7,190** | `get_definition/cpp.rs` 6,977 + `get_type/cpp.rs` 80 + ~133 LOC of `get_definition/mod.rs` C++ wiring (the 27-name block, `mod cpp`, the re-export, the six `DefinitionBatchContext` fields + five accessors `:803–1020`, the dispatch/navigation/diagnostic arms). **Residual blocker after W7: the 48 `&CppVisibilityIndex` signatures — this band cannot compile until `VisibilityIndex` is importable from the crate** |\n| **Shim floor (production)** | **~1,822** | `mod.rs` shim half ~1,095 (imports/mod decls 73, struct + `ReconciledDefinitionIndex` 57, ctors/`from_inner`/`with_updated_inner` ~120, test hooks ~150, `CodeUnitIndex` 214, `IAnalyzer` 193, `AnalyzerTestHooks` 34, `TypeAliasProvider` 9, SPI block 85, `CppEdgePass` 15, `StructuralReceiverResolver` 32, `CppDeadCodeBulk` 68); `cpp_graph/shared.rs` prod 321 (incl. the public batch API); `cpp_graph.rs` prod 179; `imports.rs` providers 99; `cache.rs` 41; `adapter.rs` `LanguageAdapter` impl ~32; `hierarchy.rs` `TypeHierarchyProvider` 18; `diagnostics.rs` entry ~15; `clones.rs` entry ~12; `identity.rs` downcast residue ~10 |\n| **Retained analyzer-bound tests** | **~2,400** | `cpp_graph/resolver.rs:9305–11018` 1,714 (38 `CppAnalyzer`, 10 `TestProject`, 5 `WorkspaceAnalyzer`), `extractor.rs:8049–8489` 441, `shared.rs` 180, `cpp_graph.rs` 64. The rest of the 5,108 in-file test LOC is analyzer-free and moves with its file: `declarations.rs`'s 1,769-LOC tail (9 `CppAdapter.parse_file` sites, retarget to a free `parse_cpp_file`), `cpp_call_match.rs` 171, `reconcile.rs` 144, `compile_context.rs` 83, `diagnostics.rs` 74, `inverted.rs` 60 |\n\n**Total 49,219** (44,111 prod / 5,108 in-file test) **+ 94 `.scm`.**\n- Moves ≈ **30,321 (62 % of the seam, 71 % of the seam outside the parks)** — the largest absolute move in the fleet by 1.85×.\n- Parked ≈ **12,117 (25 %)** — `semantic.rs` 4,927 + the definition/type route 7,190, nothing else. **Lowest park fraction in the fleet** (PHP 49 %, Ruby 61 %, C# 60 %).\n- Analysis residue ≈ **18,890**, of which **~1,822 is production shim** — **above the Go 1,598 floor and above C#'s ~1,660**, driven by four things Ruby and PHP do not have: a 214-LOC `CodeUnitIndex` impl carrying the #1134 overlay on four methods, a 193-LOC `IAnalyzer`, ~150 LOC of `test-support` hooks, and `shared.rs`'s 321-LOC public batch API.\n- Predicted analysis-frontend effect at Go's measured 0.47 s/kLOC: **≈ −14.3 s**. If the definition route follows after W7 and the `CppVisibilityIndex` inversion, moves rise to ~37,511 → **≈ −17.6 s**.\n\nMove rate outside the parks (71 %) sits between Rust's 68 % and Ruby's 76 %. C++'s high *overall* rate is entirely the zero pack band and the small `semantic.rs` relative to seam size: `semantic.rs` is 10 % of the C++ seam versus 28 % of PHP's and 24 % of Ruby's.\n\n---\n\n## The three hardest couplings\n\n**1. `CppVisibilityIndex` is the definition route's vocabulary, and the framework's batch context is parameterized on it.** `get_definition/mod.rs:8–17` imports **27 names** from `cpp_graph` — the largest such block in the fleet — and `get_definition/cpp.rs` resolves **48 signatures typed `&CppVisibilityIndex`** through the `use super::*` glob those names feed. Worse than the import count: `DefinitionBatchContext` (`get_definition/mod.rs:803–1020`), a framework struct, carries six C++-typed fields including `cpp_visibility: HashMap<ProjectFile, Arc<CppVisibilityIndex<'a>>>` and `cpp_navigation_indexes: HashMap<ProjectFile, Option<Arc<cpp::CppNavigationIndex>>>`, with an accessor at `:954` that calls `CppVisibilityIndex::build(cpp, analyzer, &roots)` on a `&'a crate::analyzer::CppAnalyzer`, and a build counter pinned by two in-file tests (`:2228, 2277`). `VisibilityIndex` itself (`cpp_graph/resolver.rs:681–744`) is 38 fields — 18 `Mutex`/`RwLock` scan-scoped memo cells including a `ThreadId`-keyed macro-cursor map — holding `cpp: &'a CppAnalyzer` and `analyzer: &'a dyn IAnalyzer`. **Favorable: the direction is one-way** — `cpp_graph/*` names `ResolutionSession`, `get_definition`, `get_type` and `DefinitionBatchContext` zero times, so this is PHP's/Ruby's inversion at 27 names, not C#'s bidirectional resolver. But the `cpp: &'a CppAnalyzer` field cannot cross the crate boundary, the 18 interior-mutability cells must keep their per-scan lifetimes, and the framework struct that caches the index by `ProjectFile` stays behind while the type leaves.\n\n**2. The searchtools identity block reaches into `cpp/identity.rs`, is invisible to the reach-in gate, and its evidence root downcasts to `CppAnalyzer`.** Five names plus two classifier types travel `cpp/identity.rs` → `analyzer/mod.rs:92–97` (a 14-name `pub(crate)` block) → `searchtools/selectors.rs:13–14` (`CppIdentityRenderCache` at `:266–346`, `cpp_callable_family_selectors` `:1016–1105`, `cpp_canonical_selectors` `:1107–1184`, `cpp_callable_signature_key` `:1192–1199`), `searchtools/sources.rs:47,56,559,566,584`, `searchtools/mod.rs:310–333`, and `usages/candidates.rs:78,160–200`. **None of those four files has an allowlist entry, and none trips the gate**: `is_language_type` requires a suffix in `{Adapter, Analyzer, Support, UsageGraphStrategy}` and `CppCallableUnitRole`/`CppOccurrenceClassifier`/`CppOccurrenceRole` match none, while the imports go through `crate::analyzer::{…}` rather than `crate::analyzer::cpp::{…}`. Only `reference_candidates.rs:1` — which imports the module directly for `cpp_is_range_for_binding_name` — needed the allowlist entry at `language_reach_in_gate.rs:146–151`. The census-era disposition is recorded verbatim at `selectors.rs:3–11` and rests on `cpp_header_body_files_are_related`, which at `identity.rs:211–223` does `resolve_analyzer::<CppAnalyzer>` and reads `cpp.include_target_index()` — so the block's evidence root cannot leave without either the downcast staying shim-side or `IncludeTargetIndex` crossing on a capability. `CppOccurrenceClassifier` additionally owns a `tree_sitter_cpp::Parser` that searchtools instantiates per file.\n\n**3. `CppAuthoritativeUsageBatch` is public cross-crate API over the graph's private internals.** `usages/mod.rs:19` is the repo's **only `pub mod <lang>_graph`** (the other ten are `pub(crate)`), and the reason is `cpp_graph.rs:39`'s `pub use shared::CppAuthoritativeUsageBatch`, imported **in production** at `src/reference_differential/mod.rs:5` and used at `:963, 990`. The struct (`shared.rs:64–74`) holds `CppQueryResolver<'a>` (which is `&'a CppAnalyzer`) and `VisibilityIndex<'a>` — coupling 1's type — as private fields, and its doc comment pins the contract that it is cancellation-free and that `UsageFinder` never enters it. Alongside it, `cpp_type_owner_for_test` (`cpp_graph.rs:42`, `cfg(any(test, feature = \"test-support\"))`) and four root-crate tests (`:1685, 1747, 1917, 1997`) do `resolve_analyzer::<CppAnalyzer>` and assert on `authoritative_visibility_build_count_for_test` / `target_spec_scan_count_for_test` — `test-support`-gated counters whose `record_*` calls sit in **production** code at `shared.rs:87,139,206`, `resolver.rs:8640,8938` and `cpp/hierarchy.rs:25`. No landed language crate exports a graph type across a crate boundary, and none threads a `test-support` feature for production-path instrumentation.\n\n**Two smaller items for completeness:** `declaration_range::node_for_exact_range` (`declaration_range.rs:146`, 23 LOC, analysis `pub(crate)`) is reached by `cpp/identity.rs:2` and `cpp_graph/resolver.rs:2` and by no landed crate — `rust_graph/extractor.rs:2405` keeps a private copy — a `route_same_owner`-class verbatim lowering; and `epoch.rs` carries **five** `#[cfg(test)] cpp_epoch_before_*` prior-salt helpers (PHP had one), each asserted by a blob-eviction test in `store/mod.rs:9862–10082` that parses with `CppAdapter`, so five invalidation guarantees depend on `CppAdapter` and `tree_sitter_cpp` surviving in `bifrost-analysis` after the grammar and the 94 `.scm` LOC leave.\n\n---\n\n## Schedule risk: upstream churn\n\n`origin/master` commits touching each language's seam in the last seven days:\n\n| cpp | rust | scala | java | csharp | python | go | ruby | js_ts | php |\n|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|\n| **72** | 51 | 35 | 30 | 26 | 25 | 24 | 20 | 20 | 17 |\n\nRust's 51 is the in-flight extraction campaign; C++'s 72 is upstream product work. Recent seam-touching commits include `b020725e` \"Keep macro-export C++ classes as namespace siblings\" (#1665), `52350db3` \"Complete cppcheck persisted replay recovery\", `b28b8950` \"Complete cppcheck C++ reference recovery\", `0be673ef` \"Fix remaining cppcheck reference differential gaps\", `bac47d42` \"Handle malformed C++ scope recovery structurally\", `a78dd303`/`041c18b6` \"Invalidate stale … C++ … blobs\", `b235b350` \"Fix C++ sentinel classes with recovered callables\", `a68db53d` \"Fix cpp #1134 reconcile fan-out (#1566)\". Twelve of the epoch salt's 37 tokens were added in 2026-08.\n\n**Last-modified dates for the five largest seam files: `declarations.rs` 2026-08-05, `extractor.rs` 2026-08-05, `get_definition/cpp.rs` 2026-08-05, `cpp/mod.rs` 2026-08-05, `resolver.rs` 2026-08-04.** Four-week churn: `resolver.rs` +11,257/−240 over 15 commits, `declarations.rs` +10,746/−274 over 27, `extractor.rs` +8,798/−309 over 15, `get_definition/cpp.rs` +7,222/−135 over 11, `cpp/mod.rs` +1,567/−196 over 29. These same five files are 87 % of the movable mass. Any extraction that holds them across more than a few days will rebase onto rewritten hunks.\n\nLowest-churn seam files (safe to move first, if a split pass is wanted): `cache.rs` (1 commit), `syntax.rs` (1), `compile_context.rs` (1), `reconcile.rs` (1), `diagnostics.rs` (1), `imports.rs` (1), `identity.rs` (2), `adapter.rs` (2), `hierarchy.rs` (2), `clones.rs` (3), `hits.rs` (3)."}],"stop_reason":"end_turn","stop_sequence":null,"stop_details":null,"usage":{"input_tokens":2,"cache_creation_input_tokens":2094,"cache_read_input_tokens":227136,"output_tokens":25830,"server_tool_use":{"web_search_requests":0,"web_fetch_requests":0},"service_tier":"standard","cache_creation":{"ephemeral_1h_input_tokens":0,"ephemeral_5m_input_tokens":2094},"inference_geo":"not_available","iterations":[{"input_tokens":2,"output_tokens":25830,"cache_read_input_tokens":227136,"cache_creation_input_tokens":2094,"cache_creation":{"ephemeral_5m_input_tokens":2094,"ephemeral_1h_input_tokens":0},"type":"message"}],"speed":"standard"},"diagnostics":null},"requestId":"req_011CdkA2eDJ2aGQpnJej6vUy","attributionAgent":"Explore","type":"assistant","uuid":"b25dd230-ac4a-4b43-9bdb-166837906066","timestamp":"2026-08-05T19:35:00.373Z","effort":"high","userType":"external","entrypoint":"cli","cwd":"/home/jonathan/Projects/bifrost2","sessionId":"8f94fdd6-1d55-4f9d-8f78-532b5b220270","version":"2.1.220","gitBranch":"worktree-agent-aa603d2da2a054a43","slug":"solve-https-github-com-brokkai-bifrost-i-atomic-hopper"}
