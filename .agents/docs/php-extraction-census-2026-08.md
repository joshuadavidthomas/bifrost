# PHP seam census for the stage-3 fleet (2026-08-05)

Agent-run census. Coordinator dispositions: accept as reported. PHP runs as a
single combined pass (prerequisites + crate move) because the R1-class block
is 131 LOC: lower route_same_owner to core (shared with java/kotlin/scala),
composer state stays shim-side with the decision logic moving behind the
graph-source trait (budget-ordering pins at finder.rs:773-830 are the
sentinels), the re-export chain inverts through the parked definition route
per the rust/python precedent, and the epoch prior-salt pin (the cfg(test)
helper plus its store invalidation test and PhpAdapter) stays in analysis.

# PHP seam census for the stage-3 fleet (2026-08-05)\n\nTaken at the W1–W6 merge tip, same shape as the Go/Rust/Python/C# predecessors. **Verdict summary: PHP needs no new shared prerequisite and has no coupling without a landed precedent.** No `semantic_model` park at all (PHP has no `external.rs`, no dependency-pack adapter, no workspace surface — the first fleet language with a zero-pack seam). No global/`static` parse memo, no rayon, one moka cache. `PreparedSyntaxTree` appears outside the lowerer only in the parked definition route. The R1-class inherent block is **131 LOC / 15 methods** — one-seventh of Python's. Projection: **~5.9k moves, ~1.12k production shim — below the Go floor.**\n\n---\n\n## 1. The seam, exact paths and `wc -l`\n\n### `crates/bifrost-analysis/src/analyzer/php/` — 9,031\n\n| File | LOC (total / prod) | Purpose |\n|---|---|---|\n| `semantic.rs` | 4,099 / 4,099 | PHP → executable-semantics IR lowerer; `impl_program_semantics_provider!(PhpAnalyzer, PhpSemanticLowerer)` at `:24`, `ADAPTER_VERSION = b\"php-value-semantics-v2\"` |\n| `diagnostics.rs` | 1,205 / 869 | `collect_php_semantic_diagnostics` (`:43`), `PhpDiagnosticCollector` (`:88`), `PHP_UNRECOGNIZED_SYMBOL`/`PHP_UNRECOGNIZED_MEMBER`; 336-LOC analyzer-bound fixture suite from `:870` |\n| `mod.rs` | 1,028 / 1,028 | `PhpAnalyzer` struct + 5 fields (`:62–68`); **28-method inherent block `:72–327`**; `TestDetectionProvider` (`:329`, empty); free fn `php_declaration_kind_for_range` (`:331–357`); `TypeHierarchyProvider` (`:359–381`); `CodeUnitIndex` (`:385–530`, 29 fns); `IAnalyzer` (`:532–715`, 28 fns); `AnalyzerTestHooks` (`:718–750`, 6 fns); free fns `php_aliases_visible_before`/`php_namespace_scope` (`:752–798`); **`PhpSupport` + `LanguageSupport` SPI block `:802–883`**; `php_import_alias_candidates`/`php_relevant_candidate_types` (`:885–942`); `PhpEdgePass` (`:944–959`); `StructuralReceiverResolver` (`:961–991`); `PhpDeadCodeBulk` (`:993–1028`) |\n| `declarations.rs` | 781 / 781 | `parse_php_file` → `ParsedFile` (`:29`), `php_declared_type_node` (`:609`), FQ/namespace naming over `FqName`/`SegmentInterner` |\n| `aliases.rs` | 638 / 638 | `PhpUseAliases` (`:7`), `PhpFileContext` (`:29`), `php_file_context_from_tree_at` (`:44`), `resolve_php_{type,function,constant}{,_node}`, `parse_php_use_aliases{,_by_kind,_from_source}`, `php_namespace_to_fq`; one `LazyLock<Regex>` (`:457`) |\n| `structural.rs` | 512 / 499 | `PHP_STRUCTURAL_SPEC` (`:20`), `PHP_KIND_TABLE` (`:22`), `impl StructuralSpec`; 13-LOC grammar-table test |\n| `tests.rs` | 280 / 280 | **Production**, not a test module — `detect_php_test_assertion_smells` (`:35`), `php_contains_tests` (`:217`); 6 `LazyLock<Regex>` |\n| `composer.rs` | 173 / 173 | `PhpComposerAutoload` (`:7`), `from_project` / `manifest_changed` / `target_is_autoloaded` / `class_is_autoloaded`, PSR-4 root decode over `serde_json` |\n| `clones.rs` | 170 / 170 | token/AST-label normalization (`:13–148`) + `build_php_clone_candidate_data` entry (`:149`) |\n| `adapter.rs` | 145 / 145 | `PhpAdapter: LanguageAdapter` (`:34`), `PHP_COGNITIVE_CONFIG` (`:9`), `php_signature_return_type_text` (`:87`), query dir `\"resources/treesitter/php\"` |\n\n### `crates/bifrost-analysis/src/analyzer/usages/` — 5,402\n\n| File | LOC (total / prod) | Purpose |\n|---|---|---|\n| `get_definition/php.rs` | 2,557 / 2,134 | `PhpDefinitionProvider` (`:20`) + its **complete eight-method `BoundedDefinitionLookup` impl** (`:31–99`), `php_type_lookup_resolution_bounded` (`:129`), `resolve_php_bounded` (`:261`), `resolve_php_with_session` (`:284`), ~65 bounded syntax helpers |\n| `php_graph/extractor.rs` | 810 / 810 | Per-symbol forward scan (`scan_file`, `Parser::new()` at `:43`) |\n| `php_graph/inverted.rs` | 514 / 514 | Whole-workspace per-file walk (`build_php_edges` `:55`, `PhpScan` `:84`); already on the W2 contract |\n| `get_type/php.rs` | 396 / 55 | `resolve_php_type_bounded` (thin; 341 LOC of tests) |\n| `php_graph/syntax.rs` | 388 / 388 | Shared PHP syntax/type helpers, used by both scans **and** by the parked `get_definition/php.rs` |\n| `php_graph/resolver.rs` | 300 / 300 | `TargetSpec`/`TargetKind` (`:13,22`), `PhpHierarchyIndex` (with a scan-local `RefCell` memo, `:79`), `node_text`, `qualified_candidate_text` |\n| `php_graph/hits.rs` | 170 / 170 | `push_hit`/`push_hit_range`/`push_import_hit`/`push_self_receiver_hit_range`/`push_override_declaration_hit` |\n| `php_graph/shared.rs` | 129 / 129 | `PhpQueryResolver`/`PhpEdgeResolver` (the two `resolve_analyzer::<PhpAnalyzer>` sites) |\n| `php_graph.rs` | 138 / 138 | `build_php_usage_edges{,_weights}`, `PhpDeadCodeBulkEligibility` + `dead_code_bulk_eligibility` (`:50–73`), `PhpUsageGraphStrategy`, and a 4-item `pub(in crate::analyzer::usages)` re-export block (`:9–14`) |\n\n**Seam total: 14,433 LOC** (13,320 prod / 1,113 in-file test) **+ 78 LOC of `.scm`** (`crates/bifrost-analysis/resources/treesitter/php/{definitions.scm,imports.scm}` = 77/1), `include_str!`'d at `analyzer/store/epoch.rs:224–232`, `lang_epoch!` salt at `epoch.rs:443–449` (path `\"treesitter/php/\"`, salt `\"synthetic-file-scope-code-units-2026-07;ast-test-detection-2026-07;fq-interned-segments-2026-07;conditional-free-function-declarations-2026-07\"`). Relocation requires a bump — and note `epoch.rs:459` carries a `#[cfg(test)] php_epoch_before_conditional_free_function_declarations()` that hard-codes the *prior* salt string and is asserted at `store/mod.rs:9697–9733`; that pin has to be re-derived at the same time.\n\n### Plan-era knowledge, re-verified\n- **Composer/import-alias candidate augmentation on `PhpSupport`**: present, `php/mod.rs:862–875` + helpers `:885–942`. PHP, Python and Rust are the only three `candidate_augmentation` implementors (`languages.rs:136` default, overridden at `rust/mod.rs:1035`, `python/mod.rs:885`, `php/mod.rs:862`). PHP is the only one using the **supplemental** arm — `languages.rs:669–681`'s doc names PHP's composer expansion as the reason the protected/supplemental split exists.\n- **`PhpUseAliases` public exports**: `analyzer/mod.rs:169–172` — `PhpAnalyzer`, `PhpUseAliases`, `parse_php_use_aliases`, `parse_php_use_aliases_by_kind`, `parse_php_use_aliases_from_source`, `php_namespace_to_fq`. Only external consumer is `tests/suite_analyzers/php_analyzer_test.rs:5,339–350`; `src/lib.rs:23` re-exports `PhpAnalyzer` only.\n- **`php_namespace_to_fq`**: one in-seam consumer, `composer.rs:103`, and it reaches it **through the hub** (`crate::analyzer::php_namespace_to_fq`) rather than `super::aliases` — a 1-line circularity to unwind on the move.\n\n### PHP root test suites (names only) — 5,113 LOC\n`tests/suite_analyzers/`: `php_analyzer_test.rs` (361), `php_analyzer_update_test.rs` (65), `php_test_detection_test.rs` (158), `php_type_hierarchy_test.rs` (235).\n`tests/suite_usages/`: `usages_php_graph_test.rs` (2,187), `usage_graph_php_test.rs` (525).\n`tests/suite_smells/`: `php_dead_code_smells.rs` (425), `php_structural_clone_smells.rs` (257), `scala_php_test_assertion_smells.rs` (411).\n`tests/suite_semantic/`: `semantic_value_php_contract.rs` (266).\n`tests/suite_lsp_parity/`: `phpactor_goto_definition.rs` (127), `phpactor_find_references.rs` (96).\nFixtures: `tests/fixtures/{testcode-php, usage-graph-php}`.\nCross-cutting suites exercising PHP without a PHP name (24 files), incl. `suite_persistence/analyzer_capability_parity.rs`, `suite_cross_language/{cross_language_self_usages, cross_language_import_hits, cross_language_attribute_target_declarations, structural_search_cross_language}.rs`, `suite_usages/receiver_language_{acceptance_matrix,scope,uncertainty}.rs`, `suite_symbols/{get_definition_test, searchtools_fuzzy_symbol_lookup}.rs`, `suite_semantic/{icfg_contract, semantic_language_conformance}.rs`.\n\n---\n\n## 2. Per-file classification\n\n**(a) core-resident already** — `Language`, `CodeUnit`, `CodeUnitType`, `ProjectFile`, `Range`, `Project` (`bifrost-core/src/analyzer/project.rs:200`), `CodeUnitIndex` (incl. `project()`, `code_unit_index.rs:24`), `ParsedFile` (W1), `FqName`/`SegmentId`/`SegmentKind`/`segment_interner`, `SignatureMetadata`/`ParameterMetadata`, `DirectDescendantIndex`/`build_direct_descendant_index`, `hash::{HashMap,HashSet}`, `text_utils::*`, `cognitive_complexity::Config`, `TestAssertionSmell`/`TestAssertionWeights`, `tree_walk::{collect_parse_errors,subtree_contains,walk_named_tree_preorder}`, `semantic_diagnostics::{node_range,node_text}`, `structural::{kinds,spec,edges,routes,occurrences,resolution,adapter_helpers}` incl. `INVERSE_REFERENCE_EDGE_SUPPORT`/`NO_IDENTITY_ROUTE_SUPPORT`/`NO_OCCURRENCE_ROLE_SUPPORT`/`NO_LEXICAL_ENVIRONMENT_SUPPORT`, `usages::{local_inference, inverted_edges (incl. `ClassRangeIndex`), model, outcome, traits::UsageScanScope, receiver_analysis, reference_site, common::{SNIPPET_CONTEXT_LINES, usage_hit, same_node, reclassify_*}}`, `BoundedDefinitionLookup` (six/eight-method), `TypeHierarchyProvider`, `TestDetectionProvider`, `QueryBatch`/`LimitedQueryRows` (W6), `PreparedSyntaxTree` (W5), `IndexedFileFacts`.\n\nClean today, zero analysis machinery: **`declarations.rs`, `aliases.rs`, `structural.rs`, `tests.rs`, `composer.rs`, `adapter.rs` bodies, `clones.rs:13–148`, `mod.rs`'s two free-fn bands.**\n\n**(b) retarget-only after the landed workstreams** — `php_graph/hits.rs` (core `usages::common` + `CodeUnitIndex`), `php_graph/inverted.rs`'s W2 half (`FileEdgeScanInput`/`PerFileEdges`/`classify_reference_node`/`ClassRangeIndex` all core; `build_edge_output`/`parse_and_collect` stay driver-side exactly as Python's `graph/inverted.rs`), `php_graph/resolver.rs`, `php_graph/syntax.rs`.\n\n**(c) analysis machinery, per file**\n\n| File | Machinery | Covered by an established pattern? |\n|---|---|---|\n| `php_graph/extractor.rs` | `&PhpAnalyzer` (11 sigs), `&dyn IAnalyzer`; only `file_context_from_source`, `parent_of`, `ranges`, `declarations`, `direct_children` reached | **Yes** — `PythonGraphSource`/`GoGraphSource` idiom, plus a 2-method PHP extension |\n| `php_graph/inverted.rs` | W2 contract + `usages::same_owner::route_same_owner` (4 sites) | **Half.** W2 covers the scan; `route_same_owner` is a 15-LOC `pub(super)` analysis fn (`same_owner.rs:36`) that **no landed language crate carries** — Go/Python inline the branch. Small lowering, shared with java/kotlin/rust/scala |\n| `php_graph/resolver.rs`, `syntax.rs` | `&PhpAnalyzer` (8 sigs each) → `get_direct_ancestors`, `is_interface`, `direct_declared_class_parent` | **Yes** — graph-source idiom |\n| `php_graph.rs` | `resolve_analyzer::<PhpAnalyzer>`, `GraphUsageAnalyzer`/`UsageAnalyzer` impls, `dead_code_bulk_eligibility` downcasting into `TargetSpec::from_target` | **Yes** — downcast-in-shim (Go's `GoQueryResolver` precedent); the eligibility fn splits at the downcast |\n| `php_graph/shared.rs` | `resolve_analyzer`, `analyzed_files_for_language`, orchestration loop | **Yes** — the analysis-side residue Python keeps in `python_graph.rs` |\n| `php/mod.rs` inherent block `:72–327` | `TreeSitterAnalyzer<PhpAdapter>` field, `store::LimitedQueryRows` at 9 sites, `moka::sync::Cache`, `build_weighted_cache` | **Yes for the shim half** (W6 landed `LimitedQueryRows`). The 131 LOC at `:196–326` is R1-class — see §3 |\n| `php/diagnostics.rs` | `analyzer.global_usage_definition_index()` → `DefinitionIndexHandle`; uses **only** `fqn`, `fqn_exists`, `package_exists` | **Yes** — byte-identical to Go/Python: takes `&dyn BoundedDefinitionLookup` (`bifrost-python/src/diagnostics.rs:15`) |\n| `php/clones.rs:149` | `clone_detection::{CloneCandidateData,compact_clone_excerpt}` (analysis) + `&PhpAnalyzer` | **Yes** — normalizer moves, ~22-LOC entry stays (Python `clones.rs` 104-in-crate precedent) |\n| `php/adapter.rs` | `tree_sitter_analyzer::ParsedFile` (core after W1), `LanguageAdapter` | **Yes** — Go's forwarding shell; nothing PHP-specific left in analysis but the impl |\n| `php/semantic.rs` | `analyzer::semantic::*`, `PreparedSyntaxTree`, `lexical_definitions::formal_parameter_slots_for_owner`, registration macro naming `PhpAnalyzer` | **Parked** — same as Go/Rust/Python/C# `semantic.rs` |\n| `get_definition/php.rs`, `get_type/php.rs` | `ResolutionSession` (51 refs, 48 fns take it), `use super::*` glob of the hub, `usages::target_kind::TypeLookupTargetKind` | **Parked** — same as the fleet's definition routes |\n\n**R1-class inherent block — sized.** One inherent `impl PhpAnalyzer` block, `mod.rs:72–327` (256 LOC / 28 methods). Constructors, `self.inner` forwarders and the six `*_limited` accessors are `:73–194` = 122 LOC / 13 methods and stay shim-side. **Language logic needing free-function rewrite: `:196–326` = 131 LOC / 15 methods, with ~28 in-seam call sites.** Compare Rust 2,896/73/303, C# 1,210/52/197, Python 916/37/117. **PHP is an order of magnitude smaller than any predecessor.** No self-recursive memo web: the only cell it fills is `direct_ancestors` (moka) via `get_direct_ancestors` → `resolve_declared_supertype` → `declaration_context` → `use_aliases_by_kind_of`, which never re-enters the cache; a **single-tier** `PhpAnalysisSource: CodeUnitIndex + TypeHierarchyProvider` with two PHP-specific methods (`file_context_from_source`, `is_interface`/`direct_declared_class_parent`) suffices. No two-tier split.\n\nSeparately, **65 free functions/signatures already take `&PhpAnalyzer`** (get_definition/php 26, extractor 11, resolver 8, syntax 8, hits 2, inverted 1, mod 2, clones 1) — the Go shape, mechanically retargetable.\n\n**`PreparedSyntaxTree` outside the lowerer.** Effectively none. Five hits in `php/semantic.rs` (`:17,47,146,539,557`); four in the parked `get_definition/php.rs` (`:1140,1147,1322,1351–1361`, all through `php_prepared_syntax_bounded` → `PhpAnalyzer::prepared_syntax_limited_cancellable`, a `self.inner` forwarder). Rust's W5-motivating problem does not exist for PHP.\n\n**moka / global caches (the RUST_TREES question).** No `static`/`LazyLock` cache anywhere in the seam. The only `LazyLock`s are `PHP_COGNITIVE_CONFIG` (`adapter.rs:9`, pure data) and 7 `Regex`es (`tests.rs` ×6, `aliases.rs:457`). All caching is instance-resident on `PhpAnalyzer`: **one** moka `Cache` (`direct_ancestors`, `mod.rs:65`, `build_weighted_cache(memo_budget / 8, …)`), **one** `OnceLock` (`direct_descendant_index`), plus `Arc<PhpComposerAutoload>`. **No `PoolSafeMemo`, no `cache.rs`, no rayon, no `Mutex`.** The one `RefCell` (`php_graph/resolver.rs:79`, `PhpHierarchyIndex::subtype_matches`) is scan-local and moves with the file. **Verdict: moka stays in analysis; no deviation to record.** No parse memo of any kind — 8 sites do a bare `Parser::new()` + `set_language(&tree_sitter_php::LANGUAGE_PHP.into())` + full reparse per call (`adapter.rs:103`, `clones.rs:119`, `diagnostics.rs:81`, `mod.rs:269,317`, `php_graph/hits.rs:143`, `php_graph/extractor.rs:43`, `get_definition/php.rs:619`).\n\n**Dead-code bulk-proof shape.** PHP is the delegating shape (unlike C#'s inline predicate). `PhpDeadCodeBulk` (`php/mod.rs:993–1028`) has `needs_precise_scan` → `usages::php_graph::dead_code_bulk_eligibility` (**still where the registry-era census put it: `usages/php_graph.rs:56–73`**), which downcasts and classifies through `resolver::TargetSpec::from_target`: `Type|Function|Method` → `BulkSafe`, `Constructor|Field|Constant` → `NeedsPrecise`. `preflight` → `analyzable_file_count`; `build` → `fqn_bulk_nodes` + `build_php_usage_edges`. There is **no PHP entry-point predicate in `code_quality/dead_code_smells.rs`** — only `php_graph_finding`/`php_method_candidate`/`php_method_graph_finding` scoring at `:1130–1195` and a `\"PHP\"` label at `:1648`. PHP therefore does not have C#'s homeless-predicate problem.\n\n---\n\n## 3. PHP-specific hard spots, examined\n\n**The composer machinery.** `PhpAnalyzer.composer_autoload: Arc<PhpComposerAutoload>` (`mod.rs:67`) is analyzer-resident state built eagerly in `from_inner` (`mod.rs:107`) from `PhpComposerAutoload::from_project(inner.project())`, and rebuilt in `update` (`mod.rs:589–597`) only when a changed file satisfies `PhpComposerAutoload::manifest_changed` (`rel_path() == \"composer.json\"`). This is the *shape* of C#'s `external_index`/the JVM realm's `JvmExternalDeclarationIndex` — analyzer-resident derived state from an out-of-band manifest — but with none of the difficulty: `composer.rs` is 173 self-contained LOC over `&dyn Project` + `serde_json`, `Project` is core, `go/packages.rs:31` already proves `&dyn Project` in a language crate, and there is no pack producer welded to it. It moves whole; only the `Arc<…>` field and the `update` refresh stay shim-side.\n\n`target_has_composer_autoload_visibility` (`mod.rs:252`) is a 3-line forwarder to `composer_autoload.target_is_autoloaded(self, target)`, which reads `analyzer.parent_of` and the PSR-4 roots. Its one consumer is `candidate_augmentation`.\n\n**The candidate augmentation that extends with EVERY analyzed PHP file.** `PhpSupport::candidate_augmentation` (`mod.rs:862–875`) does exactly two things:\n1. if `target_has_composer_autoload_visibility(target)`, `files.extend(analyzed_files_for_language(ctx.analyzer, Language::Php))` — the whole language's file set, deliberately, with the doc comment at `:858–861` recording that this is \"accurate but unbounded, and worth dropping the moment a budget bites\";\n2. `files.extend(php_import_alias_candidates(...))` (`:885–916`) which, for the target's owner type **plus every descendant** (`type_hierarchy_provider().get_descendants`), collects declaring files and then **re-reads `use_aliases_by_kind_of` for every analyzed PHP file** (`:906–915`) — a whole-workspace source read per query.\n\nBoth arms return `CandidateAugmentation::supplemental`, and `languages.rs:669–681` documents the protected/supplemental split as existing *for this*. The behavior contract is pinned by two production-code tests in `analyzer/usages/finder.rs:773–830` (budget-drop ordering; cancellation abandoning the whole query rather than yielding a partial set) sitting inside `#[cfg(test)]` from `:451`. Machinery used: `analyzed_files_for_language` is analysis-`pub(crate)` (`usages/common.rs:45`), everything else is `CodeUnitIndex`/`TypeHierarchyProvider`. So this moves as ~58 LOC behind the graph-source trait with a file-list argument, and the 14-LOC SPI shell stays.\n\n**`PhpFileContext` / `resolve_php_*` pub(crate) reach-ins from the re-export hub — full consumer enumeration.** `analyzer/mod.rs:173–175` re-exports five names `pub(crate)` (`PhpFileContext`, `php_signature_return_type_text`, `resolve_php_constant`, `resolve_php_function`, `resolve_php_type`); `php/mod.rs:49–52` additionally re-exports `php_file_context_from_tree_at`, `resolve_php_{type,function,constant}_node` inside the module. Consumers:\n\n| Consumer | Names used | Disposition |\n|---|---|---|\n| `php/mod.rs:304` | `resolve_php_type` | shim, moves with R1 |\n| `php/diagnostics.rs:5–7,254–256,414,579,602,664,673` | `PhpFileContext`, all three `resolve_php_*` | moves |\n| `usages/php_graph/inverted.rs:46–47` (+ `:97,180,343,431`) | `PhpFileContext`, all three | moves |\n| `usages/php_graph/extractor.rs:21–22` (+ 15 sites) | `PhpFileContext`, all three | moves |\n| `usages/php_graph/resolver.rs:4–5,182,198` | `PhpFileContext`, `resolve_php_type` | moves |\n| `usages/php_graph/syntax.rs:5–6,99,116,174,387` | `PhpFileContext`, `resolve_php_type`, `php_signature_return_type_text` | moves |\n| `usages/php_graph.rs:9–14` | re-exports `PhpFileContext as FileContext` + the three resolvers **outward** as `pub(in crate::analyzer::usages)`, alongside `php_node_text`/`php_qualified_candidate_text` from `resolver.rs` | shim; the aliases invert |\n| `usages/get_definition/mod.rs:43–45` | consumes that outward block purely to feed its `use super::*` glob | **parked**; must import from the crate |\n| `usages/get_definition/php.rs:6–7,146,224,321,379–399,544,997,…` (~30 sites) | `php_file_context_from_tree_at`, `resolve_php_*_node`, `resolve_php_*` | **parked**; imports from the crate |\n| `usages/get_definition/php.rs:9–14` | 8 items from `usages::php_graph::syntax` | **parked**; imports from the crate |\n\nThe direction is one-way — **`php_graph/*` has zero references to `ResolutionSession`, `get_definition`, `get_type` or `DefinitionBatchContext`** (verified by grep across all seven files). PHP therefore has none of C#'s bidirectional-resolver problem; the parked definition route imports from the landed crate, exactly as `get_definition/{python,rust,go}.rs` already do (`get_definition/rust.rs:19–23`, `python.rs:15`).\n\n**Anything with no precedent.** One small item and no large one: `usages::same_owner::route_same_owner` (`same_owner.rs:36`, 15 LOC, `pub(super)`) is called at four sites in `php_graph/inverted.rs:200,229,270,314`. Neither `brokk-bifrost-go`, `-rust` nor `-python` names it (Go and Python inline the branch; Rust's inverted scan parked). It is a trivial two-closure router and java/kotlin/rust/scala all need it where it is, so it is a verbatim core lowering of the `test_assertions.rs`/`enclosing_owner_chain` class from Py-2's decision log — not a new abstraction.\n\n---\n\n## 4. Reverse edges\n\n**Production, framework → PHP (all allowlisted or assembly, verified against `tests/suite_cross_language/language_reach_in_gate.rs`):**\n\n| Site | What |\n|---|---|\n| `analyzer/mod.rs:28,169–176` | Re-export hub: `mod php`; the 6-item `pub use php::{…}`; the 5-item `pub(crate) use php::{…}` (allowlist: \"re-export hub for the facade's public surface\") |\n| `analyzer/usages/mod.rs:30,84` | `pub(crate) mod php_graph` + `pub use php_graph::PhpUsageGraphStrategy` (allowlist) |\n| `analyzer/lib.rs:54` | Crate-root surface (allowlist) |\n| `analyzer/languages.rs:29,252,273,289,728,761,813` | `Language::Php` + `Language::Php => Some(&php::PhpSupport)` (assembly) |\n| `analyzer/multi_analyzer.rs:9,42,59,78,95,112,129,146,163,180,214,262` | `AnalyzerDelegate::Php` (assembly) |\n| `analyzer/usages/get_definition/mod.rs:43–45,82,116,144–146,1291` | 6 items from `php_graph` + `PhpAnalyzer` + `mod php` + the 3-item re-export + the dispatch arm (allowlist) |\n| `analyzer/usages/get_type/mod.rs:20,32` | `mod php` + `pub(crate) use php::resolve_php_type_bounded` (allowlist) |\n| `analyzer/usages/get_definition/call_sites.rs:346,769–771,781,954–963` | `php_call_reference_candidate` + `Language::Php` arms, incl. the `relative_scope` constructor special case (allowlist) |\n| `code_quality/dead_code_smells.rs:465,809,1130–1195,1648` | `php_graph_finding`/`php_method_candidate`/`php_method_graph_finding` scoring + label (allowlist). **No entry-point predicate** — unlike C# |\n| `analyzer/exception_handling.rs:79,505–540` | `analyze_php`, ~35 LOC of catch-clause knowledge in a framework file (the `exception_handling.rs` residue class) |\n| `analyzer/lexical_definitions.rs:481,866,914,951,1005,1042,1073,1104,1111,1170,1194,1213,1241` | 13 `Language::Php` grammar arms (shared machinery, parks) |\n| `analyzer/reference_candidates.rs:203,221` | 2 `Language::Php` arms |\n| `analyzer/structural/lexical_environment.rs:819` | 1 arm |\n| `analyzer/usages/receiver_query.rs:2748,2845` | 2 `Language::Php` arms, no type reach-in |\n| `analyzer/usages/workspace_graph.rs:34,60` | `UsageEcosystem::Php` |\n| `analyzer/value_flow/mod.rs:20` | `Language::Php` in the supported set |\n| `searchtools/selectors.rs:1573,1606`, `searchtools/scan_usages.rs:1120` | `\"php\"` / `\"PHP\"` labels |\n| `analyzer/store/epoch.rs:46,224–232,443–449,459–464` | `.scm` include paths + `lang_epoch!` salt + the cfg(test) prior-salt helper |\n\nNotably absent: **zero PHP references in `analyzer/common.rs`, `analyzer/symbol_lookup.rs` (beyond one test fixture at `:914`), `analyzer/index_warmer.rs`, `analyzer/usages/get_definition/trace.rs`, `analyzer/workspace.rs`, `analyzer/global_usage_definition_index.rs`.** `PhpSupport` overrides none of `display_symbol_name`/`source_identifier`/`alias_name_segment`.\n\n**cfg(test)-only PHP reach-ins** (each verified to sit after its file's `#[cfg(test)]`): `analyzer/structural/search/tests.rs:7,21,161–163,3786–3789` (whole file gated at `search/mod.rs:104`), `analyzer/structural/execution/derived.rs:1155,1578–1581` (gate `:1152`), `analyzer/store/mod.rs:7789,9697–9733` (gate `:7783`; `PhpAdapter` + the epoch pin), `analyzer/usages/finder.rs:730–830` (gate `:451`; the composer-budget fixtures), `analyzer/languages.rs:747,879,1032–1033` (gate `:742`), `analyzer/usages/get_definition/call_sites.rs:1351–1374` (gate `:1052`), `analyzer/cognitive_complexity_tests.rs:593–635`, `code_quality/comment_density.rs:459–593` (gate `:246`), `analyzer/symbol_lookup.rs:914` (gate `:869`), `analyzer/source_ingestion.rs:297–340`. All need `PhpAnalyzer`/`PhpAdapter` re-exported for dev-dependency use, exactly as Go and Python.\n\n**Cross-crate reach-ins: essentially none.**\n- `src/lib.rs:23` — facade re-export of `PhpAnalyzer`.\n- `bifrost-lsp`: **zero PHP type reach-ins.** Only `Language::Php` arms — `handlers/formatting.rs:294`, `handlers/hover.rs:120`, `handlers/semantic_tokens.rs:445–480` (test fixture); `tests/bifrost_lsp_server.rs:8474` asserts on the `\"bifrost-php\"` diagnostic-source string (`php/diagnostics.rs:14`).\n- `bifrost-mcp`: `searchtools_service.rs:336`, a `Language::Php` label arm.\n- `src/` root crate: `benchmark/manifest.rs:110–160,1462`, `reference_differential/mod.rs:471,1531–1534,2680–2691`, `bin/bifrost_reference_differential.rs`, `bin/bifrost_mcp_property_fuzzer.rs` — corpus-language strings only, no seam types.\n- `bifrost-core`, `bifrost-runtime`, `bifrost-policy`, `bifrost-semantic-packs`, `bifrost-nlp`: none. There is no `PhpAnalyzerConfig` in `bifrost-core/src/analyzer/config.rs`.\n\n---\n\n## 5. crates.io deps a `brokk-bifrost-php` crate needs\n\n| crate | version in `bifrost-analysis/Cargo.toml` | needed by |\n|---|---|---|\n| `brokk-bifrost-core` | path, `=` pin | everything |\n| `tree-sitter` | 0.25.10 | everything |\n| `tree-sitter-php` | **0.23.11** | 14 of the 16 in-repo sites (2 stay in analysis: `epoch.rs:461`, `store/mod.rs:9723`) |\n| `regex` | 1.12.2 | `tests.rs` (×6), `aliases.rs:457` |\n| `serde_json` | 1.0.145 | `composer.rs` only |\n\n**tree-sitter-php version quirk — two of them.** (1) The grammar is pinned at **0.23.11**, a minor generation behind Go/Python/Rust's 0.25.x, in the same class as `tree-sitter-c-sharp 0.23.1`; the PHP crate's `Cargo.toml` cannot inherit the fleet's 0.25 grammar line. (2) `tree-sitter-php` is the only fleet grammar exporting **two** language constants — `LANGUAGE_PHP` and `LANGUAGE_PHP_ONLY`. Every one of the 16 in-repo sites names `LANGUAGE_PHP` (the HTML-embedding dialect); `LANGUAGE_PHP_ONLY` is never used. Any `ParserFlavor` plumbing added later has to preserve that, and `PhpSupport::parser_language(&self, _flavor: ParserFlavor)` (`mod.rs:877`) already ignores the flavor argument.\n\n**Not** `moka` (the one cache stays shim-side), **not** `rayon` (zero uses — PHP joins C# as the second fleet language with none), **not** `semver`, **not** `walkdir`, **not** `goblin`, **not** `toml`, **not** `serde`. First landing is a **five-line dependency block**; only `serde_json` distinguishes it from Python's.\n\n---\n\n## 6. LOC accounting\n\nCalibration: Go 8,544 moved / 1,598 shim; Rust 16,375 / ~1,900; Python 10,129 / ~1,590 prod (2,471 incl. retained tests); C# projected ~8,180 / ~1,660.\n\n| Band | LOC | Content |\n|---|---:|---|\n| **Movable now** (retarget only) | **~2,684** | `declarations.rs` 781, `aliases.rs` 638, `structural.rs` 512, `tests.rs` 280, `composer.rs` 173, `clones.rs` normalizer ~136, `adapter.rs` bodies + `PHP_COGNITIVE_CONFIG` ~90, `mod.rs` free-fn bands (`:331–357`, `:752–798`) 74 |\n| **Movable after the graph-source + R1 pass** | **~3,251** | `php_graph/extractor.rs` 810, `inverted.rs` 514 (needs `route_same_owner` lowered), `syntax.rs` 388, `resolver.rs` 300, `hits.rs` 170; `diagnostics.rs` prod ~845 (Go/Python `BoundedDefinitionLookup` precedent); `mod.rs:196–326` R1 logic 131 (**15 methods, ~28 in-seam call sites**); `mod.rs:885–942` augmentation helpers 58; `php_graph.rs` free builders + eligibility split ~35 |\n| **Parked on `analyzer/semantic`** | **4,099** | `php/semantic.rs` (registration macro textually requires the `TreeSitterAnalyzer` field) |\n| **Parked on `semantic_model`** | **0** | PHP has no `external.rs`, no pack adapter, no `dependency_discovery.rs`, no workspace surface — **the fleet's first zero-pack seam** |\n| **Parked on `ResolutionSession`** | **~2,968** | `get_definition/php.rs` 2,557 + `get_type/php.rs` 396 + ~15 LOC of `get_definition/mod.rs` PHP wiring |\n| **Shim floor (production)** | **~1,120** | `mod.rs` shim half ~765 (imports ~50, struct 7, ctors/forwarders 122, `TypeHierarchyProvider` 23, `CodeUnitIndex` 146, `IAnalyzer` 184, `AnalyzerTestHooks` 33, SPI block 80 + augmentation shell 14, `PhpEdgePass` 16, `StructuralReceiverResolver` 31, `PhpDeadCodeBulk` 36); `php_graph/shared.rs` 129; `php_graph.rs` strategy wrappers ~103; `adapter.rs` `LanguageAdapter` impl ~55; `clones.rs` entry ~40; `diagnostics.rs` entry ~25 |\n| **Retained analyzer-bound tests** | **~336** | `diagnostics.rs:870–1205` fixture suite (`PhpAnalyzer` + `TestProject`) |\n\n**Total 14,433** (13,320 prod / 1,113 in-file test) **+ 78 `.scm`.**\n- Moves ≈ **5,935 (41 % of the seam, 80 % of the seam outside the parks)**.\n- Parked ≈ **7,067 (49 %)** — `semantic.rs` plus the definition/type routes, nothing else.\n- Analysis residue ≈ **8,498**, of which **~1,120 is production shim** — **below the Go 1,598 floor**, because PHP has no `cache.rs`, no `ImportAnalysisProvider` impl (verified: `PhpAnalyzer` implements none, and `imports.scm` is a single line), no separate `hierarchy.rs`, no external-index accessors, and a 12-method `LanguageSupport` block that overrides none of the name-normalization hooks.\n- Predicted analysis-frontend effect at Go's measured 0.47 s/kLOC: **≈ −2.8 s** — mid-fleet, behind Rust, Python, Go and C#-Scenario-B, ahead of C#-Scenario-A.\n\nMove rate outside the parks (80 %) is second only to Python's 93 %, and for the same reason inverted: Python got there by pre-retiring its R1 block, PHP gets there by never having had one.\n\n---\n\n## The three hardest couplings\n\n**1. The composer/import-alias candidate augmentation, and its whole-workspace behavior contract.** `PhpSupport::candidate_augmentation` (`php/mod.rs:862–875`) is the only *supplemental* augmentation in the fleet, and `languages.rs:669–681` documents the protected/supplemental split as existing specifically because \"a single autoload-visible target pulls in every analyzed PHP file\". The body reaches `target_has_composer_autoload_visibility` (→ the analyzer-resident `Arc<PhpComposerAutoload>`, rebuilt in `update` at `mod.rs:589–597` on a `composer.json` change), `analyzed_files_for_language` (analysis-`pub(crate)`, `usages/common.rs:45`), `analyzer.definitions`, `type_hierarchy_provider().get_descendants`, and then a **per-file `use_aliases_by_kind_of` source read across the entire workspace** (`mod.rs:906–915`). The pieces are individually precedented — `composer.rs` is 173 clean LOC over core `Project` (Go's `packages.rs:31` proves `&dyn Project` in a language crate) and everything else is `CodeUnitIndex`/`TypeHierarchyProvider` — but the split point is unusual: the *state* (`Arc<PhpComposerAutoload>` field, the `manifest_changed` refresh) must stay on the shim while the *decision* leaves, and the budget-ordering and cancellation semantics are pinned only by two production-code tests at `analyzer/usages/finder.rs:773–830`. Getting the file-set expansion to happen on the far side of the trait without either eagerly materializing `analyzed_files_for_language` or changing which files the budget drops first is the one place PHP can silently regress.\n\n**2. The `PhpFileContext` / `resolve_php_*` re-export chain has to invert through a parked file.** Five names travel `php/aliases.rs` → `php/mod.rs:49–52` → `analyzer/mod.rs:173–175` → `usages/php_graph.rs:9–14` (re-aliased `pub(in crate::analyzer::usages)`, `PhpFileContext as FileContext`) → `get_definition/mod.rs:43–45` → the `use super::*` glob that `get_definition/php.rs` resolves ~30 call sites through, plus 8 more items it takes directly from `usages::php_graph::syntax`. When `aliases.rs` and all of `php_graph/` move and `get_definition/php.rs` parks, that chain must be rebuilt in the opposite direction: analysis's parked definition route importing `brokk_bifrost_php::{aliases, graph::syntax}`. The precedent is exact (`get_definition/rust.rs:19–23`, `python.rs:15`), and — decisively — **`php_graph/*` imports nothing from `get_definition`/`get_type`/`ResolutionSession`**, so this is one-directional and has none of C#'s resolver bidirectionality. Two residues to unwind while doing it: `php_graph.rs`'s outward `pub(in crate::analyzer::usages)` alias block, and `composer.rs:103`'s call to `crate::analyzer::php_namespace_to_fq` (reaching its own sibling through the hub). The four `pub` alias names (`PhpUseAliases`, the three `parse_php_use_aliases*`, `php_namespace_to_fq`) are facade API consumed at `tests/suite_analyzers/php_analyzer_test.rs:5`, so the shim must re-export them at their historical `crate::analyzer::` paths — the `analyzer/mod.rs:120–123` `brokk_bifrost_go::packages` idiom.\n\n**3. The epoch-salt relocation carries a hard-coded historical pin.** Moving `resources/treesitter/php/{definitions,imports}.scm` (78 LOC) changes the salted path, so the `lang_epoch!(Php, …)` salt at `epoch.rs:443–449` bumps — routine. What is not routine is `epoch.rs:459–464`'s `#[cfg(test)] php_epoch_before_conditional_free_function_declarations()`, which recomputes the epoch from a **literal prior salt string** and `tree_sitter_php::LANGUAGE_PHP`, and is asserted end-to-end by `store/mod.rs:9697–9733` (`php_conditional_free_function_epoch_invalidates_prior_parsed_blobs`) — a persisted-blob invalidation test that writes a blob under the old epoch and asserts it is evicted under the new one. That helper's literal, its `tree_sitter_php` dependency and the `PhpAdapter` it parses with (`store/mod.rs:7789`) all have to survive in `bifrost-analysis` after the grammar crate and the `.scm` files leave, or the invalidation guarantee for existing PHP blobs loses its only test. Rust's `lang_epoch!` comment at `epoch.rs:440–442` records the same relocation problem but had no prior-salt pin to preserve.\n\nTwo smaller items for completeness: `usages::same_owner::route_same_owner` (15 LOC, `same_owner.rs:36`, four PHP call sites) is the seam's only helper with no landed-crate carrier and needs a verbatim core lowering shared with java/kotlin/rust/scala; and `usages/php_graph.rs:56–73`'s `dead_code_bulk_eligibility` splits at its `resolve_analyzer` line — the downcast stays, the `TargetSpec`-based classification follows `resolver.rs` into the crate."}],"stop_reason":"end_turn","stop_sequence":null,"stop_details":null,"usage":{"input_tokens":2,"cache_creation_input_tokens":788,"cache_read_input_tokens":177909,"output_tokens":16395,"server_tool_use":{"web_search_requests":0,"web_fetch_requests":0},"service_tier":"standard","cache_creation":{"ephemeral_1h_input_tokens":0,"ephemeral_5m_input_tokens":788},"inference_geo":"not_available","iterations":[{"input_tokens":2,"output_tokens":16395,"cache_read_input_tokens":177909,"cache_creation_input_tokens":788,"cache_creation":{"ephemeral_5m_input_tokens":788,"ephemeral_1h_input_tokens":0},"type":"message"}],"speed":"standard"},"diagnostics":null},"requestId":"req_011Cdjs5mNzzwSnjQtaDRzUk","attributionAgent":"Explore","type":"assistant","uuid":"3ee2d665-05cc-4914-a36f-5927f146ca24","timestamp":"2026-08-05T15:51:11.741Z","effort":"high","userType":"external","entrypoint":"cli","cwd":"/home/jonathan/Projects/bifrost2","sessionId":"8f94fdd6-1d55-4f9d-8f78-532b5b220270","version":"2.1.220","gitBranch":"bifrost2","slug":"solve-https-github-com-brokkai-bifrost-i-atomic-hopper"}
