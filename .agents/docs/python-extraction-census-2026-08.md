# Python seam census for the stage-3 fleet (2026-08-05)

Agent-run census at the W1-W5 tip. Coordinator dispositions, recorded here:
hard coupling 1 (PythonUsageIndex::build over bulk FileState) resolves by the
W5 idiom applied to bulk state -- the Python source trait exposes a bulk
accessor that yields narrowed per-file views (declarations, imports, source);
the chunked bulk_file_states read stays shim-side and FileState never lowers.
The two BoundedDefinitionLookup additions (types_in_package, by_normalized_fqn)
extend the core trait. parse_symbol_path lowers to core as a shared helper.
Coupling 2 (external.rs / environment packs) parks on the semantic_model
workstream with the workspace surface staying put regardless. Coupling 3 is
the Py-R1 rewrite with the two-tier source-trait split, Rust precedent.

\n\nTaken at the W1–W5 merge tip. **Verdict summary: Python needs no new shared prerequisite.** W5 (`PreparedSyntaxTree` → core) already landed and Python touches it only in the parked lowerer; there is no global parse memo (no RUST_TREES-class deviation — moka stays shim-side exactly as Go); the R1-class inherent block exists but is ~1/3 of Rust's. Two named gaps are Python-only and small (a `BoundedDefinitionLookup` two-method extension, and a bulk-`FileState` read with no precedent). Projection: **~10.1k moves, ~1.6k shim — dead on the Go floor.**\n\n## 1. The seam, exact paths and `wc -l`\n\n### `crates/bifrost-analysis/src/analyzer/python/` — 12,661\n\n| File | LOC (prod/test) | Purpose |\n|---|---|---|\n| `semantic.rs` | 3,423 / 0 | Python → executable-semantics IR lowerer; `impl_program_semantics_provider!(PythonAnalyzer, PythonSemanticLowerer)` at :26 |\n| `external.rs` | 1,795 / 0 | `PythonDependencyPackAdapter`, `PythonArtifactPackProducer`, `resolve_python_semantic_pack_dependencies` (:977); static discovery of interpreter/stub/dist-info inputs + tree-sitter API collector |\n| `mod.rs` | 1,395 / 0 | `PythonAnalyzer` struct + 6 moka caches + 2 OnceLocks + PoolSafeMemo; 31 inherent methods (:113–782); `impl CodeUnitIndex` (:793–972), `impl IAnalyzer` (:974–1180), `AnalyzerTestHooks` (:1183–1225), `PythonSupport` SPI (:1230–1307), `PythonEdgePass` (:1309–1324), `StructuralReceiverResolver` (:1326–1356), `PythonDeadCodeBulk` (:1358–1395) |\n| `imports.rs` | 1,080 / 74 | Import parsing/`StructuredImportPath`; 11 inherent `impl PythonAnalyzer` resolution methods (:279–666); `impl ImportAnalysisProvider` (:667–…) |\n| `diagnostics.rs` | 689 / 328 | Unrecognized-symbol semantic diagnostics; `PythonDiagnosticCollector` |\n| `bindings.rs` | 993 / 0 | Lexical scope inventory / direct-scope bindings, bounded (pure `Node` walking) |\n| `declarations.rs` | 907 / 0 | `PythonVisitor`, `python_module_fq`, `module_code_unit`, identifier collection, `parse_python_tree` |\n| `usage_index.rs` | 540 / 101 | `PythonUsageIndex` (re-export + importer index), `ModuleBindingTimeline`, `PythonScopeFacts`; 7 inherent accessors (:481–540) |\n| `tests.rs` | 370 / 0 | Python test detection + assertion smells (production code) |\n| `structural.rs` | 318 / 76 | `PYTHON_STRUCTURAL_SPEC` (`StructuralSpec` impl) |\n| `adapter.rs` | 168 / 0 | `PythonAdapter: LanguageAdapter` + `PYTHON_COGNITIVE_CONFIG` |\n| `syntax.rs` | 159 / 0 | `PythonOverloadDecoratorBindings`, `expression_name_node`, `decorator_callee` |\n| `clones.rs` | 121 / 0 | `build_clone_candidate_data` + Python clone token normalization |\n| `cache.rs` | 97 / 0 | `PythonUsageEdgesKey` + three moka weight functions |\n| `hierarchy.rs` | 27 / 0 | `impl TypeHierarchyProvider for PythonAnalyzer` |\n\n### `crates/bifrost-analysis/src/analyzer/usages/` — 6,897\n\n| File | LOC (prod/test) | Purpose |\n|---|---|---|\n| `python_graph/extractor.rs` | 2,427 / 71 | `PythonProjectGraph`, forward scan, scope facts, module-binding timeline; rayon+Mutex |\n| `python_graph/inverted.rs` | 766 / 0 | Whole-workspace inverted per-file walk (`build_python_edges`, `PyScan`) |\n| `python_graph/resolver.rs` | 645 / 0 | Export-name/seed inference, receiver-type resolution, annotation candidates |\n| `python_graph/hits.rs` | 62 / 0 | Hit recording (`record_hit`/`record_unproven_hit`/`record_self_receiver_hit`) |\n| `python_graph.rs` | 277 / 0 | `build_python_usage_edges`, `build_python_usage_edge_weights`, `build_cached_python_usage_edges_for_targets`, `python_usage_candidate_files`, `PythonQueryResolver`, `PythonEdgeResolver`, `PythonExportUsageGraphStrategy` |\n| `get_definition/python.rs` | 2,360 / 237 | `PythonDefinitionProvider`, `PythonDefinitionContext`, `resolve_python`, `resolve_python_bounded`, `python_type_lookup_resolution_bounded` + ~40 bounded syntax helpers |\n| `get_type/python.rs` | 52 / 0 | `resolve_python_type_bounded` thin entry |\n\n**Seam total: 19,558 LOC** (18,671 prod / 887 in-file test) **+ 183 LOC of `.scm`** (`resources/treesitter/python/{definitions,identifiers,imports}.scm`, 135/1/47), salted at `analyzer/store/epoch.rs:172–181,431–433` — the salt string is `\"synthetic-file-scope-code-units-2026-07;structured-python-import-paths-2026-07;fq-interned-segments-2026-07\"`; relocation requires a bump.\n\n### Workspace surface (intentionally-public, allowlisted)\n- `analyzer/workspace.rs:214–261` — `WorkspaceAnalyzer::activate_python_environment_packs`, plus `PythonSemanticModelWorkspaceContext` (:176) and `PythonSemanticModelActivationOutcome` (:185–207).\n- `analyzer/python/external.rs:30,33,977` — `PythonDependencyPackAdapter`, `PythonArtifactPackProducer`, `resolve_python_semantic_pack_dependencies`.\n- Re-exported through `analyzer/mod.rs:183–192, 220` and `lib.rs:54`.\n\n### Python root test suites (names only) — 10,133 LOC\n`tests/suite_analyzers/`: `python_analyzer_test.rs`, `python_module_analyzer_test.rs`, `python_type_hierarchy_test.rs`, `python_decorators_test.rs`, `python_could_import_test.rs`, `python_test_detection_test.rs`, `python_analyzer_update_test.rs`.\n`tests/suite_usages/`: `usages_python_graph_test.rs` (3,587), `usages_python_test.rs`, `usage_graph_python_test.rs`.\n`tests/suite_smells/`: `python_js_ts_dead_code_smells.rs` (1,419), `python_structural_clone_smells.rs`, `python_test_assertion_smells.rs`.\n`tests/suite_semantic/`: `python_dependency_pack.rs`, `semantic_value_python_contract.rs`, `measure_python_usage_graph_memory.rs`.\n`tests/suite_cross_language/`: `structural_search_python.rs`.\n`tests/suite_lsp_parity/`: `intellij_python_find_usages.rs`, `intellij_python_definition.rs`.\n`tests/suite_issues/`: `issue_1174_python_cross_language_claims.rs`, `issue_1225_python_annotation_inverse.rs`.\n\n## 2. Per-file import classification\n\n**(a) core-resident already** — `Language`, `CodeUnit`, `ProjectFile`, `Range`, `CodeUnitIndex`, `ImportInfo`, `StructuredImportPath{,Kind}`, `FqName`/`SegmentInterner`, `hash::{HashMap,HashSet}`, `CancellationToken`, `PoolSafeMemo`, `cognitive_complexity::Config`, `PythonAnalyzerConfig`/`PythonEnvironmentConfig`/`PythonEnvironmentLimits` (`bifrost-core/src/analyzer/config.rs:78–94`), `PreparedSyntaxTree`/`PreparedSourceIndex` (`bifrost-core/.../prepared_syntax.rs`, analysis merely re-exports at `tree_sitter_analyzer.rs:18–19`).\n\n**(b) retarget-only after the landed workstreams** — `usages::model::{ImportBinder, ExportIndex, ExportEntry, ReexportStar, ImportKind, ImportBinding, UsageHit, FuzzyResult}`, `outcome::{GraphUsageOutcome, GraphFailureReason}`, `inverted_edges::{UsageEdges, UsageEdgeWeights, CallSite, FileEdgeScanInput, PerFileEdges, classify_reference_node}`, `local_inference::LocalBindingsSnapshot`, `graph_core::{ImportEdge, ImportEdgeKind}`, `reference_site::ResolvedReferenceSite`, `receiver_analysis::ReceiverAnalysisBudget`, `traits::UsageScanScope`, `structural::adapter_helpers::*`, `tree_walk::subtree_contains`, `tree_sitter_analyzer::{walk_named_tree_preorder, WalkControl, collect_parse_errors}`, `common::node_source_text`, `text_utils::*`, `BoundedDefinitionLookup`, `TypeHierarchyProvider`/`ImportAnalysisProvider`/`TestDetectionProvider` (all `bifrost-core/src/analyzer/capabilities.rs`). Clean for: `bindings.rs`, `declarations.rs`, `syntax.rs`, `structural.rs`, `tests.rs`, `clones.rs`, `hits.rs`.\n\n**(c) analysis machinery, per file**\n\n| File | Machinery | Covered by an established pattern? |\n|---|---|---|\n| `python_graph/extractor.rs` | 19 × `&dyn IAnalyzer`, 13 × `PythonAnalyzer`; only `declarations`/`definitions`/`parent_of`/`ranges`/`get_source`/`indexed_source`/`enclosing_code_unit`/`direct_children` are called — **all `CodeUnitIndex`** | **Yes** — `&dyn CodeUnitIndex` + a `PythonAnalysisSource` trait (the `RustAnalysisSource` idiom, `analyzer/rust/graph_support.rs:31–74`) |\n| `python_graph/inverted.rs` | `parse_and_collect` + `build_edge_output` from the **analysis** driver (`usages/inverted_edges.rs:258,348`); `FileEdgeScanInput`/`PerFileEdges`/`classify_reference_node` are already core | **Yes** — W2 exactly: driver stays, `scan_python_file(input) -> PerFileEdges` moves (Go's `crates/bifrost-go/src/graph/inverted.rs:43` is the shape) |\n| `python_graph/resolver.rs` | `analyzer.global_usage_definition_index()` at :526 calling `types_in_package`, `fqn`, `by_normalized_fqn` | **Partly** — `fqn` is on `BoundedDefinitionLookup`; `types_in_package` and `by_normalized_fqn` are **not** (they exist only on `DefinitionIndexHandle`, `global_usage_definition_index.rs:603,645`). Two-method trait extension |\n| `python_graph.rs` | `resolve_analyzer::<PythonAnalyzer>`, `GraphUsageAnalyzer`/`UsageQueryResolver`/`UsageAnalyzer` impls | **Yes** — downcast-in-shim; the wrappers are the shim (Go's `GoQueryResolver`/`GoUsageGraphStrategy` precedent) |\n| `python/diagnostics.rs` | `global_usage_definition_index()`; the collector uses **only** `support.file_identifier(...)` | **Yes** — byte-identical to Go: `crates/bifrost-go/src/diagnostics.rs:50` takes `&dyn BoundedDefinitionLookup` |\n| `python/mod.rs` | `TreeSitterAnalyzer<PythonAdapter>` field, `store::LimitedQueryRows`, `FileState`, `weighted_cache::build_weighted_cache`, `moka::sync::Cache`, `IAnalyzer`, `languages::*` SPI types | **Yes for the shim half.** The 528 LOC of language logic at :255–782 is R1-class (see §3) |\n| `python/imports.rs` | `impl PythonAnalyzer` (:279–666), `impl ImportAnalysisProvider` | **Yes after R1** |\n| `python/usage_index.rs` | `analyzer.inner.bulk_file_states(batch, BulkFileStateSource::Include)` + reads `&FileState` fields | **NO precedent** — see §3/hard-coupling 1 |\n| `python/adapter.rs` | `LanguageAdapter`, `FileState` (`synthesize_hydrated_units(&mut FileState)`), `ParsedFile` (core) | **Mostly** — Go's forwarding shell (`analyzer/go/adapter.rs`) covers all but `synthesize_hydrated_units`, ~25 LOC of Python logic mutating a `pub(crate)`-field analysis struct; keeps in the shim |\n| `python/cache.rs` | moka weight fns + memo key | **Yes** — `GoMemoCaches` precedent; moka stays in analysis |\n| `python/hierarchy.rs` | `build_direct_descendant_index`, moka `direct_ancestors` | **Yes** — 27-LOC shim forwarder |\n| `python/semantic.rs` | `use crate::analyzer::semantic::*` (36.8k subsystem), `PreparedSyntaxTree`, registration macro naming `PythonAnalyzer` | **Parked** — same as Go's `semantic.rs` |\n| `python/external.rs` | 6 × `semantic_model::` (36 imported types), `Project`, `semver` | **Parked** — same workstream as Go's `artifact.rs` + `dependency_discovery.rs` |\n| `get_definition/python.rs`, `get_type/python.rs` | `ResolutionSession`, `DefinitionBatchContext`, the `super::*` glob of the hub | **Parked** — same as Go's definition routes |\n\n## 3. Python-specific hard spots, examined\n\n**R1-class (inherent-method) question.** Three inherent `impl PythonAnalyzer` blocks: `mod.rs:113–782` (670 LOC, 31 methods), `imports.rs:279–666` (388 LOC, 11 methods), `usage_index.rs:481–540` (60 LOC, 7 methods) = **1,118 LOC / 49 methods**. Of these, 141 LOC / 12 methods are constructors and thin `self.inner` forwarders that stay shim-side; the **language logic needing free-function rewrite is ~916 LOC / 37 methods**, with **117 in-seam call sites**. Compare Rust: 2,896 LOC / 73 methods / 303 sites. Python's R1 is **~32% of Rust's**. The self-recursion is real and mirrors Rust's: `export_index_of` → `compute_export_index_of` → `collect_reexport_events` → `import_binder_of`, and `usage_index()` (OnceLock) → `PythonUsageIndex::build` → `export_index_from_file_state`/`import_binder_from_imports`/`module_replacement_of`. That forces the same two-tier trait split Rust used (`RustAnalysisSource` vs `RustUsageSource`, `graph_support.rs:31,86`) — the index build must not re-enter the cell it is filling.\n\n**`PreparedSyntaxTree` outside the lowerer.** None. Five hits total, all in `python/semantic.rs` (:20, 49, 153, 491, 504). Rust's W5-motivating problem does not exist for Python.\n\n**moka / global caches (the RUST_TREES question).** No `static`/`LazyLock` cache anywhere in the seam. The only `LazyLock`s are `Regex`es in `tests.rs` and `PYTHON_COGNITIVE_CONFIG` in `adapter.rs` — pure data. All caching is instance-resident on `PythonAnalyzer`: six moka `Cache` fields (`imported_code_units`, `imported_target_files`, `referencing_files`, `export_index`, `direct_ancestors`, `usage_edges`), `Arc<OnceLock<DirectDescendantIndex>>`, `Arc<OnceLock<PythonUsageIndex>>`, `Arc<PoolSafeMemo<..>>`, plus `PythonDefinitionContext`'s `OnceLock`/`Mutex` in the parked file. **Verdict: moka stays in analysis; no deviation to record.**\n\n**`build_cached_python_usage_edges_for_targets` (dead-code pin 1).** `usages/python_graph.rs:56–68` → `PythonAnalyzer::usage_edges_for_targets` (`mod.rs:240–248`) → `self.usage_edges.get_with(PythonUsageEdgesKey::new(nodes, targets), …)`. The key sorts both name sets into `Arc<[String]>` (`cache.rs:11–18`); the value is `Arc<UsageEdges>` (core type) weighted by `weight_python_usage_edges` (`cache.rs:56`). Entered only through `PythonDeadCodeBulk::build` (`mod.rs:1380–1394`) — the doc comment at :1376 records that Python is *the only* bulk proof resolving a bounded target set. Shape: the cache + key + weight stay shim-side (moka); the builder (`inverted::build_python_edges`) moves; `usage_edges_for_targets` becomes a shim closure-taking memo, which is exactly what it already is.\n\n**Environment-pack machinery coupling.** `activate_python_environment_packs` (`workspace.rs:214`) touches, in order: `AnalyzerConfig.python` (core), `self.analyzer().project()`, `resolve_python_semantic_pack_dependencies` (`external.rs:977`), `semantic_model::prepare_dependency_semantic_packs`, `PreparationOutcome::compose_activation_request`, `semantic_model::acquire_active_semantic_models(analyzer, catalog, persistence, activation, cancellation)`. The context struct carries catalog + persistence + activation + limits + cancellation handles. `external.rs` itself imports 36 `semantic_model` types. Cross-crate consumer: `bifrost-lsp/src/lsp/server.rs:3465–3494` (`LspPythonPackConfig`, `initializationOptions.pythonEnvironment`).\n\n**Anything with no precedent.** One: `PythonUsageIndex::build` (`usage_index.rs:94–…`) calls `analyzer.inner.bulk_file_states(batch.iter().cloned(), BulkFileStateSource::Include)` in 128-file chunks and then reads `state.top_level_declarations`, `state.imports`, `state.source`. `FileState` is `crates/bifrost-analysis/src/analyzer/tree_sitter_analyzer.rs:469` with `pub(crate)` fields and carries `scala_exports`/store qualifiers — it is not core and is not a candidate for wholesale lowering. Three `PythonAnalyzer` methods take `&FileState` directly (`export_index_from_file_state`, `import_binder_from_imports`, `module_replacement_of`), and `PythonAdapter::synthesize_hydrated_units` takes `&mut FileState`. Neither Go (`crates/bifrost-go/` has zero `FileState`/`bulk_file_states` references) nor Rust's `RustAnalysisSource` (which lowers `prepared_syntax`, not bulk state) has an answer.\n\n## 4. Reverse edges\n\n**Production, framework → Python (all allowlisted or assembly, verified against `tests/suite_cross_language/language_reach_in_gate.rs`):**\n- `analyzer/mod.rs:183–192,220` — re-export hub (allowlist).\n- `analyzer/usages/mod.rs:31,85` — `PythonExportUsageGraphStrategy` re-export (allowlist).\n- `analyzer/workspace.rs:209–261` — the Python workspace surface (allowlist, reason recorded at gate line 101–103).\n- `analyzer/lib.rs:54` — crate-root surface (allowlist).\n- `analyzer/languages.rs:726` + `analyzer/multi_analyzer.rs:9,43,260` — assembly files.\n- `analyzer/usages/get_definition/mod.rs:46–50,116,148–150,537–563,774–810,1016–1055,1261` — the definition hub (allowlist); it holds `python_contexts: HashMap<ProjectFile, Arc<python::PythonDefinitionContext>>` and names `ModuleBindingTimeline`/`ModuleBindingEventKind` (pub(crate) python types) in its import list. ~110 LOC of Python-specific batching. Go's `go_contexts` sits in the same struct — same parking class.\n- `analyzer/usages/get_definition/call_sites.rs:344,779,923` and `analyzer/usages/receiver_query.rs:2749,2852–2862` — `Language::Python` match arms (no type reach-in; permitted by the gate).\n- `analyzer/lexical_definitions.rs:60,69,113–221,475,865,949,…` — `PythonMethodBinding` enum + ~15 `Language::Python` arms, shared cross-language machinery, parks.\n- `analyzer/store/epoch.rs:172–181,431–433` — `.scm` include paths + salt.\n- `searchtools/sources.rs:917–926` — a **second, duplicate** `python_expanded_comment_start` alongside `python/declarations.rs:693`.\n\n**cfg(test)-only Python reach-ins** (need `PythonAnalyzer`/`PythonAdapter` re-exported for dev-dependency use, exactly as Go): `analyzer/tree_sitter_analyzer.rs` (32 refs, all in `mod tests` from :7771), `analyzer/store/mod.rs` (9 refs from :7791), `analyzer/structural/extract.rs:247,281` (`PYTHON_STRUCTURAL_SPEC`, inside `mod tests` at :238), `analyzer/structural/index.rs:1849`, `analyzer/usages/ruby_graph.rs:234,334`, `analyzer/usages/call_relations.rs:1402,2287`, `analyzer/jvm/external.rs:2329,3265`, `relevance.rs:2337,2738,2955`, `searchtools/tests.rs:249,269`, `analyzer/cognitive_complexity_tests.rs`.\n`analyzer/js_ts/providers.rs:224` is a doc-comment mention only.\n\n**Cross-crate reach-ins:**\n- `bifrost-lsp`: `lsp/server.rs:55,3441–3494,3622–3640` (`PythonAnalyzerConfig`/`PythonEnvironmentConfig` — already core; `activate_python_environment_packs`; `PythonSemanticModelWorkspaceContext`); `lsp/handlers/util.rs:123` calls the facade's `parse_python_import_bindings`, and `:69,146` construct `tree_sitter_python::LANGUAGE` from bifrost-lsp's own dependency.\n- `bifrost-mcp`: none. `PythonToolPayload`/`new_for_python` in `searchtools_service.rs` are PyO3 binding payloads, not the language seam.\n- `bifrost-core`, `bifrost-runtime`, `bifrost-policy`, `bifrost-semantic-packs`: none.\n\n## 5. crates.io deps a `brokk-bifrost-python` crate needs\n\n`brokk-bifrost-core` (path, `=` pin), `tree-sitter 0.25.10`, `tree-sitter-python 0.25.0`, `rayon 1.10.0` (`python_graph/extractor.rs:21`), `regex 1.12.2` (`python/tests.rs`). **Not** `moka` (all six caches stay shim-side), **not** `semver` (only `external.rs:10`, parked), **not** `serde`. Same five-line dependency block as `crates/bifrost-go/Cargo.toml`, minus the `test-support` feature unless the hierarchy-relation pattern repeats.\n\n## 6. LOC accounting\n\nCalibration: Go moved 8,544 with a 1,598-LOC production shim; Rust projects ~21.4k with ~2.8k.\n\n| Band | LOC | Content |\n|---|---|---|\n| **Movable now** (no rewrite) | **~5,400** | `bindings.rs` 993, `declarations.rs` 907, `tests.rs` 370, `syntax.rs` 159, `structural.rs` 394, `clones.rs` ~100, `hits.rs` 62, `diagnostics.rs` ~988 (BoundedDefinitionLookup, Go precedent), `inverted.rs` 766 (W2), `adapter.rs` bodies ~48, `python_graph.rs` free builders ~47, `resolver.rs` ~570 |\n| **Movable after R1-class rewrite** | **~4,700** | `mod.rs:255–782` 528, `imports.rs` ~970, `usage_index.rs` ~450+101, `extractor.rs` 2,498, `resolver.rs` remainder ~75, + the two `BoundedDefinitionLookup` methods (`types_in_package`, `by_normalized_fqn`). **R1 sizing: ~916 LOC of inherent language logic, 37 methods, 117 in-seam call sites** |\n| **Parked on `semantic`** | **3,423** | `python/semantic.rs` (registration macro textually requires the `TreeSitterAnalyzer` field; `analyzer/semantic` is not stage-3 scope) |\n| **Parked on `semantic_model`** | **1,795** | `python/external.rs` (+ the `workspace.rs:176–261` public surface, ~90 LOC, which stays in analysis regardless) |\n| **Parked on `ResolutionSession`** | **2,649** | `get_definition/python.rs` 2,597 + `get_type/python.rs` 52, plus ~110 LOC of `get_definition/mod.rs` Python batching |\n| **Shim floor** | **~1,590** | `mod.rs` shim half ~867 (struct+caches 112, ctors/forwarders 141, `CodeUnitIndex` 180, `IAnalyzer` 207, `AnalyzerTestHooks` 43, SPI block 166), `adapter.rs` shell ~120, `python_graph.rs` strategy wrappers ~230, `cache.rs` 97, `imports.rs` provider impl ~110, `usage_index.rs` OnceLock accessors ~90, `diagnostics.rs` entry ~30, `hierarchy.rs` 27, `clones.rs` entry ~21 |\n\n**Total 19,558.** Moves ≈ **10,100** (52%); parked **7,867** (40%); shim **~1,590** — within 10 LOC of the Go 1,598 floor, i.e. the SPI block plus the memo shell, with no per-language excess. Predicted analysis-frontend effect at Go's measured 0.47s/kLOC: **≈ -4.7s**, second only to Rust in the fleet.\n\n## The three hardest couplings\n\n1. **`PythonUsageIndex::build` on `TreeSitterAnalyzer::bulk_file_states` + `&FileState`** (`python/usage_index.rs:94–…`, `mod.rs` `export_index_from_file_state`/`import_binder_from_imports`, `adapter.rs` `synthesize_hydrated_units(&mut FileState)`). `FileState` (`tree_sitter_analyzer.rs:469`, `pub(crate)` fields, carries `scala_exports` and store qualifiers) is not core and cannot follow `ParsedFile`; the chunked bulk read is a genuine performance contract, not incidental. Neither `crates/bifrost-go/` (zero references) nor `RustAnalysisSource` (which lowers `prepared_syntax`, not bulk state) supplies a pattern. This is the only place in the Python seam with no precedent.\n\n2. **`activate_python_environment_packs` + `external.rs`'s dual nature.** 1,795 LOC where the tree-sitter API collector (`PythonApiCollector`, :465–960) is pure language knowledge but is welded to 36 `semantic_model` types it emits, and the entry point is a `WorkspaceAnalyzer` method that is deliberately public API, named in the reach-in allowlist and consumed cross-crate at `bifrost-lsp/src/lsp/server.rs:3480`. Unlike Go's `artifact.rs`, lowering `semantic_model` is necessary but *not sufficient*: `PythonSemanticModelWorkspaceContext`/`PythonSemanticModelActivationOutcome` and the method signature stay in `analyzer/workspace.rs` unless that public surface is itself redesigned.\n\n3. **The self-recursive memo web behind the R1 rewrite.** `export_index_of` → `compute_export_index_of` → `collect_reexport_events` → `import_binder_of` → `import_binder_from_imports`, and `usage_index()` (`Arc<OnceLock<PythonUsageIndex>>`) → `build` → the same three accessors. 916 LOC / 37 methods / 117 in-seam call sites must become free functions over a **two-tier** source trait (build-time vs post-build), or the `OnceLock` re-enters itself — the identical hazard `RustAnalysisSource`/`RustUsageSource` were split to prevent (`analyzer/rust/graph_support.rs:24–30, 86`). Six moka caches and the `PoolSafeMemo` must stay shim-side with behavior preserved while the logic they memoize leaves.\n\nTwo smaller named gaps, listed for completeness: `BoundedDefinitionLookup` needs `types_in_package` and `by_normalized_fqn` (used only at `python_graph/resolver.rs:526–531`); and `symbol_lookup::parse_symbol_path` is analysis-resident with 5 Python call sites (`inverted.rs:63,114,118`, `resolver.rs:624`, `get_definition/python.rs:1977`) whose own comments state the Python behavior is exactly `rsplit('.')`."}],"stop_reason":"end_turn","stop_sequence":null,"stop_details":null,"usage":{"input_tokens":2,"cache_creation_input_tokens":2107,"cache_read_input_tokens":154482,"output_tokens":11873,"server_tool_use":{"web_search_requests":0,"web_fetch_requests":0},"service_tier":"standard","cache_creation":{"ephemeral_1h_input_tokens":0,"ephemeral_5m_input_tokens":2107},"inference_geo":"not_available","iterations":[{"input_tokens":2,"output_tokens":11873,"cache_read_input_tokens":154482,"cache_creation_input_tokens":2107,"cache_creation":{"ephemeral_5m_input_tokens":2107,"ephemeral_1h_input_tokens":0},"type":"message"}],"speed":"standard"},"diagnostics":null},"requestId":"req_011CdjMK4Xzk3hmojVWY7Dsc","attributionAgent":"Explore","type":"assistant","uuid":"8e72194d-a97f-4f44-b68a-6adac8c713da","timestamp":"2026-08-05T09:19:56.298Z","effort":"high","userType":"external","entrypoint":"cli","cwd":"/home/jonathan/Projects/bifrost2","sessionId":"8f94fdd6-1d55-4f9d-8f78-532b5b220270","version":"2.1.220","gitBranch":"bifrost2","slug":"solve-https-github-com-brokkai-bifrost-i-atomic-hopper"}
