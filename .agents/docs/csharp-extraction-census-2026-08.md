# C# seam census for the stage-3 fleet (2026-08-05)

Agent-run census. Coordinator dispositions, recorded here: (1) new shared
workstream W6 -- lower QueryBatch (the LimitedQueryRows alias) to core; the
four BoundedDefinitionLookup additions land in two halves (types_in_package +
by_normalized_fqn ride with Python's Py-1, identifier + members_for_owner_name
with the C# prerequisite pass). (2) resolver.rs takes Scenario B: the
session-free half moves with the C# crate, the session wrappers stay parked
with the definition route -- the 26-point move-rate spread decides this.
(3) The external-declaration index parks; separating it from the pack producer
joins the semantic_model workstream notes. (4) csharp_implicit_entry_point
moves into csharp_graph during the C# pass, ending the fleet's only homeless
entry-point predicate.

## C# seam census for the stage-3 fleet (2026-08-05)\n\nTaken at the W1–W5 merge tip, same shape as the Go/Rust/Python predecessors. **Verdict summary: C# needs one new shared prerequisite (lower `QueryBatch`/`LimitedQueryRows` to core) and has one structural blocker with no precedent — `csharp_graph/resolver.rs` is a bidirectional dependency with the definition route, so the whole usage graph parks with it unless it is split.** No `PreparedSyntaxTree` outside the lowerer, no global/`static` parse memo (no RUST_TREES-class deviation), no rayon anywhere in the seam. Projection: **~8.2k moves, ~1.66k shim — on the Go floor, but a 34 % move rate against a 60 % park.**\n\n---\n\n## 1. The seam, exact paths and `wc -l`\n\n### `crates/bifrost-analysis/src/analyzer/csharp/` — 13,848\n\n| File | LOC (total / prod) | Purpose |\n|---|---|---|\n| `semantic.rs` | 4,440 / 4,440 | C# → executable-semantics IR lowerer; `impl_program_semantics_provider!(CSharpAnalyzer, CSharpSemanticLowerer)` at `:25`, `ADAPTER_VERSION = b\"csharp-value-semantics-v3\"` |\n| `external.rs` | 3,811 / 3,117 | `CSharpDependencyPackAdapter` (`:621`), `CSharpAssemblyPackProducer` (`:618`), `resolve_csharp_semantic_pack_dependencies` (`:623`), **`CSharpExternalDeclarationIndex` (`:292`)**, and a hand-rolled PE/CLI-metadata decoder over `goblin` (`parse_assembly_bounded` `:1789` → `read_typedef`/`read_typeref`/`read_typespec`/`read_field`/`read_method`/`read_property`/`read_event`, `Streams`/`TableLayout`); 51 `semantic_model` types imported at `:3–16` |\n| `mod.rs` | 2,520 / 2,520 | `CSharpAnalyzer` struct + 4 fields (`:241–246`); **51-method inherent block `:250–1131`**; two pure free-fn bands (`:59–240`, `:1132–1833`); 3-method `LimitedQueryRows` forwarder block `:1834–1858`; `CodeUnitIndex` (`:1864–2010`, 30 fns); `IAnalyzer` (`:2011–2202`, 28 fns); `AnalyzerTestHooks` (`:2203–2260`, 10 fns); **`CSharpSupport` SPI block `:2261–2490`**; dead-code predicates `:2491–2520` |\n| `declarations.rs` | 1,197 / 1,197 | `parse_csharp_file` → `ParsedFile`; fq/namespace naming, `StructuredTypeIdentityBuilder`, type-identifier collection |\n| `structural.rs` | 408 / 395 | `CSHARP_STRUCTURAL_SPEC` (`:15`), `CSHARP_KIND_TABLE`, `impl StructuralSpec` (`:243`) |\n| `imports.rs` | 340 / 340 | `impl ImportAnalysisProvider` (`:12–156`) + a 2-method inherent block (`:157–…`: `implicit_reference_index`, `compute_implicit_reference_index`); `csharp_import_info_from_using_directive` |\n| `tests.rs` | 309 / 309 | C# test detection + assertion smells; 5 `LazyLock<Regex>` |\n| `hierarchy.rs` | 270 / 270 | 9-method inherent block (`:18–250`, attribute-type resolution + `usage_direct_ancestors`) + `impl TypeHierarchyProvider` (`:251`) |\n| `adapter.rs` | 241 / 241 | `CSharpAdapter: LanguageAdapter` (`:103`), `CSHARP_COGNITIVE_CONFIG` (`:15`), query dir `\"resources/treesitter/c_sharp\"` (`:109`) |\n| `clones.rs` | 133 / 133 | `build_csharp_clone_candidate_data` + C# clone token normalization |\n| `cache.rs` | 119 / 119 | `CSharpMemoCaches`: **6 moka `Cache` + 6 `OnceLock` + 2 `PoolSafeMemo`** + 7 weight fns |\n| `dependency_discovery.rs` | 60 / 60 | `project_assets_files` (`walkdir`), `MAX_ASSETS_FILES` |\n\n### `crates/bifrost-analysis/src/analyzer/usages/` — 10,463\n\n| File | LOC (total / prod) | Purpose |\n|---|---|---|\n| `get_definition/csharp.rs` | 3,708 / 3,369 | `CSharpDefinitionProvider`, `resolve_csharp`, `resolve_csharp_bounded`, `csharp_type_lookup_resolution{,_in_session}`; 42 free fns taking `&CSharpAnalyzer` |\n| `csharp_graph/resolver.rs` | 3,623 / 3,476 | **The shared C# resolution library**: 109 fns, 78 taking `&CSharpAnalyzer`; type/member/extension/arity resolution; `TargetSpec`/`TargetKind`; both `usage_*`/`forward_*` and `*_in_session` variants |\n| `csharp_graph/extractor.rs` | 997 / 997 | Per-symbol forward scan (`ScanCtx`, `prepare_file`, `scan_prepared_file`); imports 30 items from `resolver.rs` |\n| `get_type/csharp.rs` | 914 / 174 | `resolve_csharp_type`, `resolve_csharp_type_bounded` (thin; 740 LOC of tests) |\n| `csharp_graph/inverted.rs` | 867 / 867 | Whole-workspace per-file walk (`build_csharp_edges`, `CsScan`); already on the W2 contract; imports 22 items from `resolver.rs` |\n| `csharp_graph/shared.rs` | 141 / 141 | `CSharpQueryResolver`/`CSharpEdgeResolver` (`resolve_analyzer` sites) |\n| `csharp_graph.rs` | 128 / 128 | `build_csharp_usage_edges{,_weights}`, `CSharpUsageGraphStrategy`, **and 24 `pub(in crate::analyzer::usages)` re-exports of `resolver`/`extractor` internals** (`:8–31`) |\n| `csharp_graph/hits.rs` | 85 / 85 | `push_hit`/`push_unproven_hit`/`push_self_receiver_hit`; written against `extractor::ScanCtx` |\n\n**Seam total: 24,311 LOC** (22,378 prod / 1,933 in-file test) **+ 47 LOC of `.scm`** (`resources/treesitter/c_sharp/{definitions.scm,imports.scm}` = 46/1), `include_str!`'d at `analyzer/store/epoch.rs:226–231`, with the `lang_epoch!` salt at `epoch.rs:461–468` (path `\"treesitter/c_sharp/\"`, 13-token salt string). Relocation requires a bump.\n\n### C#-specific name-normalization helpers — dispositions\n\n`analyzer/mod.rs:103–114` is a **21-item `pub(crate) use csharp::{…}` block** plus `analyzer/mod.rs:116` `pub use csharp::{csharp_source_name_segment, strip_csharp_generic_arity}` and `:98` `pub use csharp::CSharpAnalyzer`, `:99–105` the 9-item `pub use csharp::external::{…}`.\n\nConsumer audit of all 23 pub(crate)/pub names (§4 has the full table): **20 of 21 pub(crate) names are consumed only inside the C# seam** (`csharp_graph/*`, `get_definition/csharp.rs`, `csharp/declarations.rs`, `csharp/structural.rs`). Only three leave it:\n\n- `csharp_callable_arity` → `get_definition/mod.rs:83` (framework hub, allowlisted). Signature `(&dyn IAnalyzer, &CodeUnit) -> CallableArity` — `CodeUnitIndex`-shaped.\n- `csharp_source_name_segment` → `bifrost-lsp/src/lsp/handlers/document_symbol.rs:8,231` (**cross-crate**).\n- `strip_csharp_generic_arity` → `src/mcp_property_fuzzer/service_probes.rs:2197` (root crate).\n\nThe registry campaign already routed `symbol_lookup.rs` and `analyzer/common.rs` off the direct helpers and onto capability methods: `CSharpSupport::display_symbol_name` → `csharp_normalize_full_name`, `::source_identifier` → `strip_csharp_generic_arity`, `::alias_name_segment` → `strip_csharp_generic_arity` (`csharp/mod.rs:2269–2280`), reached at `analyzer/common.rs:70–126` (`display_symbol_name`, `display_identifier_for_target`) and `symbol_lookup.rs:644`. So `csharp_normalize_full_name` and `csharp_source_identifier` have **zero framework call sites** today — the disposition is: the pub(crate) block collapses to nothing, the three escaping names get re-exported from the shim at their historical `crate::analyzer::` paths (the idiom `analyzer/mod.rs:120–123` already uses for `brokk_bifrost_go::packages`).\n\n### C# root test suites (names only) — 12,739 LOC in C#-named files\n`tests/suite_analyzers/`: `csharp_analyzer_test.rs`, `csharp_analyzer_update_test.rs`, `csharp_test_detection_test.rs`.\n`tests/suite_usages/`: `usages_csharp_graph_test.rs` (8,045), `usage_graph_csharp_test.rs` (1,604), `csharp_bounded_receiver_hardening.rs`.\n`tests/suite_smells/`: `csharp_dead_code_smells.rs`, `csharp_structural_clone_smells.rs`, `csharp_go_rust_test_assertion_smells.rs`.\n`tests/suite_issues/`: `issue_1325_csharp_census_complexity.rs`, `issue_csharp_verbatim_identifiers.rs`.\n`tests/`: `issue_1194_csharp_scan_complexity.rs`.\nFixtures: `tests/fixtures/{csharp-external,usage-graph-csharp}`.\nCross-cutting suites that exercise C# without a C# name: `tests/suite_lsp_parity/roslyn_goto_definition.rs`, `tests/suite_semantic/{semantic_language_conformance,semantic_cfg_contract,icfg_contract,semantic_value_language_contract,external_artifact_pack}.rs`, `tests/suite_symbols/get_definition_test.rs`, `tests/suite_persistence/analyzer_capability_parity.rs`, `tests/suite_mcp_cli/lsp_click_around_regression.rs`.\n\n---\n\n## 2. Per-file import classification\n\n**(a) core-resident already** — `Language`, `CodeUnit`, `CodeUnitType`, `ProjectFile`, `Range`, `CodeUnitIndex`, `ImportInfo`, `FqName`/`SegmentId`/`SegmentKind`/`segment_interner`, `hash::{HashMap,HashSet}`, `PoolSafeMemo`, `DirectDescendantIndex`, `build_direct_descendant_index`, `build_reverse_file_index`, `CallableArity`, `DispatchExtensibility`, `SignatureMetadata`, `ParameterMetadata`, `StructuredTypeIdentity`/`StructuredTypeName`/`StructuredTypeIdentityBuilder`, `cognitive_complexity::Config`, `CSharpAnalyzerConfig` (`bifrost-core/src/analyzer/config.rs:192`), `ParsedFile` (W1), `PreparedSyntaxTree`/`PreparedSourceIndex` (W5), `IdentifierSigil`/`node_ident_text` (R2), `structural::{kinds,occurrences,spec,adapter_helpers}`, `tree_walk::{walk_named_tree_preorder,WalkControl,collect_parse_errors}`, `usages::local_inference::{LocalInferenceEngine,LocalInferenceConfig,SymbolResolution}`, `usages::model::{UsageHit,FuzzyResult}`, `usages::inverted_edges::{FileEdgeScanInput,PerFileEdges,UsageEdges,UsageEdgeWeights,classify_reference_node}`, `usages::outcome::*`, `usages::traits::UsageScanScope`, `receiver_analysis::ReceiverAnalysisBudget`, `reference_site::ResolvedReferenceSite`, `text_utils::*`, `TestAssertionSmell`/`TestAssertionWeights`, `ImportAnalysisProvider`/`TypeHierarchyProvider`/`TestDetectionProvider`/`BoundedDefinitionLookup`.\n\nClean today: `structural.rs`, `tests.rs`, `declarations.rs` (modulo four `csharp_*` helpers that move with it), `dependency_discovery.rs`, and both free-fn bands of `mod.rs`.\n\n**(b) retarget-only** — `csharp_graph/hits.rs` (`enclosing_code_unit` is on `CodeUnitIndex` after W3a; `usages::common::{SNIPPET_CONTEXT_LINES,usage_hit,reclassify_self_receiver_hit_at,external_usage_hit_count}`), `csharp_graph/inverted.rs` (already written against W2: `parse_and_collect`/`build_edge_output`/`FileEdgeScanInput`/`PerFileEdges`), `adapter.rs` free bodies, `hierarchy.rs` post-R1.\n\n**(c) analysis machinery, per file**\n\n| File | Machinery | Covered by an established pattern? |\n|---|---|---|\n| `csharp/mod.rs` inherent block `:250–1131` | `TreeSitterAnalyzer<CSharpAdapter>` field; **`store::LimitedQueryRows` at 41 sites**; `DefinitionIndexHandle` (`identifier`, `by_normalized_fqn`, `members_for_owner_name`, `fqn`, `package_exists`); 14 memo cells | **Partly.** R1-class rewrite (see §3) *plus* a `LimitedQueryRows` lowering that neither Go nor Rust needed |\n| `csharp/imports.rs` | `ImportAnalysisProvider` impl + `self.inner.class_declarations_in_package`, `build_reverse_file_index` | **Yes** — provider impl is shim, parsing moves |\n| `csharp/hierarchy.rs` | `use super::*` glob, 9 inherent methods, moka `direct_ancestors`, `build_direct_descendant_index` | **Yes after R1** — Go/Python hierarchy precedent |\n| `csharp/adapter.rs` | `LanguageAdapter`, `ParsedFile` (core), `tree_sitter_analyzer::lookup_suffix_candidates` (**analysis-only**) | **Mostly** — Go's forwarding shell; `lookup_suffix_candidates` needs lowering or a shim callback |\n| `csharp/clones.rs` | `clone_detection::{CloneCandidateData,compact_clone_excerpt}` (analysis-resident), `&CSharpAnalyzer` | **Yes** — token normalizer moves, entry stays (Go/Python precedent) |\n| `csharp/cache.rs` | moka + `weighted_cache::build_weighted_cache` | **Yes** — `GoMemoCaches` precedent, moka stays shim-side |\n| `csharp/structural.rs` | none (all core after the adapter_helpers split; `analyzer/structural/adapter_helpers.rs:13–17` re-exports core) | **Yes** — `bifrost-rust/src/structural.rs` is the exact shape |\n| `csharp/declarations.rs` | none beyond `ParsedFile` | **Yes** |\n| `csharp/tests.rs` | none | **Yes** |\n| `csharp_graph.rs` | `resolve_analyzer::<CSharpAnalyzer>`, `GraphUsageAnalyzer`/`UsageAnalyzer` impls, **24 re-exports out to `get_definition`** | **Wrappers yes, re-exports no** — see hard coupling 1 |\n| `csharp_graph/shared.rs` | `resolve_analyzer`, `analyzed_files_for_language` | **Yes** — downcast-in-shim |\n| `csharp_graph/inverted.rs` | W2 contract + `ClassRangeIndex` (analysis, `inverted_edges.rs:52`) + 22 `resolver.rs` items | **Half.** W2 covers the scan; `ClassRangeIndex::build(&dyn IAnalyzer, &ProjectFile)` is `declarations`+`ranges` only → trivially core-lowerable. The resolver dependency is the blocker |\n| `csharp_graph/extractor.rs` | `ClassRangeIndex`, 30 `resolver.rs` items, `tree_sitter::Parser` | Same |\n| `csharp_graph/resolver.rs` | **`get_definition::ResolutionSession` (53 refs, ~33 fns / ~1,394 LOC)**, `store::LimitedQueryRows`, `usages::parsed_tree::parse_tree_sitter_file` (analysis), `ClassRangeIndex`, `common::CSHARP_IDENTIFIER_SIGIL`, 78 `&CSharpAnalyzer` params | **NO** — hard coupling 1 |\n| `csharp/semantic.rs` | `analyzer::semantic::*`, `PreparedSyntaxTree`, `lexical_definitions::formal_parameter_slots_for_owner`, registration macro naming `CSharpAnalyzer` | **Parked** — same as Go/Rust/Python `semantic.rs` |\n| `csharp/external.rs`, `dependency_discovery.rs` | 51 `semantic_model` types; `goblin`, `semver`, `serde_json`, `walkdir` | **Parked** — `semantic_model` workstream; plus hard coupling 3 |\n| `get_definition/csharp.rs`, `get_type/csharp.rs` | `ResolutionSession`, `use super::*` glob of the 2.3k-LOC hub | **Parked** |\n\n**R1-class inherent block — sized.** Three inherent `impl CSharpAnalyzer` blocks: `mod.rs:250–1131` (882 LOC, 51 methods), `hierarchy.rs:18–250` (233 LOC, 9 methods), `imports.rs:157–340` (184 LOC, 2 methods), plus the 26-LOC/3-method `mod.rs:1834–1858` forwarder block = **1,325 LOC / 65 methods**. Constructors, test hooks and `self.inner` forwarders (`clone_with_project`, `new`, `new_with_config`, `new_with_config_store_context`, `from_project`, the two hydration counters, the three `*_limited` forwarders) are ~87 LOC / 10 methods and stay shim-side; the external-index accessors (`external_declaration_index`, `external_type_candidates`, `external_member_candidates`, `:463–490`, 28 LOC) park with `external.rs`. **Language logic needing free-function rewrite: ~1,210 LOC / 52 methods, with ~197 in-seam call sites.** Compare Rust 2,896/73/303, Python 916/37/117. C# lands between Python and Rust — but with a vocabulary problem Rust and Python did not have (`LimitedQueryRows`, below). Separately, **132 free functions already take `&CSharpAnalyzer`** (resolver 78, get_definition/csharp 42, shared 4, extractor 3, get_type 3, inverted 2) — the Go shape, retargetable to a source trait mechanically.\n\n**`PreparedSyntaxTree` outside the lowerer.** **None.** Five hits total, all in `csharp/semantic.rs` (`:17, 47, 181, 708, 731`). Rust's W5-motivating problem does not exist for C#.\n\n**moka / global caches.** No `static`/`LazyLock` cache anywhere in the seam. The only `LazyLock`s are `CSHARP_COGNITIVE_CONFIG` (`adapter.rs:15`, pure data) and 7 `Regex`es (`tests.rs` ×5, `mod.rs:2511,2515`). All caching is instance-resident on `CSharpAnalyzer`: `CSharpMemoCaches` with 6 moka `Cache` (`namespace_by_file`, `using_namespaces`, `using_aliases`, `imported_code_units`, `referencing_files`, `direct_ancestors`), 6 `OnceLock` (`direct_descendant_index`, `global_using_namespaces`, `global_using_aliases`, `global_static_using_type_names`, `global_static_using_types`, `usage_global_static_using_types`), 2 `PoolSafeMemo` (`reverse_import_index`, `implicit_reference_index`), plus `CSharpAnalyzer.external_index: Arc<OnceLock<CSharpExternalDeclarationIndex>>`. **Verdict: moka stays in analysis; no deviation to record.** No rayon/`par_iter`/`Mutex` in the seam at all.\n\n**`IdentifierSigil`.** R2 moved the mechanism (`IdentifierSigil`, `node_ident_text`, `parse_source_region`) to `bifrost-core/src/analyzer/common.rs:58,74`; Rust's constant went to `bifrost-rust/src/declarations.rs:77` (`RUST_IDENTIFIER_SIGIL`). **C#'s stayed in analysis**: `CSHARP_IDENTIFIER_SIGIL` at `crates/bifrost-analysis/src/analyzer/common.rs:291` with its predicate `csharp_identifier_like_node_kind` at `:282` (verbatim-identifier `@class` → `class`, gated to the `identifier` leaf kind, issue-1128). **Exactly one consumer**: `csharp_graph/resolver.rs:20`, inside `node_text`. Disposition: 12 LOC, moves to the C# crate with `resolver.rs`'s session-free half; nothing else references it.\n\n---\n\n## 3. C#-specific hard spots, examined\n\n**`LimitedQueryRows` is the C# API vocabulary — and it is not core.** `LimitedQueryRows<T>` is a type alias to `QueryBatch<T>` at `analyzer/store/mod.rs:529`, and `QueryBatch` is defined at `crates/bifrost-analysis/src/analyzer/i_analyzer.rs:22`. It appears **41 times in `csharp/mod.rs`** — 14 of the 51 inherent methods return it (`declaration_candidates_by_identifier_limited`, `declaration_candidates_by_fqn_limited`, `member_candidates_for_owner_limited`, `namespace_of_file_limited`, `import_statements_limited`, `using_namespaces_of_limited`, `using_aliases_of_limited`, `global_using_namespaces_limited`, `global_using_aliases_limited`, `global_static_using_type_names_limited`, `partial_type_parts_limited`, plus the three `self.inner` forwarders) — and 2 more in `resolver.rs` (the `limited_query: impl FnOnce(usize) -> LimitedQueryRows<T>` budget adapter at `:64`). Grep confirms **neither `brokk-bifrost-go` nor `brokk-bifrost-rust` names it**; the only mention outside analysis is a comment in `bifrost-rust/src/graph/mod.rs:13` explaining that the files using it *parked*. Because C#'s bounded-lookup layer is not confined to the definition route — it is the way the R1 block talks — lowering `QueryBatch` to core is a **new shared prerequisite**, on the order of the W1 `ParsedFile` lowering (it imports only `Vec`, a count and a `complete` flag).\n\n**`CSharpExternalDeclarationIndex` on the analyzer struct (JVM-realm pattern, not the Go/Rust/Python one).** `CSharpAnalyzer.external_index: Arc<OnceLock<CSharpExternalDeclarationIndex>>` (`mod.rs:245`), initialized by `external_declaration_index()` (`mod.rs:463`) calling `CSharpExternalDeclarationIndex::build_for_project(&self.csharp_config, self.inner.project())`, refreshed on incremental update at `mod.rs:2068–2089` (drop-and-rebuild when a dependency input changed). The only other languages doing this are Java (`java/mod.rs:62,218`), Kotlin (`kotlin/mod.rs:119,252`) and Scala (`scala/mod.rs:259,538`) with `JvmExternalDeclarationIndex`. Go, Rust and Python have **zero** analogue: their `external.rs` is a pure `DependencyPackAdapter` that never sits on the analyzer. The `pub` accessors `external_type_candidates`/`external_member_candidates` (`mod.rs:472,485`) have **no in-repo callers** — they are public API only.\n\n**`external.rs`'s three natures.** 3,117 prod LOC that are (i) a `walkdir`/`serde_json` .NET asset resolver (`assemblies_from_assets` `:1457`, `project_output_candidates` `:1666`, `approved_package_roots` `:1649`), (ii) a **hand-written PE + CLI-metadata table decoder over `goblin`** (`parse_assembly_bounded` `:1789`, `Streams` `:2167`, `TableLayout` `:2214`, `read_typedef`/`read_typeref`/`read_typespec`/`read_field`/`read_method`/`read_property`/`read_event` `:2262–2374`, `MetadataDecodeBudget` `:3066`) — pure .NET binary-format knowledge, the most language-specific code in the seam — and (iii) the pack adapter welded to **51 `semantic_model` types**. Unlike Go's `artifact.rs`, lowering `semantic_model` is necessary but not sufficient, because `CSharpExternalDeclarationIndex` (the analyzer-resident half) is defined in the same file and consumed by the shim.\n\n**The dead-code inline bulk eligibility.** Two separate things, both real:\n- `CSharpDeadCodeBulk::needs_precise_scan` (`csharp/mod.rs:2432–2466`) carries a doc comment at `:2428–2431` stating it is *\"tested inline rather than through a `dead_code_bulk_eligibility` in the graph module, because what disqualifies a C# candidate is local to the candidate\"*. It calls: `analyzable_file_count(analyzer, Language::CSharp)`, `overloaded_function_fqns(analyzer, Language::CSharp)` (both framework helpers in `dead_code_smells.rs`), `csharp_constructor_candidate` (`mod.rs:2491`, `parent_of` + identifier equality), and `csharp_unsafe_using_member_forms_present` (`mod.rs:2498`) which reads **every** analyzable C# file's source and runs two `LazyLock<Regex>` (`^\\s*(?:global\\s+)?using\\s+static\\b` and the alias form). `build` calls `fqn_bulk_nodes` + `build_csharp_usage_edges`. All of it is `&dyn IAnalyzer` + `project().analyzable_files()` — shim-coverable, but the regex pair and the constructor predicate are language knowledge sitting in the SPI block.\n- Separately, **`csharp_implicit_entry_point` is genuinely inline in the framework file**: `code_quality/dead_code_smells.rs:1522–1550` (`csharp_implicit_entry_point` → `csharp_main_entry_point` + `csharp_test_entry_point` → `csharp_source_has_test_attribute` with a `[Test|Fact|Theory|TestMethod]` regex), called at `:334–337` and `:361–364` of `dead_code_candidates`, plus `csharp_public_like_declaration` at `:1495`. Contrast: Go routes through `usages::go_graph::go_implicit_entry_point` (`:435`) and C++ through `usages::cpp_graph::is_cpp_global_main` (`:1519`). C# is the one language whose entry-point predicate has no language-module home — ~40 LOC. `dead_code_smells.rs` is allowlisted in the reach-in gate (\"per-language dead-code scoring and entry-point predicates; follow-up\"), so this parks with `exception_handling.rs`-class residue.\n\n**`CSharpMemberName` / attribute / `nameof` helper family.** `CSharpMemberName<'tree>` (`mod.rs:1633`), `CSharpConditionalMemberAccess<'tree>` (`:1640`), `csharp_member_name` (`:1665`), `csharp_conditional_member_access` (`:1646`), `csharp_attribute_name_node` (`:1573`), `csharp_attribute_type_names` (`:1590`), `csharp_attribute_terminal_name` (`:1610`), `csharp_nameof_type_candidates` (`:1408`), `csharp_unqualified_invocation_for_name` (`:1691`), `csharp_constant_pattern_type_candidate` (`:1460`), `csharp_member_access_type_receiver` (`:1481`), `csharp_type_{reference_root,terminal_identifier,leftmost_identifier,node_identity}` (`:1305,1499,1532,1197`). **All are pure `Node<'_>`/`&str` functions with no analyzer parameter.** Consumer census, post-registry: every call site is inside the C# seam (`csharp_graph/{inverted,extractor,resolver}.rs`, `get_definition/csharp.rs`, `csharp/{declarations,structural,semantic,imports}.rs`). `receiver_query.rs` no longer imports any of them — its 39 C# references are 3 production `Language::CSharp` match arms (`:2001–2007` the `csharp_dynamic_receiver_unsupported` diagnostic string, `:2743`, `:2823`) and 36 inside `mod tests` (starts `:1949`). **The registry-era census's receiver_query finding is stale; this family is now seam-internal and moves wholesale.**\n\n**Self-recursion in the memo web.** Present but shallower than Rust's or Python's. The cycles: `global_static_using_types()` (`OnceLock`) → `type_candidates_by_fqn` → `declaration_candidates_by_fqn` → `global_usage_definition_index()`; and `visible_type_candidates_inner` → `using_aliases_of`/`namespace_of_file`/`using_namespaces_of` (three moka caches) → `visible_type_candidates_with_lookups`. Crucially, `visible_type_candidates_with_lookups` (`mod.rs:982`) is **already generic over four `FnMut` lookups** — the C# seam pre-adapted its worst recursion point into an injectable-source shape. A single-tier source trait (`CSharpAnalysisSource: CodeUnitIndex + ImportAnalysisProvider`) suffices; the two-tier `RustAnalysisSource`/`RustUsageSource` split is not needed, because no `OnceLock` build re-enters the cell it is filling.\n\n**`BoundedDefinitionLookup` gaps.** C# calls five `DefinitionIndexHandle` methods (`csharp/mod.rs:335, 407, 420, 1095, 1117`): `fqn` and `package_exists` are on the core trait; **`identifier(&str)`, `members_for_owner_name(owner, normalized, name)` and `by_normalized_fqn(&str)` are not.** `by_normalized_fqn` is the one Python also needs (per the Python census's two-method extension) — so the trait extension is `types_in_package` + `by_normalized_fqn` (Python) + `identifier` + `members_for_owner_name` (C#) = four methods total across the two languages.\n\n---\n\n## 4. Reverse edges\n\n**Production, framework → C# (all allowlisted or assembly, verified against `tests/suite_cross_language/language_reach_in_gate.rs`):**\n\n| Site | What |\n|---|---|\n| `analyzer/mod.rs:98, 99–105, 106–115, 116` | Re-export hub: `CSharpAnalyzer`; the 9-item `external::{…}` public block; the 21-item `pub(crate)` block; the 2-item `pub` name-helper block (allowlist: \"re-export hub for the facade's public surface\") |\n| `analyzer/usages/mod.rs:66` | `pub use csharp_graph::CSharpUsageGraphStrategy` (allowlist) |\n| `analyzer/lib.rs:46` | Crate-root surface (allowlist) |\n| `analyzer/languages.rs:730` | `Language::CSharp => Some(&csharp::CSharpSupport)` (assembly) |\n| `analyzer/multi_analyzer.rs:5,38,264` | `AnalyzerDelegate::CSharp` (assembly) |\n| **`analyzer/usages/get_definition/mod.rs:18–25`** | **15 items imported from `csharp_graph`** (`csharp_argument_count`, `csharp_extension_invocation_return_type_fq_name`, `csharp_first_type_child`, `csharp_is_declaration_name`, `csharp_is_type_reference_node`, `csharp_member_declared_type_fq_name`, `csharp_method_return_type_fq_name_for_arity`, `csharp_node_text`, `csharp_object_created_type`, `csharp_object_initializer_for_label`, `csharp_object_initializer_owner_type_node`, `csharp_reference_type_text`, `csharp_visible_extension_method_candidates`, `csharp_member_access_name`, `csharp_member_access_receiver`, `seed_csharp_bindings_before`) plus `:83` `csharp_callable_arity`, `:110` `mod csharp`, `:129–131` the re-export of `CSharpTypeLookupResolution`/`csharp_type_lookup_resolution{,_in_session}`/`resolve_csharp_bounded`, `:1269–1273` the dispatch arm (allowlist) |\n| `analyzer/usages/get_type/mod.rs:15,27` | `mod csharp` + `pub(crate) use csharp::{resolve_csharp_type, resolve_csharp_type_bounded}` (allowlist) |\n| `analyzer/usages/get_definition/call_sites.rs:358, 406, 783, 996` | `csharp_call_reference_candidate` + `Language::CSharp` arms (allowlist) |\n| `code_quality/dead_code_smells.rs:334, 361, 458, 802, 1091, 1495, 1522–1550, 1672` | Inline entry-point/visibility predicates + `csharp_graph_finding` (allowlist) |\n| `analyzer/usages/receiver_query.rs:2001–2007, 2743, 2823` | 3 `Language::CSharp` production arms, no type reach-in (allowlist) |\n| `analyzer/exception_handling.rs:81, 530–564` | `analyze_csharp`, 35 LOC of catch-clause knowledge in a framework file |\n| `analyzer/lexical_definitions.rs:400, 489, 965, 1007, 1047, 1075, 1095, 1196, 1215, 1247` | 10 `Language::CSharp` grammar arms (shared machinery, parks) |\n| `analyzer/reference_candidates.rs:129, 157, 225` | `is_csharp_tuple_element_name` + 2 arms |\n| `analyzer/usages/workspace_graph.rs:32, 58` | `UsageEcosystem::CSharp` |\n| `analyzer/value_flow/mod.rs:22` | `Language::CSharp` in the supported set |\n| `searchtools/selectors.rs:531, 1583` | C#-only `display_identifier_for_target` branch; label |\n| `searchtools/scan_usages.rs:1078` | `\"C#\"` label |\n| `analyzer/store/epoch.rs:226–231, 461–468` | `.scm` include paths + `lang_epoch!` salt |\n| `analyzer/global_usage_definition_index.rs:329–336` | Comments only (a `$`-boundary regression note) |\n\n**cfg(test)-only C# reach-ins** (each verified to sit after its file's `#[cfg(test)]`): `analyzer/structural/search/tests.rs:6,17` (`CSharpAnalyzer::from_project`), `analyzer/usages/receiver_sites.rs:362,368` (`csharp::structural::CSHARP_STRUCTURAL_SPEC`), `analyzer/usages/receiver_query.rs:3522, 3563–4672` (36 refs incl. `csharp.signature_metadata_limited`/`ranges_limited`/`full_hydration_count_for_test` at `:4180–4250`), `analyzer/languages.rs:747,1041`, `analyzer/multi_analyzer.rs:1579`, `analyzer/cognitive_complexity_tests.rs` (16 refs), `code_quality/comment_density.rs:467–468` (fixture strings), `analyzer/scala/declarations.rs:1099` (comment).\n\n**Cross-crate reach-ins:**\n- `bifrost-lsp/src/lsp/handlers/import_ambiguity.rs:2,19,20,59–67,174` — **imports `CSharpAnalyzer` by name**, `resolve_analyzer::<CSharpAnalyzer>`, and a local `csharp_using_import_is_ambiguous(&CSharpAnalyzer, …)` + `csharp_alias_identifier`. The one type-level cross-crate reach-in (Rust's counterpart is `type_definition.rs:97`); it consumes only `ImportAnalysisProvider::import_info_of`, so a capability or a re-export closes it.\n- `bifrost-lsp/src/lsp/handlers/document_symbol.rs:8,231` — `csharp_source_name_segment` (free fn).\n- `bifrost-lsp/src/lsp/handlers/{formatting.rs:296, hover.rs:122, semantic_tokens.rs:454}` — `Language::CSharp` arms / test fixture only.\n- `src/lib.rs:15` — facade re-export of `CSharpAnalyzer`.\n- `src/mcp_property_fuzzer/service_probes.rs:2197` — `strip_csharp_generic_arity`.\n- `src/reference_differential/mod.rs:232, 597–601, 722–838` — `csharp_is_nameof_argument`/`csharp_transparent_nameof_parent`, a tree-sitter `nameof` walk implemented in the root crate (differential harness, not the seam).\n- `bifrost-mcp`, `bifrost-core` (beyond `CSharpAnalyzerConfig` at `config.rs:10,192,318`), `bifrost-runtime`, `bifrost-policy`, `bifrost-semantic-packs`: none.\n\n---\n\n## 5. crates.io deps a `brokk-bifrost-csharp` crate needs\n\n| crate | version in `bifrost-analysis/Cargo.toml` | needed by |\n|---|---|---|\n| `brokk-bifrost-core` | path, `=` pin | everything |\n| `tree-sitter` | 0.25.10 | everything |\n| `tree-sitter-c-sharp` | **0.23.1** (older than Go/Rust/Python's 0.25.x grammars) | `adapter.rs`, `structural.rs`, `mod.rs` (`HIGHLIGHTS_QUERY`), `declarations.rs`, `csharp_graph/*` |\n| `regex` | 1.12.2 | `tests.rs`, `mod.rs:2511,2515` |\n| `goblin` | 0.10.7 | `external.rs` only — **parked**, not needed at first landing |\n| `semver` | 1.0.28 | `external.rs` only — parked |\n| `serde_json` | 1.0.145 | `external.rs` only — parked |\n| `walkdir` | 2.5.0 | `dependency_discovery.rs` + `external.rs` — parked |\n\n**Not** `rayon` (zero uses in the whole seam — the only fleet language with none), **not** `moka` (all 6 caches stay shim-side; no RUST_TREES-class global), **not** `toml`, **not** `serde`. First landing is a **four-line dependency block**, the smallest in the fleet.\n\n---\n\n## 6. LOC accounting\n\nCalibration: Go 8,544 moved / 1,598 shim; Rust 16,375 moved / ~1,900 shim (23,582 residue of which 17,494 parked by design); Python projected 10,100 / ~1,590.\n\n| Band | LOC | Content |\n|---|---:|---|\n| **Movable now** (retarget only) | **~3,470** | `declarations.rs` 1,197, `structural.rs` 408, `tests.rs` 309, `mod.rs` free-fn bands `:59–240`+`:1132–1833`+`:2491–2520` ≈ 897 (less `limited_known_values` 17), `adapter.rs` bodies + `CSHARP_COGNITIVE_CONFIG` ≈ 120, `clones.rs` token normalizer ≈ 90, `imports.rs` parsing ≈ 190, `hierarchy.rs` attribute-resolution logic ≈ 233 (post-R1), `csharp_graph.rs` free builders ≈ 25 |\n| **Movable after the R1-class rewrite** | **~1,210** | `mod.rs:250–1131` language logic (52 methods, ~197 in-seam call sites) — **gated on lowering `QueryBatch`/`LimitedQueryRows` to core** (41 sites) and on 4 new `BoundedDefinitionLookup` methods (`identifier`, `members_for_owner_name`, `by_normalized_fqn`, and Python's `types_in_package`) |\n| **Movable iff `resolver.rs` splits** | **~3,500** | `csharp_graph/extractor.rs` 997, `inverted.rs` 867, `hits.rs` 85, `resolver.rs` session-free half ≈ 1,337 + its struct/const mass ≈ 600, minus double-counting; requires `ClassRangeIndex`, `parse_tree_sitter_file` and `lookup_suffix_candidates` lowered to core |\n| **Parked on `analyzer/semantic`** | **4,440** | `csharp/semantic.rs` |\n| **Parked on `semantic_model`** | **3,871** | `csharp/external.rs` 3,811 + `dependency_discovery.rs` 60 (plus hard coupling 3 — the analyzer-resident index survives the workstream) |\n| **Parked on `ResolutionSession`** | **6,163** | `get_definition/csharp.rs` 3,708 + `get_type/csharp.rs` 914 + `resolver.rs` session-touching band ≈ 1,394 + its 147-LOC test tail, plus ~30 LOC of `get_definition/mod.rs` C# batching |\n| **Shim floor** | **~1,660** | `mod.rs` shim half ≈ 785 (imports 58, struct 9, ctors/hooks 61, `LimitedQueryRows` forwarders 26, `TestDetectionProvider` 4, `CodeUnitIndex` 147, `IAnalyzer` 192, `AnalyzerTestHooks` 58, SPI block 230), `csharp_graph.rs` strategy wrappers ≈ 103, `csharp_graph/shared.rs` 141, `cache.rs` 119, `imports.rs` provider impl ≈ 150, `adapter.rs` `LanguageAdapter` impl ≈ 121, `hierarchy.rs` provider + memo accessors ≈ 60, `clones.rs` entry ≈ 43, `external_*` accessors 28, retained tests ≈ 110 |\n\n**Total 24,311** (22,378 prod / 1,933 in-file test) **+ 47 `.scm`.**\n- **Scenario A — `resolver.rs` parks whole** (the Rust-R2 precedent: `rust_graph/{extractor,inverted,hits}` followed `get_definition/rust.rs`): moves ≈ **4,680 (19 %)**, parked ≈ 18,470 (76 %), shim ≈ 1,160. Frontend ≈ **−2.2 s**. This is the worst move rate in the fleet.\n- **Scenario B — `resolver.rs` splits at the session line**: moves ≈ **8,180 (34 %)**, parked ≈ 14,474 (60 %), shim ≈ **1,660** — within 62 LOC of the Go floor, i.e. the SPI block plus the memo shell with no per-language excess. Frontend at Go's measured 0.47 s/kLOC: **≈ −3.8 s**, fourth in the fleet behind Rust, Python and Go.\n\nThe 26-point spread between the scenarios is entirely the `resolver.rs` split decision.\n\n---\n\n## The three hardest couplings\n\n**1. `csharp_graph/resolver.rs` is a bidirectional dependency with the definition route — no precedent in the fleet.** It imports `get_definition::ResolutionSession` (`:23`, 53 references, ~33 of its 109 functions, ~1,394 LOC) *and* `get_definition/mod.rs:18–25` + `get_definition/csharp.rs:5–10` import 24 items back out of it through `csharp_graph.rs:8–31`'s `pub(in crate::analyzer::usages)` re-export block. Every C# resolution primitive exists in two spellings — `usage_member_declared_type_fq_name` / `member_declared_type_fq_name_in_session`, `visible_extension_method_candidates` / `..._in_session`, `seed_bindings_before` / `..._in_session` — so the graph half and the definition half share one 3,623-LOC body of code and differ only in whether a `ResolutionSession` budget is threaded. Go's `graph/resolver.rs` (1,847) and Rust's (1,528) both live entirely inside their language crates; Rust's scan bodies parked *because* they reached into the definition route, but they never had the reverse edge. Either resolver.rs is split (session-free core moves, session wrappers stay), or `csharp_graph/` — 5,713 LOC, 78 % of the movable-with-reshaping band — parks in full. It also drags `ClassRangeIndex` (`usages/inverted_edges.rs:52`, `declarations`+`ranges` only), `usages::parsed_tree::parse_tree_sitter_file` and `tree_sitter_analyzer::lookup_suffix_candidates`, none of which `brokk-bifrost-go` or `brokk-bifrost-rust` touches.\n\n**2. `LimitedQueryRows`/`QueryBatch` is the R1 block's return vocabulary, and it lives in analysis.** 41 sites in `csharp/mod.rs`, 14 of the 51 inherent methods returning it, plus `resolver.rs:64`'s `impl FnOnce(usize) -> LimitedQueryRows<T>` budget adapter. The type is `analyzer/store/mod.rs:529` aliasing `analyzer/i_analyzer.rs:22`. Go never used it; Rust's census listed it under \"parks with the definition route\" and R2 confirmed that — so no landed crate carries it and there is no precedent for lowering it. Without a core `QueryBatch`, the entire ~1,210-LOC R1 band stays in analysis even after the free-function rewrite, and the shim inflates by that amount. Its own contents are a `Vec<T>`, an `inspected` count and a `complete` flag — the lowering is W1-class in difficulty, but it has to be scheduled, and the four `BoundedDefinitionLookup` additions (`identifier`, `members_for_owner_name`, `by_normalized_fqn`, `types_in_package`) ride with it.\n\n**3. `CSharpExternalDeclarationIndex` is analyzer state built by a 3,117-LOC file that is simultaneously a PE decoder and a `semantic_model` pack adapter.** `CSharpAnalyzer.external_index: Arc<OnceLock<CSharpExternalDeclarationIndex>>` (`mod.rs:245`, refreshed at `:2068–2089`) is initialized from `external.rs:292`'s type, which is defined in the same file as `parse_assembly_bounded` (`goblin` PE + CLI metadata tables, `:1789–3116` — the single most language-specific body of code in the seam, and pure knowledge that belongs in a C# crate) and as `CSharpDependencyPackAdapter`/`CSharpAssemblyPackProducer`, which name 51 `semantic_model` types at `:3–16`. This is the JVM realm's shape (`java/mod.rs:62`, `kotlin/mod.rs:119`, `scala/mod.rs:259` with `JvmExternalDeclarationIndex`), and Go, Rust and Python have no analogue whatsoever. Lowering `semantic_model` is necessary but not sufficient: the index type has to separate from the pack producer before the decoder can leave, or a 3.1k-LOC file with a `OnceLock` on the shim struct stays put. There is no cross-crate consumer forcing the shape (unlike Python's `activate_python_environment_packs`), which is the one thing making it more tractable than Python's coupling 2.\n\nTwo smaller items recorded for completeness: `csharp_implicit_entry_point` and its three helpers (~40 LOC, `code_quality/dead_code_smells.rs:1522–1550`) are the fleet's only language entry-point predicate with no language-module home — Go and C++ both delegate into their graph modules; and `bifrost-lsp/src/lsp/handlers/import_ambiguity.rs:19,60` is the sole cross-crate `CSharpAnalyzer` type reach-in, consuming only `ImportAnalysisProvider::import_info_of`."}],"stop_reason":"end_turn","stop_sequence":null,"stop_details":null,"usage":{"input_tokens":2,"cache_creation_input_tokens":1265,"cache_read_input_tokens":176121,"output_tokens":17934,"server_tool_use":{"web_search_requests":0,"web_fetch_requests":0},"service_tier":"standard","cache_creation":{"ephemeral_1h_input_tokens":0,"ephemeral_5m_input_tokens":1265},"inference_geo":"not_available","iterations":[{"input_tokens":2,"output_tokens":17934,"cache_read_input_tokens":176121,"cache_creation_input_tokens":1265,"cache_creation":{"ephemeral_5m_input_tokens":1265,"ephemeral_1h_input_tokens":0},"type":"message"}],"speed":"standard"},"diagnostics":null},"requestId":"req_011CdjWfYLvfZoz22tDE7kt5","attributionAgent":"Explore","type":"assistant","uuid":"6becdeac-1c0a-4ef9-a2d5-61e757bc1b9b","timestamp":"2026-08-05T11:23:41.801Z","effort":"high","userType":"external","entrypoint":"cli","cwd":"/home/jonathan/Projects/bifrost2","sessionId":"8f94fdd6-1d55-4f9d-8f78-532b5b220270","version":"2.1.220","gitBranch":"bifrost2","slug":"solve-https-github-com-brokkai-bifrost-i-atomic-hopper"}
