# MCP property fuzzer (oracle-free contract fuzzing of the searchtools surface)


## Purpose

Bifrost's existing automated-analysis campaigns (the reference-differential runner, FIRD) audit one plane: whether the forward definition-resolution path and the inverse usage path agree about reference sites. A 2026-07-21 audit of live agent traffic (1,981 real tool calls from an 11-language P2T rollout) found six defects, and four of them were structurally invisible to FIRD because they are not reference-graph disagreements at all: a Scala class whose extracted source range stopped at its `@Inject()` constructor annotation (issue #1016), a `get_summaries` response that rendered a directory's children while simultaneously reporting that same directory `not_found` (#1017's ancestor), a `file#symbol` selector spelling that failed where the bare symbol name succeeded (#1018), and failure responses that carried no actionable hint or asserted that in-workspace symbols were "external" (#1015, #1019).

This plan builds the complementary instrument: a fuzzer that drives the same MCP tool surface agents use, generates its queries from Bifrost's own index, and checks self-consistency properties that need no external ground truth. Because every violation is self-evident from the responses in hand — no LLM judging, no human labeling, no VM rollouts — an autonomous coding agent can run it in a loop, shrink and deduplicate findings, file issues, and keep going until a full corpus pass produces no new failure signatures. That is the operating model: hand this plan to an agent and say "keep going until you run out of bugs."

After this change, a contributor can run one command against any corpus repository and receive a JSONL ledger of contract violations, each with a minimal reproduction (tool name, exact arguments, the offending response excerpt, and the invariant it violates), and can rerun any single finding in isolation to confirm it.


## Definitions

"MCP surface" means the tool descriptors and handlers Bifrost serves over the Model Context Protocol stdio server (`bifrost` with no arguments runs `--mcp searchtools`). The descriptor set is built in `src/mcp_core.rs::symbol_tool_descriptors(render_line_numbers)`. Note the surface is mode-dependent: with `render_line_numbers` false (the mode the P2T production harness uses) the server offers `get_definitions_by_reference` and `scan_usages_by_reference`; with it true, the `_by_location` variants replace them. The fuzzer must exercise both modes.

"Oracle-free" means the expected answer is not computed by any external authority (no LSP, no human labels). Each property is an internal-consistency claim: two responses from Bifrost itself, or one response and the index Bifrost itself built, must not contradict each other.

"Invariant" means one such machine-checkable property, identified by a short code (I1..I5 below).

"Failure signature" means the dedup key for a violation: `(invariant code, language, tool name, syntactic shape of the offending construct)` — for example `(I1, scala, get_symbol_sources, class-with-annotated-primary-constructor)`. Signatures exist so a corpus with ten thousand annotated Scala classes yields one ledger entry, not ten thousand.

"Shrinking" means reducing a failing case to the smallest reproduction that still violates the invariant — dropping unrelated batch entries, trimming context strings — before it is recorded.

"Ledger" means the append-only JSONL file of shrunk, deduplicated findings that survives across resumed runs, in the spirit of the FIRD runbook (`.agents/docs/reference-differential-runbook.md`). The ledger lives inside this repository under `.agents/plans/mcp-property-fuzzer/` and is committed, so findings are visible to the whole team rather than stranded on one operator's scratch disk.


## Repository orientation

The pieces this plan touches or imitates:

- `src/bin/bifrost_reference_differential.rs` — the FIRD CLI driver. The fuzzer gets a sibling binary, `src/bin/bifrost_mcp_property_fuzzer.rs`, and copies its argument conventions (corpus roots, resume, per-repo selection) where they fit.
- `src/reference_differential/mod.rs` — FIRD's engine and report schema; the model for a new `src/mcp_property_fuzzer/mod.rs`.
- `src/mcp_core.rs`, `src/searchtools_service.rs`, `src/tool_arguments.rs`, `src/searchtools_render.rs` — the descriptor definitions, the service layer that executes tool calls, argument parsing, and response rendering. The fuzzer calls the service layer in-process (like FIRD calls the analyzer) rather than spawning stdio servers, but it must construct calls exactly as the MCP handler would parse them, so selector-string handling and response rendering are genuinely under test.
- Corpus layout (identical to FIRD; see the runbook): clones under `/home/jonathan/Projects/brokkbench/clones`, which is a symlink to `/mnt/minasmorgul/repo-clones` (recreated 2026-07-21 after the previous `/mnt/T9` mount disappeared). Membership per language comes from `/home/jonathan/Projects/brokkbench/sft-tools-commits/<language>/<slug>.jsonl`; per-language priority is by task count derived from `/home/jonathan/Projects/brokkbench/tasks.py`'s task data (see Decision Log), not by `repos.csv::code_loc`. Durable run output — the ledger and any campaign state files — lives in this repository under `.agents/plans/mcp-property-fuzzer/`.


## The invariants

Every invariant below is annotated with the live defect that motivates it. When implementing, treat the motivating issues as executable acceptance tests: until the underlying bug is fixed, running the fuzzer on the named repository must reproduce the finding; after the fix, the finding must disappear.

**I1 — Range integrity.** For every symbol the index exposes: (a) the source range of a container symbol must contain the ranges of all its indexed members; (b) the text at the symbol's own range must contain the symbol's terminal name token; (c) `get_symbol_sources` for the symbol must return text identical to the file content at the reported range. Motivation: #1016 — `org.thp.thehive.connector.cortex.controllers.v0.JobCtrl` (repo TheHive-Project/TheHive) returns `start_line: 25, end_line: 26`, text `@Singleton\nclass JobCtrl @Inject()`, while its own methods are indexed at lines far below — a bare (a)-check over the corpus finds this class of bug in one pass with no tool calls at all.

**I2 — Selector-form equivalence.** For every indexed symbol, the selector spellings an agent plausibly writes — terminal name, fully qualified name, `path#terminal`, `path#qualified` — must either resolve to the same declaration or return an ambiguity/not-found response that is consistent across spellings (a strictly more specific spelling must never fail where a less specific one succeeds). Motivation: #1018 — in vuejs/core, `packages/runtime-core/src/hydration.ts#createHydrationFunctions` returned `symbol_not_found` from `get_definitions_by_reference` while bare `createHydrationFunctions` resolved. This recurred after #642 was closed, which is exactly why it belongs in a permanent harness rather than a one-off campaign.

**I3 — Cross-tool round-trips.** Three sub-checks. (a) If `get_summaries` lists symbol S under file F, then `get_symbol_sources(S)` must resolve, and its reported path must be F. (b) If `scan_usages_by_reference` reports a symbol as resolved (any status other than not-found/ambiguous), `search_symbols` for its terminal name must include that declaration among its results. (c) Self-contradiction: no response may both render content for a target and report that same target in its `not_found` list. Motivation for (c): a `get_summaries` call on doctrine/orm's `src/Query/Expr` rendered all thirteen member files and then appended `Not found: src/Query/Expr`; in production this contradiction sent an agent into a 483-call retry loop. (That specific bug is fixed at HEAD; the invariant keeps it fixed.)

**I4 — Diagnostic honesty.** A failure message must not assert a claim the index refutes. Concretely: if the response text matches phrases of the family "not indexed", "outside the indexed workspace", "external crate/module", then a `search_symbols` query for the terminal name of the failed selector must not return an in-workspace declaration with that name. Motivation: #1015 — tokio symbols wrapped in `cfg_rt!`/`cfg_coop!` macro blocks produced `unresolvable_import_boundary: … not indexed in this workspace` for `pub(crate)` items that plainly are in the workspace. (The fuzzer cannot decide whether macro-wrapped items should resolve — that is a resolution-quality question for FIRD's plane — but it can always catch the lie in the message.)

**I5 — Hint presence.** Every response with a failure status (not-found, ambiguous, invalid-location, and kin) must carry actionable next-step content: a candidate list, a corrective note, or a concrete suggested query. An empty refusal is a violation. Motivation: #1019 — C `file::struct tag` selectors (five occurrences in one rollout), TypeScript block-scoped closures, and Rust struct-field targets all dead-ended with a bare "no symbol matched" while other malformed shapes get rich hints. The check is syntactic (presence and non-triviality of hint fields/text per response schema), so it stays oracle-free.


## Query generation

Inputs come from the index, not from randomness, so coverage is exhaustive rather than probabilistic. For each corpus repository at its pinned commit: enumerate all indexed symbols (the same enumeration `search_symbols` draws from), all files, and all directories. From each symbol derive the I2 spelling set mechanically. For `get_definitions_by_reference`, derive `(symbol, context, target)` probes by taking a real line from inside the symbol's I1-verified range and a real identifier token on that line — which makes I1 a prerequisite: a truncated range would poison the probe, so ranges are validated first and range-invalid symbols are recorded under I1 and excluded from downstream probes. Batch shapes matter (the current descriptor takes `{"references": [...]}`): probe both single-entry and multi-entry batches, since single-versus-batch asymmetries are exactly the kind of surface drift agents hit.

Also probe the negative shapes real agents produced in traces, as a small fixed set: `file::struct tag` keyword-prefixed selectors, redundant `.__init__` package suffixes, file paths passed where symbols are expected. These are I5 probes — the expectation is not resolution but a non-empty corrective hint.

Two knobs bound runtime: a per-repository symbol cap (default a few thousand, sampled deterministically by hashing FQNs so reruns are stable) and per-language repo priority by task count (see Repository orientation). A full pass over one mid-size repository should take minutes, not hours; I1(a) alone is a pure index walk and should run corpus-wide routinely.


## Ledger, shrinking, and the run-until-dry loop

Each raw violation is shrunk (smallest batch, shortest context still failing), keyed by its failure signature, and appended to the ledger only if the signature is new for that run series. A ledger row records: signature, invariant, repository and pinned commit, tool, exact shrunk arguments (verbatim JSON), the relevant response excerpt(s), and for I1 the file-side evidence (expected range versus reported). Every row must be re-runnable in isolation via a `--rerun <ledger-line>` mode, mirroring FIRD's single-site rerun.

The autonomous loop an operating agent follows: run a corpus pass; for each new signature, rerun to confirm, then decide product-defect versus expected-behavior using the same discipline as the FIRD runbook ("a raw classification is a triage input, not proof"); file confirmed defects as GitHub issues on `BrokkAi/bifrost` (one per signature, carrying the shrunk repro verbatim) and record the issue number back into the ledger; fix or hand off; rerun the affected signature after any fix lands. Issue workflow specifics: `gh` is authenticated as `jbellis`; an issue is assigned to `jbellis` when the agent starts actively working its fix; fixes land directly on `master` (per repository AGENTS.md, no branches) with a thorough commit message — the commit message is the source of truth for what problem is being solved, so it must explain the defect, root cause, and fix in detail — containing `Fixes #N` so pushing to `origin/master` closes the issue; each fix is committed and pushed individually as it lands. The campaign is dry when two consecutive full passes add no new signatures. Campaign state (which repos, which pass, issue links) lives in this file's Progress section, per PLANS.md.

Resolution is language-at-a-time: for each language (processed in task-count order, heaviest agent traffic first), triage its signatures, file issues, land and push every fix — each confirmed by rerunning its signature to gone — before starting the next language's pass. Triage must never accumulate across languages. Two further rules (operator directive, 2026-07-22):

- **Generality.** When a defect affects more than one language, fix the root cause once — do not point-fix per language. A signature that fires on several languages is one issue and one fix; the fix must cover every language the defect touches, with tests proving it per language where the harness allows.
- **Escalation.** If a signature's right fix is architecturally complex — it needs significant design work, or the correct fix has a huge blast radius — comment on the issue explaining the problem and the trade-offs, assign it to David (`DavidBakerEffendi`), and move on to the next signature. Do not start open-ended architecture work inside the campaign.


## Milestones

**M1 — Skeleton and I1 on one language.** Create `src/mcp_property_fuzzer/mod.rs` and `src/bin/bifrost_mcp_property_fuzzer.rs` with corpus/repo selection flags copied from FIRD's driver, plus `--invariants I1 --repo <slug>`. Implement I1 as a pure index walk. Acceptance: running against the TheHive clone reports the `JobCtrl` range violation (issue #1016) with correct evidence, and running against a small healthy repo reports nothing. Unit tests use fixture trees with a deliberately annotated-constructor Scala class.

    cd /home/jonathan/Projects/bifrost
    cargo run --release --bin bifrost_mcp_property_fuzzer -- \
      --clones-root /home/jonathan/Projects/brokkbench/clones \
      --repo TheHive-Project__TheHive --invariants I1 \
      --out .agents/plans/mcp-property-fuzzer/m1.jsonl

**M2 — All invariants, single repository, both render modes.** Wire the service layer in-process for both `render_line_numbers` modes; implement I2–I5 and the probe generator. Acceptance: on vuejs/core the I2 check reproduces the `file#symbol` inconsistency if it still exists at HEAD (or a fixture regression test demonstrates the check fires on a synthetic case); I3(c) and I5 pass on current HEAD (their motivating bugs are fixed) but each has a fixture test proving the check can fire.

**M3 — Corpus runner, ledger, resume, dedupe, shrink, rerun.** Bounded concurrency across repositories (reuse the pattern from `.agents/plans/concurrent-reference-corpus.md`), deterministic sampling, `--resume`, `--rerun`. Acceptance: a two-language corpus run resumes cleanly after an interrupt, produces a deduplicated ledger, and every ledger line reruns to the same violation.

**M4 — Acceptance campaign and issue workflow, tiered by task count.** First complete M1–M3 with whatever smoke tests make sense. Then run the campaign language-at-a-time in widening tiers drawn from the per-language task-count ranking: first the top 1 repository per language across all languages, then top 5, then top 10. Within a tier, fully resolve one language before starting the next — triage per the discipline above, file issues for confirmed signatures per the issue workflow in the Ledger section, land and push each fix individually (confirming its signature gone via rerun), escalate architecturally complex ones to David — so no triage backlog accumulates. Fixes are general across languages, never per-language point fixes. Acceptance: the committed ledger under `.agents/plans/mcp-property-fuzzer/` covers every repo in the current tier, every confirmed signature has an issue link recorded in the ledger, and a follow-up pass after at least one fix shows its signature gone. A tier is complete only when its pass adds no new signatures; widening from top-1 to top-5 to top-10 happens tier by tier, not all at once.


## Validation

Beyond per-milestone acceptance: `cargo test` must stay green; new tests live beside the module (`tests/mcp_property_fuzzer.rs` for the engine, CLI tests mirroring `tests/bifrost_reference_differential_cli.rs`). Every invariant must have at least one fixture that makes it fire and one that proves it stays silent on healthy input — a checker that never fires is indistinguishable from a checker that is broken, so fixture-triggered firing is a hard requirement, not a nicety. When the runbook-style operational knowledge grows past this plan, split a `.agents/docs/mcp-property-fuzzer-runbook.md` out of it, as FIRD did.


## Surprises & Discoveries

- 2026-07-21: **I1(a) needs a language gate for out-of-body member declarations.** The first healthy-fixture runs fired `container-range-misses-member` on every idiomatic Rust type: Rust `impl` blocks, Go receiver methods, and out-of-line C++ `Foo::bar` definitions all declare members outside the type's declaration range by design. `containment_check_applies` now excludes `Class` parents in rust/go/c/cpp; callable parents are still checked everywhere (nested functions are lexically enclosed in all corpus languages).
- 2026-07-21: **The tree-sitter-scala truncation behind #1016 is a joint-context property and comes in two grades.** It reproduces only with verbatim file excerpts, never paraphrased fixtures — the trigger is a joint property of the import block, the `@Named(...)` constructor-parameter annotation, and the following body tokens. Mild grade (`JobSrv.scala`): an ERROR node (bytes 1692–2227) contains the truncated `class_definition`; the analyzer's ERROR-recovery (`src/analyzer/scala/declarations.rs:135–147`) attaches body members anyway, so they index out of range and I1(a) fires. Severe grade (`JobCtrl.scala`, the file #1016 actually names): the class range truncates at 1168–1202 (lines 25–26) and a *sibling* ERROR node (1203–5221) swallows the rest of the file — zero members index, and the following `PublicJob` class vanishes too. Containment has no children to check, which motivated I1(d).
- 2026-07-21: **`IAnalyzer::parse_errors` is session-fresh only.** It reads `fresh_parse_errors`, populated solely for files parsed in the current session; files served warm from the persisted blob store return `None`. I1(d) counts those files under `skipped_parse_errors_unavailable` rather than guessing. Consequence for operations: fuzzer passes must run cold (fresh clones, or `--cache-mode ephemeral`) for the check to be live — which also avoids writing `.brokk/bifrost_cache.db` into corpus clones (an early persisted-mode smoke run polluted the TheHive clone this way; removed the same day).
- 2026-07-21: **Scala auxiliary constructors index under the class name.** `def this(...)` yields a Function unit whose display identifier is the class name (e.g. `org.thp.client.ProxyWS.ProxyWS` → `ProxyWS`), which by construction never appears in the `def this` range text. This is a CodeUnit naming convention, not a contract break; I1(b) skips callables whose indexed parent is a class with the same display identifier (fuzzer-side, not an analyzer change).
- 2026-07-21: **Environment gotchas for corpus work.** `git2` (used to validate clones and read HEAD) refuses the corpus clones as "dubious ownership" until `git config --global safe.directory '*'` is set — this host's git rejects the narrower `/*`-suffix form. `gh` works only with `GIT_CONFIG_NOSYSTEM=1` in the environment and an explicit `-R BrokkAi/bifrost`.
- 2026-07-21: **TS/JS module units are named after their file, which breaks I1(b) wholesale.** On vuejs/core, 102 of 5,000 sampled symbols fired `range-name-token-absent` — a single signature, exemplar `packages/vue/compiler-sfc/index.mjs`: the Module unit's display name is `index.mjs`, its terminal token `mjs`, and that token never appears in the file text. A module's "declaration" is the file itself; there is no name token in the source. I1(b) now skips `CodeUnitType::Module` unconditionally (counted under `skipped_module_name`), the same naming-convention class as the Scala auxiliary-constructor skip; vuejs/core I1 is fully silent after the fix.
- 2026-07-21: **#1018 does not reproduce at the corpus pin.** vuejs__core @ c0606e91: the fuzzer's I2 probes on `createHydrationFunctions` were silent, and a faithful manual `get_definitions_by_reference` call through the production service (`SearchToolsService::new`, persisted cache) resolved both `createHydrationFunctions` and `packages/runtime-core/src/hydration.ts#createHydrationFunctions` to the same `scheduler.ts:173` declaration. The issue's trace base (`164af639…`) differs from the corpus pin, so this neither confirms nor refutes the issue — it stays open; M4's full-coverage ts passes will exercise the surface at the corpus pin. Manual probes ran on a `/tmp` copy because `--tool` mode persists `.brokk/` into the working repo (the corpus clone already contained pre-existing `.brokk`/`.bifrost` directories from earlier activity; left untouched).
- 2026-07-21: **`get_summaries`' display fq does not round-trip through `get_symbol_sources`.** bfg: summaries list `com.madgag.git.LFS.Pointer` under `LFS.scala`, but that exact spelling is *ambiguous* on the sources surface (`matches: [com.madgag.git.LFS$.Pointer, com.madgag.git.LFS$.Pointer$]` — class vs companion, neither an exact fq match). The sources surface is blameless here (it offers corrective selectors, satisfying I5); the inconsistency is that summaries emit a spelling their own ecosystem cannot resolve. I3(a) keeps flagging it (`summaries-listed-symbol-unresolvable`) as a genuine finding — M4 issue fodder.
- 2026-07-21: **Ambiguity is encoded inconsistently across surfaces.** `get_symbol_sources`/`get_summaries` return a structured `ambiguous` array with `matches`; `get_definitions_by_reference` reports the same condition as `status: "not_found"` plus a `diagnostics[].kind == "ambiguous_symbol"` entry. Both are machine-readable (the fuzzer reads each correctly now), but an agent written against one surface's shape will misread the other as a hard miss. Consistency candidate for M4.
- 2026-07-22: **Build profile barely matters for the probe phase; I had claimed debug-vs-release was the slowness factor and was wrong.** Measured on the same ts repo: workspace *build* is ~3x faster in release (14.6s → 4.8s), but the probe phase — where 99% of wall time lives — is serialized by the AnalyzerStore connection Mutex and IO revalidation (issue #1054), not by unoptimized Rust. The tree-sitter parsing core is C compiled `-O2` regardless of cargo profile. The actual fixes are parallel probes plus repo-level oversubscription.
- 2026-07-22: **The ts ×667 `summaries-listed-symbol-unresolvable` signature was checker naivety, not a product defect.** All 662 analyzable cases were bare-name collisions where the ambiguity answer includes the listed file's own `path#symbol` selector — the element resolves from its listing context (the listing supplies the path). `check_i3a` now treats that as consistent and still fires when matches exclude the listed path (the bfg shape: qualified spelling mapping to no exact declaration). Rerun: signature gone, no residue.
- 2026-07-22: **Same-fq symbols in different files poisoned I2 spelling groups.** oh-my-openagent vendors parallel packages (lsp-core vs omo-senpi) defining identical fq display names; grouping by `(tool, symbol_fq)` merged their sets and fabricated 43 `spelling-resolves-to-different-declaration` violations. Groups now key by `(tool, symbol_fq, symbol_path)`; rerun collapsed the signature entirely. Chisel's scala-2/scala-3 parallel trees are the same shape — its ×51 will be re-measured at scala triage.
- 2026-07-22: **I1(c) fired on unverifiable evidence.** `source-range-outside-sampled-source` (ts ×3, chisel ×30) was the checker treating "file outside the 5,000-symbol sample / text unavailable" as a violation instead of skipping. Now skipped under `skipped_unsampled_source`; fires only on genuine range-overrun with text in hand.
- 2026-07-22: **The ts ×9 `scan-resolved-symbol-absent-from-search` decomposed into two non-defects:** ultra-generic terminal names (`constructor`, `path`, `version`, `log`) whose search result set hit the file cap (absence from a truncated set is unverifiable — the response honestly signals `truncated`), and module scan targets (`*.test.mjs`) whose "name" is their file path, which search_symbols has no contract to resolve (the I1(b) module naming convention). `check_i3b` skips both under counters. Genuine contract retained: absence from a *complete* result set still fires.
- 2026-07-22: **`--invariants I1` alone runs no service phase at all** (the service phase requires at least one of I2–I5), so I1(c) checks need a service invariant enabled too. Learned while a filtered reproduction silently produced zero violations; the rerun path always carries the full recorded invariant set, so only manual filtered runs are affected.


## Decision log

- 2026-07-21: Plan authored from the P2T trace-audit findings (issues #1014–#1019). Chose the oracle-free contract plane over an identifier-census differential as the first build because triage is mechanical (both contradicting responses are in hand), which is what makes the run-until-dry loop autonomous. The census differential (an over-approximating absolute leg that would catch symmetric resolution blind spots like #1014/#1015) is explicitly out of scope here and should become its own ExecPlan.
- 2026-07-21: Queries generated from the index rather than random strings: coverage should be exhaustive over what Bifrost claims to know, and malformed-input robustness is probed only through the I2 spelling set and the small fixed set of negative shapes agents actually produced in traces.
- 2026-07-21: Corpus root corrected at execution start. The plan's original path (`/home/jonathan/Projects/brokkbench/clones` → `/mnt/T9/repo-clones`) was stale because `/mnt/T9` is no longer mounted; the 12,816-clone corpus now lives at `/mnt/minasmorgul/repo-clones` (confirmed to contain all four acceptance repos). The `clones` symlink was recreated pointing at the new location so commands in this plan work verbatim. `/mnt/optane` is likewise gone, so durable output moved into this repository.
- 2026-07-21: Ledger location changed from `/mnt/optane/tmp/mcp-property-fuzzer/` to `.agents/plans/mcp-property-fuzzer/` inside this repository, committed to git. Rationale: the operator (Jonathan) wants findings visible to the rest of the team, and a local scratch path on one machine is neither durable nor visible.
- 2026-07-21: Ticket workflow confirmed as GitHub issues on `BrokkAi/bifrost`, as originally written. An earlier same-day revision briefly considered a local `tickets.md` because `gh` authentication was broken; the operator repaired `gh`/`git` credentials (authenticated as `jbellis`) and reverted to issues, with these specifics: assign the issue to `jbellis` when the agent starts actively working it; fixes commit directly to `master` with thorough commit messages (the message is the source of truth for the problem being solved) containing `Fixes #N`; push each fix to `origin/master` as it lands.
- 2026-07-21: Per-language repository prioritization changed from `repos.csv::code_loc` size ranking to task count per repository as derived from `/home/jonathan/Projects/brokkbench/tasks.py`'s task data, per operator direction ("for each language i want to prioritize by task count"). Rationale: task count reflects where agent traffic actually concentrates, so fuzzing effort follows real usage rather than repository size. The exact derivation (which tasks.py data structure yields the per-repo counts) will be recorded here when pinned during M3 implementation.
- 2026-07-21: Campaign scale structured into tiers. M1–M3 complete first with smoke tests; M4 then proceeds language-at-a-time in widening per-language tiers — top 1 repository by task count across all languages, then top 5, then top 10 — committing and pushing each fix as it goes. Rationale: bounds triage load early (every new signature gets individual attention) while still reaching broad coverage, and gives the team a steady stream of reviewed fixes rather than one giant batch.
- 2026-07-21: I1(b) name-token checks use `display_identifier_for_target` — the same display name the MCP surface renders (e.g. stripping Scala's trailing `$` on companion objects) — rather than the raw CodeUnit identifier, so the fuzzer checks the name agents actually see and type.
- 2026-07-21: Violation records carry an `exemplars` list (capped at 25) in addition to the occurrence count, so triage can confirm whether a specific named symbol (e.g. `JobCtrl` from #1016) is covered by a signature without rerunning.
- 2026-07-21: I1 gained part (d), `declaration-truncated-at-parse-error`, after root-causing #1016 showed the named file's real failure is invisible to (a): when the parser demotes a class body to a sibling ERROR node there are no indexed children, so containment has nothing to check. The check fires when a class declaration's range ends within 2 bytes of a tree-sitter ERROR node spanning more than 8 bytes (named, tunable thresholds — adjacency distinguishes truncation from unrelated errors, the span gate filters stray-token errors).
- 2026-07-21: The auxiliary-constructor skip (Scala `def this` indexing under the class name) is implemented fuzzer-side, not as an analyzer change: the `ProxyWS.ProxyWS` naming is a CodeUnit convention whose text absence is legitimate, and the fuzzer — which owns the "name token must appear" expectation — is the right place to encode the exception.
- 2026-07-21: Fuzzer passes run with `--cache-mode ephemeral` by default for acceptance and campaign smoke runs. Two reasons, both discovered empirically: `parse_errors` is only populated for files parsed in the current session, so warm persisted caches silently disable I1(d); and persisted mode writes `.brokk/bifrost_cache.db` into the corpus clone, dirtying it (observed on TheHive, cleaned up). The flag defaults to `persisted` only because resume-oriented warmed campaigns may want it; the ledger records which mode produced each run.
- 2026-07-21: M2 service phase entry point is `run_invariants_with_service(service, analyzer, config)`, fed by two new `SearchToolsService` constructors — `new_manual_ephemeral_for_project` and `new_manual_persisted_for_project` (both delegating to a private `new_manual_with_cache`) — plus `analyzer_snapshot()` to reuse the session's analyzer for the I1 input instead of building a second workspace. The engine-only `run_invariants` keeps its M1 contract and rejects service-phase invariants (pinned by test), so I1-only callers never pay for a service they do not use.
- 2026-07-21: I1 always runs in the engine phase, even when only I2–I5 are requested, because its invalid-range set drives probe exclusion (a truncated range would poison derived probes); the violations are only *reported* when I1 is among the requested invariants. I1(c) is checked in the service phase (it needs `get_symbol_sources` responses) whenever I1 is requested.
- 2026-07-21: Probe granularity is one tool call per probe record, executed in both render modes (`render_line_numbers` on/off), giving every call an unambiguous outcome and making render-mode drift a first-class comparison; drift is filed under the I3 family (response-consistency) rather than as a sixth invariant. `scan_usages_by_reference` probes run single-mode only: the line-numbers-on surface replaces it with the `_by_location` variant, so there is no second mode to compare against.
- 2026-07-21: `SymbolFacts` gained a `display_fq` field (via `display_symbol_for_target`, the same display name the MCP surface renders — e.g. Scala companion objects without the trailing `$`), and the I2 spelling set is [terminal, display_fq, `path#terminal`, `path#display_fq`]. The fuzzer checks the names agents actually see and type, not internal identifiers.
- 2026-07-21: I1(c) compares line-anchored, not whole-line exact. Acceptance on the tiny Rust fixture false-fired on every nested declaration: `SourceBlock.text` is the declaration's exact byte slice (`searchtools.rs` `source_blocks_for_code_unit` slices `content[start_byte..end_byte]`), so an indented declaration's first line starts at its first token and the last line may end mid-line; `start_line`/`end_line` merely bracket the slice. The checker now requires interior lines to match whole file lines exactly, the first text line to be a suffix of the file's start line, the last a prefix of the end line (a single-line block may sit anywhere inside its line). Deliberate renderings stay inside these affixes (comment expansion widens to whole preceding lines; Go's `type ` re-insertion restores the file's own keyword), so no language carve-outs were needed.
- 2026-07-21: `--symbol-filter <substring>` added to the fuzzer CLI to restrict probing to matching symbols. Built for M2 acceptance targeting (#1018's `createHydrationFunctions`) and reused by M3's `--rerun` to re-execute one violation's probes. Default `--invariants` is now all five.
- 2026-07-21: Task-count derivation pinned (M3 item, settled early during acceptance prep): per-repo task count is `tasks.sft_count_for_repo` from `/home/jonathan/Projects/brokkbench/tasks.py` — the count of candidate commits passing `SFT_PREDICATES` (repo gates: not-overlarge, built, has-testsome, not-skipped; per-sha gates: binding testsome outcome ∧ generated primary prompt ∧ vetted non-fragile tests). Access goes through a Python helper importing that module, honoring its "Thou Shalt Not Read Tasks Manually" policy (enforced by `tests/test_no_manual_task_reads.py`) — the Rust runner never parses `sft-tools-commits/` itself. Ranking key per language: `sft_count` desc, then raw scan-record count desc, then slug asc. Languages where every `sft_count` is zero (today: scala — testsome binding never succeeded there, e.g. TheHive carries `bind_failed_after` in its status) fall back to the raw scan-record count, keeping "prioritize by task count" meaningful everywhere. Verified against the live corpus: all 11 languages rank nonzero except scala (rust is thin: 8 repos with tasks, tokio 139 of 170 total).
- 2026-07-21: `classify_spelling` reads ambiguity, not just status codes. `get_definitions_by_reference` encodes ambiguity as `status: "not_found"` plus a structured `diagnostics[].kind == "ambiguous_symbol"` (where `get_symbol_sources` uses a structured `ambiguous` array); the genuine selector failure is `symbol_not_found`. The first TheHive service pass flagged 75 false `more-specific-spelling-fails` violations until the classifier learned the structured `kind` field (data, not message parsing).
- 2026-07-21: Companion-object spelling sets use the raw `fq_name` (with trailing `$`) alongside the display forms. Scala companions display without `$`, but candidate lists offer the `$` form as the unique selector; probing a companion through the stripped fq resolved to its *class* (correctly — exact fq match wins), which poisoned derived probes with the wrong declaration's context lines.
- 2026-07-21: I2 rule 3 (status drift) compares only `Resolved{status}` outcomes — i.e. verdicts from the location stage onward. Ambiguous-vs-resolved uniqueness differences across spellings are the point of qualification, not drift.
- 2026-07-21: `REFERENCE_TOKEN_STOPLIST` grew 34 → 172 entries: the union of all 11 corpus languages' keywords/literals, after TheHive produced junk probes whose derived reference target was the token `implicit`. Documented in-code as a probe-quality heuristic (over-blocking only moves the probe to the next token), not a parser.
- 2026-07-21: The auxiliary-constructor flag (`SymbolFacts.aux_constructor`) is computed at collection time from a pre-sampling per-file class-name census. The first version detected aux constructors via the sampled parent class, so whether the skip fired depended on whether the parent survived sampling — nondeterministic coverage under `--max-service-symbols`.
- 2026-07-21: `--dump-probes <path>` writes every executed probe (id, tool, kind, arguments, outcome, 4 KiB-capped structured payload, mode-B payload, error) as JSONL. Triage of a violation family starts from its probe ids in the dump instead of a rerun; the dump is what made the bfg/TheHive fuzzer-bug-vs-genuine classification mechanical.
- 2026-07-21: I4 tests the name the failure message *claims* is unindexed, not the failed selector. The claim templates always name their subject in backticks ("`` `AbstractSet[A]` `` appears to cross a Scala import boundary not indexed…" — the selector was `ConcurrentSet`, honestly unresolvable because the chain crosses to an unindexed stdlib type). `disputed_name` extracts the last backticked token before the honesty phrase (covers both "`` `X` `` crosses…" and "…boundary at `` `X` ``…" templates), strips type arguments, and falls back to the selector when no subject is named. Removed 180 false positives on TheHive and 22 on bfg. The contradiction test is qualification-aware: a qualified disputed name pins the identity (any workspace hit whose display fq suffix-matches contradicts), but an unqualified name is scope-relative — the resolver already proved it invisible from the reference's context — so only a hit in the probed file itself is a provable contradiction ("`` `Success[_]` `` not indexed" is honest about `scala.util.Success` even though `JobStatus.Success` exists elsewhere).
- 2026-07-21: I3b's follow-up `search_symbols` probe sets `include_tests: true`. Scan resolves declarations anywhere (including test trees) but `search_symbols` excludes test files by default — every bfg `*Spec` family fired (8 occurrences) with the product's own note telling the caller to set `include_tests`.
- 2026-07-21: I1(c) compares line vectors end-to-end, never a join/re-split round trip: a block whose range ends on a blank line (bfg `InvocableBFG.processFor`) returns text faithfully ending `\n\n`, which re-splitting a joined expected string silently drops.
- 2026-07-21: I2's batch-vs-single comparison keys singles by the full reference (spelling + context + target), not by spelling alone. One spelling serves several unrelated reference probes (different sampled symbols share display names), and the old map made the batch entry race against whichever single the spelling last mapped (bfg `BenchmarkConfig`: the `scratchDir` batch entry was judged against the `parser` reference's single).
- 2026-07-21: I2 rules 2 (same-declaration) and 3 (location-stage drift) partition spelling sets into class vs companion by terminal `$`. A Scala class/companion pair shares a user-level name but the two are distinct declarations with legitimately different resolution behavior — `BenchmarkConfig` resolved to the case class (line 33) while `BenchmarkConfig$` resolved to the companion object (line 7), both correct, previously flagged as different-declaration drift. Cross-partition differences are expected; within-partition differences still fire (pinned by test).
- 2026-07-21: I1(c) skips noted fallback blocks. `SourceBlock.note`, like `presentation`, marks deliberate non-source renderings — file-target flat outlines, `#include` listings, module file listings — whose reported range is expressed in the synthetic text's own coordinates (start 1..outline length), not the file's. Six occurrences on TheHive (bare spellings resolving to file targets, e.g. `client` → the `TheHiveClient.scala` outline) fired until the checker learned the second marker.
- 2026-07-21: The rank helper runs under `$BROKK_BENCH_DIR/.venv/bin/python3` when that interpreter exists, falling back to `python3` from PATH, with `BROKK_BENCH_DIR` defaulting to `/home/jonathan/Projects/brokkbench`. The host's system python3 is 3.8, which rejects `tasks.py`'s `list[dict]` annotations; the venv is the interpreter tasks.py is developed against. `BROKK_BENCH_DIR` doubles as the CLI test override point (a stub `.venv/bin/python3` emitting canned ranking JSON exercises corpus selection end-to-end without the corpus).
- 2026-07-21: Corpus selection skips missing or broken clones with a warning and moves to the next ranked repository instead of failing the pass. The corpus is only partially checked out locally and clones can be corrupt; the ledger records what actually ran, and aborting a campaign because rank #3 of 200 is absent would make the ranking worthless.
- 2026-07-21: `--repo-jobs` defaults to 2. Each repository worker already fans out over `--jobs` (default 8) analyzer threads, and ephemeral workspace builds are the memory peak of the run; 2 concurrent repositories is the conservative default for corpus machines, tunable upward for small-language passes.
- 2026-07-21: M3 concurrency mirrors FIRD's worker pool verbatim (`Arc<Mutex<VecDeque<PreparedRun>>>` + `mpsc::channel`, workers pop and execute, the main thread appends ledger records serially so the ledger is never written concurrently). Resume stays FIRD-style too: implicit via CompletionKey (language, slug, repo HEAD, bifrost HEAD, config fingerprint) — there is no `--resume` flag because appending to the same ledger *is* resuming; `--force` opts out.
- 2026-07-21: `--rerun LINE` re-derives probes from the deterministic sample narrowed by `--symbol-filter <violation.symbol>` instead of replaying the recorded verbatim arguments. The recorded arguments are group-shaped (I2 spelling sets, batch+singles, follow-up chains regenerate per symbol from the sample), so verbatim replay would need a second, replay-only execution engine that drifts from the real one; re-derivation exercises the production generator end-to-end and stays minutes-cheap on large repositories. Two exception classes keep the recorded base config: I5 (negative probes derive from the whole service sample — filtering by symbol would empty the probe set) and violations with no exemplar symbol. I1 findings reproduce through the full deterministic index walk regardless, since `symbol_filter` only narrows service probes. A rerun prints `reproduced`/`MISSING` per signature and exits 2 when any recorded signature went missing — a disappeared violation is a signal (fixed or flaky), same severity as a `--strict` failure.
- 2026-07-21: The shrink surface is exactly `batch-outcome-differs-from-single`. Every other probe family is generated single-entry, and reference contexts are single source lines by construction — nothing shorter still names the token — so batch arguments are the only shrinkable dimension that exists today. Shrinking is greedy one-at-a-time minimization whose reproduction oracle re-executes the candidate batch plus each entry's single call (the violation reproduces while any entry's batched status still differs from its single status); transport errors disqualify a candidate rather than fabricating divergence. The shrunk arguments replace the recorded ones and `evidence.shrink.original_references` preserves the pre-shrink count.
- 2026-07-22: Probe execution parallelized across `--jobs` workers, driven by the operator's M4 speed directive ("if we spend hours waiting for results we have failed"). No finding ever depended on execution order — checkers key comparisons on probe identity (spelling set, full reference, symbol) — so workers claim owned work items off an atomic cursor and outcomes return via channel to their pre-assigned slots; the dump and checker input are byte-identical to sequential execution, and the summary counters are commutative additions. Thread-safety is structural, not assumed: `SearchToolsService` is the same service the MCP server shares across concurrent clients (`RwLock`/`Mutex` throughout), and the scoped-thread sharing only compiles because it is `Sync`. Empirical motivation: the sequential chisel run sustained ~191% CPU during the probe phase (one probe core plus background noise on an 8-core box) and took 85 min for 7,584 calls. `probe_parallelism` is an explicit parameter of `run_invariants_with_service`/`run_service_invariants`, deliberately *not* folded into `FuzzerConfig`: scheduling is not a semantic input and must not invalidate the run fingerprint.
- 2026-07-22: Probe dumps record per-call `elapsed_ms` (pure observability, unused by checkers) so campaign tuning sees the latency distribution per tool family rather than guessing where the tail is.
- 2026-07-22: Single-repo concurrency ceiling discovered and ticketed as issue #1054. With parallel probes live, a one-repo run saturates only ~2.4 cores steady-state: the workspace's single SQLite connection (`AnalyzerStore.conn`, `src/analyzer/store/mod.rs:183`) serializes every symbol lookup in every call, a per-call `session.write()` caps call rate, and per-call live-source revalidation stats/hashes touched files (IO-bound on the `/mnt/minasmorgul` network mount). Consequence for M4 operations: box saturation comes from **repo-level oversubscription**, not within-repo parallelism — `--repo-jobs 6-8` with modest `--jobs` each, letting independent repos' ~3-core peaks and serial phases interleave; memory is the binding constraint. The structural fix (connection pooling, dropping read-path write locks, cross-call liveness memoization) is product work tracked in #1054, not fuzzer-scope.
- 2026-07-22: `--rerun` scopes by *reproduction scope*, not always by symbol — the chisel acceptance rerun caught a real fuzzer bug this way. I3a (`summaries-listed-*`) violations record a `symbol` that is an element name from the summaries *response*, usually not a sampled workspace symbol, so `symbol_filter` emptied the probe set (`probe_calls=0`) and the signature went MISSING. (The same signature reproduced on ts only because that element happened to be a sampled symbol — the bug was probabilistic.) `FuzzerConfig` gained `path_filter` (substring over the declaring file path; `skip_serializing_if` keeps existing fingerprints and ledger configs byte-identical) and `rerun_configs` now targets `summaries-listed-*` signatures by file; the chisel signature reproduced on re-rerun (34 probe calls). `--path-filter` is exposed on the CLI for operator targeting of file-scoped families.
- 2026-07-22: M4 resolution workflow refined by operator directive: issue resolution is **language-at-a-time** — a language's signatures are triaged, filed, fixed, and pushed (each fix confirmed gone via rerun) before the next language's pass starts, so triage never accumulates across languages. Fixes must be **general**: a defect affecting several languages gets one root-cause fix, never per-language point fixes. Architecturally complex signatures (significant design work, or the right fix has a huge blast radius) get a comment on the issue explaining the problem and trade-offs, assignment to David (`DavidBakerEffendi`), and the campaign moves on — no open-ended architecture work inside the campaign. Enshrined in the Ledger section and the M4 milestone text.
- 2026-07-22: ts triage lesson on record-keeping: commit `36fa1da4` ("fuzzer I1(c): skip blocks in unsampled files") also carries the I3(b) truncated-set and module-target skips with their regression tests — a staging slip swept the untested-but-green I3(b) work into the I1(c) commit, so its message under-describes the diff. The I3(b) rationale is recorded in the Discoveries entry above ("The ts ×9 scan-resolved-symbol-absent-from-search decomposed…") to keep the ledger complete; tests were verified green before push (45 service tests) and the rerun confirmed the signature's collapse.


## Progress

- [x] (2026-07-21) Execution environment prepared: `~/Projects/brokkbench/clones` symlink recreated at `/mnt/minasmorgul/repo-clones`; `.agents/plans/mcp-property-fuzzer/` ledger directory created; `gh` authenticated as `jbellis` with push access to `BrokkAi/bifrost`.
- [x] (2026-07-21) M1 complete: `src/mcp_property_fuzzer/mod.rs` (I1 parts a/b/d as a pure index walk over a detached `I1Input`, dedup by failure signature with capped exemplars, SHA-256 deterministic sampling) and `src/bin/bifrost_mcp_property_fuzzer.rs` (FIRD-style CLI: `--clones-root/--commits-root/--repo×N/--language×N/--invariants/--out/--max-symbols/--seed/--jobs/--cache-mode/--strict/--force/--dry-run`, JSONL ledger with CompletionKey resume). 19 engine tests + 8 CLI tests green; clippy `--all-targets --all-features -D warnings` clean. Acceptance (`--cache-mode ephemeral`, full 5,637-symbol coverage) recorded in `.agents/plans/mcp-property-fuzzer/m1.jsonl`: TheHive @ d390a031 reports `(I1, scala, index, container-range-misses-member)` ×12 (all JobSrv members, mild-grade #1016) and `(I1, scala, index, declaration-truncated-at-parse-error)` ×18 (severe grade — `JobCtrl` itself plus 17 more controllers/services whose members vanish from the index silently); the healthy control `rtyley__bfg-repo-cleaner` reports zero violations with 342 containment + 434 name-token + 104 boundary checks audited as run.
- [ ] M2: in-process service wiring (both render modes), I2–I5, probe generator, per-invariant fixtures
- [x] (2026-07-21) M2 complete: `src/mcp_property_fuzzer/service_probes.rs` (probe generation from sampled `SymbolFacts`, execution in both render modes with single-mode scans, checkers I1(c)/I2/I3(a–c)/I4/I5, `--dump-probes` JSONL triage observability), `SearchToolsService::new_manual_{ephemeral,persisted}_for_project` + `analyzer_snapshot()`, CLI `--max-service-symbols/--max-scan-probes/--symbol-filter/--dump-probes` (default invariants now all five). 35 service + 20 engine + 7 CLI tests green; full `nlp,python` suite shows only the 7 pre-existing `relevance::tests` failures; clippy `--all-targets --all-features -D warnings` clean. Acceptance (`--cache-mode ephemeral`, ledgers `.agents/plans/mcp-property-fuzzer/m2.jsonl`): **bfg** 3,190 calls → 1 violation; **TheHive** 7,068 calls → 32 occurrences in 6 signatures; **vuejs/core** (I2, `--symbol-filter createHydrationFunctions`, #1018 shape) → silent. Triage drove eleven fuzzer-side fixes over three runs (I1(b) module skip, ambiguity classification, `$`-spellings, stage-aware drift, reference stoplist, aux-constructor census, I4 disputed-name + scope-precision, I3(b) include_tests, I1(c) blank-line vectors + noted-outline skip, I2 batch reference keying, class/companion partitioning); every surviving signature is genuine product behavior, carried into M4 as issue fodder:
  - `(I1, scala, index, container-range-misses-member)` ×11 and `(I1, scala, index, declaration-truncated-at-parse-error)` ×15 — the #1016 tree-sitter-scala truncation in both grades (JobSrv mild; JobCtrl & peers severe).
  - `(I2, …, more-specific-spelling-fails)` ×2 — `path#<terminal>` selectors fail on *nested* symbols (`AuditRenderer.scala#Job` → not_found while `path#<full fq>` resolves) on both the definitions and sources surfaces: the #1018 shape, alive on scala at the corpus pin.
  - `(I2, …, spelling-resolves-to-different-declaration)` ×3 + `spelling-status-drift` ×1 — bare multi-candidate names (`Job`, `client`, `post`) silently resolve to an arbitrary declaration instead of reporting ambiguity.
  - `(I3, …, summaries-listed-symbol-unresolvable)` ×1 (bfg) — `get_summaries`' display fq (`LFS.Pointer`) is ambiguous on the sources surface; the listed spelling does not round-trip.
- [x] (2026-07-22) M3 complete: corpus runner landed — `scripts/mcp-fuzzer-repo-rank.py` (task-count ranking via `tasks.py`, sft count → scan-record fallback for zero-sft languages), corpus mode (`--commits-root` + `--top N`, skip-broken-clones with warnings), FIRD-style worker pool (`--repo-jobs`, default 2) with serial ledger appends, parallel probe execution with slot-deterministic outcomes + per-call `elapsed_ms` in dumps, batch-violation shrinking (greedy minimization with re-execution oracle; `shrunk_violations` counter), and `--rerun LINE [--signature TEXT]` rebuilding per-violation configs by reproduction scope (symbol-scoped → `symbol_filter`; file-scoped `summaries-listed-*` → new `path_filter`; I5/symbol-less → base config). 38 service + 25 engine + 14 CLI tests green; clippy `--all-targets --all-features -D warnings` clean; full `nlp,python` suite shows only the pre-existing relevance/git-ranking failures (verified identical on clean HEAD via stash baseline). Acceptance (`--cache-mode ephemeral`, ledger `.agents/plans/mcp-property-fuzzer/m3.jsonl`): scala top-1 **chipsalliance__chisel** (7,584 calls → 9 signatures/233 occurrences incl. `summaries-listed-symbol-path-mismatch` ×85, I1(c) ×30, one #1016-grade truncation) and ts top-1 **code-yeongyu__oh-my-openagent** (9,356 calls → 7 signatures/821 occurrences incl. `summaries-listed-symbol-unresolvable` ×667) — all genuine-signature fodder for M4 triage. Resume/dedupe proven (immediate re-run skipped both, ledger unchanged). Rerun proof: ts 7/7 reproduced; chisel 8/9 then 9/9 after the rerun-scope fix above — chisel's records came from the *sequential debug* build and reproduced under the *parallel release* build, the determinism property on real data. Speed: ts leg 25 min parallel-release vs 111+ min unfinished sequential-debug; single-repo concurrency ceiling root-caused to `AnalyzerStore`'s single SQLite connection → issue #1054 with per-tool latency table; M4 will oversubscribe repos (`--repo-jobs 6-8`) to saturate the 24-core box. Shrink did not fire in production (`shrunk=0`: no `batch-outcome-differs-from-single` violations occurred on either repo).
- [ ] M4 tier 1: top-1 repo per language by task count, full pass, triage, issues filed, fixes pushed
- [ ] M4 tier 2: widen to top-5 repos per language after tier 1 adds no new signatures
- [ ] M4 tier 3: widen to top-10 repos per language after tier 2 adds no new signatures


## Outcomes & Retrospective

Nothing yet recorded. Filled in at major milestones and at completion, per `.agents/PLANS.md`.


---

Revision note (2026-07-21): Updated at execution start to capture operator decisions made in conversation: corrected corpus paths (`/mnt/minasmorgul/repo-clones`, `/mnt/optane` gone), moved the ledger into this repository under `.agents/plans/mcp-property-fuzzer/`, confirmed the GitHub-issue ticket workflow with assign-to-`jbellis` and `Fixes #N` commit conventions, switched per-language prioritization from code_loc to task count per `tasks.py`, and structured M4 into top-1/top-5/top-10 tiers. All changes are recorded in the Decision Log; no technical content of the invariants, milestones M1–M3, or query generation was altered.

Revision note (2026-07-21, M1 complete): Recorded M1 completion in Progress with acceptance evidence. Two scope refinements landed during implementation, both in the Decision Log: I1 gained part (d) (`declaration-truncated-at-parse-error`) because root-causing #1016 showed the named file's severe truncation is invisible to containment alone, and I1(b) learned the auxiliary-constructor naming-convention skip. Both are fuzzer-side refinements; no analyzer behavior was changed and the invariant's intent (range integrity as agents observe it) is unchanged.
