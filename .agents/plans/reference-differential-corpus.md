# Build and run a corpus reference differential

This ExecPlan is a living document. The sections `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` must be kept up to date as work proceeds. Maintain it in accordance with `.agents/PLANS.md`.

## Purpose / Big Picture

Bifrost currently learns about false-negative reference resolution after agents encounter them in production. This work adds a dedicated offline engine that audits real repositories by resolving a source reference forward to its declaration and then asking the inverse usage resolver for the same declaration. A reference resolved by the forward path but absent from the inverse path is a concrete disagreement with enough source and identity evidence to reproduce locally. The first campaign runs the engine against the largest available repository in each target corpus language, creates GitHub issues for genuine defects, fixes and closes those issues, and reports the result by language.

## Progress

- [x] (2026-07-12 03:00Z) Inspected repository instructions, current analyzer architecture, benchmark conventions, corpus metadata, clone availability, and the canonical target language registry.
- [x] (2026-07-12 03:00Z) Selected the deterministic N=1 repository for each of the eleven target corpus languages by recorded `code_loc`, restricted to available valid clones.
- [x] (2026-07-12 04:10Z) Implemented shared structured reference-candidate enumeration and the library-owned differential runner.
- [x] (2026-07-12 04:10Z) Implemented the dedicated corpus CLI with deterministic selection, commit-aware resumable JSONL reports, exact-site reruns, and bounded sampling.
- [x] (2026-07-12 04:50Z) Addressed review findings and validated the engine with all-language fixtures plus bounded real-repository preflights.
- [x] (2026-07-12 05:05Z) Committed engine checkpoint `6c056e91`; full `cargo test --features nlp,python`, all-target/all-feature clippy, formatting, and diff checks pass.
- [x] (2026-07-12 05:10Z) Pushed engine checkpoint and plan through `2c0ceff6` to `origin/master`.
- [x] (2026-07-12 11:20Z) Fixed #643's repeated SQLite scans and recursive Rust binding/re-export walks; the exact 512 KiB-stack validation completed in 554.7 seconds with 101,192 KiB peak RSS.
- [x] (2026-07-12 11:35Z) Pushed `4b3f6065`, corrected #643 with the definitive debugger diagnosis and measured validation, and closed it.
- [x] (2026-07-12 12:15Z) Completed the committed-HEAD Rust baseline and triaged all 961 disagreements into forward-focus/scope, inverse-member, and follow-on import/type clusters; filed #644 and #645.
- [x] (2026-07-12 13:05Z) Fixed and corpus-validated #644 and #645; targeted definition/usage suites and all-feature clippy pass.
- [x] (2026-07-12 15:10Z) Pushed `03646ad1`, closed #644/#645, and reran the complete Rust N=1 repository from the fixing HEAD. Missing sites fell from 961 to 628; the record is commit-pinned but reports dirty because unrelated untracked agent/cache artifacts exist in both worktrees.
- [x] (2026-07-12 16:05Z) Triaged all 628 post-fix Rust disagreements. Filed #646/#647 for the dominant cross-file type and `self`/`Self` inverse gaps, and #648/#649/#650 for independently reproduced forward scoped-focus, namespace/prelude, and let-condition defects.
- [x] (2026-07-12 16:30Z) Implemented and behavior-tested #646/#647: nested workspace file modules now retain structured module identity, and Rust class usage emits exact editor-only owner-alias hits with CodeUnit identity checks.
- [x] (2026-07-12 17:25Z) Pushed and closed #646/#647, then fixed, pushed, and closed #648 through the `get_definitions_by_location` symbols surface; no LSP-only acceptance work was retained.
- [x] (2026-07-12 18:20Z) Implemented and symbols-tested #649 with AST namespace roles, tri-state visible imports, exact lexical-module fallback, and a cached Cargo path-dependency route index shared by forward and inverse Rust resolution.
- [x] (2026-07-12 19:05Z) Pushed and closed #649, then implemented and symbols-tested #650 with ordered `let_condition`/let-chain visibility and fully iterative lexical/pattern traversal.
- [x] (2026-07-12 19:45Z) Pushed and closed #650 and completed the next Rust N=1 rerun at `edd7beaa`: missing classifications fell from 628 to 268 while consistent/editor-only coverage rose from 696 to 1,758.
- [x] (2026-07-12 20:00Z) Found and symbols-tested Scala blocker #651 before the cold Scala corpus run: inverse usage now preserves every overload CodeUnit and enforces the usage cap across their merged hit set.
- [x] (2026-07-12 20:35Z) Pushed and closed #651, then removed Rust/C++ cross-language scoped-owner leakage in #654 with mixed-language symbols regressions.
- [x] (2026-07-12 21:05Z) Implemented and symbols-tested #652: named Rust module segments in runtime/type paths now resolve through the structured reference context, while import-only sites and unrelated same-name modules remain excluded.
- [x] (2026-07-12 21:30Z) Implemented and symbols-tested #653 by generalizing the exact scoped-prefix walk from modules to class/type owners; all four corpus shapes and same-name fully qualified negatives are covered through `scan_usages_by_reference`.
- [x] (2026-07-12 22:15Z) Revalidated all six remaining Rust defect boundaries with exact-site runs on current `master`, excluded the expected import-only omission from the production surface, and filed #655-#660 for the surviving public symbols behaviors.
- [x] (2026-07-13 02:20Z) Completed Scala N=1 on `JetBrains__intellij-scala` at `dd63058d`: 1,000/18,936 eligible files, 10,000 sites, 1,000 queried target groups, and 473 missing classifications in 1,761.8 seconds; production-symbol triage is in progress.
- [x] (2026-07-13 02:35Z) Implemented and symbols-tested #656 with a shared position-aware Rust lexical-scope index; inverse scanning no longer suppresses names file-wide and the touched AST walk is iterative.
- [x] (2026-07-13 02:45Z) Implemented and symbols-tested #657: focused nonterminal Rust path segments inside macro token trees now retain owner identity instead of falling through to the terminal associated item.
- [x] (2026-07-13 03:05Z) Triaged all 473 Scala missing classifications into 25 wrong forward identities, 219 genuine inverse omissions, and 229 non-defects/artifacts; exact current-code probes confirmed four production boundaries and #661-#664 were filed.
- [x] (2026-07-13 03:35Z) Implemented and symbols-tested #663 by extending the existing Scala hierarchy-family usage proof from traits to class methods while preserving overload buckets, conflicts, caps, and exact unrelated-base negatives.
- [x] (2026-07-13 03:45Z) Implemented and symbols-tested #655 with AST-derived full focused `use` paths and one exact Rust path-to-FQN resolver shared by forward definition lookup and inverse runtime call proof; import-only sites remain excluded.
- [x] (2026-07-13 03:55Z) Implemented and symbols-tested #664: Scala forward lookup now gives lexical bindings precedence and validates term/type namespace visibility before accepting indexed identities.
- [x] (2026-07-13 04:15Z) Implemented and symbols-tested #660 with shared Rust struct-field AST roles, exact literal/pattern owner proof, lexical `Self`, declaration no-definition behavior, and matching differential sampler exclusion.
- [x] (2026-07-13 04:25Z) Implemented and symbols-tested #662 with AST-level Scala companion-`apply` and infix lowering, exact receiver/owner proof, symbolic-method candidate routing, caps, and unrelated-owner negatives.
- [x] (2026-07-13 04:40Z) Implemented and symbols-tested #659 by reusing forward Rust expression-type resolution on exact inverse receiver nodes, with tri-state owner proof across parameters, typed locals, nested fields, and returned values.
- [x] (2026-07-13 04:55Z) Implemented and symbols-tested #661 by extending Scala hierarchy proof to class fields and resolving named-argument labels through their structured invocation owners; the remaining selection/stable/type roles are pinned with exact-owner regressions.
- [x] (2026-07-13 05:05Z) Implemented and symbols-tested #658 with one exact associated-item resolver across normal and macro token-tree paths, preserving call/value roles, requested identity, import exclusion, and multi-segment owners.
- [x] (2026-07-13 05:20Z) Reopened #659 after its exact corpus site remained missing, then added structured concrete-receiver-to-trait-impl dispatch proof while leaving generic and trait-object receivers unproven.
- [x] (2026-07-13 06:20Z) Reopened #661-#663 after synthetic symbols regressions passed but their exact Scala corpus sites remained missing; generalized callable matching to persisted required/total/repeated arity and propagated structured qualified-call return types into local receiver bindings. Exact public-symbol probes now pass for inherited default arguments, companion `apply` defaults, and call-result member dispatch.
- [x] (2026-07-13 07:00Z) Completed the JavaScript N=1 run on `nodejs__node`: 2,247 forward-resolved sites, 1,720 consistent, 105 raw missing, 8 editor-only, and 90 unproven in 134.2s. Exact public-symbol probes reduced the missing set to five root boundaries filed as #665-#669.
- [x] (2026-07-13 07:15Z) Completed the PHP N=1 run on `moodle__moodle`: 1,888 forward-resolved sites, 1,331 consistent, and 91 raw missing in 311.3s. Public probes partitioned these into three product boundaries (#672-#674) plus nine false missing test sites caused by differential filter drift (#671).
- [x] (2026-07-13 08:15Z) Fixed and exact-validated all five JavaScript boundaries #665-#669: lexical/role precedence, namespace aliases, exported executable bodies and bare calls, CommonJS value roles, and modeled-property provenance. Production probes are now consistent or correctly inconclusive under the local-binding contract, with direct public inverse scans proving the latter sites.
- [x] (2026-07-13 08:25Z) Fixed and exact-validated all PHP boundaries #671-#674: aligned test filtering, prevented cross-language bare lookup, made ambiguous selectors round-trip, and separated workspace hierarchy proof from output path scope.
- [x] (2026-07-13 08:35Z) Profiled the long Go and Python 1,000-target runs after cache construction: Go repeatedly parses scoped graphs; Python repeatedly rebuilds path/module routing. Filed #675 for bounded structured bulk-query reuse, explicitly rejecting SQLite materialization and unbounded syntax-tree caches.
- [x] (2026-07-13 09:00Z) Implemented the measured Python half of #675 by removing per-target transitive import-closure parsing; the immutable `PythonUsageIndex` continues to own routing while exact candidate-plus-target graphs retain public scope and proof behavior. A four-file reachable fixture now parses only the two required files.
- [x] (2026-07-13 09:35Z) Fixed #676 after default and 64 MiB stacks both crashed in TypeScript receiver inference; iterative AST/pattern walks, shared RAII cycle state, bounded semantic proof, and fail-closed qualified calls let the exact default-stack Kibana run complete in 83.9s.
- [x] (2026-07-13 09:50Z) Completed and triaged the TypeScript N=1 run on `elastic__kibana`: 1,017 consistent, 31 missing, 20 editor-only, 82 unproven, and 8,850 inconclusive. Ten exact probes confirmed eight public-symbol boundaries filed as #678-#685.
- [x] (2026-07-13 10:00Z) Stopped the Go run after three hours when GDB proved it had never reached the differential: persisted symbols startup was eagerly rebuilding `DefinitionLookupIndex` through `all_declarations` and reparsing Go content qualifiers. Corrected #675 to Python per-query reuse and filed the distinct warm-start blocker as #677.
- [x] (2026-07-12 20:38Z) Resumed after release `0.7.6`, migrated the Azure PowerShell cache with the required external write access, and found the remaining C# forward amplification: extension visibility performed a generic SQLite parent-definition query for every exact identifier candidate. Replaced it with the persisted member namespace and per-file structural parent map; the fixed warm 1,000-site/100-target smoke completed in 232.1 seconds with a valid record.
- [x] (2026-07-12 20:50Z) Pushed C# parent lookup fix `b842208a` and completed the full Azure PowerShell N=1 record in 425.0 seconds: 3,269 forward-resolved sites, 598 consistent, 40 unproven, 1,339 missing, and 8,023 inconclusive. Initial triage shows 1,304 class/type targets; exact production rerun confirms the dominant fully qualified partial/interface type boundary, but no correctness issue is filed until a reduced authoritative-scope regression pins the resolver gap.
- [x] (2026-07-12 21:11Z) Pinned the first C# correctness boundary with a three-file authoritative-scope regression, filed #698, recognized tree-sitter C#'s `returns` field, and emitted qualified-type hits on their containing AST node. The exact Azure bytes `5677..5693` changed from missing to consistent in 146.7 seconds; focused tests, affected all-feature clippy, and the complete `nlp,python` test gate pass.
- [x] (2026-07-12 21:18Z) Pushed `7ccce3dd` to `master` and closed #698 with the exact Azure evidence; the next C# step is a complete fixing-HEAD rerun before triaging residual sites.
- [x] (2026-07-12 21:31Z) Completed the post-#698 full C# rerun at `3b2e2665` in 486.4 seconds. Consistent sites increased from 598 to 1,717 and missing sites fell from 1,339 to 220; 73 residuals form the next explicit-interface owner declaration boundary.
- [x] (2026-07-12 21:44Z) Delegated and independently reviewed the dominant C# residual, filed #701, recognized `explicit_interface_specifier` as a structured type role, passed the full validation gate, and changed exact `ApiKey.cs` bytes `2898..2913` from missing to consistent.
- [x] (2026-07-12 21:50Z) Pushed `2b617770` and closed #701 with exact and full-suite evidence.
- [x] (2026-07-12 22:12Z) Rejected the first full post-#701 record after it silently lost audited candidate files between forward and inverse phases; filed #703 and made the engine retain the original audited scope or fail explicitly.
- [x] (2026-07-12 22:35Z) Validated #703 with a complete post-#701 C# rerun at `c8524f86`: all 1,000 configured groups were queried, candidate-loss notes fell to zero, and the trustworthy C# missing set fell from 220 to 144.
- [x] (2026-07-12 22:38Z) Pushed `85aa48cd` and closed #703 with stable-scope production evidence.
- [x] (2026-07-12 23:17Z) Delegated and independently reviewed the next 39-site C# cluster, filed #704, treated generic argument lists as their own structured type boundary, passed focused/negative coverage, and changed the exact BillingBenefits site from missing to consistent.
- [x] (2026-07-12 23:24Z) Passed the complete `cargo test --features nlp,python` validation gate for #704; affected all-feature library and C# test clippy targets also pass.
- [x] (2026-07-12 23:27Z) Pushed `cf2743de` and closed #704.
- [x] (2026-07-12 23:49Z) Completed a valid post-#704 full C# rerun: all 1,000 target groups queried with zero candidate-scope-loss notes, and missing sites fell from 144 to 103.
- [x] (2026-07-13 00:10Z) Filed #705 from a reduced failing inventory-observer regression and made exact-site selection validate and point-check one project file without calling the full analyzed-file inventory.
- [x] (2026-07-13 00:26Z) Production-validated #705 on the same BillingBenefits reference: consistent semantics, one truthful eligible/audited file, and runtime reduced from 1,134.4 to 865.2 seconds.
- [x] (2026-07-13 00:31Z) Pushed and closed #705 after safely merging concurrent `master` work and restoring the unrelated Rust test edits.
- [x] (2026-07-13 00:37Z) Reproduced and filed #706 for the 34-site `using static` cluster; shared its structured AST role across inverse scanning and persisted candidate routing and bumped the C# analyzer epoch.
- [x] (2026-07-13 00:54Z) Exact production validation changed MachineLearningServices `PsHelpers` bytes `498..508` from missing to consistent after the expected C# persisted-index rebuild.
- [x] (2026-07-13 00:58Z) Passed the complete `cargo test --features nlp,python` validation gate for #706.
- [x] (2026-07-13 01:01Z) Pushed `2a365fce` and closed #706.
- [x] (2026-07-13 01:19Z) Completed the valid post-#706 full C# run with all 1,000 target groups, zero scope-loss notes, and 69 missing sites, exactly 34 fewer than before the static-import fix.
- [x] (2026-07-13 01:22Z) Paused post-#706 triage at the user's request to cut release `0.8.0`; the 69-site C# record is the explicit resumption boundary.
- [x] (2026-07-13 02:01Z) Resumed the campaign after `v0.8.0` published successfully and delegated the three cast-type families across oldskool and a child read-only reducer.
- [x] (2026-07-13 02:06Z) Filed #707 from a primary failing regression and shared the grammar-specific `as_expression.right` type role across inverse scanning and persisted candidate routing with a C# epoch bump.
- [x] (2026-07-13 02:09Z) Independently confirmed all 33 cast-family class misses share #707: 16 `IAsyncCommandRuntimeExtensions`, 10 `EventData`, and seven `JsonArray`.
- [x] (2026-07-13 02:31Z) Exact Automanage production validation changed the former cast-type miss to consistent after the persisted C# rebuild.
- [x] (2026-07-13 02:34Z) Passed the complete `cargo test --features nlp,python` gate for #707; focused C# and affected all-feature clippy validation also pass.
- [x] (2026-07-13 03:00Z) Pushed `062ae98e`, closed #707, and completed the valid post-#707 C# rerun: all 1,000 targets were queried with zero scope-loss notes, 1,899 consistent, 40 unproven, 38 missing, and 8,023 inconclusive sites in 1,265.0 seconds.
- [x] (2026-07-13 03:20Z) Delegated the 38-site residual audit. It contains 33 functions, three attribute-context `Parameter` false forwards, and two fields; the dominant ten-site `RestException<T>` cluster is a same-file generic/nongeneric declaration identity collision.
- [x] (2026-07-13 03:45Z) Added failing forward and inverse inline regressions for same-named generic/nongeneric constructors, then implemented metadata-style arity identity for every declared C# type segment, same-file partial-body merging, structured generic-reference arity, exact-before-normalized lookup, source-facing display, and a C# epoch bump.
- [x] (2026-07-13 04:35Z) Completed three delegated read-only audits and fixed every generic-root finding: receiver inference preserves arity, grouped overloads all scan, exact candidate routing is symmetric, and visible structured lookup never normalizes an arity miss. The complete all-feature suite and affected all-feature clippy targets pass; production exact validation is blocked only on explicit outside-sandbox approval.
- [x] (2026-07-13 11:35Z) Filed #726 and completed the final exact CDN production rerun in 341.0 seconds. The former false-forward now resolves to `RestException`1.RestException(System.Net.Http.HttpResponseMessage,T)` and has a proven covering inverse hit; zero exact findings remain.
- [x] (2026-07-13 12:10Z) Pushed and closed #726 after merging concurrent `master`; the complete post-fix C# run at `b75f6b53` finished in 621.0 seconds with 1,902 consistent, 40 unproven, 8,019 inconclusive, and 39 missing sites. All ten generic-constructor misses disappeared; the residual partition is 23 functions, twelve attribute names, and four fields.
- [x] (2026-07-13 13:40Z) Verified #727 was assigned only to `jbellis`, then reduced the twelve-site attribute boundary with delegated tests and review. C# attribute lookup now models exact and `Attribute`-suffixed AST identities independently, rejects indexed non-attribute ancestry, preserves partial declarations, reports ambiguous spellings, shares the role with forward/type lookup and both inverse graph paths, persists both routing identities, and handles alias/global qualification without per-target global lookup amplification. Exact Azure reruns now classify the external `[Parameter]` spelling as an honest unresolved `ParameterAttribute` import boundary and prove the fully qualified `PSArgumentCompleterAttribute` inverse hit. The complete `cargo test --features nlp,python` gate passes outside the filesystem sandbox; focused all-feature clippy passes, while all-target clippy remains blocked only by the preserved pre-existing `tests/rust_analyzer_goto_definition.rs:65` needless borrow.
- [x] (2026-07-13 14:05Z) Pushed `5a75ab33`, closed #727, and completed the post-fix Azure PowerShell run from merged/pushed `1a14bc92` in 517.8 seconds: 1,961 consistent, 36 unproven, 23 missing, and 7,980 inconclusive sites. All twelve attribute misses disappeared. The remaining actionable set is nineteen functions and four fields; delegated read-only triage partitions it into callable applicability, local-function method groups, inherited members, cast/local receivers, generic member names, partial-type initializer identity, and one deliberate `nameof` surface mismatch.
- [x] (2026-07-13 16:05Z) Filed #732 after verifying assignment only to `jbellis`, reduced both callable-applicability directions, and completed delegated read-only review. C# now persists required/total/repeated arity, applies intervals in forward/inverse/constructor/extension/return-type consumers, keeps overlapping intervals conservative, lets applicable extensions beat inapplicable direct members, persists local and global static-using owners, and filters only structurally proven incompatible extension receivers. Focused validation passes 55 get-definition, 75 forward graph, and 23 inverted graph tests; the complete `cargo test --features nlp,python` gate and affected all-feature clippy targets pass, while all-target clippy remains blocked only by the preserved pre-existing `tests/rust_analyzer_goto_definition.rs:65` needless borrow. The epoch-rebuilt Azure `eventListener.Signal` exact run is consistent in 618.6 seconds with seven applicable extension overloads and an exact inverse hit; the optional `AddMultipleTypeNameIntoPSObject` exact run changes the former miss to a retained exact-range unproven hit in 332.6 seconds.
- [x] (2026-07-13 16:18Z) Reopened #732 after its first full rerun exposed extension-visibility hydration amplification. Static-import candidates now come from the persisted exact owner/member index, namespace candidates are filtered before any parent/signature hydration, and a 256-file same-name regression permits at most two relevant-file hydrations. The complete Azure sample now terminates with 4,548 forward-resolved sites, 1,934 consistent, 45 unproven, 16 missing, and 8,005 inconclusive classifications; its 18,410,868 KiB peak stabilized instead of entering the repeated candidate-file hydration loop. Read-only triage found one #732 false forward among the sixteen: a fully qualified property receiver fell back to 25 arity-inapplicable extensions. Iterative declared-member typing now preserves value shadowing and nearest hidden-member semantics, and the exact StackHCIVM site resolves only the nine-argument `Module.Signal` with an exact retained inverse hit. The complete `cargo test --features nlp,python` gate passes outside the filesystem sandbox; affected all-feature clippy passes, while all-target clippy remains blocked only by the preserved pre-existing `tests/rust_analyzer_goto_definition.rs:65` needless borrow.
- [x] (2026-07-13 16:45Z) Pushed #732 as `545ce984`, merged concurrent `master` into pushed head `dde2fc2d`, and verified its CI matrix green. The fixing-head Azure N=1 run completed with 4,737 forward-resolved sites, 1,887 consistent, 47 unproven, 14 missing, and 8,052 inconclusive classifications at 18,372,500 KiB peak RSS. The StackHCIVM false forward is gone; the sampled Elastic method-group boundary is explicitly target-truncated rather than misclassified, so the residual actionable set is fourteen sites.
- [x] (2026-07-13 17:20Z) Filed #737 only after a reduced authoritative-scope regression proved that a uniquely declared unqualified method-group argument resolves forward but disappears from both inverse paths. Added a shared AST classifier, lexical shadowing checks, nearest-owner and partial-type hierarchy resolution, conservative overload handling, and cached inverted-graph owner/name lookup. Delegated read-only review found the final implementation clean. All 78 targeted C# graph tests, 24 whole-workspace graph tests, 60 C# definition tests, formatting, and affected all-feature clippy pass; all-target clippy remains blocked only by the preserved unrelated Rust needless borrow. The exact Azure `onDefault` site at bytes `60843..60852` is now consistent with a proven exact-range inverse hit in `/tmp/csharp-exact-737-ondefault-fixed.jsonl`.
- [x] (2026-07-13 17:27Z) Pushed #737 as `3ca1a8fe`, closed it with the exact production evidence, and verified the complete Rust/Python CI matrix green on Linux, Windows, macOS, Android, and the configured cross targets.
- [x] (2026-07-13 18:12Z) Filed #738 only after the reduced three-level inheritance test resolved `Base.Report` forward and returned zero authoritative inverse hits from the derived consumer. Replaced exact-owner matching with a shared iterative nearest-declaring-level walk across partial type parts, preserved hiding and callable applicability, kept exact generic identity ahead of normalized fallback, rejected lexical shadows, and cached both targeted and whole-workspace lookups. Delegated review found and verified fixes for shadowed-call unproven leakage, generic inverted identity, and repeated hierarchy work. All 79 targeted C# graph tests, 25 whole-workspace graph tests, 60 C# definition tests, affected all-feature clippy, and the complete `cargo test --features nlp,python` gate pass; all-target clippy remains blocked only by the preserved unrelated Rust needless borrow. The exact Azure `this.PrintSdkExceptionDetail` site at bytes `5184..5207` is consistent with a proven exact-range inverse hit in `/tmp/csharp-exact-738-inherited-final.jsonl`.
- [x] (2026-07-14) Pushed #738 as `6d89ff5c`, closed it with exact Azure evidence, and completed the post-#738 Azure PowerShell N=1 run using the binary pinned to pushed HEAD. It reports 4,737 forward-resolved sites, 1,903 consistent, 40 unproven, 6 missing, and 8,051 inconclusive classifications. The JSONL truthfully records `bifrost_dirty:true` because #739 worktree edits began while the run was in flight; this is a pushed-commit binary result, not a clean-worktree record.
- [x] (2026-07-14) Filed #739 only after a reduced inline regression proved that the fully qualified explicit generic call `PsHelpers.RunScript<T>(...)` resolves forward while an authoritative inverse scan of only the consumer returns zero hits. Implemented metadata-arity method identity, explicit generic-arity filtering before value applicability and hiding, AST-carried actual type arguments, persisted bare method-type-parameter return facts, and one shared lexical extension-visibility model across forward, targeted inverse, and whole-workspace graph resolution. The shared signature serialization change bumps the global store epoch. Delegated read-only review found and verified fixes for instance-member precedence, ambiguous-receiver gating, lexical sibling isolation, cache keys, inherited declaring-owner return resolution, wrapped-return negatives, and persistence after reopening. All 84 targeted C# usage tests, 27 whole-workspace graph tests, 60 C# definition tests, formatting, diff checks, affected all-feature clippy, and the complete `cargo test --features nlp,python` gate pass; all-target clippy remains blocked only by the preserved unrelated Rust needless borrow. The epoch-rebuilt Azure exact site at bytes `7144..7153` is consistent with the exact generic signature `` `1(CommandInvocationIntrinsics, string)`` and a proven exact-range inverse hit in `/tmp/csharp-exact-739-generic-final.jsonl`.
- [x] (2026-07-14) Pushed #739 as `9ece3b65`, verified it closed while assigned only to `jbellis`, and reproduced the exact generic-method proof on pushed HEAD in `/tmp/csharp-exact-739-generic-pushed.jsonl`. The complete post-#739 C# N=1 run in `/tmp/csharp-n1-739-pushed.jsonl` queried all 1,000 configured targets and reports 4,738 forward-resolved sites, 1,907 consistent, 37 unproven, 5 missing, and 8,051 inconclusive classifications. `RunScript<T>` accounts for the one-site improvement from the post-#738 six-miss boundary. The remaining five sites partition into three suspicious `ToString` forwards, one `nameof` field, and one object-initializer field; read-only triage is delegated before any new issue is filed.
- [x] (2026-07-14) Delegated the five-site post-#739 triage across Oldskool and two read-only agents. The `nameof(field)` site is an intentional editor-navigation/runtime-usage surface mismatch and therefore a differential-engine false positive. Three null-conditional `ToString` sites are one false-forward boundary: an explicit `(object)` cast should resolve to external `System.Object.ToString`, but forward lookup mistakes the `member_binding_expression` for an unqualified containing-class call. The remaining object-initializer field is a genuine partial-type inverse defect.
- [x] (2026-07-14) Filed #740 only after a two-file inline regression proved that adding another physical part of the initializer type drops the field-label hit while preserving `this.field`. Narrowed logical partial-type identity to the object creation's actual type AST, retained physical identity on the pre-binding value-receiver fast path, and added a PascalCase parameter-shadow negative after delegated review caught the unsafe broader change. All 86 targeted C# usage tests, affected all-feature clippy, and the complete `cargo test --features nlp,python` gate pass; all-target clippy remains blocked only by the preserved unrelated Rust needless borrow. The exact Mdp `pipeline` label at bytes `3733..3741` is consistent with a proven exact-range inverse hit in `/tmp/csharp-exact-740-partial-initializer-final.jsonl`.
- [x] (2026-07-14) Filed #741 only after an inline partial-model regression proved that a null-conditional `member_binding_expression` on an explicit `(object)` receiver fell through to unqualified lookup and selected the containing model's `ToString`. Delegated synchronous implementation to Oldskool, then review caught and corrected a trivially scoped authoritative negative before landing. The final structured fix shares direct conditional binding extraction, preserves cast and `as` receiver identities, types chained conditional properties and method returns, carries value and generic arity, and keeps compatible extension matching exact even for builtin aliases. Focused definition/targeted/inverted tests, affected all-feature clippy, formatting, diff checks, and the complete `cargo test --features nlp,python` gate pass. The rebuilt Azure exact site at bytes `7147..7155` now returns `no_definition` for external `System.Object.ToString`, with zero actionable findings in `/tmp/csharp-exact-741-conditional-final.jsonl`.
- [x] (2026-07-14) Delegated the remaining C# `nameof(member)` corpus false-positive implementation to Oldskool. The differential engine now identifies the nearest structured `nameof` invocation, classifies resolved field/property/function targets as `editor_only`, and omits them from runtime inverse grouping while ordinary reads still require a proven inverse hit. Review caught a direct-`argument_list` production AST gap; the corrected nested-call regression proves only the inner `nameof(member)` is excluded and the sibling outer-call argument remains consistent. All nine engine tests, all-feature library clippy, formatting, and diff checks pass. The rebuilt Azure exact site at bytes `15226..15254` is `editor_only` with zero actionable findings in `/tmp/csharp-exact-nameof-editor-only-final.jsonl`.
- [x] (2026-07-14) Completed the pushed-head Azure PowerShell C# N=1 closure run in `/tmp/csharp-n1-741-nameof-pushed.jsonl`. It audited 1,000 of 126,829 eligible files, sampled 10,000 sites, queried 1,000 of 2,252 target groups, and reports 1,858 consistent, 3 editor-only, 30 unproven, 0 missing, and 8,109 inconclusive classifications. One 50,000-candidate file and 2,271 target-truncated sites remain explicitly inconclusive. C# now has zero actionable findings; proceed to the C corpus.
- [x] (2026-07-14) Fixed and closed incidental Windows CI issue #743 before continuing the corpus. Oldskool synchronously implemented kill-on-close Job Object ownership with suspended assign-before-resume startup and complete job/pipe-worker draining; independent review found no remaining lifecycle defect. The first pushed head exposed only a Windows-only `needless_return` lint, which Oldskool corrected in `d4923a53`. Merged head `1f569e75` passed both Windows clippy targets, Windows ARM64 completed green, and Windows x86_64 passed the exact `bifrost_lsp_server_runtime_configuration_rebuild_releases_windows_children_and_handles` regression plus its full test command in CI run `29337853743`.
- [x] (2026-07-14) Completed the C N=1 run on `RMerl__asuswrt-merlin.ng` in `/tmp/c-n1-current.jsonl`, pinned to Bifrost `4fe9cbeb` and corpus head `e7c13912`. It audited 1,000 of 164,927 eligible files, sampled 10,000 sites, queried 1,000 of 1,332 target groups, and reports 661 consistent, 264 unproven, 126 missing, and 8,949 inconclusive classifications in 8,158.1 seconds. There were zero file errors and zero candidate-limit files; 370 target-truncated sites remain explicitly inconclusive. Highest sampled RSS was 24,051,264 KiB because the report schema does not record an exact peak. Read-only triage is partitioning the 111 field, ten class, and five function misses before any issue is filed.
- [x] (2026-07-14) Completed delegated read-only triage of all 126 C misses. The refined partition is 114 genuine inverse omissions, eleven forward false targets, and one persisted/full-corpus differential artifact that passes an exact authoritative query. Genuine roots are 35 global receiver/base references, nine intermediate member receivers, 42 nested terminal members, fourteen one-hop typedef-owned members, eight canonical typedef type references, five variadic calls, and one macro-recovered type node. False forwards are ten designated-initializer pseudo-fields plus one malformed `__iomem` field declaration. A follow-up audit corrected the original ten/41 intermediate/nested split without changing the total; exact pushed-head probes already prove representative net-snmp and igbvf intermediate chains consistent under #762-#764.
- [x] (2026-07-14) Filed #760 only after an authoritative inline regression proved that forward lookup resolves global `bs` in `bs.cluster_size` while inverse target routing returns an empty hit set. Delegated synchronous implementation to Oldskool, then review corrected local-class member shadowing, removed a non-protective implementation-shaped stress test, and added a real direct-arrow positive. The final AST receiver-edge walk handles dot, arrow, and nested chains, rejects member descendants and lexical shadows, and filters persisted global/member identity without parent lookup amplification. After merging concurrent reference-traversal changes, all 53 C++ usage and 17 C++ inverted graph tests pass. Pushed head `bfffb6e5` exact-validates RMerl bytes `21623..21625` as consistent with an exact inverse range and zero actionable findings in `/tmp/c-exact-760-bs-pushed.jsonl`; #760 is closed.
- [x] (2026-07-14) Filed #762 after a reduced consumer-only authoritative regression for `Leaf.value` returned zero expected mixed dot/arrow nested-chain hits while preserving wrong-owner and local-shadow negatives. Synchronous Oldskool implementation advances exact structural owners through nested dot/arrow, pointer, parenthesized, subscript, `this`, implicit-member, and qualified-global chains using name-indexed lookups and cached tree-sitter declaration facts. Root review rejected all-visible-unit amplification, arbitrary short-type selection, source-text field routing, unresolved-global type fallback, and missing structured roots before acceptance. The first production exact rerun exposed a second reduced boundary: persisted inline tagged typedef aliases are Class units distinct from their tag owners, and alias hops had switched away from the consumer visibility root. Cached structured alias decoding plus unique, cycle-safe owner canonicalization fixes that boundary. Commit `7f72d0a2` is pushed; the 58 C++ usage, 17 inverted graph, and 56 C++ get-definition tests pass, as does affected all-feature clippy. Pushed-head `/tmp/c-exact-762-omcast-pushed.jsonl` classifies RMerl `new_vals->stats.omcast` bytes `17343..17349` as consistent with the identical inverse range and zero actionable findings; #762 is closed.
- [x] (2026-07-14) Filed #763 only after a consumer-only authoritative regression proved that forward lookup resolves `OwnerAlias *p; p->value` to `Owner.value` while inverse routing returns no proven hit. The issue is assigned only to `jbellis`. Synchronous Oldskool implementation canonicalizes the complete direct receiver-unit set through the existing structured, unique, cycle-safe alias path before member advance or target comparison, eliminating the nested-chain-only seam without a typedef fallback. Commit `79c09476` is pushed; the 59 C++ usage, 17 inverted graph, and 56 C++ get-definition tests pass, as does affected all-feature clippy. Pushed-head `/tmp/c-exact-763-apps-pushed.jsonl` classifies RMerl `new_apps_info->new_file_name` bytes `1326..1339` as consistent with the identical inverse range and zero actionable findings; #763 is closed.
- [x] (2026-07-14) Filed #764 only after a consumer-only authoritative regression proved that forward lookup canonicalizes both `CanonicalAlias` and transitive array alias `CanonicalArray` to `Canonical`, while inverse type routing proves only the direct alias. The issue is assigned only to `jbellis`. Synchronous Oldskool implementation makes `resolves_to_type` canonicalize every persisted candidate through the cached structured, unique, cycle-safe alias path while retaining the consumer visibility root; unresolved, cyclic, or multiple distinct canonical owners remain unproven, and parser fallback cannot override persisted non-target evidence. Commit `e5388d80` is pushed; the 61 C++ usage, 17 inverted graph, and 56 C++ get-definition tests pass, as does affected all-feature clippy. Pushed-head `/tmp/c-exact-764-mpz-pushed.jsonl` classifies RMerl `mpz_t` bytes `41394..41399` as consistent with the identical inverse range and zero actionable findings; #764 is closed.
- [x] (2026-07-14) Filed #765 only after a consumer-only authoritative regression proved that forward lookup resolves valid C variadic calls while inverse routing drops every call above the fixed minimum because persisted metadata omits anonymous `...`. The issue is assigned only to `jbellis`. Synchronous Oldskool implementation structurally recognizes anonymous and named variadics plus defaults, persists required/total/repeated `CallableArity`, and replaces inverse and return-resolution exact-arity gates with `accepts`; the invalid below-minimum call remains rejected. Commit `8c1b0d45` is pushed; the 38 C++ declaration, 62 C++ usage, 17 inverted graph, and 56 C++ get-definition tests pass, as does affected all-feature clippy. The existing whole-RMerl `.brokk` cache predates the new callable metadata and still reproduces the old miss; a fresh scoped Git snapshot of the unchanged `lldpd-1.0.11` production subtree records signature `(lldpctl_conn_t *, atom_t, ...)`, and pushed-head `/tmp/c-exact-765-lldp-pushed.jsonl` classifies `_lldpctl_new_atom` bytes `5382..5399` as consistent with the identical inverse range and zero actionable findings. #765 is closed; the whole-clone cache must be refreshed before the final C rerun.
- [x] (2026-07-14) Filed #766 only after a consumer-only authoritative regression proved that forward lookup accepts tree-sitter's exact missing-`::` recovery for a macro-decorated function return type while inverse routing drops its `namespace_identifier`. The issue was assigned only to `jbellis`. Synchronous Oldskool implementation adds one shared structural predicate restricted to the direct function-definition qualified declarator, missing `::`, and pointer/reference-wrapped function-declarator shape; targeted and whole-workspace inverse walkers use it without broadly admitting namespace identifiers. The 63 C++ usage, 17 inverted graph, and 56 C++ get-definition tests pass, as does affected all-feature clippy. Commit `d04376b6` was merged with concurrent master and pushed as `cd856345`; pushed-head `/tmp/c-exact-766-routerstatus-pushed.jsonl` classifies RMerl `routerstatus_t` bytes `12384..12398` as consistent with the identical inverse range and zero actionable findings. #766 is closed.
- [x] (2026-07-14) Filed #767 only after a faithful bounded PowerPC initializer regression reproduced the ten-site false-forward cluster: tree-sitter's later preprocessor recovery persisted `.cpu_features` as a bare global, so earlier valid designators resolved to that pseudo-declaration instead of `cpu_spec.cpu_features`. The issue was assigned only to `jbellis`. Synchronous Oldskool implementation suppresses only the exact adjacent ERROR-dot/init-declarator recovery before persistence, separates structured designator recognition from owner proof, and shares aggregate-owner resolution across forward lookup, targeted inverse, and whole-workspace inverted scanning. Root review required unresolved/nested designators to stop global fallthrough and remain explicitly unproven, and required recovered-only declarations to bypass fallback persistence. The 38 C++ analyzer, 56 C++ definition, 65 targeted usage, and 17 whole-workspace graph tests pass, as does affected all-feature clippy. Commit `6ffe8f8d` is pushed; a fresh two-file Git snapshot of the unchanged production sources avoids rebuilding the whole stale corpus cache, and pushed-head `/tmp/c-exact-767-cpu-features-pushed.jsonl` classifies PowerPC `cputable.c` bytes `7942..7954` as the exact `cpu_spec.cpu_features` field with an identical inverse range and zero actionable findings. #767 is closed.
- [x] (2026-07-14) Filed #768 only after an inline pointer-field regression proved that unfielded `ERROR(identifier "__iomem")` inside `struct Reg __iomem *ip_serial_regs;` is persisted and resolved as pseudo-field `Port.__iomem`. The issue was assigned only to `jbellis`. Synchronous Oldskool implementation separates class-like discovery from repeated tree-sitter `declarator` fields, classifies those structural roles first, and consults a narrow explicit unfielded-declarator fallback only when no fielded declarator was handled; fielded ERROR recovery and #767's exact designator suppression remain intact. The 38 C++ analyzer, 56 C++ definition, 66 targeted usage, and 17 whole-workspace graph tests pass, as does affected all-feature clippy. Commit `61944019` is pushed; paired one-file snapshots of the unchanged IOC4 source prove both directions: `/tmp/c-exact-768-iomem-pushed.jsonl` changes bytes `16135..16142` to honest `no_definition`, while `/tmp/c-exact-768-field-pushed.jsonl` resolves `ip_serial_regs` bytes `25916..25930` to `ioc4_port.ip_serial_regs` with an identical exact inverse range and zero actionable findings. #768 is closed. The #767/#768 C++ epoch bumps force the final whole-clone C run to rebuild the previously stale callable metadata as well.
- [x] (2026-07-14) Completed the epoch-refreshed final C N=1 run in `/tmp/c-n1-768-pushed.jsonl`, pinned to Bifrost head `42d1ab07` and RMerl head `e7c13912`. It audited 1,000 of 164,927 eligible files, sampled 10,000 sites, queried 1,000 of 1,356 target groups, and reports 757 consistent, 297 unproven, six missing, and 8,940 inconclusive classifications in 9,316.2 seconds. There are zero file errors and zero candidate-limit files; 397 target-truncated sites remain explicitly inconclusive. Highest sampled RSS was 21,095,996 KiB, about 20.1 GiB, below the prior run's roughly 22.9 GiB sample. The six residuals are one libpng macro-decorated return type, one net-snmp parameter type, two mini-gmp `mp_size_t` references with duplicate typedef targets, the known ambiguous `mp_int` artifact, and one canonical `_PCB`/`PCB_t` alias reference. Three delegated read-only audits are classifying these before any new issue is filed.
- [x] (2026-07-14) Completed delegated read-only triage of all six final C residuals. Four sites (`png_byte`, `netsnmp_container`, and two `mp_size_t` references) are one genuine direct-typedef identity defect; `mp_int` is an overbroad-forward artifact because its fifteen globally collected targets contain incompatible LibTomMath, Dropbear, and Samba alias shapes; and `PCB_t` is a distinct genuine declaration-recovery defect. Filed #769 and #770 only after consumer-only authoritative regressions failed, and assigned each only to `jbellis` before implementation.
- [x] (2026-07-14) Delegated synchronous #769 implementation to Oldskool and accepted the narrow reviewed change: `resolves_to_type` preserves a visible candidate already identical to the queried typedef alias, while only non-direct candidates enter the existing unique, cycle-safe canonicalization path. The 68-test C++ usage suite and affected all-feature clippy pass. Full-clone exact probes classify primitive `png_byte` and tagged `netsnmp_container` as consistent with identical inverse ranges and zero actionable findings in `/tmp/c-exact-769-png-byte-fixed.jsonl` and `/tmp/c-exact-769-netsnmp-fixed.jsonl`.
- [x] (2026-07-14) Delegated synchronous #770 implementation to Oldskool after the reduced failure proved that `struct _PCB *pcb;` was persisted as false nested class `Holder$_PCB`. Exported-class recovery now accepts macro-like parser-confused class syntax only when every outer declarator is a bare identifier/type identifier; pointer, reference, array, initializer, and function declarators remain ordinary fields/objects. The regression preserves real `Holder.pcb` and true nested forward declaration `Holder$Nested`; 38 analyzer, 68 targeted usage, 17 whole-workspace graph, epoch, macro-export definition, formatting, diff, and affected all-feature clippy gates pass. The C++ epoch is bumped because emitted identities change. A Git snapshot containing byte-identical production `lpfc_mbox.c`, `lpfc_hw.h`, and `lpfc.h` classifies `PCB_t` bytes `31036..31041` as canonical `_PCB` with an identical exact inverse range and zero actionable findings in `/tmp/c-exact-770-pcb-fixed.jsonl`.
- [x] (2026-07-14) Pushed the combined root fixes as `6c23084d` and closed assigned issues #769 and #770 after pushed-head proofs. Byte-identical production snapshots at pushed HEAD reproduce primitive alias, tagged alias, and canonical tagged-typedef behavior with exact inverse ranges and zero actionable findings in `/tmp/c-exact-769-png-byte-pushed.jsonl`, `/tmp/c-exact-769-netsnmp-pushed.jsonl`, and `/tmp/c-exact-770-pcb-pushed.jsonl`. Started the required epoch-refreshed whole-RMerl C closure run in `/tmp/c-n1-770-pushed.jsonl`; C++ follows only after that run confirms the final C boundary.
- [x] (2026-07-14) Profiled the forced post-#770 RMerl refresh when machine load fell unexpectedly. The analyzer did create 120 Rayon workers, but 119 slept for roughly an hour while one pathological C++ file repeatedly executed `has_matching_declaration`; the growing declaration-set scan made typedef/macro dedup quadratic within one file. Filed assigned issue #771 only after a deterministic reduction measured 8,225 semantic-identity comparisons for 130 inputs against a linear budget of 520 while proving alias/macro dedup and overload behavior.
- [x] (2026-07-14) Delegated synchronous #771 implementation to Oldskool and accepted a parse-ephemeral counted `ParsedFile` declaration-identity index keyed by the exact former scan dimensions. Declaration storage is now private and insertion, removal, replacement, file-scope, synthetic, and recursive paths centrally maintain the multiset. The focused workload falls from 8,225 to 35 equality comparisons; multiset tests, 38 C++, 21 Python, 12 Rust, 21 Scala, 20 PHP analyzer tests, the 751-test library gate, formatting, diff checks, and affected all-feature clippy pass. The separate post-parse persistence phase remains under measured design review before the C closure rerun is accepted.
- [x] (2026-07-14) Read-only persistence profiling found two additional generic analyzer boundaries: epoch mismatch synchronously cascade-deletes about 6.7 GiB of old language pages into a 6.5 GiB WAL, then reconciliation serially executes one SQLite transaction per parsed blob after collecting every Rayon result. At 49,233 completed C++ blobs the cache represented about 18.1 million logical row writes before secondary-index mutations. Parallel SQLite writers would contend rather than help; the planned seam is generation-aware epoch visibility plus bounded parallel preparation feeding one adaptive batching writer, with deterministic overlap, transaction-count, failure-isolation, and no-synchronous-delete regressions.
- [x] (2026-07-14) Filed assigned #773 only after real analyzer and store reductions proved the collect-all and one-transaction-per-blob boundaries, then delegated synchronous implementation to Oldskool. CPU-heavy row preparation now stays on Rayon workers, an eight-item bounded channel overlaps parsing with one SQLite writer, and batches cap blobs, logical rows, and payload bytes. Root and independent review rejected the first clock-based linger and 64-item queue; the accepted deterministic `AllStarted` protocol preserves fast-result/slow-parser overlap without a timing gate. A real 257-file analyzer build commits all blobs in five transactions, rollback/bisection preserves good peers and leaves only the irreducible bad blob dirty, preparation failures reach terminal progress, and Ruby/Scala/TypeScript prepared persistence matches the legacy path. The broad library gate has 802 passes and three ignored tests; its only three sandbox-denied process tests pass outside the sandbox, and affected all-feature clippy is green. #772 generation-aware epoch visibility remains required before the forced C closure rerun.
- [x] (2026-07-14) Implemented assigned #772 after its committed reduction was the sole red CI test on `28fde2ac`. One immediate transaction now publishes a complete map of fresh monotonic generation tokens, so A to B to A cannot revive A1 and TS/TSX cannot expose a partial cutover. Every analyzer read snapshot and mutation validates its captured token; stale facts remain available only as terminal in-memory fallback and never enter endless retry or poison lazy indexes. The metadata-only v4 migration tags root blobs and path-symbol rows while preserving populated analyzer and semantic generation-zero data. Reclamation is bounded, best-effort, and generation-qualified; cache GC deletes only the generation it snapshotted; replacement batching counts cascaded old rows plus new rows. Two delegated reviews found and verified fixes for cross-process publisher races, mixed WAL snapshots, stale metadata, cleanup error semantics, partial token maps, stale lazy indexes, forever-dirty retries, and uncounted old-generation deletion cost. Store 38, tree 24, migration, reconciliation, and full-library gates pass; all-target/all-feature clippy is green. The forced pushed-head C closure rerun is now the next boundary.
- [x] (2026-07-15) Profiled the pushed-head C closure after persistence completed and found the differential forward phase itself single-thread CPU-bound. Two debugger samples on different C files stopped in C++ alias resolution repeatedly revalidating candidate paths through `LiveSnapshot::validated_oid_for_path`. Filed assigned #774 only after a deterministic two-reference batch proved eleven live-OID validations for one declaration file, followed by seven more for a separate one-reference batch. Synchronous Oldskool implementation activates the existing request-scoped analyzer cache at the centralized definition-batch seam; the regression now proves exactly one validation per batch and a fresh validation in the next batch. Independent review found all three batch entrypoints covered with correct nested-scope behavior and no call-hierarchy gap. The exact and focused definition tests, C++ analyzer/usage/clangd suites, formatting, diff checks, and all-target/all-feature clippy pass. The stopped `e21778d1` run is profiling evidence only; a fixing-head closure rerun follows the separately measured C++ all-visible alias-routing audit.
- [x] (2026-07-15) Filed assigned #775 only after the second profiler reduction resolved the intended namespace-local and qualified C++ targets while counting 820 provider-backed alias classifications for two references and 410 for one reference against a ten-per-request bound. Synchronous Oldskool implementation now routes requested type names through the existing per-file identifier/FQN visibility index before classifying aliases; namespace-relative lookup, leading-global handling, provider-only aliases, recursion/cycle safety, canonical selection, and fallback behavior remain unchanged. The retained 128-distractor regression is bounded and green, independent review found no semantic widening, the focused C++/definition suites pass, and formatting plus all-target/all-feature clippy are green. A pushed fixing-head C closure rerun is the next measurement; bounded cross-file forward parallelism is considered only if profiling still shows serial work dominant after both avoidable amplifiers are removed.
- [x] (2026-07-15) Completed the post-#774/#775 C run at pushed head `90b17078` in 891.0 seconds, down from 9,316.2 seconds on the last full epoch-refreshed record. It audited 1,000 of 164,927 files and reports 762 consistent, 297 unproven, 8,940 inconclusive, and one missing site: the previously audited `mp_int` artifact whose forward result combines fifteen incompatible LibTomMath, Dropbear, and Samba declarations. Warm-start profiling before the differential also found reconciliation issuing one `contains_parsed_blob_at_generation` point transaction per file after an authoritative bulk missing-key query. Filed assigned #776 only after a persistent-store regression proved 12 redundant queries for 12 warm paths and 11 after one duplicate blob was marked incomplete. Synchronous Oldskool implementation records each missing representative as persisted, dirty, or unparseable and eliminates the point-query pass while preserving deterministic first-seen order, duplicate OID/storage-key isolation, corrupt recovery, dirty/stale fallback, and conservative bulk-query failure. The regression now reports zero point queries in both cases; independent review is clean, the full all-feature library has 821 passes and three ignored tests, persistence/reconciliation/parity suites pass, and formatting plus all-target/all-feature clippy are green. Because #776 changes Bifrost HEAD, one final warm C closure rerun remains before C++ starts.
- [x] (2026-07-15) Completed the final pushed-head C closure at `993581e0` in `/tmp/c-n1-993581e0.jsonl`. Warm runtime fell again to 529.9 seconds. The record audited 1,000 of 164,927 files, sampled 10,000 sites, resolved 3,144 forward references across 1,356 target groups, queried 1,000 groups, and reports 762 consistent, 297 unproven, 8,940 inconclusive, zero editor-only, and one raw missing classification. The sole residual is the already-audited `mp_int` artifact: its forward target set merges fifteen declarations with three incompatible LibTomMath, Dropbear, and Samba struct signatures, so it is not a unique semantic target and no analyzer issue is warranted. There are zero genuine C defects remaining. The next campaign boundary is the cold Chromium C++ N=1 run.
- [x] (2026-07-15) Started the Chromium C++ N=1 run at pushed head `ef9793b9`; cold persistence used the existing 122-worker preparation path, while repeated forward-phase debugger samples found one CPU-bound thread scanning every visible declaration from qualified C++ type resolution. Filed assigned #778 only after a 256-distractor reduction preserved two visible duplicate `perf.Exact` declarations, a hidden same-FQN declaration, a same-FQN free function, namespace aliasing, and global qualification while measuring 261 inspected visible candidates against an exact-name bucket of three. Synchronous Oldskool implementation reuses the existing sorted per-file identifier index before the unchanged exact-FQN filter, adding no production memory or synchronization. The regression now inspects three candidates; constructor, destructor, operator, nested-type, template-head, leading-global, alias, duplicate, raw-candidate, and target-kind semantics remain covered. Independent review is clean, the focused C++ suites, formatting, and all-target/all-feature clippy pass. The obsolete pre-fix Chromium process was stopped; the next accepted measurement will restart from the partial persisted cache at the pushed fixing head.
- [x] (2026-07-15) Pushed #778 as `98911a28`, closed the assigned issue, and restarted Chromium from the partial cache. Three subsequent serial-audit samples no longer contained the all-visible qualified-candidate scan, but a later set hit the identical enclosing-context chain in all three captures: every usage hit repeated `precise_parent_of -> definitions -> sql_definition_candidates`, usually continuing through range hydration and once through live-path validation. Filed assigned #779 only after an inline two-batch reduction retained four authoritative `demo.Consumer.run` hits per batch while counting four parent-definition SQL queries in each batch against a one-per-batch boundary. The existing byte-range cache was inert and could not share work across distinct hit nodes. Synchronous Oldskool implementation populates that same-node cache and adds a scan-file-local structured owner cache keyed by the enclosing `CodeUnit`, including cached misses; SQL parent queries fall from `(4, 4)` to `(1, 1)` while separate scans remain fresh, and an unresolved-owner regression proves three hits share one SQL miss. The superseded Chromium process was stopped once the reduction proved the fixing boundary. The focused C++ suites, formatting, diff checks, and all-target/all-feature clippy pass; independent review remains the final checkpoint before push and exact-head rerun.
- [x] (2026-07-15) Pushed #779 as `08ec4288`, closed the assigned issue, and restarted Chromium. The enclosing-parent chain disappeared from every follow-up debugger sample, but repeated forward samples converged on exact-FQN candidate sorting calling `ranges -> fetch_file_state -> hydrate_file_state_with_source`; the run accumulated hundreds of logical SQLite gigabytes with physical reads flat. Root review rejected an initial reduction that conflated sorting with later location rendering. The corrected warm-persisted `definitions("demo.Shared")` reduction isolates seventeen exact candidates and proves seventeen complete file hydrations performed only to obtain the existing minimum-start-byte sort key. Filed assigned #780 after reproducing that zero-hydration boundary. Synchronous Oldskool implementation projects `MIN(unit_ranges.start_byte)` through a candidate-bounded correlated subquery using the existing `(blob_oid, lang, unit_key, ordinal)` primary key, carries the value transiently through liveness/filter/sort/dedup, and retains structured `ranges()` fallback for dirty and nonpersisted units. The seventeen-candidate regression now performs zero hydrations while preserving exact ordering and semantics; store coverage proves true minimum and missing-range behavior. Root review removed proposed per-candidate atomic test instrumentation from the hot loop. Independent review is clean, the full 469-test definition target and focused store/C++ suites pass, and formatting, diff checks, and all-target/all-feature clippy are green. The superseded Chromium process was stopped after the reduction proved the fixing boundary; the next run will use the pushed #780 head.
- [x] (2026-07-15) Completed the pushed-#780 Chromium N=1 run in `/tmp/cpp-n1-3aee987b.jsonl`, pinned to Bifrost `3aee987b` and Chromium `e52675fe`. It audited 1,000 of 105,604 eligible C++ files, sampled 10,000 sites, resolved 4,183 forward references across 1,869 target groups, queried 1,000 groups, and reports 902 consistent, 20 editor-only, 58 unproven, 8,599 inconclusive, and 421 missing classifications in 1,113.8 seconds. There were zero candidate-limit files and one explicit source-too-large file error for the 7.27 MB `normalization_rule.h`; 1,125 target-truncated sites remain inconclusive. None of the three previously fixed profiler amplifiers appeared in follow-up samples. The one-time global index build raised RSS to roughly 4.6 GiB and the run plateaued near 5 GiB; random host contention makes elapsed time descriptive rather than a performance contract.
- [x] (2026-07-15) Completed read-only triage of all 421 Chromium missing sites with exact accounting: 155 are false-forward or multi-target artifacts, three are honest semantic-scope limitations, and 263 are genuine inverse-reference defects; none are unexplained, truncated, or diagnostic failures. All 20 editor-only records are the expected C++ `self_receiver` class and are non-actionable. The dominant false-forward cluster is 142 `base` namespace-qualifier tokens that incorrectly resolved through a class-owned `using base` alias; one additional qualified `Mode` site crosses to an unrelated owner through the same #784 boundary, and one macro-broken class declaration is parsed forward as a constructor call. An exact production rerun at `android_webview/browser/aw_browser_process.cc` bytes `12108..12112` reproduced the complete missing result without batching or truncation in `/tmp/cpp-exact-attach-3aee987b.jsonl`.
- [x] (2026-07-15) Filed assigned issue #784 only after an inline reduction reproduced the qualifier false-forward boundary while retaining `base::RealType` as a positive control. Synchronous Oldskool implementation routes focused qualifiers through visible identifier buckets and exact AST-derived structural owner paths, including class-owned aliases. Repeated review required nearest-nonempty namespace tiers, enclosing-class lookup, leading-global semantics, structured template components, analyzer-generation source/tree/path caching, and per-file class-range/per-owner-chain caching. The final regressions prove 32 repeated wrong `base` sites with two candidate hydrations, 32 qualifier sites across 129 classes with one class-range build and one owner chain, nested namespaces, templates, class-local types, global-only paths, aliases, and the original three-to-zero reduction. Fresh independent review is clean; all 65 C++ definition tests, the complete 477-test definition target, formatting, and all-target/all-feature clippy pass. Commit `39d8a4f4` is pushed at merged head `31246d93`. Pushed-head `/tmp/cpp-exact-784-base-pushed-rerun.jsonl` changes `aw_browser_process.cc` bytes `12108..12112` to `no_definition` with zero actionable findings, while `/tmp/cpp-exact-784-mode-pushed.jsonl` resolves bytes `18869..18873` to `signin.PrimaryAccountAccessTokenFetcher$Mode` with the identical inverse range and zero actionable findings. #784 is closed.
- [x] (2026-07-15) Filed assigned issue #786 only after a consumer-only authoritative regression proved that seven valid references to `Owner.field` beneath dot, arrow, subscript, nested, fluent-call, qualified, and post-shadow receiver ancestors produced no inverse hits while six wrong-owner, selected-member, and lexical-shadow negatives remained excluded. Synchronous Oldskool implementation classifies each enclosing `field_expression` by structured receiver-versus-selected-field subtree containment, checks lexical shadows before same-owner proof, and restricts same-owner fallback to unqualified names. Independent review found no correctness or scope widening; the 71-test targeted C++ usage suite, 17-test whole-workspace graph suite, formatting, and all-target/all-feature clippy pass. Commit `ddd16b4d` is pushed and #786 is closed. Pushed-head `/tmp/cpp-exact-786-finish-pushed.jsonl` classifies Chromium `finish_callback_` bytes `4227..4243` as consistent with the identical inverse range and zero actionable findings; `/tmp/cpp-exact-786-progress-pushed.jsonl` retains `progress_throbber_` bytes `16903..16921` at the identical inverse range as honest unproven evidence instead of dropping it as missing, also with zero actionable findings.
- [ ] (2026-07-15) Filed #789 and verified assignment only to `jbellis` after a consumer-only authoritative reduction reproduced the dominant 53-site C++ receiver-call cluster. The first synchronous Oldskool implementation built a bounded physical-owner set from equivalent supplied targets, passed local review and gates, and was pushed in `85c1e268` at merged head `9b2b4515`; exact Chromium rerun `/tmp/cpp-exact-789-swap-pushed.jsonl` disproved its premise because `new_url.Swap` bytes `23576..23580` still resolved forward to only the parentless `url/gurl.cc` target and remained missing. Persisted-generation inspection then established the actual boundary: tree-sitter parses `class COMPONENT_EXPORT(URL) GURL` as a malformed function definition, the C++ declaration collector persisted no `url.GURL` class from `url/gurl.h`, and `type_owner_of` consequently classified `GURL.Swap` as a free function. The corrected synchronous Oldskool implementation structurally recovers the function-like export-macro class shape, bumps the C++ analysis epoch, and lets `precise_parent_of` accept only a unique exact-FQN class from the method source's direct structured includes; two direct exact-owner candidates remain ambiguous. The disproven multi-target owner layer has been removed. Fresh analyzer, definition, consumer-only authoritative single-target, hidden-owner, overload, wrong-owner, shadow, namespace-free-function, and ambiguity regressions pass; broader root gates and independent final review are in progress. Commit, pushed-head exact Chromium proof, and issue closure remain. Saved-record subtraction after #784/#786 leaves 194 genuine C++ sites, so only a fresh post-fix run can establish the current residual count.
- [x] (2026-07-15) Completed corrective #789 local validation: 39 C++ analyzer tests, 477 definition tests, 73 targeted C++ usage tests, 17 whole-workspace graph tests, formatting, diff checks, all-target/all-feature clippy, and independent review are green. Commit, pushed-head exact Chromium proof, and issue closure remain.
- [ ] (2026-07-15) Filed #790 and verified assignment only to `jbellis` after the pushed-head Chromium epoch rebuild exposed the single SQLite writer repeatedly executing `persisted_blob_mutation_cost_conn` while 122 parallel producers were backpressured. The deterministic store reduction replaces eight persisted blobs in one successful bounded transaction and observes sixteen point lookups: once per blob in the packing prepass and again inside the write transaction. The superseded #789 proof process was stopped; a separate Go exact run was identified and left untouched. Synchronous Oldskool implementation removes the prepass, validates each distinct language generation once per transaction, and persists exact cascade row and payload costs on new blob roots through a nullable v5 migration. The hot path now performs one root primary-key read per replacement without scanning satellite tables; preserved migrated rows with NULL costs use the old indexed aggregate lazily, with no corpus-wide backfill or deletion. Generation, transaction-cap, retry, reclamation, and oversize-isolation semantics remain covered; 19 cache tests, 44 analyzer-store tests, the complete feature-enabled repository suite, formatting/diff checks, all-target/all-feature clippy, and independent review are green. Commit, push, and issue closure remain.
- [x] (2026-07-15 14:05Z) Started the Go leg from clean pushed Bifrost head `da0dc303` against `aws__aws-sdk-go-v2` head `91eca463daf932474778dc4a984c41ecfcd9dc3c`, preserving its persisted `.brokk` cache and serializing all corpus processes. The required `types.S3Location` exact sanity site was consistent with an identical inverse range. The initial full `/tmp/go-n1-da0dc303.jsonl` run audited 1,000 of 25,617 eligible files, sampled 10,000 sites, resolved 3,332 forward references (`4,578` no-definition and `2,090` external-boundary outcomes), formed 1,513 target groups, queried 1,000, and reported 919 consistent, seven unproven, 8,946 inconclusive, and 128 missing classifications. Tool elapsed time was 165.7 seconds; `/usr/bin/time` observed 2:46.01 wall time and 485,552 KiB peak RSS. These are descriptive contention-sensitive observations, not requirements.
- [x] (2026-07-15 14:35Z) Triaged all 128 initial Go misses from authoritative source byte ranges: 117 focused a qualifier or intermediate selector that was incompatible with the terminal/deeper forward target identity, nine keyed literal labels had semantically wrong forward owners, and two exact-owner struct literal field labels were genuine complete inverse omissions. A consumer-only `InlineTestProject` regression failed with zero of two expected hits before implementation. Filed assigned [#791](https://github.com/BrokkAi/bifrost/issues/791), delegated substantial synchronous implementation to Oldskool, and independently narrowed the result to AST-proven keyed labels whose enclosing composite-literal type matches the requested field owner. Qualified map-key expressions, wrong owners, local shadows, and ordinary map keys remain excluded. Commit `91cddbf2` is pushed; formatting, all-target/all-feature clippy, 477 definition tests, 50 targeted Go usage tests, 16 whole-workspace Go graph tests, the complete `cargo test --features nlp,python` gate, and GitHub CI run `29423274687` are green.
- [x] (2026-07-15 15:09Z) Completed Go on the clean pushed fixing head `91cddbf297bc19646e7993e9f725a5c2d688f723`. Exact production records `/tmp/go-exact-791-signer-91cddbf2.jsonl` and `/tmp/go-exact-791-token-91cddbf2.jsonl` resolve `resolvedAuthScheme.SignerProperties` and `idempotencyToken_initializeOpStartTaskContact.tokenProvider` to their exact owners with identical proven inverse ranges. `/tmp/go-exact-s3location-91cddbf2.jsonl` also keeps the historical `types.S3Location` sanity site consistent. The final full `/tmp/go-n1-91cddbf2.jsonl` run retained every sampling and forward counter from the baseline, raised consistent classifications from 919 to 921, and reduced missing from 128 to 126; tool elapsed time was 219.1 seconds, with 3:39.30 wall time and 488,432 KiB peak RSS. Exhaustive residual triage accounts for 117 incompatible focus/target identities and nine wrong-owner keyed-label forward results: seven external `ServiceID` fields, `smithy.GenericAPIError.Code`, and `internalEndpointDiscovery.DiscoverEndpoint.Options`. None has a semantically valid forward target group, so zero genuine Go defects remain. #791 contains the fixing commit and pushed production evidence and is closed.
- [x] (2026-07-15 17:05Z) Established the Python baseline at pushed Bifrost `125afd7b` against `googleapis__google-cloud-python` head `d0b2abc2aef8d95402c026cccbc866d812b819b8`, preserving and serializing its persisted `.brokk` cache. `/tmp/python-n1-125afd7b.jsonl` audited 1,000 of 36,006 eligible files, sampled 10,000 sites from 14,849,206 source bytes and 350,382 structured candidates, resolved 3,712 forward references, queried all 681 distinct target groups, and reported 928 consistent, 8,857 inconclusive, and 215 missing classifications with no errors, truncation, or candidate limits. Tool elapsed time was 350.0 seconds; `/usr/bin/time` observed 5:52.39 wall time and 2,993,932 KiB peak RSS. Initial source triage found namespace/module-qualifier and re-export aliases, source-ordered module binding, and nested-class/class-namespace omissions; exact follow-up also identified invalid forward identities that must not be fixed in the inverse path.
- [x] (2026-07-15 18:20Z) Reduced the genuine Python boundaries with `InlineTestProject`, filed and assigned only to `jbellis` [#795](https://github.com/BrokkAi/bifrost/issues/795), [#796](https://github.com/BrokkAi/bifrost/issues/796), and [#797](https://github.com/BrokkAi/bifrost/issues/797), and implemented structured root fixes in commit `6d6d76f3`. The Python usage index now retains module routing and reverse importer evidence for bounded candidate seeding and canonical re-export resolution; module bindings use an AST-derived source-order timeline with deferred function bodies observing final module state; analyzer parent relationships identify nested class ownership; and decorators, annotations, and defaults evaluated during method declaration retain their class namespace. No regex, source splitting, or transitive per-query workspace parse was added. Focused validation passed 477 definition tests, 86 targeted Python usage tests with three intentional ignores, eight Python routing tests, and the five-test whole-workspace graph target; formatting and all-target/all-feature clippy are clean.
- [x] (2026-07-15 19:17Z) Completed Python on clean pushed Bifrost `6d6d76f390834b4e15cbc5819d33a7da7331d4aa`. Six pushed-head exact records—`/tmp/python-exact-proto-module-6d6d76f3.jsonl`, `/tmp/python-exact-reexported-function-6d6d76f3.jsonl`, `/tmp/python-exact-import-before-rebind-6d6d76f3.jsonl`, `/tmp/python-exact-redeclared-module-field-6d6d76f3.jsonl`, `/tmp/python-exact-nested-class-6d6d76f3.jsonl`, and `/tmp/python-exact-class-field-bare-6d6d76f3.jsonl`—are clean and consistent with one covering inverse site apiece. The final `/tmp/python-n1-6d6d76f3.jsonl` run retained the baseline inventory, sample, forward, and target counters, raised consistent classifications to 1,166, and reduced missing to six; it reports 8,828 inconclusive, zero editor-only/unproven, zero errors, and no limits or truncation. Tool elapsed time was 110.8 seconds; `/usr/bin/time` observed 1:51.40 wall time and 895,084 KiB peak RSS. One residual is an `ibis` table's `result.columns` incorrectly resolved forward to `UnorderedIR.columns`; the other five occur after each file rebinds imported `DEFAULT_CLIENT_INFO` to a new local `ClientInfo`. All six therefore fail the valid-forward prerequisite, leaving zero genuine Python inverse defects. The complete `cargo test --features nlp,python` gate passed, GitHub CI run `29442265694` is green, all three issues contain the fixing commit and production evidence, and #795-#797 are closed.
- [x] (2026-07-16 02:15Z) Started the Java leg from Bifrost `2d62af4b` against `googleapis__google-cloud-java` head `20f3d257f09d9509ed1d8902b89fbd03879d6b72`, preserving and serializing its persisted `.brokk` cache. The first exact inverse target remained inside `JavaAnalyzer::build_direct_descendant_index` for 2:09:06; debugger capture proved the 199,488-class workspace was executing `get_direct_ancestors -> resolve_forward_type_name -> sql_definition_candidates_vec` once per supertype. Filed [#806](https://github.com/BrokkAi/bifrost/issues/806) only after the measured root boundary and verified assignment only to `jbellis`.
- [x] (2026-07-16 02:15Z) Reduced and implemented the Java hierarchy and type-usage amplifiers in assigned [#806](https://github.com/BrokkAi/bifrost/issues/806) and [#809](https://github.com/BrokkAi/bifrost/issues/809). The hierarchy builder now consumes a class-only primary-range inventory and 4,096-unit persisted package/import/raw-supertype projections, preserving exact FQN, explicit and wildcard import, same-package, nested-type, and same-source identity semantics while performing zero point definition queries and zero full `FileState` hydration in the regression. Type-target scans now compare the structured AST terminal name before definition lookup and skip irrelevant value-type inference; a 128-distractor regression returns the one exact `Wanted` hit with at most two definition queries. The five-target production preflight `/mnt/optane/tmp/java-preflight-5-zVDz1q.jsonl` completed with zero actionable findings; type targets that formerly took 17.5 to 180.8 seconds completed in milliseconds, and the full descendant index completed in tens of seconds rather than hours. These contention-sensitive times describe the result but are not requirements.
- [x] (2026-07-16 02:15Z) Reduced and implemented assigned [#811](https://github.com/BrokkAi/bifrost/issues/811) after the remaining `AppEngineVersionInfo.Builder.buildPartial` preflight target spent most of its time in `usage_fact_rows_by_lang_conn`. The existing two-file singleton-return receiver regression failed by observing one full declaration scan before the fix and now passes with zero. Java targeted return inference uses exact method declarations plus per-method and per-file structured AST/range caches instead of constructing `TreeSitterAnalyzer::usage_facts_index`. Exact production record `/mnt/optane/tmp/java-exact-buildpartial-IEvTc1.jsonl` retains the correct honest `unproven` result with zero actionable findings, reduces peak RSS from 14,797,964 to 8,561,312 KiB, and reduces that target from 486.5 to 144.5 seconds under variable host contention. Formatting, diff checks, and a combined all-feature Java gate pass: 477 definition, 13 hierarchy/import, 48 targeted usage, and 18 whole-workspace usage tests, 556 total with zero failures.
- [x] (2026-07-16 02:47Z) Committed the validated Java checkpoint as `ea7b1d14`, merged current `origin/master` without conflict at `cfd16b0f`, and passed every required merged-head local gate. `cargo fmt --all -- --check`, diff checks, and `cargo clippy --all-targets --all-features -- -D warnings` are clean; the affected all-feature gate again passed 477 definition, 13 hierarchy/import, 48 targeted Java usage, and 18 whole-workspace Java usage tests, and the complete `cargo test --features nlp,python` repository suite is green. The automated safety reviewer rejected the first `HEAD:master` push solely because it requires explicit Java-specific default-branch authorization; no workaround was attempted.
- [x] (2026-07-16 02:47Z) Built a clean merged-head release and collected three serialized exact production witnesses at Bifrost `cfd16b0f` with `bifrost_dirty=false`. `/mnt/optane/tmp/java-exact-806-tobuilder-cfd16b0f.jsonl` classifies `request.toBuilder` consistent with a 20.154-second descendant-index build and zero missing sites; `/mnt/optane/tmp/java-exact-809-librarymetadata-cfd16b0f.jsonl` classifies `LibraryMetadata` consistent with a 3.0-millisecond inverse target and zero missing sites; `/mnt/optane/tmp/java-exact-811-buildpartial-cfd16b0f.jsonl` retains `builder.buildPartial` as honest unproven evidence with zero missing sites and no workspace usage-facts build. The #811 inverse target took 133.3 seconds and peaked at 8,567,156 KiB under current contention; timing is descriptive, while the reduced zero-full-scan assertion is the correctness/performance invariant.
- [x] (2026-07-16 05:05Z) Completed independent review before the Java push. Review found that removing the workspace-wide usage-facts return index also lost lexical resolution for simple nested return types such as `Outer.Inner`, and that the replacement helper was mini-parsing generics and arrays from source text. Delegated correction `c3eacc8a` resolves declared return types through persisted structural enclosing-class tiers, fails closed on shadowed or ambiguous qualified paths, and traverses tree-sitter `generic_type`, `array_type`, `annotated_type`, and scoped type nodes instead of splitting source. The consumer-only authoritative regression covers a colliding top-level `p.Inner`, `Outer.make().run()`, and a factory declared three enclosing types deep; targeted Java usage tests pass 49/49, whole-workspace Java usage tests pass 18/18, and fresh all-target/all-feature clippy is clean.
- [x] (2026-07-16 05:05Z) Pushed the Java stack directly to `master` at clean head `c3eacc8affe6c01bb832f95fd2443492d9de9a00`, rebuilt the release binary, and repeated all three exact google-cloud-java witnesses against repository head `20f3d257f09d9509ed1d8902b89fbd03879d6b72`. `/mnt/optane/tmp/java-exact-806-tobuilder-c3eacc8a.jsonl` classifies `request.toBuilder` consistent with an exact inverse range and zero actionable findings; `/mnt/optane/tmp/java-exact-809-librarymetadata-c3eacc8a.jsonl` does the same for `LibraryMetadata`; `/mnt/optane/tmp/java-exact-811-buildpartial-c3eacc8a.jsonl` honestly retains `builder.buildPartial` as unproven with an exact retained range and zero missing/actionable findings. GitHub Actions run `29472295619` passed 12/12 jobs. Posted the commit and production evidence to assigned #806, #809, and #811 and closed all three.
- [x] (2026-07-17) The Java top-five expansion pushed an initial fix stack at integrated master `92c7dfa2`, then exhaustively reviewed its five-repository residuals. Assigned #858, #860, #863, #864, #866, and #867 now have production-shaped fixes for anonymous-class field-scope leakage, annotated spread metadata, external inherited-method arity fallback, static wildcard/import-declaration routing, method references, and field-vs-method kind confusion. Dirty-tree exact records for all seven surviving witnesses contain zero actionable findings; formatting, all-target/all-feature Clippy, focused Java definition/targeted/workspace graph suites, and the complete feature-enabled Cargo test gate pass. A final pushed-head five-repository rerun remains the language closure gate.
- [x] (2026-07-17) The Go top-five expansion integrated #876-#879 at pushed head `45ca830f`, whose definitive corpus contained 882 raw rows: 877 non-actionable focus/partial-selector/declaration artifacts and five genuine forward defects. Assigned #880 fixes imported-alias/current-package collision and #881 removes scope-blind declaration/predeclared-type-to-field fallback while retaining lexical interface parameters. Independent review additionally restricted imports to the nearest visible vendor or exact module path without hydrating workspace declarations. The integration-candidate exact witnesses are actionable-zero, formatting, all-target/all-feature Clippy, 493 Go definition tests, persistence/canonical-FQN regressions, and the complete feature-enabled Cargo gate pass. Its five-repository record completed in 2m27s and exhaustively reconciles all 1,114 raw rows as 1,026 qualifier/middle-focus artifacts, 72 documented partial-selector diagnostics, and 16 declaration leaks, leaving zero actionable Go residuals before final merge/push and clean-head proof.
- [x] (2026-07-17) Closed the Go top-five expansion at clean pushed head `20fec8af`. Ten #876-#881 production exact records report `bifrost_dirty=false`, zero actionable findings, and either exact consistent targets or honest #881 non-definitions. The definitive five-repository record `/mnt/optane/tmp/reference-differential/go-top5-20fec8af.jsonl` completed in 2m25s at 1,100% average CPU and 9.6 GB peak RSS; all 50,000 site keys, classifications, and target groups exactly match the audited integration candidate. Its 1,114 raw rows therefore retain the exhaustive partition of 1,026 focus artifacts, 72 partial-selector diagnostics, and 16 declaration leaks, with zero genuine residuals. Full local gates pass and assigned issues #876-#881 are closed.
- [x] (2026-07-17) Established the Python top-five baseline at clean pushed head `617ef38e`. `/mnt/optane/tmp/reference-differential/python-top5-617ef38e.jsonl` completed five concurrent repositories in 9m14s at 287% average CPU and 5.5 GB peak RSS, with raw missing counts Google 6, Azure 93, Home Assistant 335, Tekton 0, and KubeVirt 0. Exhaustive delegated and root review reconciles all 434 rows: 423 constructor keyword-label inverse omissions, four genuine inverse misses across call-result/aliased-type/private-re-export resolution, and seven wrong forward identities caused by scope-blind built-in fallback or post-import module rebinding. Assigned issues #897-#902 cover the six reduced root causes; implementation, exact proofs, full rerun, and integration remain pending.
- [x] (2026-07-17) Reviewed and locally accepted the delegated #897-#902 Python fix stack. Structured keyword-argument, callable-return, annotation-alias, explicit-private-re-export, lexical-scope, and source-ordered module-binding support passes formatting, all-target/all-feature Clippy, 498 definition tests, the targeted and whole-workspace Python graph suites, and the complete feature-enabled repository test suite. Fourteen dirty-head production exact records have zero missing/actionable findings, and `/mnt/optane/tmp/reference-differential/python-top5-617ef38e-review.jsonl` completed all five repositories in 2m42s at 381% average CPU and 5.1 GB peak RSS with `missing: 0` everywhere. The residual ledger is therefore exhaustively empty; integration and clean pushed-head proof remain pending.
- [x] (2026-07-17) Closed the Python top-five expansion at clean pushed head `b9250bff`. Fourteen clean #897-#902 exact records report `bifrost_dirty=false`, zero missing/actionable findings, and the expected consistent or honest non-definition outcomes. The definitive five-repository record `/mnt/optane/tmp/reference-differential/python-top5-b9250bff.jsonl` completed in 2m25s at 399% average CPU and 5.1 GB peak RSS; all five repositories report `missing: 0`, and every summary plus sampled-site payload exactly matches the audited integration candidate. Full local gates pass and assigned issues #897-#902 contain the final evidence and are closed.
- [x] (2026-07-17) Established and exhaustively audited the PHP top-five follow-on baseline at clean head `4b61d137`. `/mnt/optane/tmp/reference-differential/php-top5-4b61d137.jsonl` completed five repositories in 8m15s at 4.0 GiB peak RSS. Moodle, Magento, and EduSoho had zero raw misses; Psalm had five and Symfony two. One row is a wrong-forward branch-flow identity covered by #890, assigned to another owner. Assigned #904/#905 cover the other six genuine inverse misses. The delegated and independently reviewed implementation now applies assignments after RHS traversal and resolves `self`/`static`/`parent` from structured declaration ownership. Six integration-candidate production exact records are actionable-zero. The fix is checkpointed as `d053aaaa`; current master was merged without rebase as `9617701e`, where formatting, Clippy, the complete feature-enabled suite, and all 43 process-isolated selector tests pass again. Publication approval, clean-head corpus proof, and issue closure remain.
- [x] (2026-07-17) Rust top-five preflight found assigned #907 before the authoritative run: rust-lang/rust workspace construction stack-overflows in recursive whole-AST `collect_rust_type_identifiers`. GDB captured more than 120 repeated frames. Delegated implementation replaces it with an explicit node stack; a valid 1,024-level type parses and collects identifiers on a 256 KiB thread, and fresh ephemeral production exact proof `/mnt/optane/tmp/reference-differential/rust-exact-907-rustc-dirty.jsonl` completes workspace construction, forward resolution, and inverse lookup on the default stack in 3m03s with actionable=0. Formatting, all-target/all-feature Clippy, the complete feature gate, and every isolated selector test pass. The next forward phase is hard-blocked by exact #850 behavior (`PointerCoercion` at `writeback.rs:22` exceeds three minutes inside `build_reference_context -> resolve_module_files`); #850 remains assigned to another owner, so evidence was posted and implementation skipped.
- [x] (2026-07-17) Scala top-five preflight isolated inverse hierarchy amplification as open assigned #908. The accepted delegated implementation, checkpointed at `cd131d74`, bulk-hydrates source-free file projections once, resolves parser-recorded supertype lookup paths through the global type index, seeds direct-ancestor caches, and preserves import precedence plus exact class/companion/source identity. Independent review added the missing companion-qualified nested-parent boundary. Scala3 descendant construction fell from about 120.8s to 3.35s and IntelliJ completed it in about 3.17s; total exact runs took about 22.6s and 23.9s. Fifteen hierarchy tests, 61 Scala graph tests, the 132-file zero-point-hydration regression, formatting, all-target/all-feature Clippy, and the complete watcher-safe feature gate pass. The authoritative Scala top-five run, exhaustive residual audit, clean pushed-head proof, and #908 closure remain.
- [x] (2026-07-17) Scala top-five forward sampling then exposed separate assigned #910. Scala3 stopped at 608/609 files on `Definitions.scala:463` `cls.entered`, Kyo at 769/770 on `LspEngine.scala:129` `LspBuiltInRoutes.textDocumentWillSave`, and both remained active in recursive `scala_receiver_type_fqn -> scala_seed_active_path` expansion until the one-hour ceiling. The reviewed fix, checkpointed at `a94c31a7`, reuses the monotonically seeded lexical prefix during prior-value inference, shares one queried-site prefix across receiver and companion proof, and retains no-prefix literal/constructed receiver paths. The two production exact sites now finish in 4.6s and 6.9s; Kyo is exact-parity and Scala3 fails closed with a structured unsupported receiver. A clean-Bifrost research run completed Kyo in 303s and Scala3 in 2,354s. Generated untracked caches make those repository records non-authoritative, and sandbox denial prevented persisted startup for IntelliJ, Akka, and ScalaTest; exhaustive semantic audit continues without treating those records as publication proof.
- [x] (2026-07-17) Corrected the audited 608-row Scala routing to #661 335, #662 137, and #663 136. Coverage at `589f8ee3` is complete for all 335 #661 rows; the exact #662 ledger has 91 covered and 46 remaining, while the exact #663 partition retains 136 rows after thirteen constructor/companion/infix rows moved to #662. Checkpoint `6d92b9fe` additionally repairs tree-sitter recovery ownership for Kyo's nested `Batch.internal.Call`, bumps the Scala analyzer epoch so stale `kyo.Call` blobs are rebuilt, and passes analyzer 23/23, direct usage 87/87, whole graph 33/33, internal graph 5/5, and public MCP coverage. `/mnt/optane/tmp/reference-differential/scala-exact-664-context-call-589f8ee3-parser-dirty.jsonl` is exact-consistent/actionable-zero in 5.1s (SHA-256 `261613bf11be10e0857e3d02de6e8b82d26e1723ec0e231024f7371a9dff23aa`).
- [x] (2026-07-16) Stopped the first full Java N=1 attempt at pushed Bifrost `99eb52eb` after 1:15:21 wall time because its serial inverse-target loop exposed neither a completed-target count nor any defensible estimate of remaining work. The runner now accepts a bounded `--jobs` target-query pool (default 8) and emits timed workspace, inventory, sampling, forward, and per-target inverse `completed/total` progress. A real persisted-cache google-cloud-java smoke sampled 100 sites, resolved 13 target groups, queried 8 with `--jobs 8`, reported every completion from 1/8 through 8/8, and finished with zero actionable findings. The inverse phase took 154.4 seconds; per-thread inspection observed five workers simultaneously active across distinct CPUs, while `/usr/bin/time` recorded 128% aggregate CPU over the whole workspace-plus-audit run and 20,433,940 KiB peak RSS. The memory increase from the stopped serial run's 9,789,792 KiB is the reason parallelism is capped rather than derived from the 120 logical CPUs.
- [x] (2026-07-16) Corrected the first parallel checkpoint after live progress made the remaining serial forward-file loop explicit. The same bounded `--jobs` pool now resolves independent per-file forward batches and inverse target groups, emitting monotonic completed/total progress for both. The two-file CLI fixture exercises two forward batches and two inverse targets. A matched real google-cloud-java 100-site smoke completed all 77 forward file batches, reduced that phase from 26.4 to 13.2 seconds, and raised whole-run CPU from 128% to 238%; peak RSS remained comparable at 20,536,020 KiB. Removing only the renamed parallelism config field, the previous serial-forward and corrected parallel-forward reports have the identical SHA-256 `032d2589517c3aca3749fe727ac1be6d3b146bb6d2de6d8c908cd0d781e3f3f1`, proving unchanged summaries, site order, targets, classifications, and evidence.
- [x] (2026-07-16) Profiled the still-slow Java inverse rather than extrapolating from CPU use. The hot usage graph repeatedly entered bounded persisted type lookup, but routing those lookups through the existing immutable global definition index exposed a racy manual `OnceLock` initializer: 120 query clones concurrently fetched and rebuilt the same 123,932-row index. Serialized fallible initialization now shares one allocation across clones and applies the same correction to `UsageFactsIndex`; a 32-thread barrier regression proves one build of each. On the matched 100-site/8-target slice, inverse time fell from 185.9 to 159.6 seconds and peak RSS from 28.8 to 8.8 GB with byte-identical normalized output. A warm 200-site run with `--jobs 120` completed workspace, inventory/sampling, forward, and 30 inverse targets in 35.8, 16.2, 24.5, and 141.4 seconds respectively; the post-initialization targets formed one roughly 69-second wave, projecting the 1,000-target acceptance phase near 11-12 minutes rather than hours.
- [x] (2026-07-16) Completed the first clean parallel Java N=1 record at pushed Bifrost `971e3ba2` and google-cloud-java `20f3d257`. `/mnt/optane/tmp/java-n1-971e3ba2-j120.jsonl` audited 1,000 of 116,890 eligible files, sampled 10,000 sites, resolved 2,680 forward references into 1,561 target groups, queried the deterministic first 1,000 groups, and reported 903 consistent, 37 editor-only, 54 unproven, 8,769 inconclusive, and 237 missing classifications in 992.4 seconds. Forward execution took about 9.4 minutes and the 120-job inverse phase about 3.0 minutes; the acceptance run therefore replaced the former multi-hour serialized path with one bounded parallel run.
- [x] (2026-07-16) Exhaustively triaged all 237 Java raw misses and filed assigned [#853](https://github.com/BrokkAi/bifrost/issues/853) only after reduced authoritative regressions proved the genuine same-file generated-code boundaries. Structured fixes resolve nested types through enclosing Java declaration scopes, use semantic Java FQN identity across duplicate physical source copies, retain class self-type references and class-body initializer ownership, and recognize exact static nested-type import paths. The resulting dirty-tree rerun `/mnt/optane/tmp/java-n1-triage-dirty-j120.jsonl` raised consistent classifications to 1,016 and reduced missing to 136 with no newly missing sites; it completed in 1,054.9 seconds at 120 jobs and 1,645% aggregate CPU. Source-byte inspection partitions the 136 residuals into 92 invalid class forward identities, 30 method rows whose sampled focus is an owner/receiver rather than the displayed terminal method, and 14 genuine method terminals. Thirteen of those terminals exposed comma-counted generic signatures, while the last supplied three `GrpcOperationsStub.create` overloads but inverse matching retained only the first overload's arity. Parsed signature metadata and the union of all supplied overload arities now cover both roots. The reduced 54-test targeted Java graph suite and 18-test whole-workspace suite pass; dirty-tree production witnesses `/mnt/optane/tmp/java-exact-generic-arity-debug.jsonl` and `/mnt/optane/tmp/java-exact-overload-final.jsonl` contain zero missing/actionable findings, with the generic fluent site retained at the identical range as honest unproven evidence and the two-argument overload site consistent at the exact range. A clean pushed-head full rerun remains the closure gate.
- [x] (2026-07-16) Completed the clean pushed-head Java N=1 closure record `/mnt/optane/tmp/java-n1-final-5440d102-j120.jsonl` at Bifrost `5440d102` and google-cloud-java `20f3d257`. It audited 1,000 of 116,890 eligible files, sampled 10,000 sites, resolved 2,680 forward references into 1,561 target groups, queried 1,000 groups, and reports 1,030 consistent, 38 editor-only, 42 unproven, 8,768 inconclusive, and 122 missing classifications. The run completed in 993.2 seconds with `--jobs 120`, 1,528% aggregate CPU, and 10,649,208 KiB peak RSS; forward resolution completed in 605.5 seconds and inverse target resolution in 367.9 seconds. Its fingerprint and sampled sites exactly match the exhaustively audited 136-miss dirty-tree record: the final missing set introduces no new site and removes exactly the fourteen genuine method terminals, covering thirteen structured generic-signature arity cases and the `GrpcOperationsStub.create` overload-arity case. The 122 survivors partition into the already-audited 92 invalid class forward identities and 30 owner/receiver-focus method rows, leaving zero genuine Java inverse defects. The fixing commit `5ad6396a` was published through merged master `5440d102`; formatting, 54 targeted Java graph tests, 18 whole-workspace Java graph tests, all-target/all-feature clippy, and the complete `cargo test --features nlp,python` suite pass. Assigned #853 now contains the clean proof and is closed. The Java leg is complete.
- [x] (2026-07-15) Superseded the stale pending #789/#790 notes after auditing their actual landed state. #789 closed with corrected function-like export-macro class recovery in `e3e68401`, published at merged head `84973e27`; `/tmp/cpp-exact-789-swap-84973e27-ephemeral.jsonl` proves the Chromium `GURL::Swap` boundary. #790 closed in `c38d06d8` with strict normalized `blob_payload_costs` and set-based replacement accounting instead of the obsolete nullable-root design; CI run `29433341246` is green.
- [x] (2026-07-15) Closed #805 after delegated implementation in `e0a56d3b` resolved qualified C++ method values through persisted owner identity. `/tmp/cpp-exact-805-on-powered-on-e0a56d3b.jsonl` classifies Chromium `arc_bluetooth_bridge.cc` bytes `98137..98148` as consistent with the exact `ArcBluetoothBridge::OnPoweredOn` inverse range and zero actionable findings; CI run `29451769204` passed 12/12 jobs.
- [x] (2026-07-15) Closed #804 after delegated structural recovery in `47106f2b` retained the stable body-bearing prefix of malformed multi-base export-macro classes without source scanning or synthesized members. `/tmp/cpp-exact-804-widget-47106f2b.jsonl` classifies Chromium `automation_manager_aura_browsertest.cc` bytes `29045..29051` as consistent with a containing inverse hit over `views::Widget` and zero actionable findings; CI run `29454581802` passed 12/12 jobs.
- [x] (2026-07-15) Closed #807 after delegated implementation in `35100658` distinguished named declarator leaves from unnamed abstract pointer/reference/array/function declarators and retained proven type hits inside the queried class declaration. `/tmp/cpp-exact-807-kiosk-owner-35100658.jsonl` classifies `kiosk_controller_impl.h` bytes `2199..2218` as consistent with the exact unnamed-parameter self-type inverse range and zero actionable findings; CI run `29459604601` passed 12/12 jobs.
- [x] (2026-07-15) Closed #808 after delegated lexical type resolution in `ba6e7344` aligned targeted and workspace inverse scans with forward namespace/class tiers, structured global qualification, alias canonicalization, nested/template types, and fail-closed ambiguity. `/tmp/cpp-exact-808-resource-request-ba6e7344.jsonl` classifies Blink `resource_loader.cc` bytes `42510..42525` as the exact `blink.ResourceRequest` inverse range with zero actionable findings; CI run `29462894550` passed 12/12 jobs.
- [x] (2026-07-15) Closed #810 after delegated template-alias indexing in `96c024b3`, published at merged head `57156922`. Template-wrapped aliases are collected exactly once with inherited namespace identity, visible-scope fallback treats only the template wrapper as transparent, and the C++ analysis epoch forces old complete blobs through generation cutover. The epoch-refreshed `/tmp/cpp-exact-810-scoped-java-global-ref-57156922.jsonl` classifies `web_contents.h` bytes `13024..13043` as consistent with the containing `13009..13055` inverse range over `base::android::ScopedJavaGlobalRef<jthrowable>` and zero actionable findings; CI run `29465358559` passed 12/12 jobs.
- [x] (2026-07-17) Reconciled the stale post-#810 boundary against clean detached head `ab0af33f` and the completed warm Chromium record `/mnt/optane/tmp/cpp-n1-ab0af33f.jsonl` at Chromium `e52675fe`. Exhaustive row audit leaves 79 missing/actionable sites and 38 structured but unproven sites. Thirty actionable rows share one direct `using`-alias identity boundary; class-owned aliases (three rows), three-or-more-component qualified owners (eight rows), receiver inference, wrong-forward identities, and declaration-frontier artifacts remain separate families rather than being folded into the alias fix.
- [x] (2026-07-17) Closed assigned #906 after publishing target-aware direct-alias resolution in `cef57b5c`. The clean pushed-head Chromium proofs `/mnt/optane/tmp/cpp-exact-906-app-id-cef57b5c.jsonl` and `/mnt/optane/tmp/cpp-exact-906-loader-freeze-mode-cef57b5c.jsonl`, both pinned to Chromium `e52675fe`, classify the exact `webapps::AppId` and `LoaderFreezeMode::kNone` sites as consistent with zero actionable findings; the latter is an exact-range inverse hit for canonical target `blink.WebLoaderFreezeMode` through its alias owner. Formatting, all-target/all-feature Clippy, the complete `cargo test --features nlp,python` gate, and CI run `29610505701` are green.
- [x] (2026-07-17 22:17Z) Reduced the independent class-owned alias boundary from warm-record rows 28, 33, and 48, filed and assigned #909, and completed its fourth local implementation pass. Class aliases persist as `Owner$Alias` children under a bumped C++ epoch; inherited lookup is breadth-first by hierarchy level; repeated paths collapse to one logical declaration; malformed export-class aliases retain indexed owner chains; and target-preserving inverse lookup now retains any requested alias encountered along an acyclic structured alias chain rather than canonicalizing past intermediate targets. Phase-4 Chromium first proved exact consistent hits for inherited `Result` and canonical `ErrorCallback`, then exposed the final `MultiStep` chain-identity asymmetry. After the faithful middle-alias regression and shared chain fix, the final strict ephemeral records `/tmp/cpp-exact-909-multistep-phase5-dirty.jsonl`, `/tmp/cpp-exact-909-result-phase5-dirty.jsonl`, and `/tmp/cpp-exact-909-error-callback-phase5-dirty.jsonl` all pin clean Chromium `e52675fe` from the final dirty fixing tree and classify their exact requested byte ranges as consistent exact inverse hits with zero actionable findings. The targets are respectively `internal.InteractiveTestPrivate$MultiStep`, `updater.UpdateService$Result`, and canonical `base.OnceCallback`. All 118 focused C++ usage-graph tests, all 65 C++ definition tests, all 42 C++ analyzer tests, formatting, diff checks, all-target/all-feature Clippy, and the complete serial `cargo test --features nlp,python` gate pass locally. Commit, push, clean-head proofs, CI, and issue closure remain pending root work.
- [x] (2026-07-17 23:03Z) Published #909 in `4a376b0b` and closed it after the complete local gates, clean-head Chromium acceptance, and CI all passed. Strict ephemeral records `/tmp/cpp-exact-909-multistep-4a376b0b.jsonl`, `/tmp/cpp-exact-909-result-4a376b0b.jsonl`, and `/tmp/cpp-exact-909-error-callback-4a376b0b.jsonl` pin clean Bifrost `4a376b0b` and clean Chromium `e52675fe`; each has one resolved and consistent site, an exact identical inverse range, and zero missing or actionable findings. CI run `29618734115` passed every Rust, Python, formatting, license, editor, and fixture-provenance job.
- [x] (2026-07-17 23:12Z) Filed #911, assigned only to `jbellis`, and completed its local structured implementation for the independent eight-site family where inverse type usages omit outer prefixes of nested qualifiers. The authoritative `alpha::Outer::Nested::member` plus inline `&Outer::callback` reduction first proved both `Outer` tokens forward-resolve to `alpha.Outer`, then failed inverse lookup with `{}` instead of exact ranges `170..175` and `353..358`; the same-spelled `beta::Outer` control remained outside the expected set. One shared AST component helper now removes the terminal value/member and exposes successive owner prefixes to both ordinary out-of-line owner resolution and target-aware static qualifier resolution. Exact matching component nodes route through `push_type_hit`, so the inline owner reference inside the target class is retained; nested qualified children remain excluded at the caller, each occurrence increments raw-match accounting once, and any ambiguous prefix rejects the whole occurrence. The exact new regression, established nested-type and nested out-of-line controls, all 119 C++ usage-graph tests, all 65 C++ definition tests, formatting, diff checks, all-target/all-feature Clippy, and the complete serial `cargo test --features nlp,python` gate pass locally. Eight final dirty-tree strict ephemeral Chromium records `/tmp/cpp-exact-911-*-dirty.jsonl` pin clean Chromium `e52675fe` and dirty fixing tree `909544ae`; NativeTheme, NewWindowDelegate, ChromeContentRendererClientBrowserTest, both IsolatedWebAppResponseReader sites, FetchUtils, and both HeatmapPalmDetector sites each resolve the intended target with an exact identical inverse range, one consistent classification, and zero missing or actionable findings. The implementation is published at `68438d2e`, and the corresponding `/tmp/cpp-exact-911-*-68438d2e.jsonl` clean-head records repeat all eight exact results with `bifrost_dirty=false` and clean Chromium. CI run `29621829894` passed every Rust, Python, formatting, license, editor, and fixture-provenance job; #911 is closed with assignment only to `jbellis`.
- [x] (2026-07-18 00:23Z) Exhaustively isolated warm-ledger rows 20 and 46 as the next independent family: bare C++ types explicitly imported by ordinary `using ns::Type;` resolve forward but disappear from complete targeted and whole-workspace inverse results. Both rows are genuine omissions with zero wrong-forward, artifact, or unproven members. Fresh duplicate review found no matching C++ issue, so #912 was filed and verified assigned only to `jbellis`. Implement structured lexical ordinary-type imports with source-order, scope, shadowing, ambiguity, declaration-token, direct-use, template-argument, and inverse-path parity coverage before exact Chromium acceptance.
- [x] (2026-07-18 00:53Z) Completed the local structured #912 implementation and audit. One per-file AST import model is initialized once into a shared `OnceLock` when each targeted or whole-workspace scan context is constructed; the bare-type hot loop performs two allocation-free borrowed passes with no cache-map lock or import-record clone. Source byte and AST scope ranges enforce declaration order and block/namespace exit, nearest active scopes hide outer imports, physical declarations with the same kind/FQN collapse, and distinct nearest identities fail closed. The authoritative namespace/block reduction proves declaration-owner, direct, and template hits on both targeted and whole-workspace surfaces while excluding pre-import/global fallback, scope-exit, beta, closer-shadow, and ambiguous tokens. The exact regression, both using-enum lexical controls, all 120 C++ usage-graph tests, and all 28 dedicated whole-workspace C++ graph tests pass with formatting and diff checks clean. Exact Chromium acceptance, publication, CI, and issue closure remain pending root work.
- [x] (2026-07-18 01:18Z) Completed both dirty-tree strict ephemeral Chromium proofs for #912 from the reviewed implementation. `/tmp/cpp-exact-912-bluetooth-dirty.jsonl` resolves five physical declarations as logical `device.BluetoothLocalGattCharacteristic` and returns the identical `77440..77472` inverse range; `/tmp/cpp-exact-912-gpu-context-dirty.jsonl` resolves `blink.GpuBenchmarkingContext` and returns the identical `57774..57796` range. Both pin clean Chromium `e52675fe`, contain one resolved and consistent site, and report zero missing/actionable findings. Formatting, diff checks, all-target/all-feature Clippy, and the complete serial `cargo test --features nlp,python` gate pass. Publication, clean-head repetition, CI, and issue closure remain pending root work.
- [x] (2026-07-18 02:35Z) Published #912 at `2f1583a3` and repeated both strict ephemeral proofs from the clean pushed head. `/tmp/cpp-exact-912-bluetooth-2f1583a3.jsonl` and `/tmp/cpp-exact-912-gpu-context-2f1583a3.jsonl` pin clean Bifrost `2f1583a335157e469c6afa985218d826d8e9d3a4` plus clean Chromium `e52675fe`, preserve the intended forward identities, return the identical `77440..77472` and `57774..57796` inverse ranges, and report zero missing/actionable findings. CI run `29625158821` is green on attempt 2. Attempt 1 was cancelled after the unrelated Windows `scan_usages_location_target_selects_typescript_static_method` test stalled for 37 minutes; its recovered log showed all preceding tests green, and the single-job Windows rerun plus all four downstream Python jobs passed. #912 is closed and remains assigned only to `jbellis`.
- [x] (2026-07-18 02:42Z) Filed #914, assigned only to `jbellis`, for the next independent C++ residual: `PasswordAccessoryController*` calls inherited `AccessoryController::OnOptionSelected` at Chromium bytes `11113..11129`; forward lookup resolves the base declaration while authoritative inverse lookup omits the call. #428 covers direct declaring-class receivers and #837 covers bare inherited members in derived owner contexts, so neither duplicates this explicit-derived-receiver boundary. Generalize the existing structured nearest-member-owner hierarchy walk for explicit receivers and share its unique declaring-owner result across targeted and whole-workspace inverse paths. The faithful `InlineTestProject` reduction must cover pointer/value positives plus direct-base control while rejecting overrides, nearer hiding declarations, unrelated same-name owners, and ambiguous multiple-base paths.
- [x] (2026-07-18 03:06Z) Completed the local structured #914 implementation and audit. The authoritative reduction first proved direct-base, derived-pointer, and derived-value calls all forward-resolve to `demo.Base.run`, then failed both targeted and whole-workspace inverse lookup with only the direct-base range. Explicit receivers now use the same cached structured declaring-member-owner resolution as bare inherited calls before target comparison or edge emission. The first same-name declaration on each base path applies C++ hiding before overload applicability; a unique logical owner is retained, while distinct or repeated base-subobject paths fail closed as ambiguous. Targeted and whole-workspace scans now agree on all three positive ranges and reject overrides, nearer inapplicable hiders, unrelated owners, and ambiguous multiple inheritance. Existing physically non-visible same-FQN receivers remain conservative rather than becoming false negatives. The exact regression, all 121 C++ usage-graph tests, and all 28 dedicated whole-workspace C++ graph tests pass; formatting and diff checks are clean. Exact Chromium acceptance, publication, CI, and issue closure remain pending root work.
- [x] (2026-07-18 03:34Z) Closed the #914 root-review boundary where the generalized helper could reinterpret a bare function identifier as an explicit receiver before implicit-owner lookup. A direct type-name collision was not a valid control because forward lookup intentionally resolves it as a constructor; the faithful replacement declares a visible global `collide` value of `Trap`, where `Trap::collide` competes structurally with direct and inherited bare calls to `Base::collide`. Both calls forward-resolve to `demo.Base.collide`, while the pre-correction targeted inverse returned `{}` instead of both exact ranges. Ordinary calls now enter declaring-owner hierarchy resolution only through a structurally explicit `field_expression`; explicit operator calls still pass their already-extracted receiver, and bare identifiers retain the established enclosing-owner-first fallback. Both exact regressions, all 122 C++ usage-graph tests, and all 28 dedicated whole-workspace C++ graph tests pass in one cleaned isolated target; formatting and diff checks are clean.
- [x] (2026-07-18 03:57Z) Completed root's independent focused validation and dirty-tree Chromium acceptance for #914. A fresh managed isolated target passed all 122 targeted C++ usage-graph tests and all 28 whole-workspace C++ graph tests, then removed itself. The strict ephemeral record `/tmp/cpp-exact-914-on-option-selected-dirty.jsonl` pins dirty Bifrost `e884bafc` and clean Chromium `e52675fe`; bytes `11113..11129` forward-resolve to `AccessoryController.OnOptionSelected(autofill::AccessoryAction)` and return the identical exact inverse range with one consistent classification and zero missing, unproven, inconclusive, or actionable findings. The cold ephemeral workspace phase completed in 563.2 seconds and the record in 583.0 seconds. Complete local gates, publication, clean-head repetition, CI, and issue closure remain pending root work.
- [x] (2026-07-18 04:09Z) Completed #914's full local gates after the exact production proof. `cargo fmt --all -- --check`, `git diff --check`, and a fresh managed `cargo clippy --all-targets --all-features -- -D warnings` pass. The complete serial `UV_CACHE_DIR=/tmp/bifrost-uv-cache cargo test --features nlp,python -- --test-threads=1` suite passes outside the filesystem sandbox, including the formerly flaky TypeScript static-method location test and all changed C++ receiver/self suites; its managed target removed itself. Commit, push, clean-head exact repetition, CI, and issue closure remain pending root work.
- [x] (2026-07-18 04:33Z) Published #914's implementation as `8dbc40fc` directly to `master`, then repeated the exact `OnOptionSelected` Chromium witness from that clean head. `/tmp/cpp-exact-914-on-option-selected-8dbc40fc.jsonl` pins clean Bifrost `8dbc40fc` and clean Chromium `e52675fe`; the sole site at bytes `11113..11129` forward-resolves to `AccessoryController.OnOptionSelected(autofill::AccessoryAction)`, returns the identical exact inverse range, and reports one consistent classification with zero missing, unproven, inconclusive, or actionable findings. The strict ephemeral run completed in 470.1 seconds. CI run `29630172954` passed every formatting, license, VS Code, Rust-platform, and Python-platform job, including x64 Windows Rust in 15m14s without the prior transient stall. The acceptance comment records the implementation, focused and complete local gates, exact production proof, and CI link; #914 is verified closed as completed and remains assigned only to `jbellis`.
- [x] (2026-07-18 05:03Z) Filed #915, assigned only to `jbellis`, after clean-head strict ephemeral artifact `/tmp/cpp-exact-row24-head5ed6e839.jsonl` reproduced the next independent C++ residual at Chromium bytes `67318..67336`: forward lookup resolves the concrete declaration, function-template declaration, and out-of-line definition for `base.HistogramTester.ExpectUniqueSample`, while the complete inverse query returns no hit. Production AST evidence shows the three argument expressions interleaved with two named comment extras, and shared `call_arity` reports five through `named_child_count()`. A faithful scratch project proves targeted and whole-workspace parity: comment-free defaulted-three and exact-four calls are retained, while two-comment variants are omitted with zero unproven hits; wrong-owner remains excluded. Count structured non-extra argument children at one shared C++ seam used by arity plus targeted and forward argument-type extraction, then cover defaulted, exact, variadic, zero/one/two-comment, definition-routing, and wrong-owner behavior without source-text parsing. Closed #869 is the Java analogue; no focused C++ duplicate exists, and unrelated #328 remains skipped because it is assigned to another user.
- [x] (2026-07-18 05:20Z) Completed the local structured #915 implementation and audit. The faithful `InlineTestProject` retains a concrete default-bearing header declaration, matching function-template declaration, and out-of-line definition for `base.HistogramTester.ExpectUniqueSample`. Its pre-fix exact run returned only three of six expected calls: zero-comment defaulted/exact calls and the accidentally arity-compatible one-comment defaulted call; the production-shaped two-comment default plus one/two-comment explicit calls were omitted. One shared direct-child iterator now retains only tree-sitter nodes with `is_named() && !is_extra()`. Shared call arity, targeted argument-type extraction, forward argument-type extraction, and existing constructor/parameter arity counts all use that seam, without source parsing. The regression proves forward three-definition routing and targeted/whole exact parity across defaulted and explicit calls with zero/one/two comments, while retaining genuine over-arity rejection, real-argument variadic minimums, comment-safe known-type overload selection, wrong-owner exclusion, and zero unproven main-query hits. The exact regression, all 123 C++ usage-graph tests, all 28 dedicated whole-workspace C++ graph tests, and all 65 C++-filtered get-definition tests pass in cleaned isolated targets; formatting and diff checks are clean. Chromium acceptance, publication, complete gates, CI, and issue closure remain pending root work.
- [x] (2026-07-18 05:35Z) Completed root review, independent validation, and dirty-tree Chromium acceptance for #915. Review confirmed one structured non-extra direct-child iterator feeds only semantic argument/parameter-list consumers and leaves unrelated AST traversals unchanged. A fresh managed target independently passed the complete 498-test definition suite plus all 123 targeted and 28 whole-workspace C++ graph tests, then removed itself. Strict ephemeral artifact `/tmp/cpp-exact-915-expect-unique-sample-dirty.jsonl` pins dirty Bifrost `76aefd37` and clean Chromium `e52675fe`; the three-target `ExpectUniqueSample` group remains intact, bytes `67318..67336` return the identical exact inverse range, and the sole site is consistent with zero missing, unproven, inconclusive, or actionable findings. The cold workspace completed in 349.4 seconds and the record in 424.1 seconds. Complete Clippy/feature-suite gates, publication, clean-head repetition, CI, and issue closure remain pending root work.
- [x] (2026-07-18 05:44Z) Completed #915's full local acceptance gates. A fresh managed `cargo clippy --all-targets --all-features -- -D warnings` passed and removed `/tmp/bifrost-cargo-target.Jnur05`. The complete serial `UV_CACHE_DIR=/tmp/bifrost-uv-cache cargo test --features nlp,python -- --test-threads=1` suite passed, including the 498-test definition suite, 123 targeted C++ usage-graph tests, 28 whole-workspace C++ graph tests, and all downstream feature-enabled integration suites; its managed target `/tmp/bifrost-cargo-target.WnHdbq` removed itself. Publication, clean-head exact repetition, CI, and issue closure remain pending root work.
- [x] (2026-07-18 06:07Z) Published #915 directly to `master` as `f9c5f7bb` and completed clean-head production and CI acceptance. Strict ephemeral artifact `/tmp/cpp-exact-915-expect-unique-sample-f9c5f7bb.jsonl` pins clean Bifrost `f9c5f7bb` and clean Chromium `e52675fe`; it retains the concrete header declaration, function-template declaration, and out-of-line definition, returns the identical exact inverse range at bytes `67318..67336`, and reports one consistent classification with zero missing, unproven, inconclusive, or actionable findings in 422.8 seconds. CI run `29632813412` passed formatting, licenses, VS Code, fixture provenance, all Rust platforms, and all four downstream Python platforms. The acceptance comment records the red artifact, dirty and clean proofs, focused and complete local gates, and CI link; #915 is verified closed as completed and remains assigned only to `jbellis`.
- [x] (2026-07-18 06:20Z) Filed #916, assigned only to `jbellis`, for the next genuine C++ residual after proving saved rows 25, 31, 35, and 45 were additional #915 comment-arity instances. Chromium `checked_address_range.cc` bytes `2432..2439` forward-resolve to `crashpad.CheckedRange.IsValid`, while targeted inverse returns no hit and whole-workspace inverse finds only self-calls. The faithful `/tmp/bifrost-template-receiver-repro` proves local template receivers and non-template out-of-line field receivers, but a field receiver inside `template <typename T> Owner<T>::Check()` remains unproven on both inverse paths. Declaration persistence records the definition as `demo.Owner<T>.Check` instead of canonical `demo.Owner.Check`, breaking the parent relation required for implicit field typing. Normalize qualified template-id owner components from tree-sitter structure, retain template signature metadata, and cover forward identity, namespace-qualified owners, targeted/whole parity, local/plain controls, wrong-owner exclusion, and zero unproven hits without source-text mini-parsing. Rows 54 and 62 are separate wrong-forward declaration-recovery families; #800/#804 are distinct closed boundaries, and unrelated #328 remains skipped because it is assigned to another user.
- [x] (2026-07-18 07:20Z) Completed the reviewed local #916 implementation and affected validation. `extract_function_info` now decomposes qualified declarator identities iteratively through tree-sitter `scope`, `name`, and `template_type` fields: namespace components remain in the package, canonical owner components use the existing `$` nesting convention without template arguments, and template metadata remains in callable signatures. A scope-less leading-global wrapper is recognized structurally and does not inherit lexical package context. The faithful regression first failed with only the local-template and plain out-of-line controls, omitting all three templated out-of-line field calls; after the fix, targeted and whole-workspace inverse scans agree on local, plain, direct-template, nested-template, namespace-qualified, and leading-global positives, exclude wrong owners, and report zero unproven hits. The mirrored fixture passes `clang++ -std=c++20 -fsyntax-only`. Root review caught and corrected both the leading-global fallback and initially compiler-invalid edge controls, then required the C++-only `templated-out-of-line-owner-identity-2026-07` epoch salt so warm stores cannot reuse stale FQNs. Fresh independent validation passes 42 C++ analyzer tests, the complete 498-test definition suite, 28 whole-workspace C++ graph tests, 124 targeted C++ graph tests, eight epoch/store tests, formatting, and diff checks. Dirty-tree Chromium acceptance, complete gates, publication, CI, and closure remain pending root work.
- [x] (2026-07-18 07:35Z) Completed #916's strict dirty-tree Chromium acceptance with the retained reviewed release binary. `/tmp/cpp-exact-916-checked-range-dirty.jsonl` pins dirty Bifrost `e4e64a6a` and clean Chromium `e52675fe`; bytes `2432..2439` forward-resolve to `crashpad.CheckedRange.IsValid` with template signature `<typename ValueType, typename SizeType = ValueType>() const`, return the identical exact inverse range, and report one consistent classification with zero missing, unproven, inconclusive, or actionable findings. The epoch-refreshed ephemeral workspace completed in 405.9 seconds and the record in 487.1 seconds. Complete Clippy/feature-suite gates, publication, clean-head repetition, CI, and closure remain pending root work.
- [x] (2026-07-18 07:44Z) Completed #916's full local gates. `cargo fmt --all -- --check`, `git diff --check`, and a fresh managed `cargo clippy --all-targets --all-features -- -D warnings` pass. The first serial feature-suite attempt reached 1,030 passing library tests but its three subprocess-backed MCP session tests were denied pipe creation by the filesystem sandbox; the identical escalated `UV_CACHE_DIR=/tmp/bifrost-uv-cache cargo test --features nlp,python -- --test-threads=1` command then passed the complete unit, integration, and doctest suite, and its managed target removed itself. Publication, clean-head exact repetition, CI, and closure remain pending root work.
- [x] (2026-07-18 08:04Z) Published #916 directly to `master` as `2c211a26` and completed clean-head production, CI, and issue acceptance. Strict ephemeral artifact `/tmp/cpp-exact-916-checked-range-2c211a26.jsonl` pins clean Bifrost `2c211a26` and clean Chromium `e52675fe`; bytes `2432..2439` retain forward target `crashpad.CheckedRange.IsValid`, return the identical exact inverse range, and report one consistent classification with zero missing, unproven, inconclusive, or actionable findings in 416.7 seconds. CI run `29636287935` passed formatting, licenses, VS Code, fixture provenance, every Rust platform, and all four downstream Python platforms. The acceptance comment records the red reduction, structural fix, dirty and clean production proofs, complete local gates, and CI; #916 is verified closed as completed and remains assigned only to `jbellis`.
- [x] (2026-07-18 08:23Z) Filed #917, assigned only to `jbellis`, after a fresh exact and comparative read-only audit of the two remaining C++ wrong-forward rows. Strict ephemeral `/tmp/cpp-exact-row54-cdm-92d66cf9.jsonl` pins clean Bifrost `92d66cf9` and Chromium `e52675fe`; `cdm_auxiliary_helper.h` bytes `1853..1879` still forward-resolve the macro-exported class member as flat `media.decoder_bypass_block_count`, whose complete inverse result correctly omits the call and yields one actionable missing classification. Tree-sitter already recovers `media.CdmMetricsData` and correctly owns `IsCdmValueSet`, but its compound body emits direct fields as `declaration` nodes that the visitor hard-codes as non-class declarations. Correct the structured recovered-class container semantics, preserve namespace/global and function-local behavior, prove forward plus targeted/whole inverse parity, and bump the C++ epoch; do not patch inverse matching or add name fallbacks. The separate row-62 artifact `/tmp/cpp-exact-row62-span-92d66cf9.jsonl` is intentionally deferred: it is exact-range consistent only because both directions select the wrong fixed-extent `base.span.first`; the dynamic-extent specialization's true member is ownerless and requires the materially broader partial-specialization plus fragmented-sibling recovery design. Fresh duplicate searches were empty, #800/#804 are distinct closed boundaries, and unrelated open #328 remains skipped because it is assigned to another user.
- [x] (2026-07-18 09:09Z) Completed the reviewed local #917 implementation and affected validation after two root-required corrections. Recovered function-definition-shaped class scopes now mark direct `declaration` nodes as fields; a shared AST predicate follows only the declaration visitor's recovery-transparent access-label, preprocessor, `ERROR`, template, linkage, and declaration-list wrappers and stops at the first genuine compound scope. Both C++ usage binding seeders and the generic C++ lexical prefilter use that same predicate, preventing recovered fields from becoming fake locals while preserving genuine method-local shadowing. The first delegated fixture put the method before its fields and was rejected because it evaded Chromium's local-binding failure; the corrected field-before-method fixture then exposed preprocessor-contained `guarded_count` as `no_definition`, which the final transparent-wrapper design fixes without inverse-target special cases. The faithful regression proves canonical owner/parent identity for plain, initialized, pointer, public-label, and guarded members; no flattened phantom; method ownership; forward and targeted/whole inverse parity for implicit-self and explicit receivers; wrong/global exclusions; and zero unproven hits. Root independently passed fresh 42/498/28/125 affected suites, C++20 syntax, formatting, and diff checks. The C++ epoch salt `macro-exported-class-field-owner-2026-07` invalidates persisted flat FQNs. Dirty-tree Chromium acceptance, complete gates, publication, clean-head proof, CI, and closure remain root-owned.
- [x] (2026-07-18 09:17Z) Completed #917's strict dirty-tree Chromium acceptance with the retained reviewed release binary. `/tmp/cpp-exact-917-cdm-dirty.jsonl` pins dirty Bifrost `60242cb4` and clean Chromium `e52675fe`; `cdm_auxiliary_helper.h` bytes `1853..1879` now forward-resolve to canonical field `media.CdmMetricsData.decoder_bypass_block_count`, return the identical exact inverse range, and report one consistent classification with zero missing, unproven, inconclusive, or actionable findings. The epoch-refreshed workspace completed in 334.4 seconds and the full record in 345.2 seconds. Complete Clippy/feature-suite gates, publication, clean-head repetition, CI, and closure remain pending root work.
- [x] (2026-07-18 09:26Z) Completed #917's full local gates. `cargo fmt --all -- --check`, `git diff --check`, and a fresh managed `cargo clippy --all-targets --all-features -- -D warnings` pass. The complete escalated serial `UV_CACHE_DIR=/tmp/bifrost-uv-cache cargo test --features nlp,python -- --test-threads=1` suite passes all 1,037 library tests, subprocess-backed server tests, the changed 125-test C++ usage suite, every downstream integration suite, and doctests; its managed target removed itself. Publication, clean-head exact repetition, CI, and closure remain pending root work.
- [x] (2026-07-18 09:47Z) Published #917 directly to `master` as `753033ea` and completed clean-head production and CI acceptance. Strict ephemeral artifact `/tmp/cpp-exact-917-cdm-753033ea.jsonl` pins clean Bifrost `753033ea` and clean Chromium `e52675fe`; `cdm_auxiliary_helper.h` bytes `1853..1879` retain canonical field target `media.CdmMetricsData.decoder_bypass_block_count`, return the identical exact inverse range, and report one consistent classification with zero missing, unproven, inconclusive, or actionable findings. The epoch-refreshed workspace completed in 327.7 seconds and the full record in 337.9 seconds. CI run `29639193559` at implementation head `753033ea0263d59ebbf10e7e5b188c8f06926115` passed formatting, licenses, VS Code, fixture provenance, every Rust platform, and all four downstream Python platforms. Acceptance comment and verified single-assignee closure remain root-owned.
- [x] (2026-07-18 09:49Z) Posted #917's acceptance record at https://github.com/BrokkAi/bifrost/issues/917#issuecomment-5010807861 and closed the issue after all gates were green. A fresh GitHub readback verifies state `CLOSED`, reason `COMPLETED`, and exactly one assignee, `jbellis`. The acceptance record ties the original flat-owner failure to the shared structural fix, both root-required regression corrections, affected and complete local gates, dirty and clean exact Chromium proofs, implementation commit, and green CI. The C++ campaign now advances to the distinct row-62 dynamic-specialization/fragmented-sibling recovery family.
- [ ] (2026-07-18 10:05Z) Filed #919, assigned only to `jbellis`, after read-only reduction and root confirmation proved row 62 is a combined partial-specialization owner-and-dispatch defect hidden by differential consistency. Chromium `hrtf_kernel.cc` bytes `4344..4349` use `base::span<float>`, whose defaulted extent selects the dynamic specialization, but forward chooses fixed-extent primary `base.span.first`; the correct dynamic `first` is flattened as `base.first`. Tree-sitter represents `class GSL_POINTER span<ElementType, dynamic_extent, InternalPtrType>` as a `template_declaration` containing a class-typed `function_definition` whose `template_function` declarator retains the specialization pattern but whose body ends at a constructor close; the remaining real members become direct namespace-list siblings through the class close. Faithful clean scratch exact artifact `/tmp/cpp-exact-row62-fragmented-clean-371551c8.jsonl` reproduces the wrong primary target at pushed head `371551c8`. Independent macro-free control `/tmp/cpp-exact-row62-control-clean-371551c8.jsonl` persists primary and specialization owners distinctly yet still dispatches `control::span<int>.first` to the primary, proving collector recovery alone is insufficient. The structural fix must preserve specialization identity and bounded sibling scope, persist template defaults/patterns and parent edges, expand receiver defaults, select the applicable most-specialized owner before member lookup, reject cross-specialization candidates, and bump the C++ epoch without source-text or inverse-target fallbacks. Fresh duplicate searches found none; #917 explicitly deferred this row and #800/#804 are distinct closed boundaries.
- [x] (2026-07-18 12:24Z) Completed the reviewed local #919 implementation and focused acceptance. The C++ collector now recognizes only the class-typed `function_definition` plus `template_function` partial-specialization shape, retains the AST-derived specialization pattern, and recovers both complete macro-decorated and constructor-fragmented bodies from structural closing tokens. A complete class boundary requires a direct `ERROR` whose sole child is `}`, immediately followed by the declaration's direct `;`; the fragmented boundary is the containing declaration container's direct `}` after its recovered siblings. Explicit recovered ranges keep late specialization members inside the class while excluding following namespace declarations. Persisted structured metadata records primary identity, parameter kinds/defaults, and specialization terms through migration 8 with integrity counts and a C++ epoch cutover. Receiver resolution expands merged, alpha-renamed defaults across physical primary redeclarations, selects only a uniquely structurally dominant applicable specialization, and fails closed for equal or incomparable partials. Root review additionally removed outer-template metadata leakage into ordinary nested classes, converted new term construction/matching/substitution to explicit stacks, excluded tree-sitter `comment` nodes that are named but not extra, preserved no-metadata/typedef fallbacks, carried specialization arguments through fields, and stopped the legacy recovered-statement fallback from overwriting an already structured binding. The behavior fixtures prove primary/dynamic dispatch for locals and fields, complete and fragmented ownership/ranges, nested-owner isolation, repeated-parameter dominance, default placement on either primary declaration, comment-bearing arguments, and conservative ambiguity. Oldskool's complete 127-test C++ graph suite plus focused persistence/cache/compiler checks pass; root independently repeated the exact base fixture in a fresh managed target. Dirty-tree Chromium proof and complete gates remain root-owned.
- [x] (2026-07-18 12:35Z) Completed #919's strict dirty-tree Chromium acceptance with the retained reviewed release binary. `/tmp/cpp-exact-919-span-dirty.jsonl` pins dirty Bifrost `240d6585` and clean Chromium `e52675fe`; `hrtf_kernel.cc` bytes `4344..4349` now forward-resolve specifically to `base.span<ElementType, dynamic_extent, InternalPtrType>.first` with signature `<typename ElementType, typename InternalPtrType>(StrictNumeric<size_t>) const`, return the identical exact inverse range, and report one consistent site with zero missing, unproven, inconclusive, or actionable findings. The epoch-refreshed workspace completed in 377.1 seconds and the full record in 452.1 seconds. Complete Clippy/feature-suite gates, publication, clean-head repetition, CI, and closure remain root-owned.
- [x] (2026-07-18 12:58Z) Completed #919's full local gates and corrected the one regression they exposed. Fresh managed `cargo clippy --all-targets --all-features -- -D warnings`, `cargo fmt --all -- --check`, and `git diff --check` pass. The first serial feature-suite attempt reached 1,033 passing library tests before the sandbox denied pipe creation to the same three subprocess-backed MCP session tests seen in earlier campaign legs. The identical unrestricted run then found three existing macro-decorated local receiver tests regressed because the new structured-binding guard treated the macro token as a real type and suppressed the older recovery of the declaration's direct `ERROR` tail. The corrected rule retains recovery only when no structured declarator exists or the declaration has a direct named `ERROR` child; complete structured specialization fields still cannot be overwritten. All four macro-local controls and the #919 exact fixture pass together, and the final unrestricted `UV_CACHE_DIR=/tmp/bifrost-uv-cache cargo test --features nlp,python -- --test-threads=1` run passes the complete unit, integration, and doctest suite with its managed target removed. Publication, clean-head exact repetition, CI, and closure remain root-owned before the mandatory fresh full Chromium N=1.
- [x] (2026-07-18 13:21Z) Published #919 directly to `master` as `4c36b5b8` and completed clean-head production, CI, and issue acceptance. A fresh retained release build produced strict ephemeral artifact `/tmp/cpp-exact-919-span-4c36b5b8.jsonl`, which pins clean Bifrost `4c36b5b86e837da115e86286a7b311dc62361ecc` and clean Chromium `e52675fe3e05dd0e3be9d7e0375240d175ed3db5`. `hrtf_kernel.cc` bytes `4344..4349` resolve specifically to `base.span<ElementType, dynamic_extent, InternalPtrType>.first` with signature `<typename ElementType, typename InternalPtrType>(StrictNumeric<size_t>) const`, and inverse returns the identical exact range. The run completed workspace construction in 370.1 seconds and the record in 444.2 seconds with `consistent=1`, `missing=0`, `unproven=0`, and `inconclusive=0`. CI run `29645378130` passed every Rust and downstream Python platform plus formatting, licenses, VS Code, Android, and fixture provenance. Acceptance evidence was posted, and #919 is verified `CLOSED`/`COMPLETED` with sole assignee `jbellis`. The next action is the mandatory fresh clean full Chromium N=1; no further C++ issue may be derived from the stale pre-#919 residual ledger.
- [x] (2026-07-18 14:10Z) Completed the mandatory fresh clean full Chromium N=1 at `/tmp/cpp-n1-3bee7f5a.jsonl`. Select the record whose status is `completed`: an initial sandboxed attempt appended an expected `engine_error` because persisted migration 8 could not write the external cache, while the identical unrestricted persisted rerun completed normally. The authoritative record pins clean Bifrost `3bee7f5a47051ad8832f813baf535a45c3d9c319` and clean Chromium `e52675fe3e05dd0e3be9d7e0375240d175ed3db5`; it audits 1,000 of 105,604 eligible files, samples 10,000 sites, resolves 4,147 references, queries all configured 1,000 target groups, and reports 1,231 consistent, 24 editor-only, 39 unproven, 8,673 inconclusive, and 33 missing classifications in 2,354.5 seconds. The old 79-site record retains only 18 site keys: 61 disappeared, while 15 newly sampled/resolved keys account for the rest. Relative to the first authoritative Chromium baseline, missing fell `421 -> 79 -> 33`; relative to the prior full run, consistent sites rose by 97 and forward-resolved sites by 67. The sole file error remains the explicit 7.27 MiB source-size exclusion for `normalization_rule.h`. The 39-minute elapsed time includes an 1,189.8-second workspace build, roughly 497 seconds of forward work, and 649.3 seconds for all 1,000 inverse groups; observed RSS stayed below 1 GiB and none of the previously filed multi-GiB or stalled-inverse profiles recurred.
- [x] (2026-07-18 14:18Z) Closed the six retained C++ performance validation issues after the fresh full run crossed each issue's real boundary: #854, #856, #857, #859, #883, and #896. Every issue has a run-specific evidence comment, and a fresh GitHub readback verifies each as `CLOSED`/`COMPLETED` with `jbellis` as its sole assignee. Pushed-head CI run `29646018448` for the #919 closure checkpoint is also fully green. The remaining 33-site correctness audit is read-only and exhaustive; another C++ issue is permitted only for a recurring shared analyzer defect demonstrated by a faithful current-head reduction.
- [x] (2026-07-18 14:42Z) Exhaustively partitioned all 33 fresh C++ missing rows before the final fixing-head rerun. Twelve valid-looking type-reference sites initially formed one signature-divergent redeclaration candidate family: nine `base::span`, two `scoped_refptr`, and one `base::flat_set`. Six further inverse omissions were isolated parser/AST boundaries (two nonterminal `FilePath` qualifier sites, one nested-template `HostPortPair`, one `Member`, one `String`, and one namespace-lost out-of-line constructor qualifier). The other fifteen rows were fully explained non-actionable forward/declaration artifacts: eight wrong-forward or missing-exported-class receiver-family rows, three explicit-specialization declaration names, three explicit-`std::` references forwarded to Abseil aliases, and one self/declaration `ScopedJavaLocalRef`. No row was unexplained. The final #921 rerun below corrects the provisional twelve-site family: ten are the shared defect, while the retained `flat_set` and `scoped_refptr` rows have independent explanations.
- [x] (2026-07-18 14:48Z) Filed #921, verified assignment only to `jbellis`, and delegated the bounded implementation to Oldskool. Strict ephemeral current-head artifact `/tmp/cpp-type-inverse-repro-b8979834.jsonl` uses a clean three-file repository where `dep.Box` is forward-declared as `<typename T, int N = 0>` and defined as `<typename T, int N>`; both qualified consumer sites resolve forward to both physical targets, while inverse queries the complete group and returns `missing=2`, `consistent=0`, and `unproven=0`. The target-aware resolver preserves the active physical target but leaves its compatible sibling as a second logical candidate because `same_logical_symbol` and `TypeScanKey` include textual template signatures, so every spec fails uniqueness. The correction must structurally reconcile compatible primary redeclarations while preserving arity/kind incompatibility, lexical competition, partial/explicit specialization identity, #919 dispatch, and #803 target-order/all-target behavior. Duplicate review excludes #803, #919, #778, and adjacent alias/qualifier issues.
- [x] (2026-07-18 14:55Z) Completed root review, production acceptance, and all local gates for #921. Target-aware type preservation now recognizes two physical units only when persisted structured metadata places both in the same primary family, both have empty specialization patterns, and the existing primary reconciler proves equal parameter count and kinds plus compatible defaults after alpha-renaming. The rule applies before and after structured alias hops; signature-sensitive `TypeScanKey` remains intact so #803 still scans every supplied physical spec, while hit-set merging deduplicates the semantic site. The authoritative inline regression proves both forward targets, qualified and nested-template inverse hits in both target orders, zero unproven hits, kind/arity incompatibility, and partial-specialization separation. Root passed all 128 C++ usage tests, formatting/diff checks, fresh all-target/all-feature Clippy, and the complete unrestricted `nlp,python` suite. Strict dirty-tree Chromium artifact `/tmp/cpp-exact-921-span-dirty.jsonl` pins clean Chromium `e52675fe` and Bifrost base `566d7020`; `iir_filter.h` bytes `923..927` retain all five physical `base.span` targets and change from missing to consistent with a covering inverse hit and zero actionable findings in 379.5 seconds. Publication, clean-head repetition, CI, and closure remain root-owned.
- [x] (2026-07-18 15:16Z) Published #921 directly to `master` as `07ad79f6c8cd807b1a6815fe0648d82d612b8d39` and completed exact production and CI acceptance. Strict ephemeral `/tmp/cpp-exact-921-span-07ad79f6.jsonl` pins clean Bifrost `07ad79f6` and clean Chromium `e52675fe`; `iir_filter.h` bytes `923..927` retains all five physical `base.span` targets, returns a covering inverse range, and reports one consistent site with zero actionable findings in 393.8 seconds. CI run `29649023842` passed all fourteen Rust, Python, formatting, license, VS Code, Android, provenance, OS, and architecture jobs. The acceptance record was posted at https://github.com/BrokkAi/bifrost/issues/921#issuecomment-5011862770; fresh readback verifies `CLOSED`/`COMPLETED` with sole assignee `jbellis`.
- [x] (2026-07-18 15:46Z) Completed the final clean pushed-head Chromium N=1 in `/tmp/cpp-n1-07ad79f6.jsonl`. The record pins clean Bifrost `07ad79f6c8cd807b1a6815fe0648d82d612b8d39` and clean Chromium `e52675fe3e05dd0e3be9d7e0375240d175ed3db5`; it reproduces the 105,604-file inventory, 1,000 audited files, 10,000 sampled sites, 4,147 forward-resolved sites, 1,897 target groups, and all 1,000 inverse queries. Classifications are 1,241 consistent, 24 editor-only, 39 unproven, 8,673 inconclusive, and 23 raw missing in 1,808.4 seconds. Relative to the pre-#921 33-site record, ten sites moved to consistent and no new missing key appeared. The retained `base::flat_set` row is non-actionable because forward merges the real alias with two unreachable clang fixture copies. The retained `scoped_refptr` row is a distinct out-of-line method-parameter type omission. It joins the six already isolated genuine parser/AST boundaries below the campaign ticket threshold; the remaining sixteen rows are invalid-forward or declaration artifacts. Exact terminal accounting is `7 isolated genuine + 16 non-actionable = 23`, with no unexplained row and no recurring unfixed C++ family. Chromium therefore closes at `421 -> 79 -> 33 -> 23` raw missing sites, while the full run crosses every former performance blocker and remains below the post-fix resource boundary.
- [x] Run N=1 for c, cpp, csharp, go, java, js, php, py, rust, scala, and ts.
- [x] Triage every reported inverse disagreement; create GitHub tickets only for genuine analyzer defects.
- [x] Fix, test, push, and close every genuine ticket found by the N=1 campaign.
- [x] (2026-07-18 15:55Z) Completed the compact canonical campaign manifest at `.agents/docs/reference-differential/n1.jsonl` and narrative at `.agents/docs/reference-differential/n1-summary.md`. The report records all eleven selected repositories, final retained counters, runtimes, residual interpretation, and the Chromium convergence decision. Final verification re-read the clean #921 record and full Chromium C++ record, validated the JSONL manifest, retained the complete local and 14-job CI gates, and explicitly documents the two historical Rust/C# telemetry fields whose ephemeral raw records are no longer present. This was the provisional language-level campaign closure; the objective-level audit below subsequently found that explicit Chromium C coverage was still missing.
- [x] (2026-07-18) Reopened the completion audit after comparing the durable report to the original named-repository objective. The N=1 C selector had legitimately chosen `RMerl__asuswrt-merlin.ng`, but that did not prove the separately requested C coverage against `chromium__chromium`; the C++ filter intentionally excludes `.c` files. The clean supplemental pre-fix record `/tmp/chromium-c-n1-92f34460.jsonl` audited 1,000 of 1,556 eligible C files, sampled 10,000 sites, queried all 1,000 configured inverse target groups, and reported 1,385 consistent, two unproven, 8,611 inconclusive, and two raw missing classifications in 346.1 seconds.
- [x] (2026-07-18) Exhaustively reduced both supplemental C rows. `celt_decoder.c` bytes `11576..11577` are the secondary declarator `i` in `int c, i;`: the forward declaration-role predicate inspects only the first declarator and incorrectly falls through to an unrelated indexed field, while inverse correctly omits the declaration token. This is a non-actionable forward/frontier artifact for the inverse campaign. `zip_extra_field.c` bytes `4211..4223` exposed a recurring inverse defect: the unnamed header prototype `_zip_ef_free(zip_extra_field_t*)` persisted as `(zip_extra_field_t)` while its named definition persisted as `(zip_extra_field_t *)`, splitting overload identity and suppressing 21 real calls across three libzip C files. A faithful two-file public `UsageFinder` reduction failed identically; duplicate review excluded #807 and #427, and assigned #923 was filed only after recurrence and root cause were proven.
- [x] (2026-07-18) Completed reviewed #923 implementation and local acceptance. Parameter signature construction now recovers only structured abstract pointer/reference/array/parenthesized/function declarator nodes when the ordinary field is absent, shares suffix construction with the named grammar families, preserves the historical named fallback and `&` versus `&&`, and bumps the C++ epoch so persisted malformed signatures are rebuilt. Root review caught and corrected an early over-narrow named nested-declarator fallback before acceptance. The faithful identity-plus-inverse regression, 42 C++ analyzer tests, 129 C++ usage-graph tests, epoch units, formatting, all-target/all-feature Clippy, and the complete unrestricted `cargo test --features nlp,python -- --test-threads=1` suite all pass.
- [x] (2026-07-18) Published #923 directly to `master` as `4d872c78dc5a68a1213939c1a75393c065fd606a` and completed the clean supplemental Chromium C acceptance record `/tmp/chromium-c-final-4d872c78.jsonl`. The cold epoch rebuild completed in 385.4 seconds and the full record in 512.8 seconds. It reproduces the 1,556-file inventory, 1,000 audited files, 657,658 structured candidates, 10,000 deterministic sites, 3,538 forward resolutions, 1,111 distinct target groups, and all 1,000 inverse queries. Classifications are 1,388 consistent, zero editor-only, zero unproven, 8,611 inconclusive, and one raw missing artifact. `_zip_ef_free` now has matching prototype/definition signatures and the identical exact inverse range `4211..4223`; no new missing family appeared. Acceptance evidence was posted at https://github.com/BrokkAi/bifrost/issues/923#issuecomment-5012385302, and #923 is verified `CLOSED`/`COMPLETED` with sole assignee `jbellis`. Per instruction, closure did not wait for CI.
- [x] (2026-07-18) Corrected the compact manifest and narrative to retain the original eleven-language N=1 selection while adding the explicit supplemental Chromium C row and proof. The objective now has clean pushed-head Chromium records for both C and C++, every recurring genuine issue found is fixed and closed, and every retained row is exhaustively classified.

- [x] (2026-07-17) Exhaustively reconciled the composite Scala top-five evidence. The clean-`d01594d9` IntelliJ, Akka, and ScalaTest records contribute 562 rows: 78 wrong forward, 62 non-acceptance artifacts, and 422 valid-forward inverse candidates. The research-only `a94c31a7` Kyo and Scala3 records contribute 974 byte-sliced rows: 395 wrong forward, 230 import/qualifier/declaration artifacts, and 349 valid-forward inverse candidates. The combined 1,536-row ledger therefore contains 473 wrong-forward identities, 292 non-acceptance artifacts, and 771 valid inverse candidates, split into 391 class, 194 field, and 186 function targets. Exact controls preserve the distinct generic nested-local receiver and union-typed receiver roots. A corrected #908 IntelliJ probe with `max_usages=10` is consistent across the two-call target with zero missing or actionable findings in 18.0s at `/tmp/scala-exact-908-fromresolved-a94c31a7.jsonl`. Issue ownership routing and structured fixes remain before authoritative publication reruns.

- [x] (2026-07-17) Reverified the relevant open and closed issue history after the exhaustive Scala ledger. #661-#664 are reopened; #662-#664 were newly assigned and #661 remains assigned to `jbellis`; all four contain the renewed corpus evidence. No open or closed duplicate covers the structured union receiver boundary, so assigned #913 records the exact Scala3 witness and AST-level acceptance criteria. David-owned #128 remains the explicit skip for the earlier 47 Java-target annotation rows. In the newer wrong-forward ledger, 43 legitimate lexical/owner identities route to #664 while 352 invalid fixture, cross-platform, or cross-project/source-set identities remain documented non-product ambiguity.
- [x] (2026-07-17) Completed the authoritative cold-cache Scala top-five run at clean detached `d7e1b037`. `/mnt/optane/tmp/reference-differential/scala-top5-d7e1b037.jsonl` contains exactly five completed records for IntelliJ Scala, Scala3, Kyo, Akka, and ScalaTest, all with clean Bifrost and repository heads plus zero file errors; wall time was 35m53s and peak RSS 4,746,616 KiB. The artifact SHA-256 is `faa32207c78cca7a0c007c4ed1bf390b46b636dc41ba587adb4de2f2eb2ed1f3`. Its 1,846 raw rows reconcile exactly to 955 owned inverse defects (#661 767, #662 123, #663 65), 91 closed David-owned #128 skips, 288 non-acceptance rows, 298 single wrong-forward rows, and 214 multi-target ambiguities. The primary ledger SHA-256 is `d79470b572a03f5834b5932a9bf4e5935b07a82689f4f51180d3745fcef01a0a`; an independently generated audit agrees after direct-source resolution of its final two disagreements.
- [x] (2026-07-17) Continued the Scala semantic campaign through detached `7cefdca2`. The complete `77dbf045` research corpus contains 1,470 raw rows, all present exactly once in the row ledger and independent audit; subsequent direct-source correction of three Kyo stable-pattern rows and four callable collisions gives 608 legitimate inverse defects (#661 335, #662 124, #663 149), 91 Java #128 skips, 250 non-acceptance rows, 323 wrong-forward/non-product rows, and 198 ambiguities. Structured fixes now make Kyo `Maybe.Absent$`, Akka nested `State.registry`, and Scala3 inherited `IndexedStepperBase.i0` exact-consistent. Corrected #908 witnesses at `7cefdca2` also pass with `max_usages=10`: Scala3 `insertText` in 30.9s and IntelliJ `fromResolved` in 18.7s. All are dirty-plan integration evidence pending clean pushed-head publication proof.
- [x] (2026-07-18) Corrected the final `bafba979` Scala issue boundary after delegated and independent source review. `TastyPrinter.isModifierTag` is visible only through `BestEffortTastyFormat` re-exporting `TastyFormat` wildcard members; direct wildcard imports already work, so the row is not ordinary #663. Exhaustive open-and-closed searches found no duplicate, and new #933 was assigned to `jbellis` before implementation. The immutable v3 ledger `/mnt/optane/tmp/reference-differential/scala-top5-bafba979-corrected-v3-audit.jsonl` (SHA-256 `d639f589c61da6de474f909319702cf7af7683cf4de2d53d2c5176e322c8d88e`) preserves all 1,105 unique audit keys and routes #661 137, #662 31, #663 127, #933 one, #664 48, Java #128 91, non-acceptance 180, and physical source-set/platform ambiguity 490. The 127 #663 rows contain 18 David-owned duplicate skips and 109 owned rows.
- [x] (2026-07-18) Checkpointed the delegated and root-reviewed #933 export-provenance implementation at detached `0350bdbf`. Structured Scala export path/selector facts now survive prepared and legacy cache writes plus point and bulk hydration; the central iterative fixed point propagates original callable identities through renames, exclusions, wildcards, chains, ambiguity, and cycles without alias CodeUnits, source-text parsing, or bulk point hydration. Root review corrected selector-order independence before acceptance. Direct Scala usage 97/97, whole graph 39/39, internal graph 5/5, parser 2/2, store 2/2, cache migration 25/25, compile, all-feature Clippy, formatting, and diff checks pass. `/mnt/optane/tmp/reference-differential/scala-exact-933-export-28422641-dirty.jsonl` is exact-consistent/actionable-zero in 22.0s with SHA-256 `4706072d0f1d2f46b415a6549e958cb326b298109f06f1e86cc22adfecf6c96d`; final closure remains gated on clean pushed-head proof.
- [x] (2026-07-17) At detached `b5b69876`, corrected four audited Scala forward collisions and ten inverse type-role rows. Two Kyo external `Chunk` factory calls now fail closed instead of resolving to an inapplicable instance `apply`; Scala3 universal construction resolves exact synthetic `FunType.FunType` and `PcConvertToNamedLambdaParameters.PcConvertToNamedLambdaParameters` identities. Exact Akka anonymous-mixin and ScalaTest infix-type witnesses are consistent, and both complete Scala graph test binaries pass. Final publication still requires the post-fix clean corpus rather than subtracting these rows from the historical ledger.
- [x] (2026-07-17) Completed the corrected 137-row Scala #662 implementation ledger at detached `220705e5`. The initial `29b2633f` checkpoint covers all 46 rows that remained after earlier qualified/generic/lexical constructor work: exact companion extractors, forward-aligned companion-apply versus universal-constructor precedence, zero-arity `new T:` nodes, rerouted synthetic constructors, and receiver-bound infix calls. Clean production probes found and fixed two nested-owner gaps in `220705e5`: callable return types now resolve through structural declaration owners, and synthetic constructors do not shadow their own bare construction syntax. Direct usage 88/88, whole graph 34/34, internal graph 5/5, MCP 1/1, formatting, diff checks, and all-feature Clippy pass. Clean exacts cover seven role families; the clean nested synthetic and nested apply-projection artifacts have SHA-256 `258125c9a8eb67857f47fb11a0acd1cb072a15c4406c0f1acf3d859c306a03fc` and `ad5064f9808810cf87a75328b31289abc188137d828259b0d686667987590a83`. ScalaTest `PathMessageRecordingNotifier(...)` and Kyo `Maybe(c)` no longer preserve their historical forward targets, so the fresh corpus will reclassify those rows rather than treating them as #662 inverse evidence. The five generated target caches remain recoverable under `cache-backup-29b2633f`.

- [x] (2026-07-18) Completed Scala #663 ordinary-callable resolution at detached `569707ea` after delegated implementation and independent Oldskool review. Structured lexical templates, self types, return-type receiver inference, stable objects, local declaration blockers, and stack-safe duplicate-eliding Scala linearization now cover the 109 owned baseline rows without implementing the 18 David-owned #502/#419/#499 duplicates. Direct Scala usage passes 99/99, whole graph 41/41, internal graph 9/9, syntax 2/2, formatting, diff checks, and all-feature Clippy. Final-diff exact production witnesses for Kyo `Chunk.isEmpty`, Browser stable dispatch, `ByteSink.toArray`, Akka `setHandler`, Akka `systemQueueGet`, and IntelliJ `getSupers` are all consistent with zero missing and zero file errors in 4.9–22.6s; their artifacts and SHA-256 values are recorded in the PHP/Rust/Scala top-five ExecPlan. Issue closure remains gated on clean pushed-head repetition.
- [x] (2026-07-19) Completed the clean detached-`6ee48553` five-repository Scala corpus and exhaustive reconciliation. The corpus artifact `/mnt/optane/tmp/reference-differential/scala-top5-6ee48553.jsonl` has SHA-256 `28316f2017fc80e5b2f43fb981688eba1e6328dc6e7ca6a5d1a9ffba925ad4b5`, five complete clean records, zero file errors, and 930 raw rows. Corrected immutable authority `/mnt/optane/tmp/reference-differential/scala-top5-6ee48553-normalized-authority-v2.jsonl` has SHA-256 `bbb002eb2936ec194012096923ffb468dcc34024f12e1a80d9dd7cbe004e389e` and exact routing #661 80, #662 15, #663 21, #664 54, David-owned skips 105, non-acceptance 144, and ambiguity/non-product 511. Root verification reproduced 930 unique raw/site/ordered-target keys and identical v1/v2 sorted identity hashes.
- [x] (2026-07-19) Completed the delegated, independently reviewed Scala #662 role/physical checkpoint at detached `51119f54`, following parameterized enum (`ae4ae9bc`), pattern-binder timing (`a692906a`), callable-list (`109412ea`), and local-receiver (`f5fb08f7`) checkpoints. Syntax selects ordinary calls, explicit construction, or primary construction before structured shape; exact physical owner identity remains authoritative through companion apply/unapply, method values, return projection, forward definitions, targeted usage, and whole-graph edges. Same-file JVM/JS evidence selects the exact replica, external duplicates fail closed, and ambiguous higher tiers cannot fall through. Root gates pass forward 18/18, targeted 103/103, whole graph 45/45, formatting, diff checks, and all-feature lib Clippy. #661/#664 namespace resolution and file-major performance issue #937 remain before final corpus publication.
- [x] (2026-07-19) Corrected the clean `6ee48553` Scala audit to immutable v3 `/mnt/optane/tmp/reference-differential/scala-top5-6ee48553-normalized-authority-v3.jsonl` (SHA-256 `fdb6c11432c7052df1ddcf3261c59d7a90614a0bbcbc664bcb70b764466974a6`). Direct source review moves two ScalaTest `FuturePropCheckerAssertingImpl.Result` rows from inverse #661 to nearer lexical wrong-forward #664, yielding exact routing #661 78, #662 15, #663 21, #664 56, David-owned skips 105, non-acceptance 144, and ambiguity/non-product 511. All 930 row identities and provenance payloads remain unchanged.
- [x] (2026-07-19) Checkpointed exact unqualified Scala type-namespace resolution at `7d9b0594` and the production-discovered curried anonymous-mixin completion at `7d5ad1ef`. Shared nearest-first resolution retains exact direct/inherited class and type-alias CodeUnits, treats local bindings and physical ambiguity as terminal, and keeps alias Type/Constructor roles separate from term/Apply/Extractor. Source-free bulk alias/ancestor/child/parent projections preserve #908's 132-file zero-point-hydration contract. Root gates pass forward 19/19, targeted 106/106, whole graph 46/46, hierarchy 17/17, internal graph 3/3, analyzer 23/23, imports 6/6, ordered imports 13/13, syntax 7/7, formatting, diff checks, and all-target/all-feature Clippy. Clean `7d5ad1ef` exacts for Kyo `FlowGraph.Builder`, Kyo `Agent.Context`, ScalaTest `InspectorAsserting.Result`, Scala3 `Periods.RunId`, and IntelliJ `ScalaCollectionAccessProblem.Factory` are all actionable-zero in 4.6-15.9s; the top-five plan records their artifact hashes. Remaining #661 roots, #664, #937, final watcher-safe gate, publication corpus, and issue closure still follow.
- [x] (2026-07-19) Checkpointed qualified Scala package/stable-object roots at detached `77c8926b`. Structured resolution carries same-logical physical roots through exact source/parent suffix hops, requires one physical qualified terminal, and rejects inherited lookup only when multiple replicas actually contribute a compatible member; bulk and lazy hierarchy paths share the same logical identity without violating #908's source-free projection contract. The immutable 78-row #661 authority now has 65 covered rows and thirteen residuals: two valid field-qualifier inverse misses, ten multi-physical wrong/ambiguous forward rows, and one unsupported-receiver forward-inconclusive row. Final dirty-tree exacts for Akka `javadsl.Flow` and Scala3 `RB.SetHelper` are consistent at the exact range in 6.65s and 22.46s with SHA-256 `7242aeb2d39ecd69ac4fb8f58493b7c9ed821867f74d4af8d50de384901617b4` and `d8642ef386d9c3706c89b01bec2d48b3d1cbef5ec69b0ae37160fb1035641b1a`. Independent review and root gates pass targeted Scala usage 107/107, whole graph 47/47, forward definitions 20/20, hierarchy 17/17 including the 132-file hydration regression, formatting, diff checks, and all-target/all-feature Clippy.
- [x] (2026-07-19) Completed the frozen authority's 67 valid-forward #661 inverse rows at detached checkpoint `43c13b2d`. Typed-local stable paths resolve exact field terminals; receiver-root fields preserve assignment-refreshed declaration identity and exact ranges; structured field-expression roles prevent preceding string/comment text from fabricating local `var` shadows. Physical receiver replicas and real local/parameter shadows remain conservative. Fresh exact Scala3 `qctx` and IntelliJ `myVarCheckbox` records are actionable-zero in 32.71s and 22.42s with SHA-256 `faba05929d3810cca9ee781435e93b2fdcf42196b05fbe63f5dfb16ee60b40aa` and `13add1a172e031ff30b7c50eacc83a93751985127ba4ce4a15f97d8532c0e476`. Targeted usage 108/108, whole graph 48/48, hierarchy/#908 hydration 17/17, formatting, diff checks, and all-target/all-feature Clippy pass. Ten multi-physical forward rows and one named-argument-poisoned forward row remain outside #661 acceptance.
- [x] (2026-07-19) Checkpointed Scala #664 named-argument binding inference at detached `27c8385f`. A shared structured predicate distinguishes call and explicit-constructor named arguments from lexical assignments, so the first `body = body.body` argument no longer destroys the inferred local receiver before `body.summary`; named-argument LHS ownership and subsequent real assignments remain exact in forward, targeted, and whole-workspace engines. Validation passes forward definitions 21/21, targeted/inverted usage 109/109, whole graph 48/48, formatting, diff checks, and all-target/all-feature Clippy. `/mnt/optane/tmp/reference-differential/scala-exact-664-body-summary-c5fda4b3-final-dirty.jsonl` completed in 30.96s and records one unique `DokkaCommentBody.summary` forward target plus an exact consistent inverse hit at bytes `6975..6982` (SHA-256 `98ef5ce9c77d707ded9f3080cfe1560231af88a6e3f85fae806da583175088ad`). Only the ten multi-physical #664 forward rows remain in the frozen Scala authority.
- [x] (2026-07-19) Completed the frozen 78-row #661 authority through physical-member checkpoint `08a0bfc5` and abstract-accessor checkpoint `bb547f0a`. Exact enclosing/ancestor `CodeUnit` identity selects the JVM `Range` and JS `ArrayBuilder` replicas without path heuristics, preserves same-owner overloads, and fails closed for external physical ambiguity; targeted inverse likewise remains source exact. Six Range/capacity/size rows became consistent immediately. The four remaining `elems` assignment rows were the already-routed #661 abstract-parameterless-contract shape: an exact descendant `var` implements the abstract zero-list method. Target-aware inverse proof now requires structural abstractness, an unapplied bare reference, parser-seeded exact field ownership, and exact stack-safe ancestry, preserving concrete-method, local-shadow, unrelated-field, call, and sibling-replica negatives. All ten production exacts are now consistent with exact inverse ranges in 32.12–34.89s. Forward definitions pass 22/22, targeted usage 111/111, whole graph 48/48, formatting, diff checks, and all-target/all-feature Clippy. Fresh corpus regeneration remains the authority refresh.
- [x] (2026-07-20) Completed and exhaustively reconciled the clean detached-`2aeb9cc4` Scala top-five research corpus. `/mnt/optane/tmp/reference-differential/scala-top5-2aeb9cc4.jsonl` (SHA-256 `22526bd842c628765aaeaf4b99016723a86321b61ab754e445664c48e7979c64`) contains exactly five completed records at Bifrost `2aeb9cc401726743497ac8cdf68eec76f4ed600a`, `bifrost_dirty=false`, and zero file errors; the exact five-job/twenty-four-worker run completed in 5h22m36s with 8,155,740 KiB peak RSS. Corrected immutable audit `/mnt/optane/tmp/reference-differential/scala-top5-2aeb9cc4-missing-audit-corrected-v2.jsonl` (SHA-256 `0ade2e7a11463a79b84b63e0e9ba2415d2ef2ef20d48c512e594c4aa791ff23a`) represents all 658 raw rows exactly once: #661 61, #662 3, #663 8, #664 91, David-owned skips 99, non-acceptance 138, and ambiguity/non-product 258. All 658 source slices are verified; corpus and audit raw/site/ordered-target key sets compare byte-identically. Direct receiver review corrected Kyo `clock.nowMonotonic` and Scala3 `value.show` from v1 inverse routing to #664 wrong-forward identities. The 163 product rows split into 72 inverse and 91 forward defects under already-open issues assigned to `jbellis`. The run measured #937's target-major tail directly: Scala3 took 12,090.4s and IntelliJ 19,351.8s. Structured fixes and final clean pushed-head regeneration remain required before closure.
- [x] (2026-07-20) Accepted and checkpointed the first corrected-v2 residual fixes at `0d6ab222` and `f39a085a`. The inverse type namespace now retains exact package/nested aliases, symbolic aliases, companion-rooted nested types, duplicate-owner singleton identity, and inherited stable extractor fields. Forward lookup now gives explicit singleton imports precedence over wildcard terms, excludes aliases from the term namespace, and applies exact companion/extractor resolution only to parser-proven roles. Independent root validation passes targeted Scala usage 115/115, whole graph 49/49, internal bulk graph 3/3, forward definitions 24/24, formatting, diff checks, and all-target/all-feature Clippy. Corrected-v2's three #662, eight #663, and remaining #664 roots are being handled as separate checkpoints before #937 and final corpus regeneration.
- [x] (2026-07-20) Accepted three more independently reviewed corrected-v2 checkpoints. Detached `d0098819` completes the three #662 root shapes with exact generic-function identifiers, stable enum type/companion role coexistence, and terminal-`CodeUnit` convergence across inherited paths; targeted usage and whole graph pass 118/118 and 50/50. Detached `1c1d9d8f` preserves tree-sitter qualified path focus and resolves only the nonterminal prefix, including exact enclosing `this` ownership, without converting a missing terminal into a parent hit; forward definitions pass 26/26. Detached `5934aecb` makes bare generic applications and parser comments part of the structured callable-shape model, fixing generic-only/contextual dispatch, commented argument arity, and comment-shifted method-value positions while keeping explicit value-list mismatches absent; targeted usage, whole graph, and syntax tests pass 119/119, 51/51, and 7/7. Formatting, diff checks, and all-target/all-feature Clippy pass. Exact production proof and the remaining #663/#664 roots precede #937 and corpus regeneration.
- [x] (2026-07-20) Accepted detached checkpoints `6449f901` and `4a8b73a7` after delegated implementation and independent review. Exact parser-resolved receiver declarations now survive typed binding creation and assignment copies, so three physical same-FQN `RedBlackTree.Tree` owners dispatch only to their own structural children while an external logical receiver remains ambiguous; targeted usage and whole graph pass 120/120 and 52/52. Forward resolution now covers the corrected-v2 authority's 24 non-receiver #664 rows: two `Fields.Pin` exports, twelve absolute/external qualified types, one typed-local member import, five same-file/enclosing types, two implicit `scala.Seq` roles, and two local collisions. Export exclusion/rename, missing external owner/path, exact-versus-ambiguous physical local owners, and lexical controls are regression-covered; forward precedence, scope residual, and IntelliJ suites pass 33/33, 8/8, and 6/6. Two receiver-forward #664 rows and the remaining #663 roots still require structured checkpoints and production proof.
- [x] (2026-07-20) Accepted detached checkpoints `194d8117`, `9ef8b1d7`, and `6a46973f`. Exact receiver ownership, sibling-replica exclusion, callable-shape filtering, and extension fallback complete corrected-v2's 91 #664 forward rows, including Kyo `clock.nowMonotonic` and Scala3 `value.show`. Parser-derived expected function input identities fix Kyo's overloaded `parse` method value before uniqueness and fail closed for unresolved, ambiguous, source-free, wrong-type, wrong-arity, or shadowed-builtin alternatives. Parser-proven default masks then flow onto concrete overrides only through a single exact and fully type-compatible inheritance chain, retaining IntelliJ `ScDocTagValueImpl.getKinds` as the target of its inherited one-argument form. Independent gates pass forward precedence 35/35, forward scope 8/8, IntelliJ forward 6/6, targeted Scala usage 122/122, whole graph 54/54, syntax 7/7, formatting, diff checks, and all-target/all-feature Clippy. Akka's inherited generic `ExtensionId[T].apply` substitution is the sole remaining corrected-v2 #663 root before exact production proof and #937.
- [x] (2026-07-20) Accepted detached `69fa8c8a`, completing corrected-v2 #663. Exact parser-recorded generic owner bindings now flow through inherited factory paths and substitute `ExtensionId[T].apply(...): T` with Akka's concrete `ClusterSharding` `CodeUnit`, enabling the downstream `EntityRef.ask` resolution without changing the inherited callable target. One-hop, multi-hop, qualified, direct-shadowing, non-factory, ambiguity, conflicting-return, and unresolved/mismatched argument boundaries are covered; targeted Scala usage passes 123/123, whole graph 55/55, syntax 7/7, formatting, diff checks, and all-target/all-feature Clippy. All eight #663 and all 91 #664 corrected-v2 product rows now have structured regression coverage pending production exact proof.
- [x] (2026-07-20) Accepted detached `c65bb501`, restoring #908's source-free bulk contract after the expanded inverse type namespace exposed point hydration during shared `ProjectTypes` initialization. The cache now initializes from bulk source-free `FileState` projections, descendant construction seeds and reuses that exact projection, and indexed source remains an on-demand scan input. Hierarchy passes 17/17, analyzer 23/23, internal graph 3/3, and the 132-file regression preserves its one deliberately warmed point entry while proving exactly one bulk projection per file. Targeted Scala usage remains 123/123, whole graph 55/55, formatting, diff checks, checking, and all-target/all-feature Clippy pass. #937 remains before release exacts and definitive corpus regeneration.
- [x] (2026-07-20) Accepted detached `776bff63`, completing assigned Scala performance issue #937's local implementation checkpoint. The inverse engine replaces target-major reparsing with one target catalog and one target-independent structured parse/walk per eligible candidate file, then intersects exact reference roles with exact physical target buckets. The same traversal continues to build whole-workspace edges, while target-source eligibility, overload applicability, hierarchy/import/companion/extension semantics, cancellation, and query-wide usage caps remain covered. A 128-physical-replica integration test restricts the authoritative scope to one file and proves exactly one parse, one walk, and one exact owning bucket. Delegated implementation plus independent root review pass targeted Scala usage 124/124, whole graph 55/55, hierarchy 17/17, focused performance/parity cases, formatting, diff checks, and all-target/all-feature Clippy. Production exact timing, corpus regeneration, and the watcher-safe full gate remain required before push and closure.
- [x] (2026-07-20) Completed and exhaustively reconciled the clean file-major Scala corpus at detached `2fb5a608`. `/mnt/optane/tmp/reference-differential/scala-top5-2fb5a608.jsonl` (SHA-256 `a28a52a20a1e0f637f87767e281281571199a680dee212d4a06092141fa7495e`) contains all five completed repositories, zero file errors, and 1,123 raw rows after a 36m08s five-job/twenty-four-worker run. Immutable exhaustive authority `/mnt/optane/tmp/reference-differential/scala-top5-2fb5a608-oldskool-exhaustive-authority-v2.jsonl` (SHA-256 `bcaefa7b5c2c26c55b6581d9d215822fc5c906c9596c674b2b35146fb1f9b668`) routes #661 478, #662 31, #663 34, #664 40, #937 parity 111, David-owned skips 87, non-acceptance 269, and ambiguity/non-product 73. Root verified every source slice and identity key independently; the 111 #937 parity rows divide into 56 fields, 39 callables, and 16 stable roots, and no new issue family was found.
- [x] (2026-07-20) Accepted Scala parity checkpoints `d7e54d7f` and `ac695041` after delegated implementation, two independent Oldskool reviews, and root corrections. Shared type/term identities now preserve Akka's inherited stable extractor, while file-major scans emit applied fields, stable singleton roots, opaque-companion apply, and dual namespace roles with exact physical ownership. Duplicate replicas, normalized class/companion member collisions, enclosing same-name fields, and inherited trait conflicts fail closed. Targeted Scala usage passes 126/126, whole graph 55/55, hierarchy 17/17, and formatting, diff checks, and all-target/all-feature Clippy pass. Production release exacts for Kyo `Chunk$.empty`, Kyo `Maybe$.apply`, and ScalaTest `Prettifier$.default` are exact-consistent/actionable-zero in 4.9s, 5.3s, and 2.9s with SHA-256 `aefe0cd376e474aec47453b6c79d3965eb171ffc2c8406aefbbb679afeebc691`, `e44e51ec39bc5ecdf5baca08bcafc2077c7b903611766d417a9d3ff62ba02835`, and `85308d429f4851a3702471b2b3345175e5fb5fdade6c9cb9368c707ab40567bd`. Final post-checkpoint corpus regeneration and the watcher-safe full gate remain before publication.
- [x] (2026-07-20) Completed and independently audited the clean post-parity Scala corpus at detached `160cd85d`. `/mnt/optane/tmp/reference-differential/scala-top5-160cd85d.jsonl` (SHA-256 `f29979fa4a0f746ef09dce98c29918c0afe9f1ce68b16a4120fde757331cddc0`) has all five completed repositories, zero file errors, and 1,033 raw rows after 37m34s with five repository jobs and twenty-four workers each. Immutable authority `/mnt/optane/tmp/reference-differential/scala-top5-160cd85d-oldskool-exhaustive-authority.jsonl` (SHA-256 `17708f668d5001bc28b71db5bf23765b7acd7be5bdddb9e16deeaefee9ebdae1`) revalidates every source slice and target file and routes #661 471, #662 17, #663 34, #664 40, #937 parity 42, David-owned skips 87, non-acceptance 268, and ambiguity/non-product 74. Root independently reproduced 1,033 unique raw/site identities and all route counts. The post-`ac695041` pass removed 70 of 111 parity rows, while 41 prior rows plus one newly lost inherited `IterableOnce.iterator` site require a final structured #937 parity checkpoint.
- [x] (2026-07-20) Accepted the final #937 parity checkpoint at detached `9ef1e084` after delegated implementation and independent Oldskool/root review. Exact file-major events now cover stable type roots, contextual and partial calls, synthetic factory projections with direct-companion precedence, generic singleton calls, parser-proven Scala 3 capture receivers, and curried-factory result receivers without weakening physical ambiguity. The fifteen-positive/one-negative matrix, direct Scala usage 127/127, whole graph 55/55, hierarchy 17/17, formatting, diff checks, and all-target/all-feature Clippy pass. Six Kyo/Akka/Scala3/IntelliJ production exacts are actionable-zero; their durable artifact hashes are recorded in the PHP/Rust/Scala top-five plan. Clean corpus proof remains required before closing #937.

## Surprises & Discoveries

- Observation: A completed language-level N=1 matrix does not prove a named-repository requirement when deterministic selection chooses a different repository for that language.
  Evidence: The canonical C record correctly covered RMerl, while Chromium's 1,556 tracked `.c` files were excluded from both that record and the C++ extension filter. The final objective audit, not a test failure, exposed the missing Chromium C leg.

- Observation: Tree-sitter's unnamed C/C++ parameter declarators use distinct abstract node kinds and may not populate the ordinary `declarator` field.
  Evidence: `zipint.h`'s `_zip_ef_free(zip_extra_field_t*)` produced `abstract_pointer_declarator` and lost `*` in its CodeUnit signature, while the named definition retained `(zip_extra_field_t *)`. Extending the structured suffix traversal unified the identities and restored the exact Chromium inverse call.

- Observation: Tree-sitter can represent a comment inside a C++ template argument list as a named node with `is_extra() == false`.
  Evidence: The first structural partial-ordering rerun decoded `chooser<int* /* structural comment */, int>` as three template arguments and failed before dominance. Filtering both grammar extras and explicit `comment` nodes at argument and term-child collection restores the two semantic arguments without source scanning.

- Observation: Macro-decorated complete and fragmented partial specializations can be nested in one malformed compound body, so function-node extent is not a valid recovered class extent.
  Evidence: The full `complete<T*>` control swallowed `after_complete`, the fragmented `span` template, and its later members into one function-shaped body, while the fragmented `span` function node ended at its constructor. The exact `ERROR('}')` plus `;` complete terminator and the containing declaration container's direct fragmented `}` provide bounded class ranges and scope requeueing that keep later namespace declarations outside.

- Observation: Flattening class-owned aliases is destructive even before a resolver query runs.
  Evidence: The #909 inline project declared `UsingOwner::Result` with `using` and `TypedefOwner::Result` with `typedef`, but declaration collection retained only one `api.Result` CodeUnit carrying the `using` signature. The shared flattened identity caused `contains_declaration_identity` to discard the typedef alias, so downstream forward and inverse resolution could not recover owner isolation.

- Observation: Correct nested alias persistence does not by itself make a bare inherited alias visible.
  Evidence: After the first #909 pass removed flattened `updater.Result`, the dirty Chromium proxy witness still forward-resolved bare `Result` to `update_client.CrxInstaller$Result`. The reduced `UpdateServiceProxy : UpdateService` project failed identically because lexical lookup tried the derived owner and namespace, then used a scope-blind terminal-name candidate without traversing the structured base-class graph.

- Observation: Multiple inheritance ambiguity is a property of declarations at the nearest hierarchy level, not of traversal paths.
  Evidence: `/tmp/cpp-exact-909-multistep-phase3-dirty.jsonl` reached the same `ui::test::InteractiveTestApi::MultiStep` alias through more than one Chromium base path and reported forward ambiguity. The reduced diamond proves those paths must collapse by logical declaration identity, while a nearer alias wins over a deeper distinct alias and two distinct aliases introduced at the same nearest level remain honestly ambiguous.

- Observation: A recovered macro-exported class can contain valid type-alias syntax represented by tree-sitter as a generic declaration.
  Evidence: `/tmp/cpp-exact-909-error-callback-phase3-dirty.jsonl` forward-canonicalized Chromium's `BluetoothGattCharacteristicClient::ErrorCallback` to `base.OnceCallback`, but inverse lookup omitted the bare method parameter. The faithful reduction showed the export macro turns the enclosing class into a malformed function-definition shape and represents `using ErrorCallback = ...` as a `declaration` with AST `type`, `declarator`, and `value` fields; declaration collection therefore emitted a namespace Field. Recovering the reserved alias keyword and declarator from those fields retains the class-owned alias, while a nested exported-outer-class control proves indexed owner reconstruction keeps outer-to-inner order.

- Observation: An inverse query can legitimately target an intermediate alias in a longer structured alias chain.
  Evidence: Phase-4 exact records made `Result` and `ErrorCallback` consistent, but `/tmp/cpp-exact-909-multistep-phase4-dirty.jsonl` forward-resolved the bare Chromium site to `internal.InteractiveTestPrivate$MultiStep` and inverse returned no hit. The reduced chain `InteractiveTestApi$MultiStep -> InteractiveTestPrivate$MultiStep -> internal.MultiStep` reproduced the miss: target-preserving resolution retained only a direct candidate target and otherwise canonicalized to the chain endpoint. Walking the structured chain while remembering the requested middle identity recovers both middle and underlying target queries; the existing self-cycle, mutual-cycle, conflicting-alias, and distinct-nearest-base controls remain empty or ambiguous.

- Observation: Resolving only the complete qualifier owner loses valid type references in outer prefixes.
  Evidence: In the #911 reduction, forward lookup resolves `Outer` in both `alpha::Outer::Nested::member` and inline `&Outer::callback` to `alpha.Outer`, but pre-fix authoritative inverse lookup returned no ranges. `static_qualifier_type_scope` removed only the terminal member and attempted the complete remaining owner, which resolves to `alpha.Outer$Nested` for the value expression; it never tested the earlier `alpha::Outer` prefix. Its `push_hit` routing also suppressed the method-value owner because it lies inside the target class declaration. Structured successive-prefix resolution plus `push_type_hit` returns exactly the two alpha owner tokens and excludes the beta control.

- Observation: Sharing an `Arc<OnceLock<_>>` does not make a manual fallible initializer single-build.
  Evidence: `try_global_usage_definition_index_handle` checked `get()`, built outside synchronization, and raced `set()`. Under 120 parallel Java inverse queries, timing emitted build counts 19 through 23 while each worker fetched all 123,932 live persisted declaration rows; peak RSS reached 28.8 GB. A shared initialization mutex with a second check preserves retry-after-error behavior while guaranteeing one successful build across analyzer clones.

- Observation: Java hierarchy construction cannot reuse the generic per-supertype forward resolver at googleapis scale.
  Evidence: The first inverse target spent more than two hours resolving raw superclass/interface names through repeated persisted definition-candidate queries across roughly 199,488 class declarations and 1,424,887 supertype rows. A bounded persisted projection of package, import, and raw-supertype facts builds the same direct-descendant identity graph without hydrating complete file states or issuing point definition lookups.

- Observation: Java targeted receiver return inference was accidentally a workspace-wide operation.
  Evidence: A single `builder.buildPartial` inverse query entered `usage_fact_rows_by_lang_conn` through `TreeSitterAnalyzer::usage_facts_index`, loading declaration and usage facts for the entire repository. The reduced two-file fixture observed the same full declaration scan; exact method candidates and their source AST/ranges contain all evidence needed for return-type inference and reduce that counter to zero.

- Observation: The reference-differential runner could saturate only one core and could not quantify remaining inverse work.
  Evidence: The stopped full Java run accumulated 4,266.65 CPU seconds in 4,521 seconds of wall time, while live inspection showed roughly one saturated worker on a 120-logical-CPU host. Its serial loop over up to 1,000 target groups emitted no progress between the forward phase and final report, so elapsed time and debugger target names could not establish a completion percentage.

- Observation: Differential agreement does not prove that the shared forward identity is semantically correct.
  Evidence: The baseline contained 32 `import logging` qualifier sites whose forward lookup selected a workspace golden `google.cloud.logging` module instead of the standard library. Once Python module qualifiers became legitimate inverse hits, those sites moved from raw missing to consistent because both directions agreed on the same wrong workspace identity. They are not inverse defects, but the result must not be described as forward-correctness proof.

- Observation: Python module bindings need two distinct notions of time.
  Evidence: Top-level defaults and expressions observe the binding visible at their source byte, so a read before a later assignment still refers to an import. A function body executes later and observes the final module binding, so the same textual order cannot be applied inside deferred bodies. The final five `DEFAULT_CLIENT_INFO.protobuf_runtime_version` residuals are after an explicit local reassignment and correctly remain absent from the imported target's inverse result.

- Observation: Python class namespace evaluation extends into method declarations but not method bodies.
  Evidence: Nested-class bare references and a method default such as `host=DEFAULT_HOST` are evaluated while the owner class namespace is active, whereas a bare `DEFAULT_HOST` in the method body does not resolve through the class. The accepted AST containment test covers decorators, annotations, and defaults while retaining the method-body negative.

- Observation: A differential site's display text can be wider than its authoritative focus range.
  Evidence: In the final Go record, 117 raw missing rows displayed selectors such as `types.AttributeValueMemberSS` or `p.options.Limit`, while the recorded byte ranges focused `types`, `options`, or another intermediate component. Comparing those exact source bytes with the forward target identity made every one an incompatible-forward artifact rather than an inverse omission.

- Observation: Go keyed elements require both structural key classification and exact composite-literal owner proof.
  Evidence: Two production struct labels were genuine missing references, but nine same-spelled labels had already resolved forward to unrelated local declarations instead of the external literal owners. The accepted inverse path recognizes only an identifier that is the keyed element's key (including tree-sitter's one-child wrapper), resolves the enclosing composite literal's structured type, and matches the requested field owner. This recovers the two valid sites without converting `keys.MapKey` or bare map keys into field references.

- Observation: The user-referenced `../brokkbench/test.py` does not exist in the current brokkbench checkout or its Git history.
  Evidence: The authoritative current registry is `/home/jonathan/Projects/brokkbench/tasks.py::LANGUAGE_RANKING_NAMES`, whose keys exactly match the eleven `sft-tools-commits` language directories: `c`, `cpp`, `csharp`, `go`, `java`, `js`, `php`, `py`, `rust`, `scala`, and `ts`.

- Observation: The N=1 repositories are exceptionally large, so an unbounded all-target inverse query campaign would become impractical.
  Evidence: Recorded sizes include 49,660,873 LOC for `RMerl__asuswrt-merlin.ng`, 25,845,431 LOC for `chromium__chromium`, and 33,055,102 LOC for `googleapis__google-cloud-java`.

- Observation: Bifrost has no distinct C analyzer.
  Evidence: `src/analyzer/model.rs::Language::Cpp` owns both C and C++ extensions. The engine must preserve corpus label `c` while filtering C seed files and reporting analyzer language `cpp`.

- Observation: Existing semantic-token code already performs the correct structured first half of the audit: iteratively enumerate grammar identifier leaves, subtract structured declaration-name ranges, and batch definition lookup per file.
  Evidence: `src/lsp/handlers/semantic_tokens.rs::reference_candidate_ranges` and `DeclarationNameRangeContext` provide this behavior; the candidate collector should move to an analyzer-owned shared module rather than be duplicated.

- Observation: None of the eleven N=1 clones has an existing Bifrost database, and the clone volume has about 219 GiB free at 94% utilization.
  Evidence: The selected checkouts occupy about 33 GB logically before analyzer caches. Run smaller analyzers first, inspect database/WAL growth and free space after each repository, and stop before free space falls below 100 GiB.

- Observation: A bounded real-repository preflight completed across every analyzer and immediately exercised genuine forward/inverse disagreements without requiring a production trajectory.
  Evidence: With at most 100 sites and 50 target groups per repository, the reviewed pass reported missing sites in C (2), C++ (4), C# (3), Go (4), JavaScript (4), Rust (12), and Scala (1), and none in Java, PHP, Python, or TypeScript. These are validation leads, not tickets, until exact-site reruns confirm them.

- Observation: The semantic-token identifier frontier was structured but narrower than the resolver's accepted syntax.
  Evidence: Existing forward resolvers handle C++ operator/destructor nodes, Rust `self`/`super`/`crate`, and receiver keywords in several languages. The differential now scans a strict superset of the old identifier frontier, while a separate shared helper preserves the LSP's prior token ranges exactly.

- Observation: The first Rust N=1 run found two pre-differential defects, neither of which was SQLite workspace construction.
  Evidence: `biomejs__gritql` completes construction and a one-target run in about 21 seconds even with a 512 KiB main stack. At the 1,000-target campaign budget, lazy `RustUsageIndex` construction repeatedly called `resolve_module_files -> analyzed_live_files -> AnalyzerStore::contains_parsed_blob -> SQLite`, driving roughly 5 GiB RSS. A debugger then captured the actual stack failure as mutual recursion between `rust_collect_binding_type_fqn` and `rust_expression_type_fqn_mode` for a binding considered in scope inside its own initializer. The fix uses a compact 306-file routing projection, a chunked set query over requested `(blob_oid, language)` keys, and iterative binding/re-export traversals. The exact 512 KiB-stack rerun completed in 554.7 seconds with 101,192 KiB peak RSS and no swaps. Filed as #643.

- Observation: The first completed high-budget Rust differential contains a large follow-on triage set, independent of #643.
  Evidence: Both the dirty-worktree validation and committed-HEAD baseline audited all 306 eligible Rust files and 10,000 sampled sites. Forward resolution uniquely resolved 1,549 sites across 784 targets; inverse comparison classified 517 consistent, 41 editor-only, 5 unproven, and 961 missing. Structured triage found 280 nonterminal focus errors, 93 local binding/declaration false resolutions, 73 terminal `Self::item` inverse gaps, and private inherent member gaps. Filed as #644 and #645. The baseline artifact is marked dirty because delegated fixes began while it ran, so the canonical clean record follows those commits.

- Observation: Macro token trees are a distinct structured Rust usage surface, not source-text noise.
  Evidence: The real `json!(..., "path": self.file)` site remained missing after private-member visibility and receiver fixes because `record_token_tree_instance_member_hits` explicitly returned for field targets. Extending its token-node walk to distinguish field access from method calls made the exact corpus site consistent without text search.

- Observation: Correcting receiver focus substantially reduced false inverse misses but exposed the owner-type surface explicitly.
  Evidence: On `03646ad1`, the complete 10,000-site gritql rerun changed forward resolution from 1,549 to 1,348 sites and reduced missing classifications from 961 to 628. All 250 same-file class misses were focused `Self`, `Self::item`, or `self.member` owner tokens; 251 more were cross-file class references. Exact reruns and independent reviews separated these from noisy residual field/function/module collisions.

- Observation: Rust workspace file modules had contradictory declaration and import identities below nested crate `src` roots.
  Evidence: `crates/cli/src/ux.rs` declared `CheckResult` under flattened package `crates.cli.src`, while `use crate::ux::CheckResult` structurally resolved the module as `crates.cli.src.ux`. A multi-file authoritative regression recovered zero of two type annotations before #646. Applying the existing file-module stem rule to nested workspace crates aligns both sides and keeps `lib.rs` at the crate root.

- Observation: Package version alone is insufficient resume identity during a fix campaign.
  Evidence: Analyzer fixes can land without changing `CARGO_PKG_VERSION`; a report produced before the fix would otherwise suppress the required rerun. Repository records and completion keys therefore include the Bifrost source HEAD as well as the target repository HEAD and configuration fingerprint.

- Observation: C# fully qualified method-return sites exposed a focus-range asymmetry rather than a partial-declaration or authoritative-scope failure.
  Evidence: The reduced #698 query received both partial interface declarations and only the consumer candidate, yet initially emitted only the terminal parameter-type identifier. The grammar names method return types `returns`, and the differential had focused `ADDomainServices` inside the qualified return. Recognizing that field and emitting the containing qualified type produced a covering `5642..5712` hit for exact site `5677..5693`.

- Observation: The #698 range/return-role fix removed 83.6% of the C# actionable set and exposed several smaller structured roles.
  Evidence: `/tmp/csharp-n1-698-fixed.jsonl` retained the same 3,269 forward-resolved sites but changed consistent/missing from 598/1,339 to 1,717/220. The largest residual is 73 interface-owner focuses inside explicit interface property and method declarations; other buckets include generic `PropertyT<T>` arguments, `using static` imports, functions, and miscellaneous class roles.

- Observation: The 73-site explicit-interface cluster is independent of partial declaration grouping and path-scoped routing.
  Evidence: Primary and delegated inline regressions both supplied two partial interface declarations to an authoritative one-file query. The only pre-fix hit was the class base-list type. Adding the dedicated tree-sitter `explicit_interface_specifier` role recovered property and method owners, and the exact Azure site became consistent with a covering AST-derived hit.

- Observation: Re-reading analyzer liveness between differential phases can invalidate an otherwise deterministic corpus record.
  Evidence: `/tmp/csharp-n1-701-fixed.jsonl` selected 1,000 audited files and forward-resolved 3,269 sites, but its later inverse path map admitted only 31 target groups. The report silently classified 1,910 sites as having no sampled file and emitted `completed`. #703 carries the authoritative audited `ProjectFile`s across phases and returns an engine error on impossible scope loss.

- Observation: The stable-scope rerun proved #701 removed 76 valid missing classifications and exposed 144 trustworthy residuals.
  Evidence: `/tmp/csharp-n1-703-fixed.jsonl` queried all 1,000 configured target groups with zero candidate-loss notes, producing 1,793 consistent, 40 unproven, 144 missing, and 8,023 inconclusive sites. The largest next cluster is 39 C# generic type arguments inside generated `PropertyT<T>` calls.

- Observation: C# generic method type arguments need a type-role stop at `type_argument_list`, not traversal into the callable `generic_name`.
  Evidence: The primary and delegated authoritative fixtures failed with zero hits before #704 and passed repeated exact positives plus three negative roles afterward. Exact BillingBenefits bytes `4325..4335` became consistent with a full qualified-argument hit.

- Observation: Exact-site mode unnecessarily inventories every analyzed file before selecting its requested path.
  Evidence: The post-#704 exact C# probe audited one file and one site but enumerated 126,829 eligible files and took 1,134.4 seconds. Avoiding this inventory is a campaign-engine performance task separate from analyzer correctness.

- Observation: The full post-#704 record removed the complete generic-argument residual and exposed `using static` as the next C# boundary.
  Evidence: `/tmp/csharp-n1-704-fixed.jsonl` is pinned to pushed `cf2743de`, shares the same repository head, run fingerprint, audited/sample counts, and zero candidate-loss notes with the valid pre-fix record, and reports 1,834 consistent, 40 unproven, 103 missing, and 8,023 inconclusive sites. The missing set is 34 `using static` classes, 34 other class roles, 33 functions, and two fields.

- Observation: The exact-site inventory cost has a direct engine seam independent of analyzer construction.
  Evidence: `run_reference_differential` calls `analyzer.analyzed_files()` before `select_audited_files` applies the exact-path filter. A safe fast path can validate the requested relative `ProjectFile`, apply the same language/test eligibility rules, point-check `is_analyzed`, and then reuse the unchanged semantic pipeline against the full persisted analyzer.

- Observation: The exact-site point lookup removes 23.7% of end-to-end time while exposing persisted analyzer startup as the remaining dominant cost.
  Evidence: `/tmp/csharp-exact-generic-type-705-fixed.jsonl` completed the same consistent BillingBenefits site in 865.2 seconds versus 1,134.4 seconds before #705. Its summary truthfully reports one eligible, audited, sampled, resolved, and consistent item. Full analyzer construction is retained so declarations outside the exact file remain resolvable.

- Observation: The 34-site `using static` cluster spans both inverse recognition and default candidate routing.
  Evidence: Delegated partial-target authoritative fixtures failed because `is_type_reference_node` rejected `qualified_name -> using_directive(static)`. Adding that structured role fixed inverse scanning, but default `UsageFinder` required the same role in persisted type-identifier collection. Negative ordinary/alias/unrelated-static imports pass, and the persisted identifier change requires a C# epoch bump.

- Observation: #706's cache-aware fix is exact-validated on the rebuilt production index.
  Evidence: `/tmp/csharp-exact-using-static-706-fixed.jsonl` reports one consistent MachineLearningServices `PsHelpers` import-type site with a proven hit covering the full `using static` directive. It completed in 831.1 seconds with one truthful eligible/audited file and no actionable finding.

- Observation: The full post-#706 corpus confirms every static-import miss was removed.
  Evidence: `/tmp/csharp-n1-706-fixed.jsonl` is pinned to pushed `2a365fce`, queried all 1,000 target groups with zero scope-loss notes, and reports 1,875 consistent, 40 unproven, 69 missing, and 8,016 inconclusive sites in 1,008.2 seconds. The remaining 34 classes partition into 16 `IAsyncCommandRuntimeExtensions`, 10 `EventData`, seven `JsonArray`, and one `Parameter`; 33 functions and two fields remain.

- Observation: All 33 post-#706 cast-family class misses are the same grammar-field mismatch.
  Evidence: Clean-`v0.8.0` primary and delegated fixtures failed for singular and partial targets because the typed operand is `as_expression.right`, not a field literally named `type`. The shared role fixes authoritative inverse scanning and default persisted routing; unrelated RHS and left-expression negatives remain excluded. `/tmp/csharp-exact-as-type-707-fixed.jsonl` confirms the rebuilt production path is consistent.

- Observation: C# attribute spellings require two independently resolved structural identities, not suffix normalization before lookup.
  Evidence: `/tmp/csharp-exact-attribute-727-parameter.jsonl` completed in 474.9 seconds and reports `[Parameter]` as an unresolved external `ParameterAttribute` boundary instead of resolving to the workspace non-attribute model. `/tmp/csharp-exact-attribute-727-psargument.jsonl` completed in 269.3 seconds and reports the fully qualified `PSArgumentCompleterAttribute` reference consistent with a proven inverse hit covering bytes `5405..5492`.

- Observation: Callable applicability is the largest post-#727 C# correctness boundary and already has a shared persisted representation.
  Evidence: `/tmp/csharp-n1-727-fixed.jsonl` retains seven wrong-forward `IEventListener.Signal` sites plus one sampled optional-argument inverse miss. Read-only reduction found that C# does not populate or consume the existing `CallableArity { required, total, repeated }` metadata used by Scala: forward filtering falls back to inapplicable direct members before extension lookup, while inverse filtering requires exact total arity.

- Observation: Arity intervals establish applicability but cannot rank overlapping C# overloads without argument-type evidence.
  Evidence: delegated .NET 8 reductions prove both counterexamples: `M(object)` loses to `M(string, int = 0)` for a string argument despite the former's exact total, while fixed and `params` overloads exchange precedence depending on whether the final argument is an array. #732 therefore retains every interval-applicable forward overload and leaves return inference untyped when applicable declarations disagree.

- Observation: Static-using extension visibility must come from persisted structured import identities, not a per-click syntax-tree walk.
  Evidence: `/tmp/csharp-exact-callable-732-signal-final.jsonl` rebuilt the C# store under the new epoch and resolved the production `using static ...Runtime.Extensions` owner to seven receiver-compatible overloads with a proven exact inverse hit. Inline coverage also proves same-namespace short targets, project-wide global static usings, external receiver best effort, and rejection of a known incompatible receiver.

- Observation: Filtering a global identifier bucket by static-import visibility cannot call `parent_of` before visibility is known.
  Evidence: Azure PowerShell contains 4,675 static `Signal` declarations. The first #732 full run hydrated same-name declaration files while checking each structural parent and retained enough full file states to reach 17.6 GiB before it was stopped. The fixed lookup unions namespace-filtered identifier candidates with exact persisted `(static owner, member)` candidates, and the inline 256-noise-file query hydrates no more than the consumer and visible declaration.

- Observation: A dotted C# receiver needs value-first, iterative declared-member typing before extension fallback.
  Evidence: The post-visibility corpus resolved `Microsoft.Azure.PowerShell.Cmdlets.StackHCIVM.Module.Instance.Signal(...)` to 25 visible but arity-inapplicable extensions because the receiver typer did not follow the fully qualified `Module.Instance` property's declared type. The fixed exact rerun resolves only `Module.Signal(string, CancellationToken, Func<EventArgs>, SignalDelegate, InvocationInfo, string, string, string, Exception)` and retains the exact inverse range. Inline negatives preserve unresolved local shadowing and nearest hidden-property semantics.

- Observation: C# unqualified method-group arguments were asymmetric between forward and inverse resolution.
  Evidence: Forward lookup treats a bare value-position identifier as an implicit-`this` member reference, but both inverse scanners previously accepted a bare method only when it was the direct callee of an invocation. The reduced `Accept(onDefault)` regression therefore resolved the unique private method forward and returned no inverse hit. The shared structured classifier now recognizes transparent wrappers, rejects declarations, types, named-argument labels, `nameof`, and lexical shadows, and the exact Azure site is consistent.

- Observation: Precise C# receiver typing did not imply precise declaring-member identity across inheritance.
  Evidence: `this.PrintSdkExceptionDetail(ex)` typed its receiver as the derived Azure cmdlet, but targeted inverse scanning accepted only exact equality with the base declaration owner and the whole-workspace graph synthesized `Derived.PrintSdkExceptionDetail`. The shared nearest-level walk now reaches the indexed base declaration, stops at derived hiding members even when their arity is inapplicable, unions partial type parts at every level, and preserves metadata arity when generic and nongeneric owners share a normalized name.

- Observation: C# method generic arity is a declaration and lookup identity dimension distinct from value-argument arity.
  Evidence: `RunScript<T>(CommandInvocation, string)` and its nongeneric sibling have the same source-facing FQN and value-parameter shape. Before #739 their internal signature keys collided and generic calls stopped at `generic_name` without carrying metadata arity into inverse matching. The reduced regression and production site now exercise one shared generic-arity selector across forward lookup, targeted inverse scanning, whole-workspace edges, extension lookup, hiding, and return inference.

- Observation: C# extension visibility is lexical, not merely a property of the enclosing namespace FQN.
  Evidence: Same-FQN sibling namespace blocks can have different local usings; file-scoped namespaces distinguish pre-marker compilation-unit usings from post-marker namespace usings; qualified namespaces expose syntactic parent frames; and global usings apply across files. The #739 tests prove these scopes independently and require cache keys to retain the exact AST frame range so sibling blocks cannot leak candidates.

- Observation: A C# type AST should compare logical partial identity, but source expressions must still honor value bindings before visible type names.
  Evidence: The #740 initializer `new HttpPipeline { pipeline = this.pipeline }` resolved its type to a different physical partial declaration than the target field owner, so physical `CodeUnit` equality dropped the label. Broadly replacing physical equality with FQN equality created a false hit when a PascalCase parameter shadowed the same-spelled partial type. The fixed path applies logical identity only to the object creation's structured type node.

- Observation: Null-conditional C# member bindings need receiver classification before unqualified-member fallback.
  Evidence: In three Azure serializers, `(((object)value)?.ToString())` is a `conditional_access_expression` with a `member_binding_expression`. Forward lookup did not climb that structure, treated `ToString` as unqualified, and selected the containing partial model's override even though the explicit cast makes the semantic target external `System.Object.ToString`. Inverse analysis correctly omitted those sites for the model targets.

- Observation: C# `nameof` is an intentional editor-only reference surface in this campaign.
  Evidence: Forward definition lookup resolves `nameof(field)` for navigation, while inverse runtime usage tests deliberately exclude both bare and qualified `nameof` arguments. The differential engine currently samples the identifier and reports a false missing classification; the symbols analyzer behavior should remain unchanged.

## Decision Log

- Decision: Preserve the original RMerl C N=1 record and add Chromium C as an explicit supplemental record rather than replacing history.
  Rationale: RMerl remains the deterministic largest-repository selection and its campaign findings are valid, but the user separately named Chromium for both C and C++. Recording both makes the selection rule and the objective-level exception auditable.
  Date/Author: 2026-07-18 / Codex

- Decision: Treat the secondary `i` declarator as a retained forward/frontier artifact and file only the recurring unnamed-abstract-declarator inverse defect.
  Rationale: The `i` token is a declaration, not a reference; inverse behavior is correct, and broadening this campaign into a forward-only declaration-role cleanup would not change the terminal inverse-defect count. The `_zip_ef_free` split suppressed 21 real calls and reproduced through the public symbols path, so it crossed the filing threshold as #923.
  Date/Author: 2026-07-18 / Codex

- Decision: Select C++ partial specializations by structured dominance, never by a scalar specificity score.
  Rationale: A score would incorrectly choose between incomparable patterns such as `<T*, U>` and `<T, int>` for `<int*, int>`. AST-term unification first filters applicable candidates; one candidate is selected only when its pattern is strictly more specialized than every other distinct applicable pattern. Repeated constraints such as `<T*, T>` therefore beat `<T*, U>`, while equal and cross-shaped candidates fail closed. Physical primary redeclarations are reconciled by parameter position and kind with alpha-renamed, structurally equal defaults so selection is deterministic across declaration order and warm persistence.
  Date/Author: 2026-07-18 / Codex

- Decision: Model recovered macro-exported class bodies as a structured declaration scope shared by declaration collection and every C++ local-binding consumer.
  Rationale: Tree-sitter represents these class bodies as compound statements and their members as ordinary declarations, including behind access labels and preprocessor wrappers. Correcting only persisted FQNs leaves the same member declarations seeded as fake locals in forward and inverse resolution. A shared ancestry predicate that mirrors the declaration visitor's transparent wrappers, stops at genuine compound scopes, and is consumed by both graph seeders plus lexical lookup keeps class fields and method locals semantically distinct without source-text or target-specific fallbacks.
  Date/Author: 2026-07-18 / Codex

- Decision: Derive templated C++ out-of-line callable owners from qualified-declarator AST components and keep template arguments out of persisted declaration identity.
  Rationale: A definition such as `Owner<T>::Check` belongs to the canonical class `Owner`; persisting `Owner<T>` as the owner disconnects the definition from its class and prevents implicit field typing. Iteratively reading tree-sitter `scope`, `name`, and `template_type` nodes preserves namespace and nested-owner structure without source splitting, while callable signatures retain the template metadata needed for overload identity. Leading-global wrappers are a distinct AST shape and bypass lexical package context. Because the corrected FQN is persisted, a C++-local analysis epoch cutover is required for warm stores.
  Date/Author: 2026-07-18 / Codex

- Decision: Define C++ positional arguments as direct tree-sitter children that are both named and non-extra, and share that structured iterator across arity and argument-type consumers.
  Rationale: Comments are named extra nodes in C++ `argument_list` trees but are not positional expressions. Counting them inflates defaulted, exact, and variadic arity, while typing them inserts fake unknown positions that defeats overload narrowing. A direct-child iterator preserves nested expression boundaries and existing constructor/initializer shapes, excludes every grammar extra without naming comment kinds, and keeps targeted inverse, whole-workspace inverse, and forward definition lookup on the same argument sequence without source-text parsing.
  Date/Author: 2026-07-18 / Codex

- Decision: Resolve the unique C++ declaring member owner from an explicit receiver's structured type hierarchy before comparing a targeted method or emitting a whole-workspace call edge.
  Rationale: A derived receiver does not physically own an inherited method. The first same-name declaration on each structured base path implements C++ hiding before callable applicability is considered; equal physical copies collapse by logical identity, while distinct declarations or repeated base-subobject paths are ambiguous. Sharing and caching this owner resolution keeps targeted and inverted analysis aligned without source scanning or repeated hierarchy walks, while preserving conservative treatment of physically non-visible same-FQN owners. The hierarchy walk applies only when the AST provides an explicit receiver; a bare function identifier must reach enclosing-owner lookup before same-named types or values can be considered as receiver candidates.
  Date/Author: 2026-07-18 / Codex

- Decision: Model ordinary C++ type using-declarations as a source-ordered per-file AST scope index shared by targeted and whole-workspace scans.
  Rationale: Unqualified lookup precedence is `closer lexical declaration > nearest active import > outer/global fallback`. A declaration imported with `using ns::Type;` must therefore beat the resolver's scope-blind terminal fallback, while a type declared in a more deeply nested named scope still wins. Imports are active only after their declaration and within their AST scope; distinct logical identities at the nearest scope are ambiguous, while duplicate physical declarations with the same kind and FQN represent one import. Initializing one shared cell per scan context keeps per-node resolution lock-free, borrowed, and allocation-free without source scanning.
  Date/Author: 2026-07-18 / Codex

- Decision: Persist C++ aliases declared inside a class with the same `Parent$Nested` short-name convention used by nested class-like declarations, and attach them through the existing parent-child relation.
  Rationale: A class-owned alias is a member of its enclosing type, not its namespace. Reusing the established nested-type identity preserves distinct same-named aliases, gives forward and inverse resolution the structured owner edge they already understand, and avoids introducing an alias-specific naming or text-resolution path. Namespace-owned aliases continue to attach to their module as other namespace declarations do.
  Date/Author: 2026-07-17 / Codex

- Decision: Resolve a bare C++ type through direct and inherited enclosing-class tiers inside the shared visibility index, and expose that same structured resolution to forward definition lookup.
  Rationale: C++ makes a base class's nested type visible in a derived class. A breadth-first `TypeHierarchyProvider` walk recovers that language rule without source scanning and applies hiding at the globally nearest hierarchy level. Repeated paths to the same logical declaration collapse, while distinct declarations at that level return ambiguity instead of choosing an unrelated visible terminal name. Separate canonical, alias-preserving forward, and direct-target inverse modes retain the established alias behavior on one common tier walk.
  Date/Author: 2026-07-17 / Codex

- Decision: Recover type aliases inside malformed macro-exported C++ classes from tree-sitter declaration fields, and reconstruct only that recovered class shape's lexical owner chain from indexed parent relations.
  Rationale: The parser still supplies structured `type`, `declarator`, and `value` fields even when an export macro prevents `alias_declaration` classification. Recognizing the reserved `using`/`typedef` AST type token preserves the same alias representation as well-formed syntax without scanning or reparsing source text. Restricting indexed scope reconstruction to the malformed class-as-function shape avoids duplicate enclosing-unit queries on ordinary references, while indexed parent relations restore outer recovered owners that cannot appear in the local AST ancestor chain.
  Date/Author: 2026-07-17 / Codex

- Decision: Preserve a requested C++ alias target when it appears at any depth of an acyclic structured alias chain.
  Rationale: Forward definition lookup may stop at an indexed intermediate alias even when another alias points to it and it points onward. Inverse lookup must therefore prove that the source candidate's structured chain contains the exact requested identity, not require that identity to be the first candidate or canonical endpoint. The walk continues after a match so self and mutual cycles still fail closed; unresolved steps before a match fail, and distinct resolved candidate outcomes remain ambiguous.
  Date/Author: 2026-07-17 / Codex

- Decision: Resolve C++ type owners in value/member qualifiers one structured component prefix at a time and emit the exact matched component as a type hit.
  Rationale: Every type prefix in `Outer::Nested::member` and `&Outer::callback` is an independently navigable type reference. Reusing one tree-sitter component-node decomposition with out-of-line member definitions avoids source parsing and preserves exact ranges. Target-aware lexical resolution at each successive prefix maintains namespace, global qualification, alias, and ambiguity rules; `push_type_hit` deliberately retains references nested inside the queried class declaration, while ordinary member/value tokens remain excluded.
  Date/Author: 2026-07-17 / Codex

- Decision: Implement a library-owned engine plus a dedicated Rust binary, not a unit test or brokkbench production trajectory.
  Rationale: The engine needs direct access to structured definition and usage internals, must run independently over large clones, and must emit durable campaign artifacts even when interrupted.
  Date/Author: 2026-07-12 / Codex

- Decision: Use grammar-derived identifier leaves from real source, with structured declaration-name exclusion, rather than text search or generated programs.
  Rationale: This uses the user's real-world corpus and respects the repository rule against string-scanning substitutes for analyzer structure.
  Date/Author: 2026-07-12 / Codex

- Decision: Deterministically hash-sample eligible file paths before parsing, then hash-sample reference sites by repository-relative path and byte range, group resolved sites by exact declaration set, and run the inverse resolver once per group.
  Rationale: Path-order truncation would bias large repositories toward a few directories, while parsing all 170,000 matching files in the largest C checkout merely to select 10,000 sites would make sampling the bottleneck. Two-stage stable sampling bounds parsing while preserving repository-wide path coverage; reports retain total eligible files and audited files so coverage is explicit.
  Date/Author: 2026-07-12 / Codex

- Decision: Restrict each inverse query to the files containing sampled forward references for that declaration and mark the scope authoritative.
  Rationale: The differential asks whether inverse resolution can recover already-known sites, not whether candidate discovery can rediscover the whole workspace. This isolates semantic disagreement and prevents whole-repository candidate enumeration per target.
  Date/Author: 2026-07-12 / Codex

- Decision: Apply a second deterministic cap of 1,000 unique target groups after forward resolution.
  Rationale: Ten thousand sampled sites can approach ten thousand unique inverse queries. A target-group cap retains wider forward coverage and makes omitted sites explicitly inconclusive without allowing the inverse phase to dominate the campaign.
  Date/Author: 2026-07-12 / Codex

- Decision: Treat ambiguous forward results, inverse failures, call-site caps, and truncated unproven samples as inconclusive rather than defects.
  Rationale: Only a unique forward declaration coupled with a complete inverse answer can prove a contradiction. The report must separate unsupported or bounded work from actionable missing references.
  Date/Author: 2026-07-12 / Codex

- Decision: Key resumable records by Bifrost source HEAD in addition to target HEAD and run configuration.
  Rationale: Every landed analyzer fix must invalidate earlier evidence automatically even when the package version and corpus checkout are unchanged.
  Date/Author: 2026-07-12 / Codex

- Decision: Differential inverse scope is the stable audited file set chosen before sampling; it must not be reconstructed from a later analyzer liveness snapshot.
  Rationale: The experiment compares forward and inverse answers over one fixed sample. Allowing candidate identity to drift mid-run creates false inconclusive results and can make an invalid partial record look complete.
  Date/Author: 2026-07-12 / Codex

- Decision: Treat the symbols MCP toolset as the production acceptance surface for corpus findings; LSP coverage is incidental and must not expand campaign scope.
  Rationale: The campaign exists to improve `get_definitions_by_location`, `get_definitions_by_reference`, usage scanning, symbol sources, and related symbols tools. Their shared resolver may also improve LSP behavior, but LSP-only discrepancies are outside this campaign.
  Date/Author: 2026-07-12 / Codex

- Decision: Resolve indexed Rust path dependencies through one cached Cargo route index shared by forward module lookup and the inverse usage index.
  Rationale: Per-reference manifest parsing would be a hot-path regression, while a forward-only filesystem fallback would recreate definition/usage disagreement. The compact route index parses indexed manifests once, preserves importer-scoped aliases and library names, and refuses to guess registry dependencies from coincidental workspace names.
  Date/Author: 2026-07-12 / Codex

- Decision: Use persisted callable arity only to reject inapplicable C# declarations; do not rank overlapping intervals without structured argument-type evidence.
  Rationale: Required/total/repeated counts can prove that a declaration cannot accept a call, but C# overload precedence also depends on conversions and normal versus expanded `params` form. Preserving grouped candidates and declining conflicting return inference is safer than introducing wrong definitions or receiver types.
  Date/Author: 2026-07-13 / Codex

- Decision: Construct the C# extension visibility set from persisted namespace and exact static-owner indexes before hydrating semantic declaration details.
  Rationale: Namespace identity and resolved static-import owner identity are already persisted and sufficient to exclude workspace-global same-name declarations. Parent/signature hydration belongs only after that bounded visibility union, preserving structured resolution without global fanout.
  Date/Author: 2026-07-13 / Codex

- Decision: Prove an unqualified C# method-group inverse hit only when structured lexical and owner lookup leaves one callable declaration; retain overloaded groups as unproven until delegate-parameter typing exists.
  Rationale: A bare argument has no invocation arity of its own, and borrowing the containing call's argument count would be incorrect. AST context, lexical shadowing, and nearest-owner hierarchy lookup can prove a unique member without guessing overload applicability.
  Date/Author: 2026-07-13 / Codex

- Decision: Resolve ordinary C# inverse member ownership at the nearest hierarchy level that declares the exact name, expanding every logical partial type before descending.
  Rationale: Accepting any ancestor would misattribute hidden members, while flattening the hierarchy would lose the level where C# name hiding becomes decisive. Callable arity filters candidates only after the declaring level is fixed; an inapplicable nearer declaration must not reopen a base member. Per-file caches keep the structured breadth-first lookup bounded in both targeted and whole-workspace scans.
  Date/Author: 2026-07-13 / Codex

- Decision: Encode C# method generic arity in the structural signature key while retaining the source-facing FQN, and apply an explicit call-site arity before value applicability and hiding.
  Rationale: Metadata arity distinguishes declarations that C# permits to share the same name and value-parameter shape. An explicit `M<T>` call can reject a wrong-arity declaration structurally, while a call without explicit type arguments must leave generic inference eligible rather than inventing a zero-arity constraint.
  Date/Author: 2026-07-14 / Codex

- Decision: Carry generic call arguments from tree-sitter nodes and persist only an AST-proven bare method-type-parameter return marker for substitution.
  Rationale: Exact `T` returns can be substituted from the explicit call arguments, while `T[]`, `T?`, and constructed forms require their existing structured return facts and must not be treated as bare `T`. Persisting this fact avoids source mini-parsing and supports reopened analyzers; changing shared bincode metadata requires a global store-epoch bump rather than relying on serde defaults.
  Date/Author: 2026-07-14 / Codex

- Decision: Derive C# extension visibility from ordered AST lexical frames and share it across forward, targeted inverse, and whole-workspace resolution.
  Rationale: Visibility must respect namespace-block range, file-scoped pre/post partitions, qualified parents, static aliases, and global usings. A common structured candidate set preserves instance-member precedence and prevents both false inverse hits and cache leakage between same-named namespace blocks.
  Date/Author: 2026-07-14 / Codex

- Decision: Use logical C# type identity only after structured syntax proves a type context; do not widen pre-binding expression receivers from physical declarations to logical FQNs.
  Rationale: Partial declarations represent one semantic type, so an object creation's type node must match every physical part with the same exact FQN and metadata arity. Ordinary receivers can be locals, parameters, or fields whose spelling resembles a type; value bindings must win before static type lookup.
  Date/Author: 2026-07-14 / Codex

- Decision: Model Go composite-literal field labels through tree-sitter keyed-element and type nodes, with exact target-owner agreement, instead of source punctuation or name-only fallback.
  Rationale: A colon does not distinguish a struct field label from a map key, and a matching name does not prove the forward declaration's owner. Structured key containment plus composite type resolution provides the inverse evidence while preserving qualified map expressions and honest unresolved cases.
  Date/Author: 2026-07-15 / Codex

- Decision: Exclude the final 126 Go raw misses from the genuine-defect count because their forward identities fail the campaign's semantic-validity prerequisite.
  Rationale: The 117 range/target mismatches do not ask the inverse resolver for the focused token's identity, and the nine exact-spelling sites point to declarations owned by a different type than the literal being initialized. Fixing the inverse resolver to return those targets would create false references.
  Date/Author: 2026-07-15 / Codex

- Decision: Reuse the immutable Python usage index for importer candidate seeding and module/re-export canonicalization.
  Rationale: The index already owns structured module routes and import edges for the analyzed generation. Retaining its module map and reverse importer projection expands only files proven relevant before the usage cap, avoids per-target transitive parsing, and lets namespace aliases resolve to the canonical declaration without a text-search fallback.
  Date/Author: 2026-07-15 / Codex

- Decision: Model Python top-level bindings as AST-derived source-ordered events, with deferred function bodies evaluated against the final module state.
  Rationale: Function-wide shadow sets incorrectly erased valid reads before a later assignment, while applying source order blindly inside function bodies would keep imports that are replaced before the function executes. Assignment visibility begins after its right-hand side, and the target's own module definition is not treated as a shadow of itself.
  Date/Author: 2026-07-15 / Codex

- Decision: Exclude the final six Python raw misses from the genuine-defect count because their forward identities are semantically incompatible with the source site.
  Rationale: `result` at the BigFrames site is an `ibis` table rather than `UnorderedIR`, and every residual `DEFAULT_CLIENT_INFO` receiver is a newly assigned local `ClientInfo` rather than the earlier imported transport field. Returning those imported or class-owned targets from inverse usage would manufacture false references.
  Date/Author: 2026-07-15 / Codex

- Decision: Construct the Java descendant index from a class-only declaration inventory and bounded persisted package/import/raw-supertype projections.
  Rationale: Java's forward type resolver is designed for one focused reference, not millions of hierarchy edges. Replaying its structured resolution order over exact FQN buckets preserves explicit-import, wildcard-import, same-package, nested-type, and same-source identity semantics while removing per-edge SQLite and full-file hydration amplification.
  Date/Author: 2026-07-16 / Codex

- Decision: Gate Java type-target resolution by the structured AST terminal name before querying definitions, and do not infer receiver/value types when the requested target is itself a type.
  Rationale: An annotation whose terminal identifier differs from the requested type cannot match. Applying that proof before SQL preserves Java lexical-shadow and owner semantics while preventing unrelated type annotations from multiplying resolver work; type targets do not need the method/field receiver inference used for value members.
  Date/Author: 2026-07-16 / Codex

- Decision: Infer Java targeted method-call return types from exact candidate declarations and structured per-file AST/range caches rather than `UsageFactsIndex`.
  Rationale: A targeted query already has the candidate method identity, and its declaration source contains the return type. Loading all workspace declaration and usage facts to answer that local question adds unrelated work and memory without improving proof; exact declaration ambiguity still fails closed.
  Date/Author: 2026-07-16 / Codex

- Decision: Resolve independent forward file batches and inverse target groups through one dedicated bounded Rayon pool, then merge forward results in stable file order and serialize only inverse record classification and progress publication.
  Rationale: Forward batches own disjoint source files, while resolved target groups partition sampled site indexes; both expensive operations can run concurrently against the analyzer's reference-counted query scope. Indexed parallel collection followed by local-index rebasing preserves deterministic forward record order. Applying each completed inverse result under a short lock avoids retaining every potentially large query result until the slowest worker finishes; separate progress locks keep completed counts monotonic. An explicit `--jobs` value is serialized into the report and run fingerprint, preserving reproducibility while the default cap of eight limits the measured memory tradeoff.
  Date/Author: 2026-07-16 / Codex

- Decision: Keep ordinary Java forward type lookup on the bounded persisted query, but let Java usage-graph resolution reuse its existing immutable global definition index and serialize lazy index initialization across query clones.
  Rationale: Public focused lookups should not materialize a workspace index, while the usage graph already requires that index for hierarchy and owner proof. Re-querying SQLite for AST type nodes duplicates data already owned by the graph. The measured failure was not evidence that Rust materialization is universally superior to SQLite; it was a racy initializer rebuilding the same index per worker. One shared fallible build removed duplicate full scans and memory amplification without caching stale-generation failures.
  Date/Author: 2026-07-16 / Codex

## Outcomes & Retrospective

The C++ Chromium leg is complete at clean pushed head `07ad79f6`. Across authoritative full runs, raw missing classifications fell `421 -> 79 -> 33 -> 23` while every configured 1,000-target inverse phase completed. The final record contains 1,241 consistent, 24 editor-only, 39 unproven, 8,673 inconclusive, and 23 exhaustively accounted raw missing sites: seven isolated genuine parser/AST omissions below the campaign ticket threshold and sixteen invalid-forward or declaration artifacts. No recurring genuine C++ family remains. The final shared redeclaration fix #921 has behavior coverage, exact production proof, complete local gates, green 14-job CI, and verified closure; all retained C++ performance-validation issues are also closed after the final full runs crossed their boundaries.

The supplemental C-on-Chromium leg is complete at clean pushed head `4d872c78`. Its pre-fix two-row raw set contained one declaration/frontier artifact and one recurring 21-call inverse defect. Assigned #923 structurally unified unnamed abstract-declarator prototype signatures with named definitions, bumped the persisted C++ epoch, passed the complete local gate, and is closed from a clean full Chromium proof without waiting for CI. The final 10,000-site record contains 1,388 consistent, zero editor-only, zero unproven, 8,611 inconclusive, and the single retained artifact. Therefore Chromium has zero recurring genuine inverse defect in either C or C++.

The overall N=1 campaign plus the explicit Chromium C supplement is complete. The Go leg closed on Bifrost `91cddbf297bc19646e7993e9f725a5c2d688f723` and AWS SDK for Go v2 `91eca463daf932474778dc4a984c41ecfcd9dc3c`; its final 10,000-site record contains 921 consistent, seven unproven, 8,946 inconclusive, zero editor-only, and 126 raw missing classifications. The only genuine inverse boundary found in the complete audit was fixed by [#791](https://github.com/BrokkAi/bifrost/issues/791) / `91cddbf2`; both production witnesses and the established `types.S3Location` control have exact proven inverse ranges on the pushed head. Every residual has an incompatible or semantically invalid forward identity, leaving zero genuine actionable Go defects.

The Python leg is also complete. On Bifrost `6d6d76f390834b4e15cbc5819d33a7da7331d4aa` and googleapis `d0b2abc2aef8d95402c026cccbc866d812b819b8`, the final 10,000-site record contains 1,166 consistent, 8,828 inconclusive, zero editor-only/unproven, and six raw missing classifications. The three genuine root boundaries are fixed and closed through [#795](https://github.com/BrokkAi/bifrost/issues/795), [#796](https://github.com/BrokkAi/bifrost/issues/796), and [#797](https://github.com/BrokkAi/bifrost/issues/797) in `6d6d76f3`; all six exact production witnesses are consistent on the clean pushed head, and the complete feature-enabled repository test gate passes. Exhaustive residual triage identifies only invalid forward identities, leaving zero genuine actionable Python defects.

## Context and Orientation

`src/analyzer/usages/get_definition/` resolves a source reference forward to one or more `CodeUnit` declarations. A `CodeUnit` is Bifrost's structured declaration identity and includes source path, declaration kind, package/name, signature, and synthetic status. `src/analyzer/usages/finder.rs::UsageFinder` resolves in the opposite direction, from declarations to `UsageHit` source sites. A proven differential defect exists when forward resolution uniquely identifies a declaration set, the inverse query completes without truncation, and no proven inverse hit covers the original reference token.

`src/lsp/handlers/semantic_tokens.rs` currently owns a useful but overly local `reference_candidate_ranges` helper. Move that iterative tree-sitter traversal into `src/analyzer/reference_candidates.rs` and reuse it from both semantic tokens and the new engine. Continue to use `src/analyzer/declaration_range.rs::DeclarationNameRangeContext` so declaration identifiers are not mistaken for references.

The new library module belongs under `src/reference_differential/`. It owns serializable configuration, stable declaration identity, per-site evidence, repository summaries, deterministic sampling, forward batching, inverse grouping, and comparison. The separate binary `src/bin/bifrost_reference_differential.rs` owns command-line parsing, corpus selection, Git metadata, JSONL output, progress, and exit behavior.

The corpus lives at `/home/jonathan/Projects/brokkbench/clones`, a symlink to `/mnt/T9/repo-clones`. Repository membership is the set of canonical `<commits-root>/<language>/<slug>.jsonl` files, excluding `.testsome.jsonl` sidecars. Repository size comes from `/home/jonathan/Projects/brokkbench/sft-tools-commits/repos.csv::code_loc`. Missing or invalid size metadata and invalid clones must be reported rather than silently ranked as zero.

## Plan of Work

First extract the structured reference-candidate traversal from semantic tokens without changing LSP behavior. Build the engine around one persisted `WorkspaceAnalyzer` per repository. Filter audited files by requested corpus language; C uses C-family `.c` seeds and C++ uses the remaining C++ family, while both resolve through `Language::Cpp`. Enumerate the full eligible path inventory, retain the `--max-files` lowest stable path hashes, then for each retained file read the analyzer-generation source, parse it once through `DeclarationNameRangeContext`, subtract declaration-name ranges, and feed a stable site hash-priority sampler. This makes both file and site selection independent of lexical traversal order without reparsing the entire largest checkout.

Batch sampled forward lookups per file with `resolve_definition_batch_with_source`. Preserve every status count, but assert only resolved sites whose declaration identities form one semantic target group. Exclude a recursive definition-contained site only when `analyzer.enclosing_code_unit` equals one of its forward targets, matching existing usage-hit behavior.

Group remaining sites by the full sorted `CodeUnit` set. For each target group, create an explicit candidate provider containing only files with sampled sites, set authoritative scope, and call `UsageFinder::query_with_provider` once. Compare proven and unproven hits by file and byte range. An exact or containing proven hit is consistent. A proven import or self-receiver hit is editor-only and not a production `scan_usages` defect. A retained unproven hit is reported as unproven, not inverse-missing. Incomplete inverse outcomes remain inconclusive. Only complete queries with no covering proven or unproven hit become actionable findings.

The CLI selects N repositories per language by recorded LOC and available clone, supports repeated language/repository filters, records repository HEAD and dirty state, writes append-safe JSONL records, and supports exact-site reruns. Progress goes to stderr. Normal corpus findings do not make the process fail; `--strict` makes actionable findings return a nonzero exit code for later CI use.

After small-fixture and local-repository validation, run the eleven selected repositories sequentially. Preserve reports under `.agents/docs/reference-differential/` because they are agent-facing campaign evidence, not public documentation. Triage each actionable record against source and analyzer behavior. File GitHub issues only after confirming the forward identity and inverse absence are semantically valid. Fix root causes through structured analyzer support, add behavior regressions, run CI-equivalent checks, push `master`, comment on and close fixed issues, then resume the campaign until every target language has a completed report.

## Concrete Steps

Work from `/home/jonathan/Projects/bifrost`.

Build and smoke-test the engine:

    cargo fmt --all -- --check
    cargo test --features nlp,python --test bifrost_reference_differential_cli
    cargo run --release --bin bifrost_reference_differential -- run-repo \
      --root /home/jonathan/Projects/bifrost \
      --language rust --max-sites 200 \
      --output /tmp/bifrost-reference-differential-smoke.jsonl

Run the corpus campaign with N=1 and resumable output:

    cargo run --release --bin bifrost_reference_differential -- run-corpus \
      --clones-root /home/jonathan/Projects/brokkbench/clones \
      --commits-root /home/jonathan/Projects/brokkbench/sft-tools-commits \
      --repos-per-language 1 --max-files 1000 --max-sites 10000 --max-targets 1000 \
      --output .agents/docs/reference-differential/n1.jsonl

The deterministic N=1 selection is:

    c       RMerl__asuswrt-merlin.ng       49,660,873 LOC
    cpp     chromium__chromium              25,845,431 LOC
    csharp  Azure__azure-powershell         17,025,991 LOC
    go      aws__aws-sdk-go-v2              13,062,919 LOC
    java    googleapis__google-cloud-java   33,055,102 LOC
    js      nodejs__node                    11,009,467 LOC
    php     moodle__moodle                   4,155,681 LOC
    py      googleapis__google-cloud-python 14,880,589 LOC
    rust    biomejs__gritql                  5,863,967 LOC
    scala   JetBrains__intellij-scala          749,890 LOC
    ts      elastic__kibana                  9,622,097 LOC

Before each push, run:

    cargo fmt --all -- --check
    cargo clippy --all-targets --all-features -- -D warnings
    cargo test --features nlp,python

If the complete suite is impractical after a narrow language fix, the ExecPlan must record the targeted suites and why any broader gate was not run; before final completion the full required gate must pass.

## Validation and Acceptance

The engine is accepted when a dedicated CLI can select repositories reproducibly, scan real structured references, emit deterministic resumable evidence, and reproduce one site exactly. A controlled fixture must prove that a forward-resolved reference present in inverse results is classified consistent and that an intentionally withheld inverse site is classified missing without using text search.

The campaign is accepted only when all eleven target language buckets have a completed N=1 repository record. Every actionable disagreement must be triaged. Every genuine defect must have an issue URL, a root-cause fix on pushed `master`, behavior-focused regression coverage, and a closed issue containing the fixing commit. The final summary must state per language: selected repository, sampled and forward-resolved site counts, consistent/editor-only/unproven/inconclusive/actionable counts, runtime, issues created, and fixes landed.

## Idempotence and Recovery

Repository selection and sampling are deterministic for the same metadata, HEAD, seed, and budgets. JSONL output is append-safe and identifies completed repositories so an interrupted campaign can resume without re-running them unless `--force` is supplied. Analyzer caches are the existing per-clone `.brokk/bifrost_cache.db`; do not invent another cache. Do not alter corpus checkouts. Exact-site reruns must be read-only. Existing unrelated untracked `.agents/docs` and `.brokk` files in the Bifrost worktree must remain untouched.

If a corpus repository fails to build an analyzer, record an engine-error repository summary with the failure and continue to the next language. Such a failure does not satisfy campaign completion until it is fixed or shown to be an environmental limitation outside Bifrost and explicitly documented.

## Artifacts and Notes

The canonical campaign output will live at `.agents/docs/reference-differential/n1.jsonl`, with a concise final narrative at `.agents/docs/reference-differential/n1-summary.md`. These are LLM-facing run artifacts and therefore belong under `.agents/docs`, not public `docs/`.

The N=1 ranking uses whole-repository recorded LOC, not per-language LOC. This is the corpus's existing uniform size measure. The report also records matching tracked-file counts so mixed-language repositories remain interpretable.

The post-#738 C# result is a binary-pinned pushed-commit measurement whose JSONL records a dirty worktree because #739 edits overlapped the run. Treat its counts as the aggregate #737/#738 boundary and use the post-#739 clean-head rerun as the next durable resumption record.

The #739 production proof is `/tmp/csharp-exact-739-generic-final.jsonl`: Azure PowerShell `PsExtensions.cs` bytes `7144..7153` resolve to `Microsoft.Azure.PowerShell.Cmdlets.CustomProviders.Runtime.PowerShell.PsHelpers.RunScript` with signature `` `1(CommandInvocationIntrinsics, string)`` and retain a proven exact inverse range after the global store-epoch rebuild.

The pushed-head #739 proof is `/tmp/csharp-exact-739-generic-pushed.jsonl`, and the complete post-fix resumption record is `/tmp/csharp-n1-739-pushed.jsonl`. Both are pinned to `9ece3b65`; their dirty flag reflects preserved unrelated working-tree files, not analyzer-source drift. The full record audited 1,000 of 126,829 eligible C# files, sampled 10,000 sites, queried 1,000 of 2,136 distinct target groups, and explicitly records one candidate-limit-exceeded file plus 2,082 target-truncated sites as inconclusive rather than actionable.

The #740 production proof is `/tmp/csharp-exact-740-partial-initializer-final.jsonl`: Azure PowerShell `generated/Mdp/Mdp.Autorest/generated/runtime/ISendAsync.cs` bytes `3733..3741` resolve to `Microsoft.Azure.PowerShell.Cmdlets.Mdp.Runtime.HttpPipeline.pipeline` and retain a proven exact inverse range across the type's physical partial declarations.

The #741 production proof is the final record in `/tmp/csharp-exact-741-conditional-final.jsonl`: Azure PowerShell `generated/MachineLearningServices/MachineLearningServices.Autorest/generated/api/Models/PartialBatchDeployment.json.cs` bytes `7147..7155` no longer resolve to the sibling partial model's `ToString`; the structured explicit `(object)` receiver produces `no_definition`, an inconclusive external boundary, and zero actionable findings.

The C# `nameof(member)` differential-engine proof is the final record in `/tmp/csharp-exact-nameof-editor-only-final.jsonl`: Azure PowerShell `generated/ConnectedMachine/ConnectedMachine.Autorest/generated/api/Models/HybridIdentityMetadata.cs` bytes `15226..15254` remain forward-resolved for editor navigation but are classified `editor_only`, omitted from inverse grouping, and produce zero missing or actionable findings.

The durable C# N=1 closure record is `/tmp/csharp-n1-741-nameof-pushed.jsonl`, pinned to pushed head `1a1bc901`. Its dirty flag reflects the preserved unrelated Rust test and untracked working artifacts, not analyzer or differential-engine drift. The completed report contains zero missing/actionable sites, so later work should not reopen C# unless another language exposes a shared root cause.

The durable Python closure record is `/tmp/python-n1-6d6d76f3.jsonl`, pinned to clean pushed head `6d6d76f390834b4e15cbc5819d33a7da7331d4aa` and googleapis corpus head `d0b2abc2aef8d95402c026cccbc866d812b819b8`. Its six raw missing records are fully accounted for as one wrong receiver-type forward result and five post-rebind wrong-import forward results. The six fixing witnesses are the `/tmp/python-exact-*-6d6d76f3.jsonl` files named in `Progress`; each records `bifrost_dirty=false`, one consistent classification, and zero missing classifications.

The post-triage C++ fixing witnesses are `/tmp/cpp-exact-805-on-powered-on-e0a56d3b.jsonl`, `/tmp/cpp-exact-804-widget-47106f2b.jsonl`, `/tmp/cpp-exact-807-kiosk-owner-35100658.jsonl`, `/tmp/cpp-exact-808-resource-request-ba6e7344.jsonl`, and `/tmp/cpp-exact-810-scoped-java-global-ref-57156922.jsonl`. Every record is completed, Bifrost-clean, consistent, and contains zero missing/actionable classifications. The #810 witness additionally proves the C++ epoch generation cutover rebuilt the previously complete Chromium alias-header blob and recovered the qualified template-alias inverse range.

## Interfaces and Dependencies

In `src/analyzer/reference_candidates.rs`, provide an iterative function that accepts a tree-sitter root and `Language` and returns stable `Range` values for structured identifier leaves, with a caller-provided limit or explicit overflow result. Semantic tokens and the differential engine must call this shared function.

In `src/reference_differential/mod.rs`, expose serializable report types and a repository runner. The core configuration must include corpus language label, site/target/file/usage limits, deterministic seed, test inclusion, and optional exact site. Stable declaration identity must include normalized path, fully qualified name, kind, signature, and synthetic status. The runner must accept an already-built analyzer so tests can use transient workspaces while the CLI uses persisted workspaces.

In `src/bin/bifrost_reference_differential.rs`, provide `run-repo` and `run-corpus` subcommands, `--help`, JSONL output, stderr progress, and `--strict`. Add the `csv` crate only if structured CSV parsing cannot reuse an existing dependency; do not parse `repos.csv` with string splitting.

Revision note (2026-07-12): Created the initial self-contained plan after architecture and corpus inventory. It records the full N=1 campaign, not merely engine construction, because completion requires triage and fixes across every target language.

Revision note (2026-07-12): Changed sampling to a deterministic two-stage file/site design after confirming that the largest selected repositories contain more than 100,000 matching source files. This preserves broad path coverage without parsing every file solely for sample selection.

Revision note (2026-07-12): Added a deterministic target-group cap and disk-safety checks after inventory showed that all large-repository analyzer databases must be built cold and that sampled sites may otherwise produce thousands of separate inverse queries.

Revision note (2026-07-12): Recorded the all-language preflight and made resume identity commit-aware after review showed that package-version-only records could survive analyzer fixes incorrectly.

Revision note (2026-07-12): Corrected the #643 diagnosis after debugger capture identified self-initializer binding recursion as the stack overflow, and recorded the exact low-stack/RSS validation of the SQLite and traversal fixes.

Revision note (2026-07-12): Recorded the first complete Rust differential triage and the #644/#645 root fixes, including the macro-token field gap found by exact-site validation.

Revision note (2026-07-12): Recorded pushed and closed #698 and its exact Azure production proof, separating C# qualified-type focus/range semantics from partial target grouping and authoritative candidate routing.

Revision note (2026-07-12): Recorded the full post-#698 C# rerun, its 83.6% missing-site reduction, and the next explicit-interface declaration triage boundary.

Revision note (2026-07-12): Recorded delegated #701 investigation, complete validation, and exact production proof for the explicit-interface owner role.

Revision note (2026-07-12): Recorded #703 after rejecting the invalid post-#701 C# record and made stable audited scope a campaign-engine invariant.

Revision note (2026-07-12): Recorded the valid post-#701/#703 full C# rerun and the next 39-site generic-argument boundary.

Revision note (2026-07-12): Recorded delegated #704 investigation, exact production proof, and exact-mode full-inventory cost for the next engine optimization.

Revision note (2026-07-12): Recorded pushed/closed #704, the valid post-fix full C# record, its 103-site residual partition, and the reviewed exact-mode fast-path boundary.

Revision note (2026-07-13): Recorded reduced #705, its production timing and truthful summary semantics, and the delegated shared `using static` type-role boundary.

Revision note (2026-07-13): Recorded pushed/closed #705 and #706's reduced shared-role fix, persistence epoch bump, focused validation, and exact production proof.

Revision note (2026-07-13): Recorded pushed/closed #706, the valid complete post-fix C# record, removal of all 34 static-import misses, and the 69-site residual partition.

Revision note (2026-07-13): Marked the campaign deliberately paused after #706 for release `0.8.0` without changing the remaining campaign scope.

Revision note (2026-07-13): Recorded post-release resumption, delegated #707 analysis across all cast families, the shared persisted/inverse fix, and exact production validation.

Revision note (2026-07-13): Recorded pushed/closed #707, the complete post-fix 1,000-target record and its 38-site residual, plus the reduced generic-identity boundary and local structured fix awaiting production validation.

Revision note (2026-07-13): Recorded #726, the audited generic identity fix, and exact Azure validation before the fixing checkpoint.

Revision note (2026-07-13): Recorded #727's delegated reduction and review, dual-spelling structured attribute resolution, complete feature-suite validation, and both exact Azure production outcomes.

Revision note (2026-07-13): Recorded pushed/closed #727, the complete post-fix C# record, elimination of every attribute miss, and delegated prioritization of the nineteen-function/four-field residual.

Revision note (2026-07-13): Recorded #732's delegated review, conservative callable-interval implementation, persisted local/global static-import visibility, receiver compatibility, focused validation, and both exact Azure production outcomes.

Revision note (2026-07-13): Recorded pushed #732, its green CI and final fixing-head Azure record, plus #737's reduced method-group boundary, reviewed structured fix, complete affected validation, and exact production proof.

Revision note (2026-07-13): Recorded pushed/closed #737 with green CI, then #738's reduced inherited-member differential, reviewed nearest-level implementation, full feature-suite validation, and exact Azure production proof.

Revision note (2026-07-14): Recorded pushed/closed #738 and its binary-pinned post-fix corpus counts, then #739's reduced explicit-generic-method boundary, shared generic-arity and lexical-visibility design, delegated review, complete feature-suite validation, and exact Azure production proof.

Revision note (2026-07-14): Recorded pushed/closed #739, its pushed-head exact proof, the complete post-fix Azure N=1 counts, and delegated the five-site residual partition for read-only triage.

Revision note (2026-07-14): Recorded the five-site residual triage, #740's reduced partial-type initializer defect, the narrowed logical-identity fix and shadowing negative, delegated review, and exact Azure production proof.

Revision note (2026-07-14): Recorded the synchronous Oldskool implementation, review, Windows-only clippy repair, exact CI proof, and closure of incidental issue #743 before the C corpus run.

Revision note (2026-07-14): Recorded the completed C N=1 corpus counts, commit pins, resource observation, and delegated read-only triage boundary.

Revision note (2026-07-14): Recorded the complete C missing-site partition, pushed/closed #760 with exact production evidence, and the reduced assigned #762 nested-member boundary.

Revision note (2026-07-14): Recorded #762's reviewed structural chain implementation, the production-only inline-tag typedef boundary found by exact validation, its structured canonicalization fix, and the successful dirty-tree exact rerun pending pushed-head proof.

Revision note (2026-07-14): Recorded pushed commit `7f72d0a2`, the clean exact-range production proof for nested typedef member chains, focused gate counts, and closure of #762.

Revision note (2026-07-14): Recorded #763's reduced forward/inverse typedef-receiver boundary, assigned issue, reviewed root implementation, and successful dirty-tree exact production proof pending push.

Revision note (2026-07-14): Recorded pushed commit `79c09476`, exact production proof, and closure of #763; also recorded #764's reduced transitive typedef boundary, assigned issue, reviewed implementation, and conservative ambiguity/cycle handling pending production proof.

Revision note (2026-07-14): Recorded pushed commit `e5388d80`, exact GMP typedef-chain proof, focused gate counts, and closure of #764.

Revision note (2026-07-14): Recorded #765's reduced variadic arity boundary, assigned issue, structured callable-metadata implementation, fresh-index production proof, and the stale whole-clone cache requirement for final C validation.

Revision note (2026-07-14): Recorded pushed commit `8c1b0d45`, fresh-index exact production proof, focused gate counts, and closure of #765 while retaining the final cache-refresh requirement.

Revision note (2026-07-14): Corrected the intermediate/nested C partition after exact pushed-head audits and recorded #766's reduced macro-return boundary, reviewed delegated implementation, focused gates, and pending production proof.

Revision note (2026-07-14): Recorded pushed/closed #766 with exact production evidence, then #767's faithful preprocessor-recovery reduction, reviewed structured designator implementation, focused gates, and fresh-snapshot production proof pending push.

Revision note (2026-07-14): Recorded pushed commit `6ffe8f8d`, exact unchanged-snapshot PowerPC designator proof, and closure of #767.

Revision note (2026-07-14): Recorded #768's reduced unfielded-annotation boundary, reviewed structural declarator routing, focused gates, and paired IOC4 production proofs pending push.

Revision note (2026-07-14): Recorded pushed commit `61944019`, paired IOC4 false-forward/positive-control proofs, closure of #768, and the epoch-driven final C cache refresh.

Revision note (2026-07-14): Recorded the complete epoch-refreshed C rerun, improved classifications/resource sample, six-site residual partition, and delegated read-only triage boundary.

Revision note (2026-07-14): Recorded final C residual triage, assigned #769/#770 only after reduced failures, reviewed synchronous Oldskool implementations, complete affected validation, and three successful exact production proofs; `mp_int` remains an intentional overbroad-forward artifact.

Revision note (2026-07-14): Recorded pushed commit `6c23084d`, closure of #769/#770 after pushed-head byte-identical production proofs, and the required final epoch-refreshed whole-C rerun boundary.

Revision note (2026-07-14): Recorded live profiling of the unexpectedly serial RMerl refresh, assigned #771 after a deterministic quadratic-complexity reduction, the reviewed Oldskool semantic-identity index and complete affected gates, and the distinct measured epoch/persistence pipeline design boundary.

Revision note (2026-07-14): Recorded assigned #773, synchronous Oldskool implementation, root and delegated review corrections, deterministic real-pipeline batching/overlap proofs, failure isolation, bounded-flow telemetry, persistence parity, and broad validation. #772 remains the final refresh-performance prerequisite.

Revision note (2026-07-14): Recorded the reviewed #772 generation implementation and CI repair, including atomic multi-key publication, captured-token snapshots and writes, metadata-only migration, terminal stale handling, generation-safe GC, bounded reclamation/replacement cost, concurrency tests, and full validation. The forced C closure rerun follows the push.

Revision note (2026-07-15): Recorded the complete pushed-#780 Chromium N=1 measurement, exhaustive corrected 421-site classification, and pushed/closed #784 with delegated AST-structured scope resolution, bounded batch caches, repeated review corrections, and two exact Chromium proofs.

Revision note (2026-07-15): Recorded pushed/closed #786 with two exact Chromium outcomes and the exact 69-site saved-record subtraction. Corrected #789 after its first pushed target-owner implementation failed the exact Chromium proof: persisted evidence now identifies missing function-like export-macro class recovery as the root cause, and the replacement delegated implementation combines structured declaration recovery with unique direct-include exact-owner resolution while removing the disproven layer.

Revision note (2026-07-15): Recorded the complete Go baseline, exhaustive 128-site triage, delegated and independently reviewed #791 implementation, full local/CI validation, three pushed-head exact proofs, final 10,000-site fixing-head counts and resource observations, closure of #791, and the exact 126-site non-actionable residual partition establishing zero genuine Go defects.

Revision note (2026-07-15): Recorded the Python baseline and exhaustive miss-family triage, pushed/closed #795-#797 in `6d6d76f3`, six clean pushed-head exact production proofs, the complete feature-enabled test gate, final 10,000-site counts and resource observations, and the exact six-site invalid-forward residual partition establishing zero genuine Python inverse defects.

Revision note (2026-07-16): Recorded the Java startup/inverse blockers #806, #809, and #811, their reduced structured fixes, the independent nested-return review correction, clean push at `c3eacc8a`, three repeated pushed-head witnesses, green 12-job CI, and issue closures. The full Java N=1 run and exhaustive residual triage remain explicit work after the serialized Chromium closing run.

Revision note (2026-07-16): Recorded the evidence-based stop of the opaque serial Java N=1 attempt, bounded parallel inverse-target implementation, fine-grained timed progress contract, real eight-target google-cloud-java multicore smoke, and measured memory tradeoff. The complete Java N=1 rerun and exhaustive residual triage remain open.

Revision note (2026-07-16): Corrected the incomplete parallelization after the first progress-enabled acceptance attempt exposed serial forward resolution. Recorded the shared bounded forward/inverse pool, per-file forward progress, two-file behavior test, matched google-cloud-java speed/CPU evidence, and byte-identical semantic report hash. The complete Java N=1 rerun and exhaustive residual triage remain open.

Revision note (2026-07-16): Recorded the profiled Java SQLite/index investigation, the duplicate lazy-index build race, shared fallible initialization fix, concurrent regression, matched semantic/performance evidence, and the measured 120-worker projection that replaces the earlier multi-hour estimate. The complete Java N=1 rerun and exhaustive residual triage remain open.

Revision note (2026-07-16): Recorded the pushed #853 implementation, clean full Java N=1 closure run, exact saved-record subtraction of all fourteen genuine method defects, exhaustive non-actionable 122-site residual partition, final resource evidence, complete local validation, and issue closure. The Java leg is complete.

Revision note (2026-07-17): Linked the Java top-five follow-on audit at pushed head `3bd7b75a`: five concurrent repositories completed in 1h02m13s with 806 raw missing rows, whose exhaustive review isolated three legitimate residual families. Assigned issues #866, #868, and #869 now cover structured `this`/`super` method-reference ownership, packaged/default/scoped forward type identity, and comment-safe callable arity; eight production exact witnesses and the complete local feature-enabled gate pass before integration and final pushed-head rerun.

Revision note (2026-07-17): Closed the Java top-five follow-on at clean pushed head `431f1292`. The definitive five-repository run completed in 1h38m48s and retained 601 raw missing rows (Google 90, AWS 53, IntelliJ 149, Dragonwell 121, Telegram 188). Exhaustive byte-level review proves all 601 are non-terminal focus, inferred receiver-class, or test-fixture identity artifacts; zero genuine defects remain. Issues #858, #860-#864, and #866-#869 are fixed across `92c7dfa2`, `3bd7b75a`, and `431f1292`; eight exact production witnesses, formatting, Clippy, and the complete feature-enabled local test gate pass.

Revision note (2026-07-17): Recorded top-five Go blocker #875. The first five-repository run exposed an infinite forward type-inference cycle when a range variable shadows its iterable; GDB proved alternating `go_expression_type_text` / `go_range_binding_type_fqn` frames. Resolving the RHS at its own source position restores lexical scope, the exact Kubernetes 10,000-site campaign now completes in 33.9s, and the complete local feature-enabled gate passes before integration and full rerun.

Revision note (2026-07-17): Closed the Go top-five follow-on at clean pushed head `20fec8af`: ten exact #876-#881 production proofs are actionable-zero, the final 50,000-site record is semantically identical to the exhaustive 1,114-row non-actionable audit, all local gates pass, and #876-#881 are closed.

Revision note (2026-07-17): Recorded the PHP top-five baseline, exhaustive seven-row partition, assigned #904/#905, reviewed delegated fixes, six actionable-zero integration-candidate witnesses, and complete local coverage under the host watcher ceiling.

Revision note (2026-07-17): Recorded PHP checkpoint `d053aaaa`, integrated head `9617701e`, and repeated complete local gates before the approval-blocked publication boundary.

Revision note (2026-07-15): Corrected the completed #789/#790 state and recorded pushed/closed #804, #805, #807, #808, and #810 with delegated implementations, independent review corrections, clean exact Chromium proofs, epoch-aware persisted-cache validation, and green 12-job CI runs. The next accepted C++ boundary is a fresh warm post-#810 Chromium N=1; saved-record subtraction is not a final residual count.

Revision note (2026-07-17): Replaced the stale #906 publication boundary with its clean `cef57b5c` exact proofs, green CI, and closure, then recorded both #909 root causes: flattened class-owned alias persistence and scope-blind bare inherited-type routing. The plan now captures the nested identity/epoch change, the shared iterative inherited-owner resolver used by forward and inverse lookup, the exact pre-fix `CrxInstaller$Result` failure, and focused local validation. Publication and rerun exact Chromium acceptance remain explicit pending work.

Revision note (2026-07-17): Recorded #909's phase-3 Chromium evidence, the repeated-path `MultiStep` and macro-exported-class `ErrorCallback` follow-on boundaries, the breadth-first nearest-level hierarchy correction, structured malformed-alias recovery, nested recovered-owner ordering control, and green 118/65/42 focused gates. Exact dirty-tree Chromium reruns remain root-owned acceptance work.

Revision note (2026-07-17): Recorded phase-4 exact `Result` and `ErrorCallback` successes, the remaining production `MultiStep` intermediate-alias miss, its faithful red reduction, the shared acyclic target-preserving alias-chain fix, retained cycle/ambiguity safeguards, and repeated green 118/65/42 focused gates. The final exact `MultiStep` rerun remains root-owned acceptance work.

Revision note (2026-07-17): Recorded the final phase-5 dirty-tree acceptance artifacts for all three #909 Chromium witnesses. Each strict ephemeral run pins Chromium `e52675fe`, resolves the intended alias or canonical target, and returns an exact inverse hit over the requested bytes with zero actionable findings. Complete local gates, publication, clean-head reruns, CI, and closure remain pending.

Revision note (2026-07-17): Recorded #909 publication at `4a376b0b`, three clean-head exact Chromium proofs, green CI run `29618734115`, and issue closure. Fresh duplicate review then established the independent nested-qualifier-prefix family, filed it as #911 assigned only to `jbellis`, and retained exact witnesses plus the structured implementation boundary for the next delegated pass.

Revision note (2026-07-17): Recorded #911's zero-hit authoritative reduction, the shared tree-sitter owner-component decomposition, target-aware successive-prefix resolution, type-aware declaration-contained hit routing, explicit outermost-node and ambiguity invariants, and green 119/65 focused gates. Production proofs, complete gates, publication, CI, and closure remain root-owned.

Revision note (2026-07-17): Recorded the eight final dirty-tree #911 Chromium proofs. All exact strict ephemeral runs pin Chromium `e52675fe`, preserve the intended forward identity, return the identical inverse byte range, and report one consistent site with zero actionable findings. Complete gates, publication, clean-head reruns, CI, and closure remain pending.

Revision note (2026-07-17): Recorded #911 publication at `68438d2e`, all eight clean-head exact Chromium proofs, green CI run `29621829894`, verified single-assignee closure, and the transition to read-only triage of the next independent C++ residual family.

Revision note (2026-07-18): Recorded #912 publication at `2f1583a3`, both clean-head exact Chromium proofs, complete local gates, green CI run `29625158821` after one evidence-backed Windows single-job rerun, and verified single-assignee closure. Recorded the distinct structured explicit-derived-receiver boundary as assigned #914 with its Chromium witness, existing-issue exclusions, shared hierarchy-resolution seam, and faithful behavior-test controls.

Revision note (2026-07-18): Recorded #914's red/green explicit-derived-receiver reduction, shared declaring-owner hierarchy resolution, root-review bare-call collision correction, 122/28 focused suites, strict dirty-tree Chromium proof, and complete local formatting, Clippy, and feature-suite gates. Publication, clean-head proof, CI, and closure remain root-owned.

Revision note (2026-07-18): Recorded #914 publication at `8dbc40fc`, its clean-head strict exact Chromium proof, green CI run `29630172954`, acceptance comment, and verified single-assignee closure.

Revision note (2026-07-18): Recorded the clean-head #915 production miss, comment-inflated C++ argument arity root cause, faithful targeted/whole-workspace reduction, duplicate exclusions, and verified single-assignee issue boundary.

Revision note (2026-07-18): Recorded #915's shared structured non-extra argument sequence, forward/targeted/whole-workspace parity regression, root review, independent 649-test affected validation, strict dirty-tree Chromium proof, and complete all-feature Clippy and feature-enabled local test gates. Publication, clean-head repetition, CI, and closure remain root-owned.

Revision note (2026-07-18): Recorded #915 publication at `f9c5f7bb`, its clean-head strict exact Chromium proof, green CI run `29632813412`, acceptance comment, and verified single-assignee closure.

Revision note (2026-07-18): Recorded post-#915 receiver triage, excluded four saved rows now fixed by comment-safe arity, separated two wrong-forward declaration-recovery rows, and established assigned #916 from the reduced templated out-of-line canonical-owner identity split.

Revision note (2026-07-18): Recorded #916's AST-structured canonical owner decomposition, root-review leading-global and realistic-fixture corrections, C++ epoch cutover, red/green inverse behavior, compiler syntax proof, and complete affected validation. Production acceptance and complete gates remain root-owned.

Revision note (2026-07-18): Recorded #916's strict dirty-tree Chromium proof after the C++ epoch cutover, with exact forward identity, identical inverse bytes, and zero actionable findings. Complete gates and publication remain root-owned.

Revision note (2026-07-18): Recorded #916's complete formatting, all-feature Clippy, and serial feature-suite gates, including the environment-only subprocess denial and successful identical escalated rerun. Publication and clean-head acceptance remain root-owned.

Revision note (2026-07-18): Recorded #916 publication at `2c211a26`, its clean-head strict exact Chromium proof, and green CI run `29636287935`. Acceptance comment and verified closure remain root-owned.

Revision note (2026-07-18): Recorded #916's acceptance comment and verified completed closure with `jbellis` as its sole assignee. The campaign continues with the distinct macro-exported declaration and template-specialization wrong-forward families.

Revision note (2026-07-18): Recorded exact clean-head row-54 and row-62 proofs, the read-only comparative root-cause audit, and assigned #917 as the narrower recovered macro-class field-owner defect. The broader dynamic-specialization fragmentation family remains explicitly deferred.

Revision note (2026-07-18): Recorded #917's reviewed recovered-class declaration scope, shared transparent-wrapper predicate across declaration and local-binding consumers, two root-required regression corrections, epoch cutover, faithful red/green behavior, and independent 42/498/28/125 affected validation. Production acceptance and complete gates remain root-owned.

Revision note (2026-07-18): Recorded #917's strict dirty-tree Chromium proof after the C++ epoch cutover, with corrected canonical forward field identity, identical exact inverse bytes, and zero actionable findings. Complete gates and publication remain root-owned.

Revision note (2026-07-18): Recorded #917's complete formatting, all-target all-feature Clippy, and serial feature-enabled local test gates. Publication and clean-head acceptance remain root-owned.

Revision note (2026-07-18): Recorded #917 publication at `753033ea`, its clean-head strict exact Chromium proof, and green CI run `29639193559`. Acceptance comment and verified closure remain root-owned.

Revision note (2026-07-18): Recorded #917's acceptance comment and verified completed closure with `jbellis` as its sole assignee. The campaign continues with the broader row-62 partial-specialization and fragmented-sibling recovery family.

Revision note (2026-07-18): Recorded the row-62 AST reduction, misleading exact consistency, faithful fragmented and macro-free specialization controls, duplicate exclusions, and assigned #919 as the complete specialization-owner plus receiver-dispatch family.

Revision note (2026-07-18): Recorded #919's reviewed structural recovery, exact recovered ranges, persisted template metadata, deterministic primary-default reconciliation, dominance-based specialization dispatch, root-required scope/comment/stack-safety/field-binding corrections, and green focused validation. Dirty Chromium acceptance and complete gates remain root-owned before the mandatory fresh full Chromium N=1.

Revision note (2026-07-18): Recorded #919's dirty production proof and complete local validation, including the full-suite discovery and structural correction of the macro-decorated local-declaration regression. Publication, clean-head proof, CI, and closure remain before the mandatory fresh full Chromium N=1.

Revision note (2026-07-18): Recorded clean publication and closure of #919 at `4c36b5b8`, its exact pushed-head Chromium proof and green 14-job CI matrix, and the explicit transition from stale-ledger triage to the mandatory fresh full Chromium N=1 scoreboard.

Revision note (2026-07-18): Recorded #921 publication at `07ad79f6`, its clean exact Chromium proof, complete local and 14-job CI gates, acceptance comment, and verified single-assignee closure. The final clean Chromium N=1 fixes ten shared redeclaration sites with no new misses, corrects the two provisional family classifications, and closes C++ at 23 exhaustively explained residuals with no recurring unfixed family.

Revision note (2026-07-18): Added the eleven-language compact manifest and final narrative, recorded the two unavailable historical telemetry fields honestly, completed final verification, and marked the reference-differential N=1 campaign complete.

Revision note (2026-07-18): Corrected the provisional closure after an objective-level audit found that the N=1 C selection did not satisfy explicit Chromium C coverage. Recorded the supplemental baseline, exhaustive two-row triage, assigned/fixed/closed #923, complete local gates, clean pushed-head full Chromium C proof, and updated manifest/narrative. Chromium C and C++ are now both explicitly complete.

Revision note (2026-07-17): Recorded Rust top-five preflight #907, its delegated stack-safe implementation and full gate, and the separate #850-owned rust-lang forward-performance blocker that prevents the authoritative corpus run.

Revision note (2026-07-17): Recorded Scala top-five preflight #908, its reviewed source-free bulk hierarchy implementation and companion-object parity correction, measured production timings, and the complete watcher-safe local gate before the definitive corpus run.

Revision note (2026-07-17): Recorded Scala forward batch blocker #910, its reviewed lexical-prefix reuse fix at `a94c31a7`, bounded exact and full-repository results, and the managed persisted-cache/artifact approval boundary that keeps current clean-Bifrost records research-only pending final publication proof.

Revision note (2026-07-17): Recorded the exhaustive composite 1,536-row Scala audit, target-kind totals, distinct generic/union receiver roots, and the conclusive two-call #908 IntelliJ exact witness.

Revision note (2026-07-17): Recorded reopened assigned #661-#664, newly filed assigned union receiver #913, the David-owned #128 skip, and the exact legitimate-versus-fixture disposition of the newer wrong-forward ledger.

Revision note (2026-07-17): Corrected Scala #661/#662/#663 row routing and coverage, then recorded structured indentation-recovery ownership, epoch invalidation, and exact Kyo `Context.Call` production proof at `6d92b9fe`.

Revision note (2026-07-17): Recorded completion of the corrected Scala #662 implementation ledger through `220705e5`, clean production exact coverage, nested companion/synthetic owner corrections, watcher-safe focused gates, recoverable cold-cache preparation, and the two historical rows requiring fresh forward reclassification.

Revision note (2026-07-18): Recorded the complete five-repository Scala baseline `/mnt/optane/tmp/reference-differential/scala-top5-bafba979.jsonl` (SHA-256 `4872bc26e2942ab41881c465a76142ba223bf1898033a76728f63846dcc592bc`) and its exact 1,105-row independent audit. Root review preserved every audit key but corrected six ownership mistakes: Java `SbtBundle.message` moves to David-owned #128; one synthetic constructor and three receiver-bound infix calls move to #662; and Scala3 `MainProxies.TypeTree` moves to #664 because `untpd.*`, not `tpd.*`, is active at the site. A subsequent exact export-provenance review moves `TastyPrinter.isModifierTag` from #663 to new owned #933. The current v3 routing ledger is `/mnt/optane/tmp/reference-differential/scala-top5-bafba979-corrected-v3-audit.jsonl` (SHA-256 `d639f589c61da6de474f909319702cf7af7683cf4de2d53d2c5176e322c8d88e`), with #661 137, #662 31, #663 127, #933 one, #664 48, Java #128 91, non-acceptance 180, and physical source-set/platform ambiguity 490. Detached checkpoint `a53df14f` fixes #661/#662. #663 ownership review identifies two closed David-owned #502 extension duplicates and 16 closed David-owned #419/#499 trait/default-inheritance duplicates, leaving 109 owned rows. Checkpoints `9bff10d0` and `611f27a9` fix the 51 lexical-outer and two fresh-instance rows with five actionable-zero production exacts; direct Scala 95/95, whole graph 39/39, formatting, diff checks, and all-target/all-feature Clippy pass. The remaining #663/#933/#664 roots, four corrected #662 exacts, and complete watcher-safe feature gate remain before integration and publication.

Revision note (2026-07-18): Recorded #933 completion at `0350bdbf`, including parser-backed persisted export facts, cycle-safe original-identity propagation, bulk-hydration proof, root selector-order correction, complete focused Scala/cache gates, and the 22.0-second actionable-zero Scala3 exact witness. Remaining owned Scala roots are #663 and #664 before the final watcher-safe gate and publication run.

Revision note (2026-07-19): Recorded Scala qualified-root checkpoint `77c8926b`, its exact physical-suffix/logical-hierarchy identity split, the corrected 65-covered/13-residual #661 authority, focused gates, and actionable-zero Akka/Scala3 production witnesses.

Revision note (2026-07-19): Recorded Scala stable-field checkpoint `43c13b2d`, completion of every valid-forward #661 row in the frozen authority, fresh exact Scala3/IntelliJ evidence, and the eleven remaining forward-owned rows.

Revision note (2026-07-19): Recorded named-argument checkpoint `27c8385f`, the structured binding-refresh boundary for calls and explicit constructors, the actionable-zero Scala3 production witness, and the resulting ten-row physical-identity residual set.

Revision note (2026-07-19): Recorded Scala physical-member checkpoint `08a0bfc5`, abstract-accessor completion `bb547f0a`, exhaustive exact proof for the last ten frozen rows, and full local completion of the 78-row #661 authority before fresh corpus publication.

Revision note (2026-07-20): Recorded Scala checkpoints `194d8117`, `9ef8b1d7`, and `6a46973f`, completion of all 91 corrected-v2 #664 rows, typed Kyo method-value selection, exact IntelliJ inherited-default dispatch, their conservative negative boundaries, and independently green affected suites/Clippy. Only Akka's inherited generic factory substitution remains among corrected-v2 #663 before #937 and publication proof.

Revision note (2026-07-20): Recorded Scala checkpoints `69fa8c8a` and `c65bb501`, completing all corrected-v2 #663 semantic roots with inherited generic factory substitution and restoring #908's source-free shared project-type hydration contract before #937 and final production proof.

Revision note (2026-07-20): Recorded #937 checkpoint `776bff63`, replacing target-major inverse reparsing with one exact query catalog and one shared structured scan per eligible file, plus the 128-replica parse/walk-count proof and independent Scala validation. Release production evidence and the final watcher-safe publication gate remain.

Revision note (2026-07-20): Recorded the clean detached-`160cd85d` post-parity Scala corpus and independently source-verified 1,033-row authority, with exact hashes and residual routing; 42 #937 parity rows remain before publication proof.

Revision note (2026-07-20): Recorded final #937 parity checkpoint `9ef1e084`, its structured stable/factory/call/capture/receiver coverage, focused gates and independent review, plus six actionable-zero production exacts pending clean corpus closure proof.
