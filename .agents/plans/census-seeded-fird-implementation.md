# Census-seeded FIRD: implement the census probe seed (M1-M3) and run the 11-language campaign

This ExecPlan is a living document. The sections `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` must be kept up to date as work proceeds. It is maintained in accordance with `.agents/PLANS.md` (read that file before editing this plan).

It builds on the design in `.agents/plans/reference-differential-census-seeding.md` (checked in). That design is incorporated by reference; the essential parts are restated below so this plan is self-contained.

## Purpose / Big Picture

The forward-vs-inverse reference differential (FIRD) audits whether Bifrost's inverse usage query can recover the sites that forward definition-resolution finds. Today it can only probe sites the analyzer's own filtered candidate frontier proposes (`reference_candidate_ranges` in `crates/bifrost-analysis/src/analyzer/reference_candidates.rs`). Constructs the analyzer mis-parses produce no probe, so joint blindness (both forward and inverse silently failing on the same construct) reads as health.

After this change a contributor can run:

    target/release/bifrost_reference_differential run-repo --root <clone> --language rust \
      --probe-seed census --tiers 1,2,3 --output <out.jsonl> ...

and receive a report whose probe sites came from a **raw tree-sitter identifier census** (every identifier-class leaf token, ignorant of the analyzer index), so it surfaces usage-loss at sites the index never proposed. Forward-resolved census sites flow through the existing inverse comparison (a miss is a finding with no external referee). Forward-unresolvable census sites are graded into deterministic evidence tiers. The end state is a per-language campaign over the top-30 task-weighted repos of all 11 corpus languages, producing triaged issues assigned to `jbellis`, fixed and closed depth-first per language, merged to `origin/master` as we go.

Scope is the MCP `symbols` toolset and the associated Rust and Python APIs; LSP comes along for the ride but is not a focus. M4 (external referee) is intentionally **not** built; tier-3 residue is adjudicated by the agent during triage.

## Progress

- [x] (2026-08-07) Recon: confirmed census seeding is unimplemented (no `census.rs`, no `--probe-seed`); mapped FIRD engine seam (`collect_sampled_sites` at `src/reference_differential/mod.rs:555`), report schema, CLI parsers, and the candidate walk (`collect_candidate_ranges`); confirmed tasks.py selector (`task_repos(SFT_PREDICATES, langs=[lang])` ranked by exact `(-task_count, repo_slug)`) and the 11 languages. `SFT_PREDICATES.not_overlarge` excludes `large-repos.csv`.
- [x] (2026-08-07) M1: census walk (`CandidateFrontier::Census` + `census_identifier_ranges`), `ProbeSeed`/`TierSelection` in engine config + CLI (`--probe-seed`, `--tiers`), `collect_sampled_sites` seed branch, `classify_census_gaps` (tier 1/2/3 via `census_site_role` + same-file decl), report rows tagged `seed`/`tier`. Tests: frontier superset unit test, two engine integration tests (census proposes macro-body occurrence + stays silent without same-file decl), all green. End-to-end tokio census pass succeeded (seed-tagged sites, tier-2 gaps, 5 forward-adjudicated misses).
- [ ] M2: `--probe-seed` pluggability in `collect_sampled_sites`, forward adjudication of census sites tagged `seed: census`, tier-2/3 classification, ledger/shrink/single-line rerun.
- [ ] M3: inverse-precision check (name-literal), sharded corpus runner (`--shard K/N`), per-language ranking; two-language corpus pass.
- [x] (2026-08-10) CSHARP ranks 21-30 complete.
  - The final 10-repository tier-1 census is `/mnt/optane/tmp/bifrost-fird/csharp-ranks21-30-current-tier1.jsonl`.
  - Seven repositories report zero `missing` rows. The 14 raw rows in Marten, Elsa, and dotnet-bumper are invalid forward targets, external members, a declaration name, generic identity collisions, or local-function bindings. Three exact dotnet-bumper probes return `actionable=0`.
  - Reopened #1798 now accepts collection, dictionary, and array initializer entries as structured method-group value contexts. The exact PowerFx `LanguageCode` and xUnit `DeserializeTraits` rows return `actionable=0`.
  - #1934 recovers generic calls that tree-sitter parses as relational `<` and `>` expressions. The structured route validates the type argument, receiver, arity, lexical shadows, assignment-chain shape, and method-group argument. The exact xUnit `DispatchWhen` and cleanup-handler rows return `actionable=0`.
  - Validation passed: 142 targeted C# tests, targeted Clippy, `suite_usages` 1724/1724, `suite_analyzers` 781/781, formatting, diff checks, and the warning-level policy gate.
- [x] (2026-08-11) C ranks 21-30 complete.
  - The final 10-repository tier-1 census is `/mnt/optane/tmp/bifrost-fird/c-ranks21-30-push-final.jsonl`.
  - #1950 keeps focus on an inner call when that call is the callee of an outer call. The exact NeoMutt `CRYPT_MOD_CALL` site now resolves the macro instead of the outer invocation.
  - Reopened #1819 now scans indexed macro targets. It proves exact active bindings and keeps unresolved conditional alternatives as unproven. It rejects same-name macros from unrelated translation units. Exact NeoMutt `CRYPT_MOD_CALL`, libhtp `bstr_len`, Inspektor Gadget `MAX_ENTRIES`, and jemalloc `JE_USDT` sites return no actionable finding.
  - The remaining raw rows are not inverse gaps. Inspektor Gadget `SEC` comes from an external BPF header. Chipsec rows are local function pointers or compile-context-dependent kernel declarations. jemalloc `realloc` is external libc. Its `pthread_create_wrapper` and `prof_idump` rows remain in the conditional include and declaration family tracked by #1816.
  - Validation passed: focused #1819 and #1950 tests, targeted Clippy, `suite_usages` 1729/1729, `suite_analyzers` 781/781, release exact probes, formatting, diff checks, and the warning-level policy gate with zero new findings.
- [ ] Campaign: per-language depth-first passes over top-30 repos; file/fix/close/merge; per-language summary. The first 20 repositories are complete for all languages. The rank-21-through-30 extension is in progress. Rediscovery audit against #1526/#1527/#1528/#1537/#1377 (#1376 already fixed and closed).
  - [ ] (2026-08-08) TOP-30 EXTENSION: audit ranks 21-30 in the existing depth-first order: rust, java, python, go, ts, js, csharp, c, cpp, scala, php. Use `tasks.task_repos(tasks.SFT_PREDICATES, langs=[lang])`, sorted by exact `(-task_count, repo_slug)`. This preserves the original campaign selector and its `large-repos.csv` exclusion.
  - [x] (2026-08-09) RUST ranks 21-30 complete. Reopened and fixed Jonathan-owned #994, #1279, and #1283 after a final census rerun found associated-type implementation declarations, import terminals, include-owner imports, exported macros, local-module precedence, crate-alias re-exports, nested `Self` owners, external-type collisions, and enum-as-callable collisions. #1902 was also fixed and closed. Exact corpus witnesses now return no actionable finding. Dave-owned cfg/trait families #433/#1377/#1749 were skipped; current SysRegex evidence was added to #1749. Validation: `suite_analyzers` 775/775, `suite_usages` 1686/1686 plus the isolated C# timing test, targeted Clippy clean, and all focused Rust regressions green.
  - [x] (2026-08-09) JAVA ranks 21-30 complete. The 10-repository census at `58a256a0` produced 11 `missing` rows: 7 MariaDB `module-info.java` service providers and 4 ShardingSphere Lombok-generated constructors. #1906 fixed targeted and whole-graph module-descriptor references without colliding named modules with package modules; exact first and repeated MariaDB provider probes now return `actionable=0`. Tier-2 exploration found #1905: iText nested classes lost inherited fields and lexical outer methods. The structured hierarchy/enclosing-owner fix now makes exact `FieldBase.data`, `Item.myOffset`, `PdfCanvasProcessor.getColor`, and `Cleaner.createSafeElement` probes return `actionable=0`. #1905 and #1906 are closed. The four ShardingSphere rows are forward-identity gaps caused by absent Lombok constructors; exact evidence is recorded on open Dave-owned #1153, so no duplicate inverse issue was filed. Validation on the merged tree: `suite_analyzers` 781/781, `suite_usages` 1694/1694 plus the isolated timing test, targeted Clippy clean, release build clean, and the warning-level policy gate clean.
  - [x] (2026-08-10) TYPESCRIPT ranks 21-30 complete. Both head-scoped census shards (`ts-ranks-first-f0d87158-tier1.jsonl` and `ts-ranks-remaining-f0d87158-tier1.jsonl`) completed across all 10 selected repositories with zero `missing` rows. No new issue or code change was required.
  - [x] (2026-08-10) JAVASCRIPT ranks 21-30 complete. The first census produced 18 `missing` rows across PDF.js, Bruno, Middy, Svelte, github-readme-stats, and Fastify. Exact probes separated invalid forward identities and unindexed external/CommonJS boundaries from three valid inverse families. #1926 fixes the first valid family: tree-sitter-javascript exposes a class `field_definition` name through its structured `property` field, while the declaration visitor asked for `name` and dropped all JavaScript class fields. The reduced exact-owner regression passes, and the original PDF.js `CurrentPointers.#pointerType` site now reports `consistent` with an exact inverse hit. Reopened #667 fixes the second PDF.js family by retaining a modeled local property's provenance through structured chained-assignment aliases such as `const button = (this.#button = value)`; the exact `this.#prevButton.disabled` row now returns an inverse hit, while different private fields and the same private name in another class remain excluded. Reopened #1313 fixes the Svelte singleton residual: the exported object literal was inside a JSDoc cast and `parenthesized_expression`, so the inverse did not recover the variable binding. The shared structured property helper now walks transparent parentheses, the reduced fixture preserves decoy and shadow near misses, and the exact `ElementInteractivity.NonInteractive` site reports `consistent`. Reopened #1787 fixes the third valid family: a unique program-scope declaration in a classic script is now visible to other classic scripts during inverse scanning. Modules, lexical shadows, and competing script-global declarations remain excluded. The exact PDF.js `canRequestBody` call now reports `consistent` with its exact inverse hit. #1930 fixes CommonJS destructuring through `module.exports = localObject`: the export index now projects the unique program-scope object's named fields, forward lookup prefers the exact exported local binding over same-terminal class members, and inverse scanning keeps direct named property imports as values instead of overwriting them with the exported-object receiver role. Reduced decoy, opaque-export, and lexical-shadow cases stay excluded. Exact Bruno `getPreferences` and Fastify `FST_ERR_LOG_INVALID_LOGGER_CONFIG` probes now report `actionable=0`.
    - Current #1930 validation: `suite_usages` 1,718/1,718, `suite_analyzers` 781/781, all 48 TypeScript usage regressions, targeted Clippy, formatting, diff, release build, and warning-level policy checks pass.
    - #1931 fixes the github-readme-stats default-initializer row. Definition lookup used a full subtree scan to decide whether a destructuring pattern bound a name, so it treated calls in an `assignment_pattern` value as declarations. It now uses the shared structured binder projection. The reduced outer-function call and parameter-shadow control pass, all 53 JavaScript usage regressions pass, and the exact `getDefaultLanguagesCountByLayout` probe reports `actionable=0`. The upstream RQL merge also introduced one policy finding; moving string decoding after the terminator scan removed the repeated nested parse, and all 130 `brokk-bifrost-rql` tests pass.
    - Reopened #1781 fixes the Svelte private-field focus row. The exact bytes select `#out` in `child.#out.length`, but the structured chain builder rejected `#` as a JavaScript identifier byte and left the terminal `length` segment selected. The corrected focus resolves only `Renderer.#out`; the reduced private-field/decoy regression passes, and the exact Svelte probe reports `actionable=0`.
    - #1932 fixes twelve Middy inverse misses after owner data flow. Forward resolution retains logical property identities such as `request.response.headers`, but inverse scanning discarded the `response` owner after `const { response } = request` and after passing that object into a local helper parameter. The scanner now reads the target's structured receiver chain, maps shorthand and renamed object-pattern binders to the remaining owner path, and propagates exact target-object arguments into uniquely resolved local function parameters with a structured fixed point. Different destructuring roots and helper arguments remain excluded. Exact `headers`, `body`, `statusCode`, and all three `addHeaderPart` probes now report `actionable=0`.
    - #1933 fixes the final Astro `CILogger.logTest` row. The exported-local-property route accepted fields only, but JavaScript object properties holding arrow functions are indexed as function CodeUnits. The shared structured property/export proof now admits field and function members. The reduced imported-call fixture excludes a second exported object with the same method, and the exact Astro probe reports `actionable=0`.
    - The remaining Bruno `settings.encodeUrl` row is not an inverse gap. `settings` is unbound, but forward lookup selects unrelated `formattedJson.settings.encodeUrl` fields. Reopened Dave-owned #682 with the exact witness and three-segment guard diagnosis. The campaign excludes this invalid forward identity and leaves the general receiver-proof correction with its owner.
    - Final proof: the clean-head report `/mnt/optane/tmp/bifrost-fird/js-ranks21-30-72e1fa73-tier1.jsonl` contains all 10 selected repository records at Bifrost `72e1fa73`, with zero legitimate unowned `missing` rows. Middy and Astro have zero missing rows. Bruno has the one invalid forward identity assigned to #682. `suite_usages` passed 1,722/1,722, `suite_analyzers` passed 781/781, the 10 JavaScript residual tests passed, targeted Clippy passed, and the warning-level policy gate reported zero active findings. Superseded JavaScript exact and intermediate outputs were removed.
  - [x] (2026-08-07) RUST run complete (20/20 repos, `--tiers 1`, clone HEADs, `/mnt/optane/tmp/bifrost-fird/rust-census-54eee258.jsonl`). 471 actionable = 293 forward-adjudicated misses + 178 tier-1 gaps. Signal/noise: ~183 are prelude/std noise (`Err` 116, `Ok` 48, `drop` 19) - forward can't resolve external std, or mis-resolves `Err` to a same-named local enum (nom). Many remaining forward-adjudicated misses are forward mis-resolutions to a *module* target (e.g. `field::ValueSet` -> `tracing.field` module), which the runbook says is not a legitimate inverse miss. Clearest genuine finding: tracing-subscriber `Filter` trait-reference inverse miss (6 witnesses, forward-correct, complete-but-absent). It does not minimize (plain trait-bound/cross-module/simple-cfg cases are all Consistent), so it was FILED + ESCALATED to Dave as #1749 (cfg-gated-trait-in-multi-crate-workspace interaction, same area as #1377).
  - [x] (2026-08-07) RUST tail triaged. Four mechanisms now account for the non-noise clusters: (1) #1638 recursion drop (cross-language, fix in flight); (2) #1750 re-export receiver typing (rayon self.len x3, filed, mine); (3) MACRO-WRAPPED DECLARATIONS - minimized to a 4-file fixture (macro_rules! item wrapper, no cfg needed; plain cfg is consistent) and posted to #1749, which it fully explains (tracing-subscriber declares Filter inside `feature! {}`); also explains tokio's SpawnMeta cluster and plausibly much of tokio's 65 misses (cfg_rt!/cfg_trace! idiom); (4) #1377 local-decl/import shadowing family - new nom witness (test fn named `recognize` with function-scoped `use crate::combinator::recognize`, 6 sites) posted to #1377. Unminimized leftover: rayon-demo `crate::seeded_rng` x3 (bin+lib dual-root fixture and simple bin-crate fixture both consistent; needs multi-package workspace context) - low priority, not filed. nom/just test-name collisions fold into (4). neon `sys::string::*` rows are forward mis-resolutions to a module target (runbook: not a legitimate inverse miss).
  - [x] (2026-08-07) `rayon self.len` cluster ROOT-CAUSED and FILED as #1750 (assigned jbellis). NOT the #1638 recursion drop: the enclosing method (`opt_len`) differs from the target (`len`). Trigger is the standard adapter-module idiom (`mod zip;` + `pub use self::zip::Zip;` + `use super::*;` in zip.rs): receiver typing of `self` lands on the re-export FQN `iter.Zip`, `canonical_rust_hierarchy_type` does not see through a `pub use` binder, and `receiver_owner_proof`'s fallthrough (extractor.rs ~2035/~2084) returns `Mismatches` (silent, no unproven row) because a `pub use` binder is not a `type` alias. scan_usages then reports a FALSE `verified_absent`. Minimal 3-file repro + perturbation matrix (rename alias / private parent mod / explicit import each fix it) in the issue.
  - [x] (2026-08-07) #1638 FIXED (dce91490, merged to origin/master, issue auto-closed). Cross-language: `classify_recursive_hit` (core) + `classify_recursive_hits` (analysis, with declaration-name-range refusal against the indexed snapshot); push-time classification in Java/Kotlin/Scala/C#/PHP/Go, post-filter replacement in Rust/Python/Ruby/Go/JS-TS. Removing the drops unmasked and fixed: Rust decl-name self-hits, JS/TS field decl-name self-hit, Python decorator-names-decoratee misresolution, and TWO real C++ defects (declarator-chain terminal never accepted in `declaration_name_node_from_fields` -> text-search fallback could empty textDocument/references for any C++ method whose body mentions its own name; `push_recursive_reference_hit` only covered the split decl/def shape). Pinned #1479 parity tests flipped (complete-row-set parity clean; external-only gap split into its own test). All new tests verified failing pre-fix. Follow-ups filed: #1753 (free-function decl-name coverage for C#/PHP/Scala/JS-TS, mine), #1752 (pre-existing master conformance failure: candidates_of external_unknown boundary returns empty rows, unassigned). Review corrections applied during my pass: name ranges must read `analyzer.indexed_source`, not disk (scan_usages.rs:3034 precedent).

  - [x] (2026-08-07) JAVA run complete (20/20 repos, post-#1638 binary dde4fad3, `/mnt/optane/tmp/bifrost-fird/java-census-dde4fad3.jsonl`). 190k sampled sites -> 48 actionable, 17 of 20 repos fully clean. Triage: ALL of it is FORWARD-resolver error, inverse is right every time. Two mechanisms, both minimized and filed assigned to jbellis: #1754 (JLS obscuring ignored - value-position bare name resolves to the type over a same-named field; fastjson x46 via `static String DefaultJSONParser = type(DefaultJSONParser.class)`, poi enum-constant variant; 2-file fixture) and #1755 (call binds a wrong-arity local overload when the true target is inherited from an UNINDEXED external superclass; graphhopper/dropwizard witness; 1-file fixture + indexed-super control that resolves correctly). Fix implementation delegated to Opus (both are get_definition/java.rs resolution-order changes).
  - [x] (2026-08-07) JAVA fixes LANDED (feb905a4 + clippy fix-forward 8b34da16, merged to origin/master, #1754 + #1755 auto-closed). One flipped test total (the JBList repaint case, which IS the #1755 shape). Java summary delivered.
  - [x] (2026-08-07) #1750 FIXED and LANDED (merged to origin/master, closed). Opus's instrumentation CORRECTED my traced mechanism: receiver typing was fine; the real root cause was resolve_rust_hierarchy_ref treating two routes to ONE declaration (local decl + glob re-export binder) as ambiguity, emptying the type's hierarchy, so the dispatch check read empty ancestors as a foreign owner. Fix: dedup candidates by declaration identity (repairs hierarchy index + trait-for-impl-member + alias canonicalization together) plus the foreign_receiver_verdict evidence rule (empty/alias resolution -> Unknown, never Mismatches). Verified end-to-end: the original rayon zip.rs census site now classifies editor_only with a self_receiver inverse hit. RUST language: fully complete.
  - [x] (2026-08-07) PYTHON census run complete (20/20, binary d3eca66b, `/mnt/optane/tmp/bifrost-fird/py-census-d3eca66b.jsonl`): 522 actionable (423 forward-adjudicated + 99 tier-1). NOTE: aaugustin__websockets and python-websockets__websockets are the SAME codebase (org rename) - dedupe when counting. Four mechanisms triaged:
    1. FILED #1760 (mine): identifiers inside f-string interpolations invisible to BOTH surfaces (forward no_definition, no inverse hit) - the census's first true both-sides-blind find; 1-file repro at /mnt/optane/tmp/bifrost-fird/py-fixtures/fstring; websockets witness x18.
    2. Cirq flat-namespace cluster (~300): `cirq.OP_TREE`/`cirq.Qid`/... attribute refs through __init__ re-exports; inverse misses in-repo, but the minimal package fixture (/mnt/optane/tmp/bifrost-fird/py-fixtures2/reexport) is CONSISTENT - trigger needs Cirq's real context (multi-root cirq-core/ layout? conditional imports? scale?). Bisect from real Cirq like #1749/#1750. Many sites also have module-target ('cirq') forward mis-resolutions = runbook noise. Related infra issue: #1649 (open, unassigned).
    3. websockets `asyncio_timeout` x10 (aliased relative import `from .async_timeout import timeout as asyncio_timeout`): minimal fixture (/mnt/optane/tmp/bifrost-fird/py-fixtures2/alias) CONSISTENT; real trigger likely the sys.version_info-conditional import in websockets - bisect from the real repo.
    4. keras `backend.standardize_dtype` x10: forward resolves the attribute head to the FUNCTION `keras.src.backend.config.backend` (re-exported into the backend package that shares its name) instead of the module binding - forward mis-resolution, obscuring-family analogue; minimal modcollide fixture did not reproduce, needs the real keras layout.
  - [x] (2026-08-07) #1760 f-string fix LANDED (800c131b, merged to origin/master, closed). Inverse was never blind - the differential's null inverse_hit was cascade from forward failure (no targets -> no inverse query). One-predicate fix in python_is_non_reference_context: stop the string-opacity walk at interpolation/format_expression holes. JS/TS/Ruby/Scala/C# probed: not blind.
  - [x] (2026-08-07) keras cluster BISECTED (subagent) and FILED as #1762 (mine): forward mis-resolution, inverse correct. record_single_reexport_event (crates/bifrost-python/src/graph_support.rs:335-347) records a from-import submodule export as "look up S inside P.S" instead of "the export IS module P.S", so the head `backend` resolves to the same-named function the subpackage re-exports. 4-file fixture + 16-row perturbation matrix at /mnt/optane/tmp/bifrost-fird/bisect-keras/. Fix shape: module-valued ExportEntry form.
  - [x] (2026-08-07) Cirq cluster BISECTED (subagent) and FILED as #1763 (mine): my re-export hypothesis was WRONG - the trigger is module-qualified ANNOTATION references to non-class targets (2-file repro: `import op_tree` + `def g() -> op_tree.OP_TREE`). Two cooperating defects: annotation resolver reuses constructor resolution with .filter(is_class) (resolver.rs ~512/~305) so a module-level type alias yields Some(vec![]); extractor's empty-candidate fallback (extractor.rs:715) is gated on target_member.is_some() so module-owned targets consume the node and skip handle_attribute_candidate + children. inverted.rs:501 does NOT share defect 2 (paths disagree). Explains ~300 Cirq sites (all the *.LIKE/OP_TREE/TParamVal aliases). Artifacts: /mnt/optane/tmp/bifrost-fird/bisect-cirq/.
  - [x] (2026-08-07) websockets cluster BISECTED (subagent) and FILED as #1764 (mine): collect_reexport_events (graph_support.rs:262) walks depth-1 only; import_order_requires_source (:590) switches to it exactly on import-name/top-level-declaration collisions, silently dropping block-nested re-exports -> no reexport_edges -> seeds never expand -> file_may_reference_target rejects consumers. 4-file repro + 31-row matrix at /mnt/optane/tmp/bifrost-fird/bisect-ws/. Secondary unverified: compute_export_index_of uses the weak collector unconditionally.
  - [x] (2026-08-07) PYTHON fix phase LANDED (f4303f99, merged to origin/master, #1762 + #1763 + #1764 auto-closed). One coherent change: ExportEntry::ReexportedModule (terminal module form, aligned with the import binder), unfiltered namespace_qualified_declarations for annotations (class filter stays with constructor callees) + extractor empty-candidate gate dropped to match inverted.rs, and collect_reexport_events as an iterative all-statement-blocks walk refusing only function/class bodies (also fixed the confirmed secondary consumer compute_export_index_of). One flipped expectation (submodule-declares-own-name: ambiguous -> module, the inner decl was a different FQN). End-to-end: all three originating corpus sites verified fixed (keras head -> module; Cirq + websockets consistent). PYTHON language: fully complete. Next: language 4 census.

  - [x] (2026-08-07) GO census run + triage complete (20/20, binary 4ab522eb, `/mnt/optane/tmp/bifrost-fird/go-census-4ab522eb.jsonl`): 2494 actionable, but 2462 (99%) are ONE design gap - method receiver type mentions (`func (a AclResourceType) String()`) absent from find-references on every surface (the index frontier deliberately excludes them; gopls lists them). FILED #1765 (mine), fix = #1638 SelfReceiver precedent (editor-visible, external-excluded), delegated to Opus. Tail: example `main` x10 + blank `_` x7 (noise), and 9 tier-1 `append` sites = CENSUS-SIDE grading noise (a same-file METHOD named append makes the bare builtin call look tier-1; consider excluding Go builtins from tier-1 grading in the engine - low priority).
  - [x] (2026-08-07) GO #1765 LANDED (32ab2fb7, merged to origin/master, closed). Premise corrected by implementation: the frontier never excluded receiver types - the Go extractor's declaration-name guard (ast.rs is_method_receiver_type, from #1551) swallowed them. Fix: narrow is_method_receiver_type_name (peels pointer/generic to the base name; type params stay bindings), record via the shared SelfReceiver idiom, receiver position skips the local-shadow check (`func (Config Config)` legal). BONUS FIX: rename was producing non-compiling Go (receiver headers left unrenamed) - now pinned. inverted-edges intentionally unchanged. GO language: fully complete.
  - [x] (2026-08-07) TS census run + triage (20/20, binary 1aaeac0d, `/mnt/optane/tmp/bifrost-fird/ts-census-1aaeac0d.jsonl`): cleanest yet - 68 actionable, 8 repos at zero. Dominant mechanism MINIMIZED TO ONE FILE and FILED as #1769 (mine, fix delegated): uses of module-level DESTRUCTURED bindings (`const { cyan } = palette;`) missing from find_usages on every surface; plain module consts consistent. Covers vite colors, strapi ApplicationError, storybook document/globalWindow, carbon ItemMouseMove. Tail: globaleaks `parseInt` tier-1 x3 = builtin-shadow census grading noise (same class as Go append); woodpecker resolvedTheme to be adjudicated by the fix agent (covered or separate).
  - [x] (2026-08-07) TS #1769 LANDED (782003c6, merged to origin/master, closed). Root cause: destructured declarator binders were registered as shadows including the binder that DECLARES the target, so binds_target rejected every read of its own binding. Fix: register_declarator_pattern_shadows carves target-declaration binders out via pattern_binder_identifiers + is_target_declaration_binding; function params keep full shadowing; nested-scope same-name still shadows (near-miss pinned). Woodpecker resolvedTheme confirmed covered (it was a RENAMED useColorMode destructuring, not a plain const - census evidence misread). TS language: fully complete.
  - [x] (2026-08-07) JS census run + triage (20/20, binary 037bd8fd, `/mnt/optane/tmp/bifrost-fird/js-census-037bd8fd.jsonl`): 261 actionable, 203 in IBM__CRAIG alone - ALL census over-grading, fixed in the ENGINE (366ece87): unresolvable_import_boundary is an adjudicated forward answer, no longer graded as a tier gap (CRAIG's destructured `require("lazy-z")` binders doubled as same-file decls). Verified: CRAIG shape now 0 missing. RESIDUE not yet reviewed (small): weblate browser-global uses (`location` -> window.location x7, `WLT.Utils` x5 - browser-global modeling), yarn x17, go-ethereum x8 - triage next session before declaring js complete.
  - [x] (2026-08-07) JS residue rerun + 4-way parallel diagnosis complete. Census rerun on fixed classifier (`js-census-635f74c5.jsonl`): 261 -> 43 missing, confirming 366ece87 killed the boundary noise. Four Opus diagnosis agents (weblate globals, yarn Flow, bare globals, property chains) minimized everything; fixtures under /mnt/optane/tmp/bifrost-fird/js-fixtures/. FILED 9 fix issues (mine) + 2 escalations (Dave):
    - #1777 JS inverse confines unexported namespace/property-field targets to declaring file (lookup_only_local_property; extractor.rs:73-81,150-160,1153-1155; + candidates.rs:581 function-only union gate). Weblate WLT.* x12.
    - #1778 JS inverse misses browser-global bare reads in receiver position (extractor.rs:1022 object-suppression vs :1158-1170 member branch). Weblate location.hash x7.
    - #1779 usage strategies drop all but overloads.first() - CROSS-LANGUAGE (js_ts_graph.rs:148, rust_graph.rs:106/300, go_graph.rs:137/270). pipecd bootstrap x2; decisive dist-sorts-first flip.
    - #1780 object-literal keys under member-assignment RHS invisible to direct_object_pair_receiver (syntax.rs:359-381 + graph/mod.rs:234). CRAIG x3, curia x1.
    - #1781 expand_reference_expression byte-walk truncates optional chains (reference_site.rs:269-317; design-rule violation).
    - #1782 var hoisting/function-scoping not modeled in shared lexical resolver (lexical_definitions.rs:596-671; JsTsLexicalBindingIndex already correct). web3.js x5.
    - #1783 census tier-1 same-file evidence counts unbindable declarations (mod.rs:1417-1444; both false-positive AND false-negative faces; per-language bindability hook proposed).
    - #1784 census grades candidates inside ERROR subtrees (reference_candidates.rs:107-179; per-candidate test, not per-file). yarn misparse fallout.
    - #1785 hits.rs:63 silently drops hits with no enclosing CodeUnit (report-don't-swallow).
    - #1786 ESCALATED (Dave): Flow-pragma .js through plain JS grammar destroys declaration nodes (yarn 151/160 ERROR files vs 9/160 under TS grammar; needs LanguageDialect variant = cache-epoch + content-dependent dialect selection + JSX blast radius). All 10 yarn sites under this umbrella.
    - #1787 ESCALATED (Dave): bare-name script-global cross-file route - model-scope design question, 1 site (angular isNumber).
    - Adjudicated as corpus noise: react-dom.production.min.js b.stateNode x3, web3 CryptoJS x2 (minified/bundled vendored files; mechanisms covered by #1779/#1780 fixtures).
  - [x] (2026-08-07) JS fix phase COMPLETE: all 9 fix issues landed on origin/master via parallel worktree agents in 3 waves + 2 follow-up rounds.
    - Wave 1 (pushed 8b76aff6): #1784 census ERROR-subtree exclusion (per-subtree, census frontier only); #1780 object-literal keys under member-assignment RHS (2nd seam in issue was WRONG - lifting exported_local_property_binding = 1 false positive, 0 recovered; reverted with locking test); #1781 optional-chain reference sites (bug was NOT JS-only: C#+Kotlin truncated identically; canonical dotted text, tree threaded into resolve_reference_site, core stays dependency-free); #1782 var hoisting (additive fallback, NOT the issue's hoisted-branch edit - the start_byte cut fires before var children are pushed; delegates rule to js-ts syntax helper).
    - Wave 2 (pushed 7344a001): #1785 unenclosed-hit drop (premise corrected: top-level JS hits were NEVER dropped - Module/FileScope covers indexed files; real gap = analyzer-refused files via LSP explicit-candidate path; fix = CodeUnit::file_scope fallback; follow-up #1788 filed for 7 other languages); #1783 census bindability (JS/TS lexical is_bound_at hook; owner-filter default REJECTED by probe - Python decls are module-qualified so it prunes everything; per-language policy table); #1778 browser-global receiver-position reads; #1777 cross-file namespace fields (only the external-module test lifted, not the whole gate - program-scope half reuses JsTsLexicalBindingIndex; early return STAYS - fall-through would flood unproven hits for ownerless targets; candidates.rs field widening proven load-bearing by ablation).
    - Wave 3 (#1779 + follow-ups): #1779 overloads.first() collapse fixed at contract level (CandidateUsageHits + union_candidate_usages in core, proven-wins dedup; Rust real fixture, Go honest no-test, Java/Kotlin/C#/Scala already correct); AUDIT FOUND Python same truncation -> #1791 filed+fixed (worse than Rust: losing copy first = ENTIRE answer empty); cherry-pick of #1779 created a silent duplicate-fn build break in suite_usages caught by #1791's agent (origin/master never affected - gate discipline held).
    - VERIFICATION census (js-census-verify-3d4fba5a): 43 -> 12 missing. All mechanism clusters resolved (weblate 17->0 old sites, CRAIG, curia, pipecd, angular, web3 all clean). Residue: 11 yarn sites = #1786 Flow-misparse umbrella (Dave); 1 new second-order find -> #1792 filed+fixed: my inverse-blindness diagnosis was WRONG (static_member_receiver naturally steps over optional_chain); real bug = #1781's canonical text broke byte-offset caret narrowing in jsts_site_for_focus (structural narrowing now; inverse was correct all along - different parameter bindings).
    - Final round (pending push): #1779 (3d4fba5a) + #1791 (9cf51e6d) + #1792 (3681e025), combined gate then push.
  - [x] (2026-08-07) JS COMPLETE. Final round pushed (17d7bc5d on origin/master, after two mid-validation upstream races with Dave's bifrost2 diagnostics drops - Ruby-only final delta, post-push nextest 9266/9268 green with only the 2 known failures). #1779/#1791/#1792 closed. JS totals: 11 fix issues closed (9 planned + 2 follow-ups #1791/#1792), 2 escalations to Dave (#1786 Flow grammar routing, #1787 bare script-global design), 1 cross-language audit follow-up open (#1788 unenclosed-hit drops in 7 other languages, mine), 2 census-engine precision fixes (#1783, #1784) on top of the earlier import-boundary fix (366ece87). Summary delivered.
  - [x] (2026-08-07) CSHARP diagnosis complete (3 parallel agents; fixtures /mnt/optane/tmp/bifrost-fird/csharp-fixtures/). 33 census sites + deep-sweep extras reduce to 8 mechanisms + 1 escalation. FILED: #1796 named-arg labels resolve to same-named types (forward; V4 decisive - label matching NO attribute member still answers a type; obj-initializer path is the fix template); #1797 arity-inapplicable fallback binding (3 faces: external true callee -> should answer boundary; record primary ctors UNINDEXED; preproc-hidden decls) + acts-on-already-computed Inapplicable verdict; #1798 method-group value contexts missing ==/!=/ternary (NOT inherited-members - premise falsified); #1799 goto labels resolve to methods; #1800 catch-clause misparse mints constructor CodeUnits named `catch` (search_symbols pollution); #1801 nested-base simple-name hierarchy blind (empty ancestors for all 10 nunit fragments; hierarchy path has file scope, type-ref path has enclosing scope); #1802 enclosing-type member walk missing (innermost-only csharp_enclosing_class; ordering constraint: base chain before outward); #1804 residuals tracking (MatchJust x2, CollectionTallyResult, Contains decl-exclusion miss). ESCALATED #1803 (Dave): preproc directives inside declarations destroy extraction (StringSlice indexes ZERO methods after line 136; census ERROR guard makes census UNDER-REPORT this - index frontier sees it; same family as C/C++ at larger scale).
  - [x] (2026-08-07) CSHARP fix phase COMPLETE: all 8 fix issues landed on origin/master (7f06019a). Wave 1 (#1798 method-group contexts incl dedup-blind test correction; #1799 label exclusion incl census-frontier decision; #1800 ctor-minting invariant incl 3-condition repro bisect + CSharpEnclosingType correct-by-construction; #1801 hierarchy enclosing scope unifying 4 supertype walks + first C# descendants test) + #1805 mixed-language generations panic (14 seams guarded). Wave 2 (#1796 named-arg labels incl latent extractor false-positive fix; #1802 enclosing-type walk with naive-design disproof, follow-ups #1807 receiver-base per-scope + #1808 inverse cross-type proving; #1797 arity fallback DELETED + record/class primary ctors indexed + delegate-field mirror gap). One semantic merge conflict reconciled by hand (7f06019a). VERIFICATION census: 33 -> 1 missing; survivor = #1810 (filed+delegated): arity-rejected calls to object's statics answer no_definition because the boundary walk skips the implicit object base. Summary delivered.
  - [x] (2026-08-07) ALL 11 LANGUAGES HAVE CENSUS DATA. Languages 8-11 (binary 64063177 for c/cpp/php; 09071ef6 for scala): c = 2518 missing (unicorn 453, libyang 329, libgit2 256, Pillow 256 - macro/preproc-heavy corpus, expect #1803-family dominance); cpp = 1611 (c-blosc2 384, log4cxx 237, abseil 215); php = 59 (healthy: Carbon 15, monolog 12); scala = 1145 (zio-http 165, airframe 138, chisel 90, midonet 86) - scala's first two runs CRASHED on the #1805 mixed-language generations panic (found+fixed+landed 09071ef6; the unpiped rerun exposed it after a grep pipe masked the first death). Triage depth-first after csharp fix phase.
  - [x] (2026-08-08) C (language 8) diagnosis + filing complete (2 parallel agents; fixtures /mnt/optane/tmp/bifrost-fird/c-fixtures/). PREMISE FALSIFIED: zero TU-static-scoping failures. 2518 sites partition: ~1300 = #1811 single-candidate-discarded-as-ambiguous-empty (unresolvable QUOTED includes transitively poison arity proof; 98% of ambiguous sites in poisoned files; libyang flip proof via 2 stub headers); ~700 = #1812 no-candidates-answers-Ambiguous preempting the same-file macro fallback (RBN_RIGHT; glpk 'error' = in-file #define aliases NOT dup functions; 382/706 name a same-file macro); 38 = #1813-B1 self-recursion invisible (activation at closing brace not declarator end; ALL of chibicc); 76 = #1813-B2 same-guard declarations rejected (guard check never consults reference guards; all secp256k1); 355/360 typedef sites = #1814 cross-file guard subset test contradicting the documented merge rule (extern-C idiom permanently false in C files; rejected alias VANISHES past name_mentions instead of degrading); #1815 type_alias_target_text string-parses typedefs (fn-ptr typedef -> return type; repo-rule violation); #1816 residuals (libuv same-file x5, pacemaker x8, qhull x2, nng include-chain); B3 macro-token declarations + poisoning-scope -> #1803 comment. cpp verified NOT sharing #1779. Fix wave COMPLETE: #1811-#1815 all landed on origin/master (bba1c5da). Landing notes: #1811's 'make Ambiguous unrepresentable' premise was WRONG (five deliberate contracts need empty/single-candidate Ambiguous - Go promoted boundaries, template partial specializations, base subobjects; state made explicit via ambiguous_without_candidates + targeted asserts instead); #1813 fixed C++ free-function recursion too and exposed #1818 (recursive sites invisible to scan_usages - two hit paths disagree); #1814 flipped 5 C++ test expectations to the documented branch-selection model (reviewed+accepted; one recorded precision loss on #1816) and fixed the C++ direction too (premise 'C++ already worked' was wrong); #1815 found the string-parse defect in THREE copies (the cited one was not even on the witness path) and unified them structurally. New follow-ups: #1818 (recursion editor-visibility), #1819 (macro targets unsupported by inverse - 382 corpus sites' proving blocked on it). VERIFICATION census (c-census-verify-bba1c5da): 2518 -> 93 (96% reduction); residue = #1803 macro-token declarations (CJSON_PUBLIC, GIT_EXTERN, qemu attribute macros - dominant), #1816 residuals, #1819 macro proving, 2 genuine ambiguous-with-candidates. C language COMPLETE; summary delivered.
- [x] (2026-08-08) CPP (language 9) re-census + diagnosis + filing complete. Re-census on the post-c-wave binary FIRST (decision: the c fixes share crates/bifrost-cpp): 1611 -> 950 missing (cpp-census-bba1c5da.jsonl) with zero cpp-specific work. 4 parallel diagnosis agents over buckets A (no_indexed_definition 523), B (ambiguous 211), C (inverse-miss-on-resolved 178), D (receiver/overload tail 38); reports at /mnt/optane/tmp/bifrost-fird/cpp-diagnosis/{A,B,C,D}-report.md, fixtures cpp-fixtures/. Headline premises FALSIFIED in every bucket: B's decl/def-pair-counting theory wrong (40 sites Ambiguous with ONE target; real defect = bare member-call branch cpp.rs:4023 unconditional-ambiguous, only sibling without #1811 tolerance - this->prepare resolves, bare prepare fails); C's typedef-inverse-blindness wrong (typedef/using scan is fine and alias-transitive; real = angle-include visibility + parameter_declaration recovery); A's XXH_FORCE_INLINE macro theory wrong (plain static under #if/#else fails identically); D's three diagnostic kinds map to seven families crossing all label boundaries (unsupported_cpp_receiver is itself a mislabel from empty candidate sets). FILED 12 fix issues (mine): #1824 callable guard-coverage (123, port preprocessor_guard_terms_cover_all_paths to callable path), #1825 namespace-macro wrapper (132, ABSL_NAMESPACE_BEGIN synthetic function_definition; ancestor walk discards under compound_statement), #1826 bare-member lone-candidate arity (113 after dedupe ladder), #1827 signature identity (trailing-qualifier whitespace-mismatch drop declarations.rs:3749 + top-level const per [dcl.fct]/5), #1828 alias-unresolvable answers Ambiguous (resolver.rs:3183), #1829 inverse angle-include blindness (60, all log4cxx), #1830 parameter_declaration recovery container (44, XXH_NOESCAPE), #1831 ns-qualified out-of-line destructor (10), #1832 Owner::member out-of-line proves owner binding (21, wuffs), #1833 base walk string-splits signature - template-derived classes fail (19, read supertype edges; #1815 shape), #1834 census seeds ctor/dtor declarators (grading, #1784 precedent), #1835 using Base::name overload merge (2). ESCALATED (Dave): #1836 nondeterministic content-keyed resolution (qpid; flips on unrelated 17-byte comment; deterministic per content, jobs-independent; harness cpp-fixtures/bisect/qpid), #1837 .inc not indexed as C++ (40 abseil casualties; extension-claiming design), #1803 comment (C++ face DOMINATED BY CLASS HEADS not fn declarators: cpp_export_macro_token ALL-CAPS heuristic eats class names >=74, ESPDEPRECATED/ABSL_LOCKABLE positional namespace demotion 10 - scheduler.h loses 583/753 lines, PN_CPP_EXTERN phantom `std` field decls 10). #1838 residual tracker (A5 82, F4 16, F7 5, C-E/F/G 11, O-M/O-S, 58 genuine-overload tier-1 residue). Methodology notes: reusing one --output path across run-repo probes silently replays stale verdicts (invalidated one bisection before caught); site counts are sampled lower bounds. Fix wave: 5 parallel worktree agents launched (1: #1824+#1825 resolver visibility; 2: #1826+#1833+#1835 member path; 3: #1827+#1828 signature/alias; 4: #1829-#1832 inverse+visibility; 5: #1834 engine grading).
  - [x] (2026-08-08) CPP fix waves LANDED on origin/master. Round 1 (12 fixes #1824-#1835, pushed b2084178): landing notes - #1834's witnesses were NOT plain declarators (all four sit in macro-recovered class bodies where `Foo();` parses as a call; two-branch structural test); #1827 found+fixed a THIRD unreported split (`override` split every overriding member from its own body; epoch salt bumped); #1824 needed a target-selection collapse beyond visibility (both guard branches = alternate spellings of one declaration); #1825's L-B site was a different seam (cpp_enclosing_local_scope picking the recovery wrapper as a local scope - fixed too); #1832's containment gate premise falsified by its own witness (file-scoped rule implemented); #1829's angle fix needed the unique-suffix fallback as load-bearing; #1830's suggested negative control was wrong (Foo Bar* p SHOULD claim Foo at its token). Gate caught ONE real regression pre-push: the #1835 merge consulted get_ancestors before checking for using-declarations, sweeping all visible aliases (fixed by gate reorder e1308630; guarded by cpp_type_definition_routing_classifies_only_name_bounded_candidates). New follow-up #1840: MCP tool path and resolve_definition_batch_with_source answer the same C++ site differently at the same commit (found by #1826's agent - fixture tests against the tool surface prove nothing about the probe path). Mass-failure scare during post-push validation (2055 instant fails) was self-inflicted target/debug contention from concurrent cargo runs - clean rerun 9481/9483.
  - [x] (2026-08-08) CPP VERIFICATION census (cpp-census-verify-b2084178): 950 -> 644 (317 fixed, 11 new). Underdelivery diagnosed: xxhash A1 family (91) = guard-x-macro-declarator INTERSECTION (guards fixed by #1824, macro half = #1803; original diagnosis perturbed one factor at a time and each alone reproduced - confounding); log4cxx 60 inverse-miss survivors = FOUR distinct defects (verify-lx diagnosis agent): #1829 PROVEN working on the real repo; 21 LevelPtr = #1844 (forward answers out-of-closure same-FQN twin); 16 logchar = #1845 (fail-closed on same-FQN decls with differing alias targets; real blocker = resolved_type_import dedupe, NOT the report's named site - and the escape hatch fails on guard MERGING, not its preconditions); 6 helpers::Pool = #1803 face (LOG4CXX_NS segment dropped in one file); 4 .format = #1843 REGRESSION from the #1835 merge (missing [namespace.udecl]/14 hiding). Of 11 new sites: 4 = the regression, 7 = churn (pre-wave inconclusive now adjudicated), 0 nondeterminism. Round 2 landed (pushed f64993f0): #1843 (per-relationship CppHidingSet, #1827 signature identity + object_macro_replacement_at expansion, unpinnable macro = covering; Y-a needed NO macro expansion - out-of-line def carries the comparable list), #1844 (collapse_same_fqn_alias_spellings prefers is_physically_visible spelling; fwd-decl+def are same_logical_symbol so collapse restricted to aliases), #1845 (VisibilityIndex::same_fqn_type_spelling_for_target consulted before both fail-closed points). Upstream during the wave: Dave's tree-sitter-scala 0.26.2 replacement (#1823) landed - directly relevant to the scala wave next.
  - [x] (2026-08-08) CPP COMPLETE. #1844 reopened after the final census (21 LevelPtr sites unchanged - the landed fix repaired the fixture's ambiguous-both shape but the corpus reaches a SINGLETON through the resolve_in_enclosing_scopes fallback at get_definition/cpp.rs:3104, which returned the FIRST INDEXED declaration with no reachability test; reached only via a 3-link chain: export-macro class head indexed as forward (#1803 face) -> ForwardsOnly(2) fails closed (filed #1846) -> namespace-less lexical scope). Same agent resumed via SendMessage, made the fixture faithful by reduction against the clone (each ingredient verified necessary), fixed the fallback with a reachability-filtered accept (is_physically_visible), verified the round trip on the real clone; landed 2b100d82, #1844 closed. Spot-check on rebuilt binary: 8/8 LevelPtr witnesses non-missing via the differential probe. FINAL NUMBERS: 1611 (pre-c-wave baseline) -> 950 (c fixes alone, shared analyzer) -> 644 (round 1) -> 623 measured at f64993f0 (round 2) -> ~602 effective after the #1844 corpus fix. CPP totals: 15 fix issues closed (#1824-#1835, #1843-#1845), 1 wave regression caught-and-fixed by verification (#1843), 2 escalations to Dave (#1836 content-keyed nondeterminism, #1837 .inc claiming) + 3 evidence comments on #1803 (class heads dominate C++), follow-ups #1840 (tool-vs-probe divergence), #1846 (ForwardsOnly fail-closed), tracker #1838 (residue: ~238 #1803 faces, 84 genuine-overload tier-1 residue, A5/A6/C/D residuals, F13/F14 side lead). Residue fully attributed. Summary delivered. Next: scala (language 10) - RE-CENSUS FIRST on current binary (tree-sitter-scala 0.26.2 makes scala-census-09071ef6 stale), then php (59).
  - [x] (2026-08-08) SCALA re-census + diagnosis + filing complete. Re-census on 90d65613 (new parser): 1145 -> 1144, parser swap NOT implicated (per-site diffs confirm across all three buckets). 3 parallel diagnosis agents (A typed-overload 783, B inverse-miss 229, C tail 132); reports scala-diagnosis/{A,B,C}-report.md, fixtures scala-fixtures/. HEADLINE: one leak = 44% of the language - scala_exact_owner_typed_overload_resolution (scala.rs:7783) early-returns Ambiguous at :7819 on ANY unresolvable/duplicated transitive supertype BEFORE the candidate-count guard, discarding collected candidates (fires with 1 or even 0 candidates; its success diagnostic occurs 0 times in 194k sites - the selector never completes anywhere). Every briefed premise falsified in all three buckets: Left/Right fail via the ENCLOSING class's parent not target external-ness; update-sugar works; 32/35 local_variable_reference are census-wrong not resolver-wrong (zero actual cpp-style mislabels); 13/13 wildcard-import ambiguities have ONE distinct target (counted per statement, dedup unreachable); #1638 does NOT cover Scala bare calls (query.rs:692-714 unreachable); s3_website 46-site 'inverse miss' is a FORWARD false positive ($-stripped companion wildcard + unseeded for-binders). FILED 10 fix issues (mine): #1849 ancestor leak (~660), #1850 ambiguous-with-candidates answer shape (123), #1851 lexical type namespace poisoning (20 + tier-3 inflation), #1852 wildcard dedup (13), #1853 call-shape fails closed on function-valued results (31), #1854 inverse self-shadow on bare recursive calls (70, 15 repos), #1855 Java-scan mechanical gaps (~28: ctors/nested-quals/parenless/annotations), #1856 forward FP $-stripping + for-binders (46), #1857 scope-boundary pair (10), #1858 census grading (Scala bindability unconditional-true + local_variable_reference graded as gap; #1783 was JS/TS-only). ESCALATED (Dave): #1859 Java instance-receiver scan design (reuse Scala scan vs extend duplicate), #1860 self-type + anonymous-class members unmodelled (no CodeUnit). #1861 residual tracker. Fix wave: 5 parallel worktree agents launched (1: #1849+#1850+#1851 ancestor discipline; 2: #1854+#1855 inverse; 3: #1852+#1853 forward relations; 4: #1856+#1857; 5: #1858 engine).
  - [x] (2026-08-08) PHP census REFRESHED on 90d65613 (stop-hook prompt; old 64063177 baseline predated c/cpp waves): 59 missing, IDENTICAL to stale baseline, all no_indexed_definition (Carbon 15, monolog 12, tenancy 8). Diagnosis agent launched in parallel with the scala fix wave (diagnosis only - php issue-filing waits for scala fixes per depth-first).
  - [x] (2026-08-08) SCALA fix wave LANDED on origin/master (da0eb56a; all 10 issues #1849-#1858 closed). Landing notes: #1849's briefed deferred-flag fix was INSUFFICIENT - real fix = new ScalaDirectAncestorResolution::Incomplete variant separating unindexed (safe to walk past; inverted.rs:1012 already did this - the forward producer was the outlier) from multiply-declared (fail-closed stays: the answer IS in the workspace and skipping levels answers from shadowed scopes; consumer-only patching was tried and REVERTED against 2 pinned tests); leak also had to close in scala_exact_owner_member_candidate_units (else 660 sites just migrate to ambiguous_scala_enclosing_member); #1849 tests only observable after #1851 (bare-call fast path answers ambiguous_scala_type first); N7 boundary premise wrong (object indexed - now resolves). #1850 flipped 2 precedence tests (old expectations encoded the empty-ambiguity defect). #1854 made #1638 classification reachable for bare Scala calls (recursion = SelfReceiver editor-visible); #1855's briefed mechanisms half-wrong (paren-less = no-arg-list early return not the len gate; annotations = private is_declaration_name duplicate not is_type_like_reference; deleted the duplicate); #1856's for-binder defect was the BINDER OCCURRENCE not uses, and $-identity fixed at the wildcard boundary not scala_normalized_fq_name (60 callers, inverse already correct; Scala3 enum cases + package members pinned as controls); #1857's forward-miss premise false at head - real defect was the inverse Type-role hit (applied_constructor_type never climbed); #1858 found a SECOND mis-graded kind (declaration_or_import_site, all languages) + built ScalaBareNameDeclarationScopes on the CensusBareNameBindings seam (over-approximating reach so tier-1 keeps real gaps: rejection lands on tier-3). Follow-up #1863 (duplicated supertype answers no_definition instead of Ambiguous-with-candidates). Cross-agent picks needed 2 semantic reconciliations (1852 mut + 1856 ctx.scala; verified by combined test run). ENVIRONMENT: /home/jonathan hit 100% mid-wave - freed ~710G (main-repo target/debug/incremental 291G, issue699 55G, idle worktree targets; analyzer-sqlite-store worktree+branch removed on Jonathan's explicit instruction, its uncommitted July edits discarded); one agent hit a cross-worktree git-stash collision (stashes are shared across worktrees - agent repaired, no loss; patch files used thereafter). Upstream during the wave: Dave landed fixes for MY ESCALATIONS #1837 (.inc claiming), #1786 (Flow contamination), #1787 (bare-name script-global). Verification census running on da0eb56a. PHP issues filed (#1866 resolver global-namespace fallback - the one real defect; #1867 census bindability arm falsely grouping PHP with Ruby, 59/59 over-graded, 18 credited against PROPERTIES) and single fix agent launched, #1866 mandated first.

## Surprises & Discoveries

- Observation: Census seeding was entirely unbuilt despite being a detailed design doc; all its milestones were unchecked.
  Evidence: `grep -rn "probe.seed\|census" src/reference_differential/ src/bin/` finds nothing; `find . -name census.rs` empty.
- Observation: The FIRD engine lives in the facade crate at `src/reference_differential/mod.rs`, not `crates/bifrost-analysis/...` as the design doc's path suggested.
  Evidence: `find . -path '*reference_differential*' -name '*.rs'` -> `./src/reference_differential/mod.rs`, `./src/bin/bifrost_reference_differential.rs`.
- Observation: The index (reference) frontier already proposes identifier occurrences inside `macro_rules!` bodies, contrary to the design doc's premise that macro-block items are index-blind. The census's actual difference from the index frontier is the per-language reference *exclusions* (Go/C# decl names, Rust associated-type decl names, JS export aliases) plus receiver keywords - not macro bodies.
  Evidence: an inline census-vs-index engine test showed both seeds propose the macro-body `frobnicate` occurrence at the same byte; the first draft assertion (index excludes it) failed.
- Observation: On a 60-file tokio census smoke run, the census produced 5 forward-adjudicated misses (forward resolved, inverse missed) and 8 tier-2 gaps, with every site tagged `seed: census`. The forward-adjudicated misses are FIRD's classic finding class, now reachable at census sites.
  Evidence: `/mnt/optane/tmp/bifrost-fird/smoke-tokio-census.jsonl`; `.report.config.probe_seed == "census"`.
- Observation: The `Mismatches` arm of `receiver_owner_proof` is a silent total drop - a receiver that resolves to the "wrong" FQN produces neither a proven nor an unproven row, so downstream absence claims escalate to `verified_absent`. Any FQN-identity blind spot upstream (here: pub-use re-export aliasing) therefore converts directly into the strongest false claim the tool can make. Census findings that look like "self-call misses" can have at least two unrelated mechanisms (#1638 enclosing==target drop vs #1750 re-export receiver typing); the enclosing-vs-target relation is the cheap discriminator.
  Evidence: #1750 minimal repro; `scan_usages_by_location` on it returns `total_hits: 0, unproven_hits: 0, status: verified_absent` while the forward resolver states the edge.

## Decision Log

- Decision: Build M1-M3, skip M4 (external referee); the agent adjudicates tier-3 during triage.
  Rationale: Forward-adjudicated misses plus tiers 1-2 need no referee and carry the designed yield; M4 is marked optional/cuttable and only pre-ranks the tier-3 long tail. User confirmed 2026-08-07.
  Date/Author: 2026-08-07, Claude (Opus) with Jonathan.
- Decision: Implement the census walk as a new `CandidateFrontier::Census` variant of the existing iterative `collect_candidate_ranges` rather than a separate module.
  Rationale: The traversal, limit handling, and cancellation are already correct and stack-safe; census differs only in the leaf predicate (identifier-class, comment/string excluded) and the absence of per-language reference exclusions. Reuse avoids a parallel divergent walker. (Revisit if scope-path/role recording makes the shared function unwieldy.)
  Date/Author: 2026-08-07, Claude (Opus).
- Decision: M2's ledger/shrink/rerun is realized as a post-processing triage script (`/mnt/optane/tmp/bifrost-fird/triage.py`) over the report JSONL rather than embedded in the engine; M3's `--shard` is realized as subagent-level parallelism (independent per-repo/per-language runs) rather than an in-process shard flag.
  Rationale: The report JSONL already carries every evidence field the ledger needs (seed, tier, forward_status, targets, inverse_hit, source_evidence, repo_head), and this campaign is agent-adjudicated (not the autonomous run-until-dry loop the in-engine ledger/shrink was designed for). Subagent parallelism (user-authorized) covers corpus throughput without an in-process sharder. Keeps engine risk low and gets to real findings faster. The census FINDING capability (forward adjudication + tier grading) is fully in the engine.
  Date/Author: 2026-08-07, Claude (Opus).
- Decision: Campaign runs use `--tiers 1` (actionable = forward-adjudicated misses + tier-1 gaps) at first, widening only if the high-precision set dries up.
  Rationale: A 60-file tokio smoke run already produced ~160 tier-3 rows; tier-3 is exploration-grade and would swamp hand-adjudication at full budget. Forward-adjudicated misses are reported regardless of `--tiers`, so `--tiers 1` keeps the actionable set high-precision.
  Date/Author: 2026-08-07, Claude (Opus).
- Decision: Campaign runs analyze each clone at its current clean HEAD (recorded as `repo_head`) rather than force-checking-out corpus pins.
  Rationale: `run-corpus` reads and records the clone HEAD; all sampled clones are clean. A bug reproduced at a clean checkout is a valid finding, and the recorded head makes it reproducible. Force-checkout of 220 clones is out of scope for finding+fixing analyzer bugs.
  Date/Author: 2026-08-07, Claude (Opus).

## Outcomes & Retrospective

- [x] (2026-08-09) SCALA COMPLETE. Verification census (scala-census-verify-da0eb56a, 19 repos) + midonet slice (midonet-verify-7db6d0c0, post-#1878): **1,144 -> 221 (81%)**; 962+61 fixed, 14 new (all inverse churn from newly-resolving forwards). Residue attributed on #1861: 77+21 inverse-miss deferred families (#1859 instance receivers, labels, super, replicas), 44+3 honest Ambiguous-with-candidates (grading-policy residue), 20 enclosing-member remnant (#1863 next layer), 18 = #1860 self-types, 16+1 callable-shape tail. The midonet crash during verification exposed #1878 (nested-namespace FqName panic - issue premise falsified: NOT C++17 shorthand but Language::None from extension-derived naming on #1837-claimed extensionless TBB headers, plus an independent shorthand-levels defect; both fixed, midonet 86 -> 25).
- [x] (2026-08-09) PHP COMPLETE: **59 -> 0**, the campaign's first fully clean language. #1866 (global-namespace fallback as an ordered candidate pair, symmetric across forward/inverse/diagnostics; all 3 corpus witnesses exact-range) + #1867 (PhpBareNameFunctionScopes on the CensusBareNameBindings seam; ~40 of 59 sites clear via #1866's honest unresolvable_import_boundary answer instead). Falsified: f3's "must stay tier-1" (a resolved site is ungradable - same as the JS #1783 documentation).
- TOP-20 CAMPAIGN COMPLETE (all 11 languages, depth-first): rust, java, python, go, ts, js, csharp, c (2518->93), cpp (1611->~602), scala (1144->221), php (59->0). ~50 fix issues closed on origin/master; escalations #1786/#1787/#1836/#1837 fixed by Dave DURING the campaign; #1803 (macro-declaration family, the dominant cross-language residue: C ~84, C++ ~238), #1859, #1860 remain his. Engine grading precision fixes landed for js/ts (366ece87, #1783, #1784), csharp (#1799), cpp (#1834), scala+cross-language (#1858), php (#1867). Diagnosis reports preserved at .agents/docs/fird-campaign-2026-08/; temp outputs at /mnt/optane/tmp/bifrost-fird cleaned. The top-30 extension remains.
- Retrospective, method: (1) EVERY diagnosis brief and most issue briefs contained at least one false premise - the falsify-first discipline (fixtures + single-factor perturbation + pre-fix-failing tests) caught all of them before they shipped; (2) re-censusing on the current binary before triage paid twice (cpp 1611->950 free; scala parser-swap ruled out); (3) verification censuses caught one genuine wave regression (#1843) and one fixture-vs-corpus divergence (#1844's singleton path) that fixture-level validation missed; (4) confounded factors (guard x macro in xxhash) defeat one-factor-at-a-time perturbation - when a fix underdelivers, suspect intersection families; (5) shared infrastructure hazards found the hard way: cross-worktree git stashes collide, concurrent cargo runs on one target/debug corrupt a running nextest, piped exit codes mask crashes, --output reuse silently replays stale verdicts.

## Context and Orientation

FIRD engine: `src/reference_differential/mod.rs`. Key items:
- `ReferenceDifferentialConfig` (line ~34): run knobs (`max_files`, `max_sites`, `seed`, `exact_site`, ...). Add `probe_seed: ProbeSeed` and `tiers: TierSelection` here.
- `collect_sampled_sites` (line ~514): parses each file via `DeclarationNameRangeContext`, calls `reference_candidate_ranges(root, language, max)` at line ~555, subtracts declaration-name ranges, and pushes `SampledSite`s into a bounded heap keyed by `site_priority`. This is the seed seam.
- `forward_resolve_sites`/`forward_resolve_file` (line ~638/687): forward definition lookup per site, producing `ReferenceDifferentialSite` records and `ResolvedSite`s.
- `compare_inverse` (line ~347): runs the inverse usage query per resolved group and marks each site consistent/missing.
- `ReferenceClassification` enum (line ~134): `Consistent`, `EditorOnly`, `Unproven`, `Inconclusive`, `Missing`.
- `ReferenceDifferentialSite` (line ~143): the per-site report row. Add `seed` and `tier` fields.

Candidate walk: `crates/bifrost-analysis/src/analyzer/reference_candidates.rs`. `collect_candidate_ranges` is an iterative stack walk with a `CandidateFrontier` (References | SemanticTokens). A leaf qualifies when its kind matches the frontier predicate, it is not an excluded reference candidate, it is a leaf (or a compound reference), and it is non-empty.

CLI: `src/bin/bifrost_reference_differential.rs`. `parse_run_repo_args`/`parse_run_corpus_args` plus shared option parsing (`--max-*`, `--seed`, ...). Add `--probe-seed index|census`, `--tiers 1,2,3`, and (M3) `--shard K/N`.

Corpus tooling: `~/Projects/brokkbench/tasks.py`. `task_repos(SFT_PREDICATES, langs=[lang])` returns `RepoRef{lang, repo_slug, task_count}`. `SFT_PREDICATES.not_overlarge` excludes `sft-tools-commits/large-repos.csv`. The 11 languages are the dir names under `~/Projects/brokkbench/sft-tools-commits/`: c, cpp, csharp, go, java, js, php, py, rust, scala, ts. Clones live at `~/Projects/brokkbench/clones` -> `/mnt/T9/repo-clones`.

Terms: "census" = every identifier-class leaf token from a tree-sitter parse (comments/strings excluded by node kind), independent of the analyzer index. "Seed" = the generator of probe sites; pluggable as `index` (today) or `census`. "Tier" = deterministic evidence grade for a forward-unresolvable census site (tier 1 same-scope member/field with same-file/module decl and no shadow; tier 2 same-module or import-connected; tier 3 else). "Forward adjudication" = using forward resolution itself as the referee: a census site forward-resolves to D and the inverse query for D misses the site.

## Plan of Work

M1 — Census walk + tier 1 + CLI. In `reference_candidates.rs` add `CandidateFrontier::Census` and a public `census_identifier_ranges(root, language, limit)` (plus a cancellable variant) that collects identifier-class leaf ranges with no reference exclusions, excluding comment/string node kinds. Add a `ProbeSeed` enum and `TierSelection` to the engine config and CLI. In `collect_sampled_sites`, branch on `config.probe_seed` to choose the walk. Add a census scope/role helper for tier-1: for a forward-unresolved census member/field occurrence of name N, check the census's own same-file/same-module declaration of N with no local shadow. Add unit tests + a firing fixture (unindexed same-file receiver call yields one tier-1 finding) and a silent healthy fixture.

M2 — Forward adjudication + tiers + ledger. Route census sites through the unchanged forward/inverse pipeline; tag report rows `seed: census`. Classify forward-unresolved sites into tiers 2/3. Emit a ledger (append-only JSONL, dedup key `(tier, language, syntactic shape)`, minimal repro, single-line rerun) under `/mnt/optane/tmp/bifrost-fird/`.

M3 — Inverse-precision + sharded runner. After inverse comparison, check every inverse hit's range corresponds to a census occurrence of the name (name-literal); unbacked hits become their own signature class. Add `--shard K/N` (hash-partition files) to `run-corpus`. Wire per-language ranking from tasks.py.

Campaign — Per language A..K (depth-first): select top-30 repos via tasks.py; build the release runner from clean HEAD; run census FIRD (tier-1 first, then 2-3); triage every finding per the runbook; file issues assigned to `jbellis` (skip issues already assigned to others; escalate non-generalizable/hacky ones to `DavidBakerEffendi` with a comment); fix with structured (non-regex) solutions + regressions; `cargo test` green; commit; merge to `origin/master`; summarize the language before moving on.

## Concrete Steps

Build: `cd /mnt/optane/bifrost-fird && scripts/with-isolated-cargo-target.sh cargo build --release --bin bifrost_reference_differential` (final campaign builds must be non-isolated and durable per the runbook; use isolated target only for iteration).

Select repos (per language): import `tasks.py` directly and print the top-30 `SFT_PREDICATES` repos by exact `(-task_count, repo_slug)` order. Store the resulting manifest under `/mnt/optane/tmp/bifrost-fird/`.

Run: `run-repo`/`run-corpus` with `--probe-seed census --tiers 1,2,3` and the standard semantic budget from the runbook. Outputs and ledgers under `/mnt/optane/tmp/bifrost-fird/`.

## Validation and Acceptance

- M1: `cargo test -p brokk-bifrost-analysis reference_candidates` (census walk unit tests) and the engine fixture tests pass; the firing fixture yields exactly one tier-1 finding and the healthy fixture yields zero.
- M2: one census pass on a rust and a go corpus repo produces report rows tagged `seed: census`; tier-1/2 rows rerun deterministically via the single-line rerun command.
- M3: a two-language corpus pass completes; an injected fabricated inverse hit surfaces as an inverse-precision signature.
- Campaign per language: `cargo test` green after fixes; issues filed/closed; language summary delivered.

## Idempotence and Recovery

All runs write unique head-scoped outputs under `/mnt/optane/tmp/bifrost-fird/`; reruns overwrite their own unique paths. `run-corpus` is append-only and resume-safe at repo-record granularity. Temporary outputs are cleaned at campaign end. Code changes are committed frequently to `bifrost-fird` and merged to `origin/master` per language.

## Interfaces and Dependencies

In `crates/bifrost-analysis/src/analyzer/reference_candidates.rs`:

    pub fn census_identifier_ranges(root: Node<'_>, language: Language, limit: usize) -> ReferenceCandidateRanges;

In `src/reference_differential/mod.rs`:

    pub enum ProbeSeed { Index, Census }
    pub struct TierSelection { /* which of tiers 1,2,3 to report */ }
    // ReferenceDifferentialConfig gains: pub probe_seed: ProbeSeed, pub tiers: TierSelection
    // ReferenceDifferentialSite gains: pub seed: &'static str, pub tier: Option<u8>

CLI (`src/bin/bifrost_reference_differential.rs`): `--probe-seed index|census` (default index), `--tiers 1,2,3` (default all), `--shard K/N` (run-corpus, M3).

## Artifacts and Notes

(Transcripts and evidence appended per milestone.)

Plan revision, 2026-08-08: The completed campaign covered 20 repositories per language. The user requested 30. This revision adds the rank-21-through-30 extension. It also names `SFT_PREDICATES` as the selector used by the campaign. This selector applies the required `large-repos.csv` exclusion.
